# APSGo V7 当前工作约定

- 统一数值新 8.2.1（2026-09-17，实施前 `57a4543`）：共用片段拼接、桥接续算和选中资源写入已连成有界原生步骤，首轮/拆单父单移除/精修共用；原直接单材双材顺序、事件、容量重试和序号错误保留。共享必要 197 项/134.17 秒通过，真实前缀及 2 万次窗口与冻结基线字节一致；精确树同范围复验见本项提交正文。计划 v0.20 将新 8.2 细分为三个工程单元；当前仅 8.2.1 完成，下一项 8.2.2 整候选原生计算，再做 8.2.3 多线程筛查，不宣称已并行或提速。配置/库、用户文件、历史交期问题保持，Ponytail 关闭。

- 统一数值新 8.1（2026-09-17，实施前 `e8959ee`）：公共批次从描述进入同一候选计算，两个变体共用描述校验、输出独立；代次内复用有界私有缓冲和已校验评价参数，消费/作废后先使旧视图失效。共享必要 216＋9 项通过，精确树同 225 项见提交正文。真实单条/8 条窗口全部 5560 个消费输出、轨迹/资源/完整返回相同，旧 7.1 对照也一致、双审计通过；8 条路径工作区分配 223 次（原剖析 36747 次），不据诊断时间宣称整体提速。计划 v0.19、`stage8.md` 已记录；下一项新 8.2 整候选原生调度和同内核并行，不能只并行最后评价；当前仍有界串行协调，未实施并行。正式配置/库、用户文件、历史交期问题保持，Ponytail 关闭。

- 统一数值 7.3（2026-09-17，实施前 `e1af523`）：六类原生生成器在暂停点恢复后立即检查终止标记，提前弃用不扫描后续候选；1000 组真实小例存活数组/净原生分配/净管理块归零。共享必要 250＋2 项通过，精确树同 252 项见提交正文；真实 2 万次窗口字节一致，完整 40 万次结果除时间外一致且双审计通过。首次单次 248.236088 秒、峰值 1754677248 字节仅附带诊断，不替代 7.2 热性能结论。计划 v0.18、`stage7.md` 已记录；阶段 7 收口，下一项新 8.1 公共批次/有界工作区和不变参数复用，尚未并行。配置/库、用户文件和历史交期问题保持，Ponytail 关闭。

- 统一数值 7.2（2026-09-17，实施前 `927f650`）：完整 40 万次与冻结基线公开结果、构造/阶段/扣额/接受全流一致；新旧各首次＋3 热样本均双审计通过。新/旧热中位数 158.343551/153.324088 秒，新慢 3.27%，峰值驻留 2667446272/769359872 字节；不称提速或一分钟通过。预热后 2 万窗口定位准备/桥接/重复分配校验成本，且真实原生生成器提前弃用小例 1000 组数组未释放，耗尽为零；无 `close()`，受控标记恢复后立即返回小例可归零。计划 v0.17、`stage7.md` 记录，下一项新增 7.3 修复六类生成器生命周期，再进入新 8.1，暂不并行。只释放已暂停生成器，不扫描剩余候选、不改顺序/额度；源码尚未修复。共享工具保护 2 项/0.62 秒，精确树见提交正文；配置/库及用户文件、历史问题不动，Ponytail 关闭。

- 统一数值 7.1（2026-09-17，实施前 `6b457b9`）：全流程旧准备失败桩/传递调用保护与双审计通过；追踪和单条/8 条对照工具已改挂公共消费，禁止空采样假通过。共享累计 4078 项/215.43 秒；正常前缀后 2 万次真实窗口的 5560 个完整消费逐项评分、轨迹、资源、完整返回一致，双审计通过。精确树见 `stage7.md`/提交正文，计划 v0.16；下一项 7.2 完整 40 万次同标准及无重型探针性能组。诊断“正式编辑数”只随接受产生，不能为恢复旧物理计数构造拒绝对象；业务额度/接受/结果不放宽。尚未实施原生批次/并行，历史交期问题及保护范围保持，Ponytail 关闭。

- 统一数值 6.2（2026-09-16，实施前 `f631653`）：精修和回收直达公共私有候选/评价/消费发布，撤除生产旧准备、旧索引及描述适配；清理后/原样尝试、原扣额、容量重试、取消及首改善作废保持。真实 2 万次窗口与冻结 JSON 字节一致，新增 5560 完整评价/18 接受；物理 5601 预计算/41 作废独立统计，18 正式物化、旧布局调用 0。共享必要 219 项/121.49 秒，精确树见 `stage6.md`/提交正文，计划 v0.15；阶段 6 完成、下一项 7.1。当前是有界逐条公共数值预准备，不宣称整候选已编译并行；新完整质量/性能组及历史交期问题未收口。保护范围和 Ponytail 关闭保持。

- 统一数值 6.1（2026-09-16，实施前 `d8de4dd`）：精修枚举/重点及交期键/平铺结构归属已借用权威数组，实际编译生成器保存原生游标，深扫每 256 步返回取消检查；原 4/64 轮转、直接优先、移动交换交替和链共享推进保持。真实正常前缀后 2 万次窗口，19564 个生成提案全流摘要、扣额/接受/身份及结果与冻结基线字节一致；共享必要 187 项/120.56 秒，精确树见 `stage6.md`/提交正文。计划 v0.14，下一项 6.2；本项到旧准备仍有纯描述元数据映射，6.2 必须撤除，不称精修修复已经统一。冷诊断不作性能结论；配置/库、用户文件、历史问题及 Ponytail 关闭保持。

- 统一数值 5.2（2026-09-16，实施前 `39d956c`）：两类拆单已用公共私有准备，保留成功准备后扣额、再期锁/评价的原边界；未评价/过期结果禁止消费发布，扩容不多扣额。拆后只重放一次已统一首轮。真实完整前缀与冻结基线字节相同：134226 检查/1128 完整评价/442 接受，两类拆单各 1、重放 1；精修前 22 链/360 吨虚拟、零禁止/欠重。共享必要 190 项/111.54 秒，精确树见 `stage5.md`/提交正文；计划 v0.13，阶段 5 完成、下一项 6.1 精修描述与结构归属。冷编译/追踪不作性能结论，完整双审计/并行及历史交期问题仍未收口；保护和 Ponytail 关闭保持。

- 统一数值 5.1（2026-09-16，实施前 `d7f9715`）：首轮整链、真实节点移动、虚拟填充和整链调序已接公共描述/候选计算/原序消费发布；链重、到期锚点、反向轮廓及直接插入前检读权威数组，原动作/扣额/首改善保持。拒绝路径不调用旧 `_layout()`、正式资源扩展或候选方案；接受才生成原身份。真实首轮完整扣额摘要、361 次接受及四个结束快照与冻结基线字节一致：110964 检查/848 完整评价/1920 桥接需求/16 个已接收虚拟。共享必要 175 项/114.76 秒，精确树见 `stage5.md`/提交正文，计划 v0.12；下一项 5.2 两类拆单与唯一重放。冷编译/带探针对照不作性能结论；后置精修、并行与历史交期问题未收口，保护及 Ponytail 关闭保持。

- 统一数值 4.2（2026-09-16，实施前 `74e2d12`）：初始路径按原追加/截链条件编译扫描，借用连续数组切片，共用链绑定/换序/最终平铺及权威链规则；每段最多 256 单，取消不发布部分方案。真实 531 单从图到 30 条初始链的数组/顺序/编号/期/评分/身份与冻结基线 JSON 字节一致，候选额度 0。共享必要 151 项/64.48 秒，精确树见 `stage4.md`/提交正文，计划 v0.11；阶段 4 完成、下一项 5.1 首轮四类局部搜索。冷编译诊断不作性能结论，尚未切换搜索/拆单/精修；保护范围与 Ponytail 关闭保持。

- 统一数值 4.1（2026-09-16，实施前 `1463fd8`）：有序邻接直接写数组，共用权威边判定；原随机源/调用及稳定键序保持，广度分层队列、深度增广栈/游标与路径输出已编译数值化，深扫 256 步有界续算。真实 531 单图/匹配/路径及身份与冻结基线 JSON 字节相同，140715 检查/32536 允许/17 路径，候选额度 0。共享必要 132 项/53.62 秒，精确树见 `stage4.md`/提交正文，计划 v0.10；下一项 4.2。本项只到路径覆盖，不含初始链/搜索或性能验收；保护和 Ponytail 关闭保持。

- 统一数值 3.3（2026-09-16，实施前 `93b2e25`）：原序消费、第二修复原扣额及错误延迟已实现；首改善才按原事件物化正式资源/祖先、构造方案执行守恒/期锁、同内核明细复核后唯一 `NumericSearchState.commit()`。失败不改当前方案/接受序号，不往任务扩展缓存写入失败候选；真实已消费评价计数仍记录。新旧接受指纹/资源/轨迹一致，首轮整链调序空行轨迹与精修移动行轨迹用显式策略保留；共享数值/架构 310 项通过 134.32 秒，精确树见 `stage3.md`/提交正文，计划 v0.9。阶段 3 小例闭环，下一项 4.1；主流程未切换，未跑新完整性能/并行，所有保护及 Ponytail 关闭保持。

- 统一数值 3.2（2026-09-16，实施前 `2996e37`）：公共单次候选入口已支持原 14 类动作，数值片段/链映射组合公共修复和资源，不调用旧阶段准备或正式任务/方案扩展。清理与原样每次只计算一个显式变体，不自行扣额；阶段禁止类型、桥接/变化链上限等政策显式传入，不能统一成零违规拦截。小例逐项对照及失败/容量/取消/拒绝无正式构造见 `stage3.md`，集中及精确树见提交正文，计划 v0.8；下一项 3.3 消费发布。主流程未切换，Python 仍保留有界拼接/资源步骤调度，未宣称全候选已无对象编译、已并行或已提速。保护项、独立交期问题和 Ponytail 关闭保持。

- 统一数值 3.1（2026-09-16，实施前 `1519292`）：基础/私有字段使用双段数组视图，规则同函数编译读取，不拼接任务列；正式平铺方案作为零变化视图调用同一完整评价内核。候选视图直接读原链/变化链，不拼全方案；链事实、跨虚拟/方案规则、交期及九级公式保持，明细同原语重算。缓存只复用权威基准且核对链身份/期/节点；加强视图全字段归属与有效区保护。必要数值/架构集中与精确树见统一数值 `stage3.md`/提交正文，计划 v0.7；下一项 3.2。主流程候选准备/资源扩展/发布尚未统一，旧热路径未退出，不称全流程完成或提速；保护项、历史问题和 Ponytail 关闭保持。

- 统一数值 2.2（2026-09-16，实施前 `6f64c98`）：共用模板派生、私有拆片/分隔材/资源事件已实现；最大片重切分、按片序累计四舍五入分毫秒、两类授权目标期及原事件分组保持。中间乘积超过 int64 用精确整数除法分解，不改存储/舍入口径；分隔材继续调用原权威链规则按禁止数/严重度/平滑度择优，只投影三行、不扩展整任务。真实 5 个资源案例（同期间 2、未来归还 3）全部字段与事件匹配冻结旧实现，不是完整求解接受了 5 次拆单。集中及精确树见统一数值 `stage2.md`/提交正文；计划 v0.6，阶段 2 完成、下一项 3.1 公共视图与权威评价。主求解/接受发布尚未切换，不称提速；其余保护保持，Ponytail 关闭。

- 统一数值 2.1（2026-09-16，前置修复 `795a107`）：原型快速掩码及正式/私有边判断已共用原权威公式；直接/单材/双材顺序和严格平滑同分规则保持，扫描有界续算，选中才写私有全字段及原扩展事件。冻结 531 单/27 原型共 2124 组桥接对照，372 直接、982 无解、676 单材、94 双材，选择及全部新节点/派生列字节一致。必要集中及精确树验证见统一数值 `stage2.md`/提交正文，计划 v0.5；下一项 2.2。主求解未切换到私有资源，不称性能改善；六项保护、旧残留和历史交期问题保持，Ponytail 关闭。

- 统一数值阶段 2 前置修复（2026-09-16，实施前 `473596c`）：非自适应温区路径在没有单材解时读取未初始化 `best`，冻结旧源码已独立复现。用户明确允许修复：仅初始化为空，恢复原双材枚举和无解返回，不改规则/顺序；GQGA4 自适应路径不受影响。独立回归及精确树记录见统一数值 `stage2.md` 和提交正文。本修复不算 2.1 完成；公共资源改造继续，配置/库及用户文件保护不变，Ponytail 关闭。

- 统一数值 1.2（2026-09-16，实施前 `44c426c`）：共用编译链读取/有序片段写入、反向、插删、移动交换、切合链和换序已实现；未变链借用原数组，仅变化链写私有区，切链换序只改映射。代次索引复用既有节点/原单位置权威数组，基础操作不决定规则、改期或接受。精确登记新增模块 NumPy/Numba，保护负例保持；共享阶段集中 165 项通过，精确树同范围验证见本项提交。计划 v0.4，`stage1.md` 已记录；阶段 1 完成，下一项 **2.1 统一边判定与数值桥接修复**。主流程未接新原语，未跑新性能或宣称全流程完成；其余保护及持续实施约定保持，Ponytail 关闭。

- 统一数值 1.1（2026-09-16，实施前 `52c5e82`）：公共描述、原方案/变化链视图、候选私有全字段尾部、分步资源事件及容量/失效代次已实现；实际 Numba 只读访问见证通过。动作原枚举与原六状态码移至公共状态层、原搜索导入保留，同数值/身份语义不变。共享必要 156 项通过，精确树同范围验证见本项提交；证据 `apsgo_v7_unified_numeric_solver/stage1.md`，计划 v0.3。下一项 **1.2 共用链操作与代次索引**；新主流程尚未接线，不宣称全流程完成或提速。保持持续推进、中文独立提交和保护范围；Ponytail 关闭。

- 统一数值改造持续实施（2026-09-16，实施前 `2539ab0`）：用户已授权按新统一计划持续推进，必要业务/范围决策才暂停。新阶段 0 完成：生产代码未变，独立导出运行 400000 候选、78775 完整评价、486 接受、两类拆分各 1、唯一重放 1；22 链/360 吨虚拟、零禁止/欠重、双审计通过；与原 7.4 同标准完整公开结果除时间外一致。新增诊断工具只留流式摘要和前后有界样本，阶段调用/扣额/资源/随机与计数含义见 `docs/implementation/evidence/apsgo_v7_unified_numeric_solver/stage0.md`；诊断 189.3497125 秒不是性能验收。计划 v0.2，下一项 **1.1 共用描述、方案视图和私有工作区**，新公共层尚未接线。原 8.2 暂停，历史交期退步不关闭；正式配置/SQLite、四份用户文件与历史证据不动；Ponytail 保持关闭。每功能单元必要验证、精确树同范围复验、中文独立提交，不逐编辑跑全量。下方“只写计划/待授权”为历史记录。

- 统一数值实现计划入口（2026-09-16，编写前 `39a50f8`）：已按统一设计 v0.1 建立 `docs/implementation/apsgo_v7_unified_numeric_solver_implementation_plan.md` v0.1，为后续唯一实施入口。顺序为阶段 0 冻结全流程基线，阶段 1～3 建立公共数据/链操作/私有资源/评价与统一候选事务，阶段 4～6 接通构造、首轮、拆单唯一重放和精修回收，阶段 7 完成全流程串行一致性及耗时，阶段 8 再试点同内核数值批次与并行，阶段 9 清理交付。**本轮只写计划并同步入口，新计划所有代码步骤均未开始；用户授权后从阶段 0 开始，不直接恢复旧 8.2。**各阶段须标全名，新 8.1/8.2 与原计划的同号历史步骤不同。必要测试按功能单元与检查点集中进行，不逐次编辑重复全量；每单元精确暂存、同范围导出复验、中文 `#feat`/`#fix` 独立提交。公共实现必须覆盖所有阶段，不以精修窗口替代全流程闭环；同标准候选顺序/额度/首改善/资源身份/结果保持，旧交期质量问题单独未关闭。正式配置/数据库、四份用户文件、历史证据与外部接口不动，不应用 Ponytail 限制。设计仅补执行链接，旧计划仅补继任入口；以下“下一步另编计划”等保留为历史状态。

- 统一数值实现设计入口（2026-09-16，核验前 `64eb929`）：用户要求清空上一轮未提交草稿，按**公共数值计算层**重写。新设计为 `docs/design/apsgo_v7_unified_numeric_solver_design.md` v0.1；统一权威字段、链操作、连接修复、私有资源、候选计算和评价，各阶段只保留自身算法控制，不能仅各自使用 NumPy 而保留多套候选准备。首轮、拆单/唯一重放、后置精修和回收共用候选计算入口；构图和初始构造共享原语，不强套首改善事务。数值标准、规则、九级顺序、候选有效顺序、额度和首改善不变，同标准行为必须等价。**当前代码完成至 8.1 `fd5a5f7`；`64eb929` 仅为 8.2 扩围文档；原 8.2.1、8.2.2、8.3、9.1 均未开始，现暂停。**原计划 v0.34 第 9.3 节登记检查点，原设计只标注后续架构入口；下一步据新设计另编实施计划，先全流程统一串行闭环再评估候选并行。本轮仅四份文档，不改代码、不运行求解；保护四份用户文件、正式 YAML/SQLite、阶段 8 证据和独立交期问题。历史交期退步保持未解决；**不应用 Ponytail 任何模式限制**。以下局部试点“当前/下一项”均为历史状态，不再作为执行授权。

- NumPy 阶段 8.2 试点扩围文档（2026-09-16，修订前 `fd5a5f7`）：用户确认不再只并行完整评价，将试点前移到**后置结构精修候选修复、构造与评价**。已有 2 万次精修剖析窗口中修复构造累计 9.644905 秒、桥接含资源 7.572454 秒，批量评价仅 0.457257 秒；子调用互有包含，不可相加，不作为完整性能结论。设计 v0.18、实施计划 v0.33 为当前入口：**8.2.1 先数值串行修复构造，8.2.2 再同内核候选并行，8.3 以当前/新串行/同内核并行 1/2/4/8 线程分组对照决定采用**。共享基础列只读、私有资源隔离，拒绝路径不反复复制整任务/生成资源对象与指纹，接受时按原扩展顺序物化正式身份；原型择优、两种修复尝试顺序及各自原扣额点、4/64 轮转、首改善和双审计均不变。只并行独立结构描述，不扩围首轮/拆单/重放；无端到端并行收益可保留经验证的新串行。**本轮仅同步设计、实施计划和本文件；不改代码、不运行新实验，8.2 尚未实施。**下一代码入口为 8.2.1，分别提交；四份用户文件、正式 YAML/SQLite、历史证据均不动，Ponytail 关闭。历史交期退步保持独立未解决；以下“本轮停在 8.1”等均保留当时含义。

- NumPy 阶段 8.1（2026-09-16，实施前 `96aa4e7`）：按用户新授权实现后置精修扁平候选批次与同内核批量串行；默认 8 条描述，保留两种修复尝试及原序扣额/首改善，候选资源尾部隔离，正式编号仅接受时确认。批量循环调用唯一 `evaluate_kernel()`，未启用线程或 `prange`。共享必要回归 177 项通过；真实前缀消费 134226 次后，再比较 20000 次精修窗口，5560 个完整消费候选的数值输出/轨迹/编号与最终公开结果一致、双审计通过；实际 1237 次批量调用、5599 次预计算、5560 次消费、39 次作废。窗口带额外逐项重算，不作为性能结论。设计 v0.17、计划 v0.32、`stage8.md` 为当前入口，精确树验证及提交见本项 Git 记录。**本轮停在 8.1，不进入 8.2，不重跑 40 万次；交期质量退步独立问题保持未解决。**正式配置/库、输入与四份用户文件不动；Ponytail 关闭。以下停在 7.4 的说明保留为当时授权。

- NumPy 阶段 7.4 串行接线完成（2026-09-16，实施前 `ea93072`）：规则、资源、交期及九级评分统一调用 `_numeric_kernel.py` 的实际 Numba 编译内核；普通拒绝候选只返回数值数组，接受/审核从同原语生成明细，未变链在原生内核复用。首轮、动态桥接/填充、两类拆分及唯一重放、后置精修与普通连接材回收均已接线；Python 保留候选生成、覆盖准备、额度、取消及按序接受，不宣称整个搜索流程均已编译。固定 40 万次对照，旧版 233.390746 秒，新版首次 154.426290 秒、同进程第二次 138.965682 秒；完整公开结果除实测时间外一致，准备请求/交期报告字节一致，400000 候选、78775 完整评价、486 接受、两类拆分各 1 次、22 链/360 吨虚拟、零禁止/欠重及双审计均保持。共享回归 4239 项通过；精确暂存树同范围验证与提交身份见本项 Git 记录。设计 v0.16、计划 v0.31 和阶段 7 证据已同步。**按用户授权停在 7.4，不进入阶段 8；一分钟目标未达到，批量候选与并行尚未实施。**正式 YAML/SQLite 和四份既有用户文件不动，Ponytail 关闭。以下“未接生产/下一项 7.x”均为当时阶段记录。

- 阶段 7.3（实施前 `3c13b78`）：完整汇总直接返回数值数组，明细构造器失败桩下仍可评价；未变链在原生内核中复用。接受/审核明细由同内核生成，容量不足外层扩容后重跑，汇总/明细逐项核对。尚未切换生产搜索；下一项 7.4 接通后集中验证并停止，不进入批量/并行。

- 阶段 7.2（实施前 `a3e9e8a`）：完整整数规则/资源/交期/九级评分内核已实际 Numba 无对象编译；固定小样例和 256 个实际精修候选逐项匹配冻结参考。尚未接生产、未宣称性能改善；下一项 7.3 为汇总、延迟明细及未变链数值缓存，随后 7.4 串行接线并停止。阶段记录见 NumPy 实施计划和 `stage7.md`。

- 当前阶段 7 授权（2026-09-16，实施前 `a03fdf5`）：按 NumPy 设计 v0.15、实施计划 v0.30 持续实现 7.1～7.4，完成完整数值评价串行接线后停止，不进入阶段 8。Ponytail 已关闭。7.1 已冻结测试侧整数迁移参考及真实精修窗口，24 个小方案和 256 个实际候选逐项一致；下一项 7.2 为权威编译内核。下方“阶段 6 完成直接清理”已由新计划取代；不得改规则/九级顺序/候选顺序/额度/首改善行为，不动正式配置和数据库及用户文件。独立提交与必要集中验证保持。

- NumPy 阶段 6.2 完整候选评价并行适用性试验（2026-09-16，实施前 `6a89312`）：从冻结真实请求截取同一权威 `evaluate_numeric_overlay_candidate()` 的 256 组实际输入，分别用 1/2/4/8 个工作线程预热一次并测量 5 次；全部规则结果、违规、资源、交期和九级评分逐项一致。1/2/4/8 线程经过时间中位数为 0.369328/0.376141/0.382813/0.396968 秒，2/4/8 线程分别慢 1.84%/3.65%/7.48%，CPU 时间分别多 3.27%/10.89%/20.08%。本机报告 12 个逻辑处理器、Numba 可用 12 线程，但当前完整评价入口及核心仍是普通 Python 函数，不是可直接由 Numba `prange` 调度的编译内核；不得为强行并行另写第二套规则/九级评分实现。按收益门槛不接入生产线程、不运行 40 万次多线程组，阶段 6.1 的 8 条描述批次继续串行消费；生产源码/测试与 `HEAD` 哈希一致，复用阶段 6.1 的 3869 项双树回归。阶段 6 完成，下一项 7.1 清理与收口；一分钟目标仍未达到。正式 YAML/SQLite、规则、评分、候选顺序、额度、资源编号及外部接口不变。

- NumPy 阶段 6.1 有界有序候选批次（2026-09-16，实施前 `ab4969e`，代码提交 `690de02`）：后置结构精修现在由主线程按既有候选顺序最多预取 8 条描述，仍逐条调用同一数值候选入口；来源游标只在描述实际消费后推进，首个改善接受后同批旧方案描述立即作废，取消或停止前未消费描述不扣逻辑额度、不提交资源或正式编号。批次大小为内部试验常量，不增加 HTTP/规则/求解配置；动态桥接和第二修复变体仍在原串行候选事务内，尚未开启线程或 Numba 并行。逐条 1 项与批次 8 项的 1000 次有接受样本在方案、九级评分、8 个接受序号、资源序号、完整评价数、候选额度和停止原因上完全一致；冻结真实请求 20000 次对照与 `ab4969e` 的结果/确定性/轨迹指纹、质量、全部主要计数和停止原因一致，单次 3.045050→3.079705 秒，不宣称提速。专项 15/35 项、共享树及精确暂存树累计各 3869 项通过；精确树 `87ecf37729e15c0ea2d15019a5229c47d05716ec`。阶段 6.1 完成，下一项 6.2 才试点同内核多线程；正式 YAML/SQLite、规则、九级评分、40 万逻辑额度及接口不变。

- NumPy 步骤 5.5 完整串行质量与耗时收口（2026-09-16，实施前 `aba0cce`）：固定同一提交、输入、种子、九级目标和 400000 次逻辑候选额度完成 3 次独立预热运行，经过时间分别为 234.085026、235.305340、233.441435 秒，中位数 234.085026 秒；CPU 时间中位数 233.470224 秒。三次结果指纹、确定性运行指纹、轨迹指纹、全部计数、交期报告字节和双审计一致，均消费 400000 次候选、78775 次完整评价、接受 486 次结构调整及两类拆单各 1 次，得到 22 链、360 吨虚拟材、零禁止和零欠重。九级质量为 `(0,0,0,0,541.2236111111111111111111111,1181514.034911111111111111111,11606,360,22)`；旧欠 96 单/5386.03 吨于 2026-06-23 13:13:24.885 前完成，本月新增晚交 24 单/1182.86 吨。40 万次比同树 15 万次继续改善旧欠清空、累计负担和宽差，虚拟重量/链数/新增晚交数量不变。数值串行搜索占完整耗时约 98.9%，一分钟目标未达到；步骤 5.5 完成，下一项是阶段 6.1 的有界有序候选批次，尚未实施并行。正式 YAML/SQLite、规则、评分、候选顺序、额度和对外接口不变。

- NumPy 步骤 5.5 候选资源工作区复用（2026-09-16，实施前 `605e934`）：同一当前任务、同一动态虚拟材/拆片会被不同结构候选重复探测，原先每次都复制全部数值列并重算任务、规则和九级评分身份。现直接为不可变 `extend_resource_workspace()` 增加 128 项有界最近使用复用；100000 次诊断中 3407 次调用只有 813 种输入，128 项覆盖 2540 次短期重复。固定 15 万次带剖析 A/B 的三类身份、全部计数、九级评分、交期报告和双审计一致；实际任务扩展 15484→7467 次，经过时间 38.064036→35.125718 秒，CPU 37.963624→35.041390 秒，峰值内存 116981760→143130624 字节。64 项对照为 35.708884 秒/137166848 字节，综合耗时和约 6 MB 增量后保留 128 项。数值专项 99 项、共享树及干净导出累计各 3866 项通过；下一步独立提交，再复核 40 万次前的新热点。规则、评分、候选顺序、额度、配置和数据库不变。

- NumPy 步骤 5.5 边可行性快速判定（2026-09-16，实施前 `754ef8f`）：构图、候选连接和虚拟桥扫描只需要“允许/禁止”结论，原先仍为每条边构造完整违规对象；现由一份共用的 `_edge_rule_allowed()` 承担边规则布尔语义，详细评价继续复用该判定并只在需要时生成违规和严重度。全部任务节点两两组合的快速/详细差分见证通过。固定 15 万次带剖析 A/B 的结果、确定性运行、轨迹三类指纹、全部计数、九级评分、交期报告和双审计一致；经过时间 45.561432→38.064036 秒，CPU 45.414914→37.963624 秒，构图 3.670774→1.158368 秒。数值专项 98 项、共享树及干净导出累计各 3865 项通过。下一步独立提交，再依据新热点决定是否继续处理动态任务扩展；规则、评分、额度、配置和数据库不变。

- NumPy 步骤 5.5 规则身份模板复用（2026-09-16，实施前 `68b20f1`）：候选追加虚拟材或拆片后，规则程序身份原先每次都把不变的 17 条编译规则重新规范 JSON 编码；现按规则集身份和不可变编译规则缓存一份规范编码模板，每个候选仍将新的任务指纹代入同一 JSON 字节并重新计算 SHA-256。单测直接与原 `fingerprint({...})` 公式核对初始及重绑定身份。固定 15 万次带剖析 A/B 的结果、确定性运行、轨迹三类指纹、全部计数、九级评分和双审计一致；经过时间 51.906655→45.561432 秒，CPU 51.825093→45.414914 秒，规则程序重绑定约 6.21→0.39 秒。数值专项 97 项、仓内累计 3864 项通过。下一步继续处理虚拟桥边判定与候选资源扩展，不改变任何身份算法、规则、评分、额度、配置或数据库。

- NumPy 步骤 5.5 交期时钟向量化（2026-09-16，实施前 `d01e529`）：完整候选评价原先用 Python 循环逐节点累加工时，并逐原订单计算晚交、等待秒数和吨秒负担；现对相同前后缀、累计工时、原单完成位置、晚交掩码和四舍五入秒数使用 `int64` NumPy 数组，候选变化段总工时及最终吨秒总和仍以 Python 宽整数精确汇总后执行 `int64` 校验。固定 15 万次带剖析 A/B 的结果身份、确定性运行身份、轨迹身份、全部计数、九级评分和双审计逐项一致；经过时间 59.250986→51.906655 秒，CPU 59.082988→51.825093 秒，交期评价累计耗时约 10.01→0.999 秒。1.5 秒→2 秒、4.5 秒→5 秒的四舍五入见证通过，数值专项 97 项、仓内累计 3864 项通过。下一步继续处理虚拟桥候选资源扩展与身份编码热点，再决定 40 万次正式对照；规则、评分、额度、配置和数据库不变。

- NumPy 步骤 5.5 链序排序复用（2026-09-16，实施前 `c769458`）：结构精修原先在每个原订单候选流开始时重复执行相同的链交期排序，15 万次剖析中 `_chain_order()` 被调用 18834 次、累计约 6.25 秒。现只在片段动作族需要时按当前方案代次计算一次，并由该动作族的全部原订单流共享；其他动作族不再计算无用链序。修改前后固定 15 万次带剖析运行的结果身份、九级评分、150000 次候选、5469 次完整评价、460 次接受、两类拆单和双审计逐项一致；耗时 65.854874→59.250986 秒，CPU 65.852647→59.082988 秒，`_chain_order()` 已退出热点榜。专项 12 项、数值专项 96 项、仓内累计 3863 项通过；架构门禁同步登记 `_numeric_refinement` 和 `_numeric_resources` 为 NumPy 数值内核模块。下一步继续处理候选资源扩展、虚拟桥和交期评价热点，不提前宣称 40 万次或一分钟目标通过；规则、评分、额度、配置和数据库不变。

- NumPy 步骤 5.5 候选通道直接生成（2026-09-16，实施前 `7598408`）：重点与常规结构动作不再先生成完整组合后由 `lane_filtered` 丢弃；节点、片段、切链和整链调序在进入目标位置/区间深层扫描前按实际涉及的原订单直接决定所属通道，存活候选顺序与原过滤结果保持。修改前后固定 12 万次的结果身份、确定性运行身份、轨迹身份、全部计数、九级评分和双审计逐项一致；耗时 13.446534 / 13.794469 秒属正常波动。固定 20 万次现可在 109.666584 秒完成，200000 次候选、19550 次完整评价、469 次接受，零禁止、零欠重、22 链、360 吨虚拟，两个拆单与双审计通过；此前同边界超过 7 分钟仍未完成并人工中止。数值专项 96 项通过。下一步继续按 20 万次新热点定位，不提前进入并行或宣称一分钟目标/40 万次验收通过；规则、评分、额度、配置和数据库不变。

- NumPy 步骤 5.5 候选扩展任务评价复用（2026-09-16，实施前 `5f644a5`）：`extend_numeric_task()` 现在记录可信的只追加任务祖先身份；候选仅追加虚拟连接材或拆片资源且规则/九级目标不变时，评价器复用未变化链结果和链事实，只重算变化链、当前任务节点指标、交期及方案级汇总。非只追加任务继续完整评价。小型差分见证中扩展候选只重算变化链，结果与权威全量评价逐项一致；数值专项 96 项通过。固定 12 万次探针 13.446534 秒，120000 次候选、1051 次完整评价、368 次接受、2 次拆单（同期间和未来借入归还各 1），核心/应用双审计通过，状态为允许偏差可发布。固定 20 万次探针在超过 7 分钟后人工中止，栈位于重点/常规通道的 `lane_filtered` 生成后过滤，不得记为完整样本；下一步改为候选在生成时直接按通道归属，禁止靠并行掩盖该串行热点。正式配置/库、规则、评分和额度不变。

- NumPy 步骤 5.5 拆片计划期保护（2026-09-16，实施前 `e74f15d`）：受控拆单片段必须始终位于拆单授权的目标计划期。后置结构精修与首轮/拆单的两个统一候选事务边界都在完整评价前检查全部已调度拆片，无效候选不进入完整评价或提交。`NumericPlan.build()` 保留构造未来借入拆分归还过程所需的临时中间布局，不单独承担事务规则；20 万次探针曾证明把期锁放在通用构造器会在 111455 次检查处把第二次拆单候选误报为输入错误，现已纠正。未来借入拆分见证验证最终片段均处于授权目标期，精确暂存树数值专项 95 项通过。下一步处理步骤 5.5 已确认的候选扩展任务全方案评价放大，再重跑 40 万次；规则、评分、候选顺序、额度、拆单授权本身和正式配置/库不变。

- NumPy 结构精修步骤 5.5 预检（2026-09-16，实施前 `293c697`）：节点/片段结构动作族原先会为同一次动作族扫描中的每个原订单重复构造位置优先级和片段区间拥有者；现按动作族扫描只构造一次只读 NumPy 归属索引并由各原订单流共享，不改变结构候选、原顺序、额度或评价。固定 10 万次请求的 100000 次候选、788 次完整评价、301 次接受、21 条链和九级评分与历史同标准结果一致，耗时 14.428400→13.756 秒；数值专项 94 项通过。首次 40 万次运行完成候选搜索但最终审计发现 2 个拆单片段离开授权目标计划期，同时后半程 88118 次完整评价使数值搜索耗时 3497.949 秒；该运行失败，不作为质量/性能样本。下一步先修复计划期锁根因，再处理候选扩展任务导致的全方案重复评价，之后重跑 40 万次；正式配置/库、规则、评分和额度不变。

- NumPy 结构精修步骤 5.3 回修与 5.4 复验（2026-09-16，实施前 `812a2ea`）：候选已改为按结构归属直接唯一生成。节点动作由节点位置生成，片段动作由唯一拥有位置生成；切链和同期间整链调序每条链只建立一个结构生成器，多个原订单通道只按既有每次 4 个候选的节奏推进它，保持原规范候选顺序。生产路径已删除跨来源 `seen`/字典去重和重复过滤诊断。6 单单链直接生成 10 个切链候选，不再生成 60 个后过滤 50 个；拆分片段区间无重复。独立导出的 `812a2ea` 同标准基线与当前 202 个穷尽候选逐项一致；1000 次有接受样本的 8 个接受序号、最终方案、九级评分和停止原因一致，接受 8 次仅正式物化 8 次。专项 10 项、数值专项 93 项通过；步骤 5.3 回修和步骤 5.4 回修后复验完成。下一项 5.5 为固定 40 万次完整串行质量、重复性及热点对照；本项未运行完整排程，规则、评分、额度、配置、数据库和对外接口不变。

- NumPy 结构精修步骤 5.4（2026-09-16，实施前 `0b277e8`）：后置结构精修候选使用当前方案加变化链覆盖进行评价，未变化链复用只读 NumPy 视图；拒绝候选不构造正式 `NumericPlan`，不重建全局位置、原单片段索引或正式方案指纹。覆盖评价严格改善九级评分后才构造下一代正式方案，并逐项核对九级评分、违规、规则结果、资源重量、逐链事实和交期数组一致后原子发布。数值专项 91 项通过；100 次有界拒绝样本为 62 次完整评价、0 次正式方案构造、0 次 `_layout()`，普通连接材回收接受见证的正式构造数与接受数均为 1。下一项 5.5 为固定 40 万次完整串行质量、重复性及热点对照；正式配置/库、规则、评分、额度和对外接口不变。

- [历史信息，已由上方 5.3 回修取代] NumPy 结构精修步骤 5.3（2026-09-15，实施前 `0e86c35`）：重点/常规通道在数值区间判定后共享当前方案代次的唯一配方集合；同一动作、链、区间和目标位置只进入其正确通道一次。逻辑候选额度仍在唯一配方即将执行结构/规则检查时消费；原始生成、通道过滤和重复过滤分别计数。长目标位置扫描和普通连接材扫描加入共享时间/取消轮询，不新增第二套额度。6 单单链切链见证从 60 个原始组合过滤 50 个重复，最终只送出 10 个唯一组合；取消见证在生成内部及时形成 `user_cancelled` 且不消费候选额度，专项 7 项通过。固定额度下后续轨迹允许因不再重复候选而变化。下一项 5.4 为受影响链覆盖和接受后完整方案物化；40 万完整运行仍暂停。

- NumPy 结构精修步骤 5.2（2026-09-15，实施前 `f6a4084`）：新增每个已接受方案代次只建立一次的 `NumericRefinementIndex`，直接保存只读 NumPy 链视图、链长、稳定链标识定位、原单全部片段位置，以及按当前重点原单集合生成的全局节点累计计数。候选生成、交期重点原单发现、动作排序、重点/常规通道判定和普通连接材扫描已改为读取该索引；`_recipe_real_sources()` 不再调用 `_layout()`。固定小样本的配方顺序与通道结果保持，6 项专项通过；同一 100 次有界样本 `_layout()` 从步骤 5.1 的 492 次降至 200 次，剩余调用全部位于候选结构构造/评价前，归步骤 5.4。下一项 5.3 为唯一候选归属和额度/取消边界；40 万完整运行仍暂停。

- NumPy 结构精修步骤 5.1（2026-09-15，实施前 `5c5b9dc`）：新增汇总级 `NumericRefinementDiagnostics`，按重点/常规通道及动作族区分原始组合、代次内唯一组合、通道过滤、实际送出、逻辑候选消费、完整评价、接受和完整方案构造；另记录 `_layout()` 调用次数/耗时及生成器单次推进最长耗时，不逐候选写日志。8 单/2 链/100 次额度有界样本实际生成 251 个原始组合、183 个代次内唯一组合、150 次通道过滤、100 次候选消费、57 次完整评价/方案构造、0 接受，发生 492 次 `_layout()`；证明此前大量生成/过滤/转换位于逻辑额度之外。下一项 5.2 删除生成与通道过滤对完整 `_layout()` 的依赖并建立按方案代次复用的只读数值索引；40 万完整运行仍暂停。前置桥接/调序试验已独立提交 `5c5b9dc`；规则、评分、正式配置/库、额度和对外接口不变。

- 数值结构精修根因修订入口（2026-09-15，实施前已提交基线 `7e52383`，当前分支 `codex/delivery-objective-optimization`）：用户已确认先同步[专项设计 v0.12](docs/design/apsgo_v7_numpy_numeric_search_and_calculation_design.md)和[实施计划 v0.12](docs/implementation/apsgo_v7_numpy_numeric_search_and_calculation_implementation_plan.md)。阶段 5 预检确认 `NumericPlan` 虽使用连续 NumPy 数组，候选编辑仍通过 `_layout()` 恢复完整 Python 链元组/列表；后置结构精修又在 `_recipe_real_sources()` 的原始组合通道过滤中逐项调用，而逻辑候选额度在 `next(recipes)` 返回后才扣除，因此被过滤组合及转换工作不受 40 万次额度约束。10 万次当前工作树预检为 14.428400 秒、788 次完整评价；随后 40 万次运行长时间停在该生成/过滤调用链并由用户中止，仅有输入身份和准备请求，不得记为质量或性能结果。下一步按 5.1～5.4 依次实施分层计数、按方案代次只读数值索引、候选唯一通道归属及额度/取消边界、受影响链覆盖与接受后完整方案物化；每项独立中文 `#fix`/`#feat` 提交。5.1～5.4 完成前不重新运行 40 万次完整排程，原完整对照顺延为 5.5；规则、九级评分、40 万正式额度、起排/速度、来源/重量/工时/期锁/编号及独立双审计不变。当前工作树已有静态虚拟桥接、链序评分预览、测试和测量工具改动，须先按原子提交边界核验，不与根因修复混交；两份旧并行实施材料及两份未跟踪后端技术说明继续保留、不修改、不暂存。正式 YAML/SQLite、服务、前端、发布、部署、分支合并与推送均不在本次文档同步范围。

- NumPy 数值审计与后端接线子项 4.2（2026-09-15，实施前 `94b19ae`）：显式新数值身份 `integer_physical_ms_half_up_v1` 已接入公共服务，运行清单使用 `numeric-path-cover-local-search-v1`；旧活动配置仍按旧身份运行，正式 YAML/SQLite 未迁移。最终方案/评价/谱系由数值状态恢复，核心审计从冻结输入和拆分授权重建数组并无搜索缓存重算，应用审计继续核对请求/结果及发布身份。交期报告使用同一毫秒时钟和四舍五入，包含逐节点毫秒、晚交明细和转换摘要；不兼容规则声明返回定位错误，不回落旧求解。专项 6 项、交期相关 40 项、架构/资源 103 项及共享树累计 4209 项通过，见新计划[阶段 4](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage4.md)。本条原计划直接进入完整对照，已由上方 v0.12 根因修订入口取代；当前尚不宣称正式质量、性能、Windows 或可部署通过。

- NumPy 同标准增量复用子项 4.1（2026-09-15，实施前 `a0d5c03`）：候选评价已按稳定链标识、所属期和有序节点行复用未变链规则结果及总重/工时/真实/虚拟/借用摘要，变化链仍整链执行同一数值规则。交期复用相同前缀，后缀仅在节点序列及进入时刻同时一致时复用；原单最后片段位置始终来自新方案索引。任务、规则或目标身份变化执行完整新数值评价，不回落旧对象计算。4 项专项及共享树累计 4203 项通过，见新计划[阶段 4](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage4.md)。下一项 4.2 为独立双审计、输出和后端身份；正式求解、配置/库、质量、耗时及 Windows 均未验收。

- NumPy 完整数值串行搜索阶段 3（2026-09-15，实施前 `65889ec`）：后置链内/节点/片段移动交换、切链、同期间整链调序和普通连接材回收已接入数值状态；拆分分隔材受保护，候选私有桥接沿用配置上限。重点/常规通道保持每原单 4 提案、每动作族 64 提案及首改善接受；统一入口按首轮、拆分/唯一重放、后置精修共享候选额度、取消和停止原因。共享树累计 4199 项通过，见新计划[阶段 3](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage3.md)。阶段 3 完成，下一项 4.1 为链摘要、资源和交期增量复用；正式求解接线、双审计、质量、耗时及 Windows 均未验收。

- NumPy 动态资源子项 3.3（2026-09-15，实施前 `e33a86a`）：动态虚拟材、桥接、欠重填充、同期间/未来借入归还拆分及拆后单次首轮重放已接入候选私有数值任务；拒绝不发布行或正式序号，接受时任务/规则/评分/方案/评价原子提交。共享树累计 4195 项通过，见新计划[阶段 3](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage3.md)。下一项 3.4 为后置节点/片段精修、连接材回收、4/64 轮转和完整串行主流程；正式求解入口、质量、耗时及 Windows 均未验收。

- NumPy 静态首轮搜索子项 3.2（2026-09-15，实施前 `cce9f7e`）：新增绑定任务、方案指纹、代次、逻辑候选序号和稳定链/节点身份的数值候选描述；整链直接合并、真实节点移动和整链调序共用唯一完整数值评价及严格九级首改善接受点。动态桥接所需候选只计数并留给 3.3，不把不可直连位置永久删除；虚拟填充、动态节点、拆分/重放尚未接入。1525 项集中检查通过；下一项 3.3，正式求解入口仍未切换。

- NumPy 初始构造子项 3.1（2026-09-15，实施前 `72b3d31`）：整数连接图、压缩邻接表、确定性最大匹配/最小路径覆盖及直接追加或切链的初始方案已实现，1519 项集中检查通过，见新计划[阶段 3](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage3.md)。冻结 531 单检查 140715 条边、允许 32536 条，形成 17 条覆盖路径和 30 条初始链；与同输入旧流程的连接边集合一致，精确整数排序取代旧浮点并列后允许邻接顺序和路径变化。候选额度仍为 0 次；正式搜索、配置/库和服务未切换。下一项 3.2 为数值候选编辑、接受和首轮搜索。

- NumPy 数值评价子项 2.2（2026-09-15，实施前 `11792e0`）：全量整数评价、毫秒生产时钟、原单最后片段完成归属和已确认九级评分已实现；1310 项集中检查及冻结 531 单固定方案重评通过，见新计划[阶段 2](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage2.md)。新投影名称与旧语义分开，新编译器拒绝旧投影；正式 SQLite/YAML 和当前生产服务未切换。阶段 2 完成，下一项 3.1 为数值连接、遍历与初始构造；尚不宣称完整排程、质量、耗时或 Windows 验收。

- NumPy 数值规则子项 2.1（2026-09-15，实施前 `c930fd3`）：全部 20 类注册规则均有显式编译，冻结 GQGA4 的 17 条启用规则完成整数边/链/静态方案/拆单资格判定；1174 项集中检查及 531 单静态扫描通过，见新计划[阶段 2](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage2.md)。任务、规则程序与方案身份绑定，启用规则不静默跳过；交期生产时钟和九级评分仍归下一项 2.2。新内核尚未接入正式搜索，不宣称质量、耗时或 Windows 验收；配置/库、服务和前序无关文件保持。

- NumPy 阶段 1 数据基础（2026-09-15，1.2 开始 `19479aa`）：权威列、原单/原型/拆分组表、分类/优先级及全部片段位置索引完成，108 项集中检查与 531 单/27 原型物理还原和毫秒转换检查通过，见新计划[阶段 1](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage1.md)。下一项 2.1 规则编译。新数值基础尚未接入生产搜索；动态授权/唯一接受、全量评分及独立双审计仍按后续阶段实施，不宣称新算法质量或耗时改善。旧服务、配置/库和工作区保护不变。

- NumPy 数值子项 1.1（2026-09-15，开始 `0a7d3c4`）：单位/毫秒/四舍五入/拆片工时分配/范围与身份基础已实现，42 项聚焦检查通过，见新计划[阶段 1](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage1.md)。尚未接入正式求解；下一项 1.2 权威列和索引，随后集中检查。阶段 0 已提交 `0a7d3c4`。保持前序文件、配置/库、服务和输入，持续按新计划推进。

- NumPy 权威数值实施授权（2026-09-15，开始 `3a43ae8`）：用户授权按新计划持续推进，不以最小改动偏好缩减范围。阶段 0 已完成请求/规则加载、两份旧结果回读及字段/20 类注册规则/动作/旧路径覆盖清单，见[阶段 0](docs/implementation/evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage0.md)。下一项 1.1 数值标准与转换，1.2 后集中必要检查；不在每个小步骤跑全量。既有工作区两份脏文档、两份未跟踪技术说明、YAML/SQLite、原诊断与运行服务保持。各提交精确白名单、中文独立提交，不自动部署/迁库/合并/推送。以下仅文档/等待授权的条目为历史边界。

- NumPy 权威数值搜索实施计划入口（2026-09-15，编写前 `d9ea03d`）：已创建[实施计划 v0.1](docs/implementation/apsgo_v7_numpy_numeric_search_and_calculation_implementation_plan.md)，[设计 v0.3](docs/design/apsgo_v7_numpy_numeric_search_and_calculation_design.md)仅补计划入口，数值约定仍为已确认 v0.2。阶段顺序为 0 基线、1 权威数值结构、2 全量规则/时钟/评分、3 完整数值串行搜索、4 增量/双审计/输出与后端身份、5 串行质量及耗时、6 热点候选并行、7 清理收口；阶段 0～7 均未开始，下一步须获实施授权。本次仅新增计划、同步设计必要状态与本入口，不改代码、测试、依赖、YAML/SQLite 或运行排程。计划不以 Ponytail Lite 最小改动偏好限制已确认重构，保留单字段权威值、四舍五入、九级优先级、40 万逻辑额度、4/64 轮转、来源/期锁/编号及独立审核，不保留旧数值回落；新标准内部全量/增量/串并行一致。实施按计划集中必要检查点，不要求每个小提交全量测试；每个提交单元仍中文 `#feat`/`#fix`、精确白名单和导出，中间未验提交不得部署。新声明/身份先由独立实验请求验证，不自动迁移/启用规则库；质量、当前平台一分钟目标、Windows 和正式切换分别记状态。保持上一计划及 `stage3.md` 既有改动、两份未跟踪技术说明与原诊断；不批量同步历史文档、不恢复旧并行性能组、不部署/合并/推送。以下“下一步编写计划”为历史入口。

- NumPy 权威数值搜索设计入口（2026-09-15，确认修订前 `70efdfc`）：[基于 NumPy 数值结构的搜索与计算专项设计](docs/design/apsgo_v7_numpy_numeric_search_and_calculation_design.md) 已更新为 v0.2。用户确认第 15 节全部四项约定：任务内物理量精确定点及超范围拒绝、毫秒时钟和拆片工时守恒、取消内部比较余量、逐违规百万分之一严重度聚合；随后明确统一使用四舍五入，不使用半偶舍入。所有量化点采用 `ROUND_HALF_UP` 语义，中点远离零，含工时/拆片分配、整秒评分、欠重两位及严重度六位；物理原值与重量的精确转换不因此新增舍入。总体方向仍为一字段一份权威值，搜索/排序/评分/审核同一标准，不兼容旧浮点/Decimal 轨迹、不保留旧精算回落求解器，不受 Ponytail Lite 最小改动偏好限制。新目标态取代下文旧精算及半偶约束，历史实现/证据保留；当前代码和生产行为尚未改变。下一步为编写新实施计划，本次仅同步设计与本入口，不自动实施、改配置/库或运行排程。保留九级优先级、40 万逻辑额度、4/64 算法轮转、来源/期锁/谱系与独立双审计；新标准内部全量/增量/串并行必须一致。上一实施计划及 `stage3.md` 既有改动、两份未跟踪后端技术说明保持；不批量同步历史文档、不恢复旧性能测试、不部署/合并/推送。后续实施计划须包含阶段说明、设计依据、执行记录及中文独立提交表；本次仅做文档和舍入公式核对，不运行算法累计回归。

- 数值化专项第三阶段授权（2026-09-15，开始 `98cbbf9`）：用户确认进入第三阶段，暂不考虑 Windows 性能。先整链候选的有界串行数值批次、再 1/2/4/8 线程源码对照；首批收益检查后决定节点/片段扩展，不以线程数替代收益。默认执行仍串行旧入口，不改请求、九级评分、40 万逻辑额度、精确时钟、期锁/编号和双审计；Windows 3.4 保留待验。只修改本专项生产/测试/工具、设计/计划及证据，每提交单元中文独立提交。两份无关未跟踪技术说明、YAML/SQLite、服务/发布/前端/依赖和原诊断保持；不部署、合并或推送。

- 数值化专项第二阶段收口（2026-09-15，开始 `9ad3289`）：2.1～2.4 完成，第三阶段未进入。前三项分别提交 `38c946a`、`8fa738e`、`85dcd22`，本项独立收口提交由 Git 历史标识；设计 v0.4 / 计划 v0.5，详情见本专项 `stage2.md` / `stage2_measurements.json`。最终共享/精确导出各 4110 项通过；40 万影子逐候选核验及正常两次完整对照通过，全部方案/评价/轨迹、原单精确日期、逻辑额度、编号和双审计不变。实际 804 接受、881 完整候选评价、171431 快速拒绝，零禁止/零欠重，26 链、820 吨虚拟、14954 宽差；完整构造仍有 180032 次，不能把完整评价数量下降当作全部计算省略。正常 720.471532 / 739.976318 秒，均值较第一阶段两次下降约 35.81%；一分钟未达、Windows 未验。测试与测量源码树 `834189d`，最终仅补文档证据并按路径核对源码/工具/测试不变，不再重复累计。额外发布测试的库版本 5/14 断言已在前置 `9ad3289` 复现，未改库或发布脚本。配置、库、旧结果、两份无关未跟踪技术说明、服务/前端/发布/依赖均保持；不部署、合并或推送。以下“第二阶段实施中”属于历史入口。

- 数值化专项第二阶段授权（2026-09-15，开始 `9ad3289`）：用户确认继续新版计划 2.1～2.4，第三阶段不进入。按资源/连续段复用、精确交期增量、按需完整评价、集中影子与完整重复对照顺序实施；保持原九级评分、40 万逻辑额度、候选顺序、精度、编号及双审计。各提交单元中文独立提交，测试集中必要检查点；两份未跟踪技术说明文档保持不动。以下第一阶段停止条目是历史授权边界。

- 数值化专项第一阶段收口（2026-09-15，开始 `dde6609`）：阶段 0 与第一阶段完成，按用户要求停止，第二阶段未进入。设计 v0.2 / 计划 v0.3；共享与精确导出各 4095 项通过，2560 个原/新提案两轮续访同序。`stage1_full_01` / `stage1_repeat_01` 均为 400000 候选、172312 完整评价、804 接受，方案/评价/全部轨迹/精确日期及计数、编号一致并与原版等价，双审计通过；零禁止/零欠重，26 链、820 吨虚拟、14954 宽差。本轮原版 1126.143692 秒，新版 1123.220659 / 1152.119645 秒，平均多约 1.02%，未证实稳定提速，一分钟未达。布局准备 0.500419 / 0.535594 秒、805 次、峰值目录 576 行；批量读取与逐边命中如实分列。测量及导出测试树 `a583244b88c60feb3dedc93a7f3ae22d998a025f`，最终只补文档证据并逐路径验证源码/测试未变，不为此再跑第三遍累计。见本专项 `stage1.md`、`stage1_checks.json`、`stage1_measurements.json`；保持配置/SQLite/旧产物/服务/发布/前端不变，不合并或部署，后续 2.1 须另获授权。

- 数值化专项 1.2（2026-09-15，开始 `0f91f9e`）：轻量描述绑定原位置配方、代次/序号/保护模式与实际节点，原函数仍组装并完整评价；连接缓存改完整语义整数键、已知边批量标记，未知原游标查询，直接/延后组及取消轮询不变。位置索引已接后置调度；切链/回收/拆单/桥接仍串行原守卫。159 项相关检查通过，下一项 1.3 累计及完整重复；正式配置/库不改，第二阶段未进入。完整入口对象组装、描述还原及数值布局统计只加诊断工具，不改公共指标身份。

- 数值化专项 1.1（2026-09-15，开始 `80d1bc1`）：只读物理列/缺值和可投影标记、整数行/偏移/位置与全部拆片索引已接线；重量/工时仍保留精确原值，原完整评价不变。视图在接受前准备、唯一提交后替换，拒绝不提升；4 项薄检查通过，下一项 1.2，集中回归和完整重复仍留 1.3。本轮止于第一阶段。工作中出现的 `docs/apsgo_v7_backend_technical_guide.md/.html` 为非本任务未跟踪文件，保留不改、不提交。

- 数值化专项实施授权（2026-09-15，开始 `7964d9d`）：用户仅授权新版计划阶段 0～1，第一阶段集中验证与完整对照后停止，不进入第二阶段。阶段 0 请求身份、七文件摘要、原两次完整方案/评价/轨迹/原单日期对照通过，见新版计划 v0.2 及本专项 `stage0.md`；现场未优化对照运行单独保存，未完成前不宣称性能。复用既有评价与候选工具；每个提交单元中文 `#feat`/`#fix`，测试集中于必要检查点，不每小步全量重跑。配置/SQLite/旧产物/前端/服务/发布/规则/九级评分及逻辑扣费不改；以下“仅文档、待实施”是历史状态。

- 数值化搜索与候选评价优化当前入口（2026-09-15，编写前 `400b8ce`）：用户确认将上一对文档标过时，按更深入的重构方向创建[新版设计](docs/design/apsgo_v7_numeric_search_incremental_parallel_design.md)及[新版实施计划](docs/implementation/apsgo_v7_numeric_search_incremental_parallel_implementation_plan.md) v0.1。顺序仍为阶段 0 基线、阶段 1 数值化数据/轻量描述、阶段 2 缓存/精确增量/按需完整评价、阶段 3 候选数值并行；阶段说明、设计依赖、执行记录和中文独立提交表齐备，代码均未开始，待后续实施指令。保留公开领域模型和唯一接受状态，复用现有连接/链摘要/数值桥接能力，不以最少修改行数限制必要重构；快筛只能证明不改善，可能改善、临界或不支持须精确确认。原始工时不改为整数秒，不放宽 Decimal/规则精度；九级顺序、40 万逻辑额度、4/64 轮转、原候选顺序、来源/期锁/编号和双审计保持。并行先整链后扩展无资源副作用的节点/片段，优先既有 Numba 多线程、主线程原序接受；多进程/joblib 仅为需确认的替代，不默认建设两套后端。实际完整评价可减少、投机计算可增加，分别如实计数；预算或资源副作用处保持串行边界。此次只修改新版两文档、旧版两文档过时标记与本文件，不改生产/测试/依赖/配置/库/服务/发布/前端，不重跑求解，不创建分支、部署、合并或推送。按文档范围验证保护摘要及精确导出；后续测试集中功能检查点，不逐个小编辑全量测试。以下旧入口已过时，历史结果不改写成新版通过。

- [历史信息，已过时] 数据结构、缓存与并行优化文档入口（2026-09-15，编写前 `0f8b4a0`）：用户要求按“数据结构与查询 → 缓存与增量评价 → 有序并行试验”顺序规划。新增[专项设计](docs/design/apsgo_v7_solver_data_cache_parallel_optimization_design.md)及[实施计划](docs/implementation/apsgo_v7_solver_data_cache_parallel_optimization_implementation_plan.md) v0.1；采用一对共用文档、三个顺序阶段和阶段 0 基线核对，各阶段有说明、设计依据/依赖/计划提交/状态总表、实际执行与提交表。当前仅两份文档及本文件，代码阶段均未开始，须后续实施指令。复用现有不可变模型、ChainSummary、有向连接缓存和未变链评价；先补当前方案索引，再做精确交期/资源/插入判断复用，最后只试整链换位候选只读并行、主进程按原顺序接受。三阶段分别冻结结果，不同步旧文档或一次混改。40 万组合原请求的 9999+10 秒与服务 YAML 的 300+10 秒分开；九级评分、精度、候选/4/64 轮转、源守恒/期锁/编号和独立审计不变，缓存统计及额外预计算如实分列。并行方式、私有接线和样本检查为设计提案，默认串行；本轮不安装依赖、不改代码/测试/YAML/SQLite、服务/发布/前端，不运行新求解或代签 Windows/一分钟目标，不建分支、合并或推送。文档提交只做归属、UTF-8、链接、白名单、保护哈希和精确导出检查；后续测试集中功能检查点，每提交使用中文 `#feat` / `#fix`。

- 40 万次采用与耗时优化排查（2026-09-15，开始 `735cc14`）：用户明确选择 400000 次候选额度。当前服务 YAML 从 200000 改为 400000，仅这一项；保留 310 秒总时限/300 秒搜索＋10 秒收尾，不改 SQLite 活动规则或部署/重启服务。后续算法优化以组合 40 万次独立结果为对照，该诊断请求为 9999+10 秒，不能与服务配置混称同请求；一分钟只是新优化目标，不是已经达标或 60 秒硬截断。现有完整计时为精修 802.29 秒、拆单与重放 251.83 秒、首轮 155.97 秒；首轮整链换位约 136.91 秒。有界函数探针指向交期全量计算、直接插入位置筛选、受影响链规则、资源汇总和候选节点验证；探针不是完整阶段比例或新质量结果。配置 72 项测试通过；详情见本专项 `performance_priority_400000.md` 和计划第 12 节。当前仅采用额度并做只读排查，没有实施性能算法改动；历史保护表的 YAML 摘要是本次授权前值，不改写旧试验。

- 组合百万次独立试验完成（2026-09-14，开始 `6554106`）：仅候选上限 400000→1000000，生产源码、9999+10 秒、种子/起排/速度/规则/九级评分不变。实际 1000000 次、433269 完整评价、1090 接受，3297.38 秒/CPU 3285.31 秒，候选额度用完；前 804 次接受与组合 40 万次完全一致，首轮及精修入口快照一致，来源、双审计和独立核心复审通过。旧欠清空 6 月 15 日 03:35→6 月 11 日 03:35，本月晚交 14 单/685.33 吨→18 单/762.02 吨，虚拟 820→880 吨、链数 26→27，零禁止/零欠重；评分改善但不称全面更优。新输出 `diagnostics/critical_delivery_search_and_bridge_reclamation/combined_1000000_01`，旧产物保留；仅文档及证据提交，不改正式 YAML/SQLite，不追加重复或新种子。计划 v0.5 第 11 节及专项 `budget1000000.md`、`budget1000000_comparison.json` 为本次证据；不同预算单样本不是正式性能验收或默认配置调整，原阶段 0～5 结论保持历史含义。

- 关键交期专项阶段 5 收口（2026-09-14，实施前 `728f65c`）：设计/计划 v0.4，阶段 0～5 完成；最终共享累计 4075 项/58.57 秒通过，精确暂存树同范围复验结果随收口提交正文。首轮累计唯一失败是架构把合法三节点/三类动作中的数字 3 误认成基线数字；只在指定模块/函数/完整语法结构精确识别，保留原禁止样例并补反例，生产代码不变。组合两次同 400000 次结果一致：旧欠 6 月 15 日 03:35 清空、本月 14 单/685.33 吨晚交、26 链/820 吨虚拟，零禁止/零欠重、来源与双审计通过；相对原代码交期/虚拟/耗时有明确代价，见本专项 `stage4.md`、`stage5.md`。40 万仅本专项独立请求，9999+10 秒不变，正式 YAML/SQLite 和九项旧材料保持；未部署/合并/推送/自动采用，未进行 Windows 或正式性能验收，不开启额外调参。以下“下一项阶段 5”等为历史进度。

- 关键交期专项阶段 4 完成（2026-09-14，生产 `66cf04e`）：40 万次三组已独立比较；组合两次为 1214.566601 / 1216.076893 秒、172312 完整评价/804 接受，方案/轨迹/计数/缓存/531 单日期及生成上界完全一致，零禁止/零欠重、来源与双审计通过。旧欠 6 月 15 日 03:35 清空，本月 14 单/685.33 吨晚交，26 链、820 吨虚拟、14954 宽差。相对原基线旧欠提前约 6.9 天，但本月多 2 单晚交、虚拟多 240 吨；相对仅调度旧欠再提前约 32.87 小时、本月少 1 单，但等待均值/耗时增加。过程删除 50 个/1000 吨、新增 68 个/1360 吨，库存及编号 27→95 闭合，毛回收不能称净节省。30 项薄工具检查通过；证据见本专项 `stage4.md`。下一项阶段 5 最终累计回归，正式 YAML/SQLite/旧材料不改，不自动启用、部署、合并或推送。

- 关键交期专项阶段 3（2026-09-14，实施前 `82106e3`）：秒级模式接普通桥接段独立回收、五类结构动作清理修复/原保留回退；精确删除声明、用途及受影响链校验，旧模式/受保护材料/原始值和新桥接身份保护不变。490 项集中回归通过，14 个独立普通桥见证经生产入口接受且核心审计通过，分隔材仍拒绝。诊断快照保存生成序号上界；旧快照仅只读/不新建桥接诊断，要求恢复生成时拒绝缺失上界。下一项阶段 4 组合完整同预算与重复运行；不提前声称总体节省或交期改善，正式配置/库不改。

- 关键交期专项阶段 2 完成（2026-09-14，生产 `fe6c54d`）：同 400000 次、9999+10 秒完整对照及 531 原单复算通过。旧欠清空 6 月 22 日 01:11:45→6 月 16 日 12:27:38；本月 12 单/653.90 吨→15 单/715.37 吨晚交，虚拟 580→820 吨，25 链不变，宽差 15210→14968，耗时 1181.76→1100.63 秒。零禁止/零欠重与双审计通过，但不称全面更优或已采用。首轮快照、前 414 次接受及精修入口指标一致；重点/常规实际扣费 104248/102212 闭合。结果已独立冻结，下一项阶段 3 普通桥接回收；正式配置/库不改。

- 关键交期专项阶段 1（2026-09-14，实施前 `375bc3e`）：仅秒级模式新增重点/常规 64 提案交替、同一语义归属与接受后稳定身份续访，评分和回收保护未改。96 项集中检查通过；历史固定见证有效移动由原第 110116 提案提前至 30409，原接受与核心审计仍通过，不代表完整质量改善。下一项阶段 2 在本提交运行同 400000 次完整对照，先冻结结果再接阶段 3 回收；正式 YAML/SQLite 与旧产物不改。

- 关键交期专项阶段 0 完成（2026-09-14，实施前 `5b0c662`）：40 万次原代码基线为 1181.760883 秒/CPU 1177.198096 秒、178216 次完整评价/880 接受，零禁止/零欠重和双审计通过；旧欠 6 月 22 日 01:11:45 清空，本月 12 单/653.90 吨晚交，25 链/580 吨虚拟/15210 宽差。历史百万次固定见证复现：8.14 吨移动接受并核心审计通过，原后置第 110116 提案出现；45 个普通桥各自删除有 14 个独立改善且核心审计通过，旧入口拒绝；分隔材删除仍审计失败。3 项薄工具检查通过，阶段 0 证据/机器摘要独立保存；下一项阶段 1 仅调度改进，之后阶段 2 完整同 40 万次对照，不提前接回收。不改规则/评分/正式 YAML/SQLite，不将本机单次运行称为正式性能门通过。

- 关键交期专项持续实施授权（2026-09-14，实施前 `5b0c662`）：用户要求候选上限改为 400000，持续按本专项计划执行。设计/计划 v0.2；40 万输入与原百万次逐值仅候选上限不同，时间仍 9999+10 秒、评分/规则/起排/工时保持，正式 YAML/SQLite 不改。阶段 0 先运行原代码 40 万次同预算基线并固化历史单动作见证，随后按阶段 1～5 实施、集中验证和中文独立提交；若预算在后置阶段前用尽须如实报告，不擅改前置流程或加额度。以下“仅文档/等待指令”和百万次同条件比较为已被本轮授权更新的历史约定。

- 关键交期候选与连接材回收文档入口（2026-09-14，核验 `896dba5`）：用户要求先形成前两项改善的设计和实施计划；新增[专项设计](docs/design/apsgo_v7_critical_delivery_search_and_bridge_reclamation_design.md)与[实施计划](docs/implementation/apsgo_v7_critical_delivery_search_and_bridge_reclamation_implementation_plan.md) v0.1，本轮仅两份文档及本文件。方向为现有后置精修内重点/常规 64 提案批次交替、后移潜力与接受后续访，以及普通连接虚拟材的独立/随结构调整回收；原单 4 提案轮转和总额度仍共用，桥接额外扣费不变。九级顺序、整秒评分、初始构造/必要拆单、规则、期锁/谱系和双审计保持；分隔材/填充材/真实材不得借清理删除，删除旧虚拟材不回退生成上界。首批只接既有秒级旧欠试验模式，旧模式与正式活动配置不变。计划含阶段说明、执行记录及提交表；阶段 0～5 均未实施，须获得后续代码指令，先固化见证再分别保存“仅调度”和“两项组合”的同百万次/9999+10 秒对照。本轮按文档提交口径仅作 UTF-8、链接、白名单、保护哈希和精确导出检查，不运行业务测试/完整求解或构建；不改生产/测试、YAML/SQLite、旧文档/产物、前端/发布，不部署/合并/推送。以下旧欠专项收口及早期入口保持历史含义，不将本次设计视为新功能完成。

- 旧欠改善专项阶段 6 收口（2026-09-14，实施前 `adb5377`）：阶段 5.1 代码 `bf2dbf8`、5.2 完整对照 `adb5377` 已提交；专项设计/计划 v0.11 和[阶段 6 记录](docs/implementation/evidence/apsgo_v7_backlog_priority_search/stage6.md)保存最终回归及采用边界。两次百万次结果完全一致：0 禁止/0 欠重、26 链、虚拟 1220 吨、宽差 14406；旧欠 6 月 12 日 16:10:53 清空，本月 15 单/677.06 吨晚交，2829.531622 / 2787.722412 秒。相比历史百万次消除了 154 条欠重链，但交期及耗时不是全面更优；实现正确且重复性通过，综合业务收益有取舍，暂不直接替换正式方案。阶段 6 共享累计 4023 项 / 57.43 秒通过，最终精确导出同范围验证随独立提交正文；34 个保护/产物哈希及生产源码保持，共享残留仍仅历史八项缺失。只改四份文档，不改正式 YAML/SQLite、输入/结果、生产代码或测试；未进行正式性能/Windows 验收、未启用活动规则、未重启服务、未合并/推送，不自动开启新调参。以下早期“下一步”和“未执行”均按历史记录理解。

- 秒级评分与欠重优先阶段 5.1 已实现（2026-09-14，实施前 `4fa0f98`）：用户已授权按修订计划持续实施。专项设计/计划 v0.9；新增默认关闭的 `second_precision`，交期规则版本 3 与 `score_time_unit=second` 明确绑定新顺序，CLI 为 `delivery-backlog-seconds`。不改原工时累计、晚交统计、候选流程、4/64 轮转或双审计；原九级与历史交期优先九级按旧版本回读。114 项相关、4023 项含服务累计通过；真实百万次轨迹前 47 次精确复现，第 47 次旧欠仅减少 `2E-25` 小时而负担增加，新整秒比较通过原完整候选入口拒绝。新规则指纹 `e809e2eafbcd426527fe8f55d7971579ec8290b0b848cb8d1d6cae0f9f8a40dd`。证据见[阶段 5.1](docs/implementation/evidence/apsgo_v7_backlog_priority_search/stage5_1.md)，精确导出验证随独立中文提交记录。下一步阶段 5.2：百万次/9999+10 秒完整运行及必要复跑，然后阶段 6；不自动改正式 YAML/SQLite、不部署/合并/推送，历史产物不改。以下“仅文档、等待实施”为已关闭的历史状态。

- 秒级评分与欠重优先仅同步文档（2026-09-14，修订前 `694c1f8`，工作区干净）：用户确认交换当前第 3/4 与第 5/6 两组，组内不变；目标态为禁止数、禁止严重度、欠重链数、欠重缺口、旧欠全部完成用时、累计等待与延期负担、宽差、虚拟重量、链数。交期内部仍旧欠清空在先，晚交吨位只统计，不恢复到含晚交吨位评分的原九级。[专项设计](docs/design/apsgo_v7_backlog_priority_search_and_clearance_design.md)与[实施计划](docs/implementation/apsgo_v7_backlog_priority_search_and_clearance_implementation_plan.md) v0.8 明确评分时间按整秒半偶舍入，工时累计保留精度；累计负担先按原重量×整秒时长汇总吨·秒再转吨·小时，原始完成时间和晚交统计不变，不新增交期宽限。按完整九级比较，不另加欠重数量/缺口各自不增加的拦截；纯整链与结构精修的前缀边界按设计区分。当前代码仍历史交期第 3/4 位、未量化时间，新顺序与秒级语义必须生成新声明/指纹，旧请求和结果按旧版本回读。**本轮仅三份文档；阶段 5.1“秒级评分与欠重优先接线”、5.2“新口径完整对照复测”待后续实施指令，之后再进入阶段 6**。后续复测计划沿用百万次/9999+10 秒独立输入，不改正式 YAML/SQLite、候选流程、4/64 轮转或历史产物，不运行新求解、不部署/合并/推送。下方交期优先取舍及“先讨论精度”的状态均为历史记录，以本条最新确认覆盖目标态。

- 新九级百万次补测完成（2026-09-14，运行前 `61e5ec0`）：用户仅授权独立候选上限 400000→1000000，新九级/规则/工时/起排/种子/9999+10 秒/生产源码保持，专项设计和计划 v0.7。单次公共入口退出 0，1662.660864 秒 / CPU 1658.265003 秒，1000000 次检查、291703 次完整评价、2547 接受后额度停止。首轮在 521369 次结束，两次同期间拆单消除禁止，655878 次进入后置精修；前 38 次接受精确延续 40 万次。最终零禁止、来源守恒和双审计通过，旧欠 6 月 6 日 11:56:58 清空，本月晚交 5 单/378.36 吨；但 170 条链中 154 条欠重、缺口 88412.21 吨，虚拟 1540 吨/4.98803%，**仍 BEST_EFFORT，不自动采用**。143 次切链不等于 143 次拆原订单；另有 87 次接受仅减少 `1E-25～2E-25` 小时、67 次累计负担反增，精度需单独核查，本轮未修。证据见[百万次补测](docs/implementation/evidence/apsgo_v7_backlog_priority_search/budget1000000.md)及其机器摘要，完整 531 单比较保存在独立 diagnostics 目录。只改实验输入和五份文档/证据，未改生产/工具/测试/正式 YAML/SQLite、未覆盖旧结果，不部署/合并/推送。**阶段 6 未执行，后续先讨论欠重代价与比较精度；不能继续把“必须改流程才能完成必要拆单”当作已证实结论。** 下方 40 万次失败与等待流程确认保持历史含义。

- 旧欠改善阶段 5 已完成试验但发布门失败（2026-09-14，运行源码 `c0975a9`）：专项设计/计划 v0.6；同输入/400000 次/9999+10 秒新九级两次耗时 21.708541 / 21.929744 秒，均 `complete_not_publishable`。首轮单节点消耗 392776 次检查、仅 479 次完整评价，必要拆单及后置精修未执行，留下窄钢连续 1170.3>500 禁止；核心审计与搜索一致且拒绝，应用审计未运行。两次方案/评价/38 次接受轨迹/计数/缓存及 531 单日期完全一致，独立回读审计同值。较阶段 3，诊断旧欠清空早 31.666452 小时但 74 单延后、加权平均等待 6.58→13.14 天，本月晚交 4→24 单、欠重 0→6 条；不可当成可发布改善或 22 秒性能收益。仅改薄比较工具/测试及本专项文档证据，相关 60 项通过；[阶段 5 记录](docs/implementation/evidence/apsgo_v7_backlog_priority_search/stage5.md)保存三组对照与全部逐单明细哈希，精确导出结果见独立中文提交。**阶段 6 未执行，等待确认必要规则修复/拆单与可选搜索的流程衔接修订**；不改评分顺序、精度、预算、生产代码、SQLite/YAML 或旧产物，不部署、合并或推送。下方阶段 5 未执行为历史状态。

- 旧欠改善阶段 4 已实现（2026-09-14，实施前 `5ce8a39`）：专项设计/计划 v0.5；显式 `include_backlog_clearance=True` / `--variant delivery-backlog-priority` 接入禁止数、禁止严重度、旧欠清空小时、累计负担、欠重链数、欠重缺口、宽差、虚拟重量、链数。晚交吨位仅统计但仍复算审计；新模式取消独立欠重不增加拦截，禁止/允许偏差、来源/期锁/拆单和完整审计保留，旧模式不变。新专项 50 项通过，最终集中及精确导出见[阶段 4 记录](docs/implementation/evidence/apsgo_v7_backlog_priority_search/stage4.md)和独立中文提交。原九级请求/完整评价/日期回读一致，新规则指纹 `160a764f3e5444e47ce74d689c4fb99d1c76246e3fa1f74ba5ea755fccdffede`；在阶段 3 旧方案上重算清空 587.4388867756726549279204323 小时、核心审计通过，**不是新评分完整求解结果**。阶段 5 三组完整对照、阶段 6 收口尚未执行；下一步按相同 400000 / 9999+10 预算试新九级，不自动正式采用。SQLite / YAML、旧输入/产物保持，不部署、合并或推送。下方阶段 4 未实施为历史状态。

- 旧欠改善阶段 3 完成（2026-09-14，实施前 `e0d55e5`）：专项设计/计划 v0.4 保留原九级、原请求和 400000/9999+10 预算。主运行 `diagnostics/backlog_priority_search/stage3_search_only_01/` 为 824.528608 秒/CPU 822.910104 秒、400000 次/163814 次完整评价/726 接受，零禁止/零欠重及双审计通过；同样 4 单/114.90 吨晚交，旧欠清空提前约 23.20 小时、加权等待减少约 29.62%，虚拟 680→1400 吨、宽差 12397→14348、链数 23→24。原前 341 次接受精确相同，仅后置精修分歧。观察复跑 `stage3_search_only_observed_02/` 为 835.803299 秒，首轮和最终方案/评价/轨迹/计数/缓存/日期报告完全相同；后置 235354 提案/检查、113970 次完整评价、385 接受闭合，96 单旧欠有结构动作机会但并非每个位置已尝试。两条尾部样例仍于 6 月 24～25 日完成，不称已达到尽早清空；[阶段 3 记录](docs/implementation/evidence/apsgo_v7_backlog_priority_search/stage3.md)保留全部取舍、531 单明细哈希及 28 项相关检查，精确导出同范围结果见本轮独立中文提交。只改薄观察/比较工具、必要测试及本专项文档，不改生产、正式规则/配置/库或部署。保留为阶段 4 新评分试验的对照，不自动正式采用；阶段 4～6 尚未执行。

- 旧欠改善阶段 2 修正完成（2026-09-14，实施前 `de9015f`）：用户确认“直接可连接插入位置优先、其他仍保留”，本专项设计/计划 v0.3；仅交期且有旧欠的跨链单节点插入复用现有有向连接缓存排序，不改 4 / 64 轮转、原九级、欠重保护、实际候选扣费或独立审计。两例节点提案位次为 29741 / 6389（原 120667 / 165345），五类共同轮转在 96966 / 25881 次出现，机会门通过；独立完整移动仍各 1 次检查、接受且核心审计通过，与阶段 0 样例一致。最终含服务累计 3909 项 / 51.06 秒通过；精确暂存树同范围复验见本次独立中文提交正文。[阶段 2 修正证据](docs/implementation/evidence/apsgo_v7_backlog_priority_search/stage2_direct_first.md)保留枚举真实开销、重复性、架构编号修正及旧失败记录。阶段 3～6 尚未执行，新九级尚未实施；不将机会诊断当作完整排程改善。SQLite / YAML、原输入/结果不改，无部署、合并或推送；下一步阶段 3 保留原九级全流程对照。下方“停在阶段 2、待确认”为已被本次修正关闭的历史状态。

- 旧欠改善停在阶段 2 的设计取舍（2026-09-14，实施前 `3062099`）：阶段 0 / 1 已提交 `c16dba8` / `3062099`。64 提案动作轮转与接受后稳定身份续访已接线，54 项专项和 867 项相关测试通过；但[阶段 2 机会证据](docs/implementation/evidence/apsgo_v7_backlog_priority_search/stage2.md)未达要求：原单虽到第 2 / 5 位，两个有效节点动作反而到第 430854 / 406737 提案，五类共享 40 万次纯枚举未命中。阶段 2 仅保存试验检查点、不可部署；阶段 3～6 未执行，新九级尚未实现。待用户确认是否将目标链内原序改为“现有连接缓存判断的直接可连接插入位置优先，其余仍保留”；建议尚未实现，不自行改 4 / 64、预算或评分来掩盖失败。SQLite / YAML、旧输入和结果保持，未部署、合并或推送；恢复时先核对工作区与本专项记录。

- 旧欠改善阶段 1 已实现、待集中验证（2026-09-14，实施前 `c16dba8`）：新增显式旧欠尾部排序和五类惰性原单轮换，生产入口尚未接线；现有九级、动作构造与完整接受不改。四文件语法及差异检查通过，业务测试在阶段 2；此中间提交不可部署。阶段 0 已提交 `c16dba8`。下一步按专项计划接入 64 提案动作轮转及接受后续访，并核验冻结样例机会，不提前宣称改善。

- 旧欠改善已获持续实施授权，阶段 0 完成（2026-09-14，实施前 `6991c54`）：按[专项实施计划](docs/implementation/apsgo_v7_backlog_priority_search_and_clearance_implementation_plan.md)执行阶段 0～6。新增薄诊断工具复用原候选接受和核心审计；两例独立前移均接受，原提案序 120667 / 165345，冻结输入和完整评价身份通过。证据见 `docs/implementation/evidence/apsgo_v7_backlog_priority_search/stage0.md`，完整明细位于 `diagnostics/backlog_priority_search/stage0_witnesses_01/`。下一步阶段 1～2，保持原九级，仅改搜索机会，4 提案/原单、64 提案/动作类；集中验证后阶段 3 完整对照，阶段 4 才改新评分。每阶段中文独立提交，正式配置/数据库和旧产物不改；以下“仅文档、等待授权”为历史记录。

- 旧欠优先九级评分已确认、仅同步文档（2026-09-14，修订前 `f045b65`，工作区干净）：[专项设计](docs/design/apsgo_v7_backlog_priority_search_and_clearance_design.md)和[实施计划](docs/implementation/apsgo_v7_backlog_priority_search_and_clearance_implementation_plan.md)升至 v0.2。新顺序为禁止数、禁止严重度、旧欠全部完成用时、累计等待与延期负担、欠重链数、欠重缺口、链间宽差、虚拟重量、链数；新增晚交原订单吨位继续统计，但不参与评分、同分裁决或隐藏否决。旧欠消除优先于本月延期改善，交期优先于欠重；新模式取消独立欠重数量/缺口分别不增加保护，仍保留禁止、允许偏差种类、来源/期锁/拆单和双审计，正式零欠重门不改。第一步保留原九级仅作搜索改动对照，第二步试整组新评分及接受语义，不再执行旧十级提案或额外八级试验；两种九级须按有序指标键与指纹区分，移除晚交评分后不能跳过精修或误读正式欠重门。阶段说明、测试和执行/提交表已同步，代码阶段 0～6 均未开始，需后续实施指令；只改本专项两文档及本文件，代码/配置/数据库/旧结果未改。以下 v0.1 第 6 位/十级提案及旧欠重保护仅按历史或原九级对照解释，不覆盖本条新模式。

- 旧欠订单两步改善文档入口（2026-09-14，基线 `codex/delivery-objective-optimization@ce0b1f4`，编写前工作区干净）：新增[专项设计](docs/design/apsgo_v7_backlog_priority_search_and_clearance_design.md)与[实施计划](docs/implementation/apsgo_v7_backlog_priority_search_and_clearance_implementation_plan.md) v0.1，本轮只改这两份文档及本文件，代码阶段 0～6 均未开始。第一步保持九级目标，改善旧欠尾部优先、原单/动作类小批轮转与接受后的续访；第二步提案增加“旧欠订单全部完成用时”，放在第 6 位，新增晚交吨位仍第 5、欠重仍第 3/4；原累计负担顺延且公式不变。两步分别复测，后续实施需明确指令；4 提案/原单、64 提案/动作类是待测工程试验值，不是额外预算或业务配置。原 40 万次/9999 秒的 4 单晚交、1 条欠重结果仍为 BEST_EFFORT；单动作前移诊断不是完整新结果。阶段说明、设计依据、执行记录与提交表见新计划，测试集中阶段 2/4/6，不要求每小步重复全量。只同步本专项与必要证据；旧设计和实绩保留，不改正式配置/规则库、HTTP/前端、旧输入/结果，不部署、合并或推送。

- 回退版本长时限复测完成（2026-09-12，代码 `0efe8ae` 与 `b1ab8af` 相同）：用户仅授权搜索时限 300→9999 秒，另留 10 秒收尾，总策略 10009，候选仍 400000；正式配置/数据库不改。实际 821.146364 秒/CPU 818.951500 秒，400000 次候选、155813 次完整评价、394 次接受，因候选额度停止。晚交 11 单/288.89 吨→4 单/114.90 吨，7 单转准交、无新增晚交；剩余全为 6 月 10 日交期。欠重仍 1 条/41.01 吨、零禁止、双审计通过，仍 BEST_EFFORT。虚拟 540→680 吨、宽差 12151→12397、链数 23；上月加权等待略降但最后完成晚约 3.28 小时。前 369 次采纳精确一致，后置精修额外接受 25 次，首次进一步减少晚交约在 423.46 秒/231183 次检查。计划 v0.10 第 14 节及 [长时限报告](docs/implementation/evidence/apsgo_v7_urgent_order_search/restored_search9999.md) 为最新入口；只使用独立实验输入，无算法/目标优先级/默认策略变更，不能把 4 单当最优或自动追加额度。

- 回退版本 40 万次/300 秒已复测（2026-09-12，代码 `4162a88` 与 `b1ab8af` 相同）：用户授权仅将候选提高到 400000，仍交期第 5/6 位和欠重不增加保护，搜索 300 秒+10 秒收尾。实际 301.167615 秒/CPU 300.710441 秒、192551 次候选/68269 次完整评价/369 次接受，因时间停止；最终有序方案、评价、全部采纳轨迹和 531 单日期与原 `candidate_01` 相同，仍晚交 11 单/288.89 吨、欠重 1 条/41.01 吨、零禁止、双审计通过。原基线 192021 次也未用完 20 万，新增额度未发挥作用。仅请求/报告身份随预算改变，不冒称整个文件相同。计划 v0.9 第 13 节及 [回退版本40万报告](docs/implementation/evidence/apsgo_v7_urgent_order_search/restored_budget400000.md) 为最新结果。源码/工具/正式配置和规则库未改，40 万仅是本次试验输入，仍 BEST_EFFORT；后续改时限/流程待用户要求。

- 已回到 11 单晚交版本（2026-09-12，回退前 `0b30bad`）：恢复最近的试验前代码 `b1ab8af`，保留临期定向动作，交期第 5/6 位、欠重第 3/4 位，后置欠重数量/缺口分别不增加。该版本历史 `candidate_01` 为晚交 11 单/288.89 吨、欠重 1 条/41.01 吨、零禁止且双审计通过，仍 BEST_EFFORT，不把它说成零欠重通过或本轮重新求解。`delivery-first`/`delivery_first` 试验入口及专用测试撤回；源码/测试/工具与 `b1ab8af` 精确一致，试验报告与结果保留，历史命令仅适用于当时提交。当前使用 `--variant delivery`、原 20 万次输入；不改正式配置/库、服务、分支或历史。计划 v0.8 第 12 节记录精确树 `afb5370d565fe9f7d386debdda6dc4ab83392144` 导出累计 3886 项/52.57 秒及残留/语法/wheel 通过，后续仅文档回填；以新反向提交记录恢复，不重写 Git 历史。

- 40 万次交期优先对照完成（2026-09-12 收口，9 月 11 日运行，代码 `1df881c`）：用户授权仅提高本次候选上限 200000→400000，其他请求字段逐值一致，正式配置/库不改。197.826468 秒/CPU 197.266088 秒，400000 次候选/48011 次完整评价/276 次接受；首轮 303785 次后进入拆单，2 次同期间拆单清除禁止，回放中额度耗尽，后置精修未执行。最终 13 单/336.87 吨晚交、5 条/1356.65 吨欠重、零禁止、26 链/240 吨虚拟，双审计通过，可带允许偏差发布但正式门仍 BEST_EFFORT。优于交期优先 20 万次（不可发布），仍未超过旧九级 11 单/288.89 吨及 1 条/41.01 吨欠重；不同额度不能声称同预算优劣。前 20 次接受与 20 万次完全一致，输入/旧证据哈希保持。计划 v0.7 第 11 节及 [40 万次报告](docs/implementation/evidence/apsgo_v7_urgent_order_search/budget400000.md) 为最新记录；本次未改源码、工具、默认预算或部署，后续增加额度/调整流程待用户要求。

- 交期优先试验已完成但失败（2026-09-11，代码 `deec648`）：计划 v0.6 阶段 7～9 完成，显式 `delivery-first` 将交期放第 3/4 位并允许欠重代价；默认旧九级未改。真实运行 19.716060 秒/CPU 19.277731 秒、200000 次候选提前用完，仅 1909 次完整评价和 20 次接受；单节点阶段消耗 191411 次，拆单及后置精修未执行。诊断候选晚交 21 单/738.22 吨、欠重 6 条/1771.68 吨，窄钢连续重 1170.3>500 禁止项未修复，`complete_not_publishable`，核心审计拒绝、应用审计未执行。不能将短耗时当提速或将诊断日期作为可生产结果。第 2 次接受起与旧顺序分歧，当前阶段式修复与交期优先存在搜索路径/额度衔接问题；下一步须讨论流程，不自动增预算或放规则。精确树 `30e422d6d7b5d8ec4be592d961fed0460a6bf61a` 累计 3892 项/53.71 秒、残留/语法/wheel 通过；后续仅汇总工具与文档。详见[试验报告](docs/implementation/evidence/apsgo_v7_urgent_order_search/delivery_first.md)。未部署、合并、推送，正式库/配置/旧证据未改。

- 交期优先试验授权（2026-09-11，实施前 `b1ab8af`）：用户明确允许两个交期目标移至第 3/4 位，并允许欠重链数/缺口增加作为交期改善代价；本计划 v0.5 第 10 节阶段 7～9 为当前入口。新顺序仍严格完整比较，只有规则明示的链重下限偏差允许，其他禁止、期锁、守恒及审计不变；正式零欠重验收不放宽。保留旧七级和旧九级默认对照，通过后端显式参数/重放变体试新顺序；不改正式库、前端、预算或部署。下方“条件备选未授权”和两项欠重不增加仅适用于历史旧九级，不再限制本轮新顺序。

- 临期订单搜索专项实施收口（2026-09-11，实施前 `fa83370`）：[本计划](docs/implementation/apsgo_v7_urgent_order_search_implementation_plan.md) v0.4 阶段 0～6 完成；排序、允许已有欠重进入精修、两项欠重分别不增加、链内前移及节点/片段/切链/整链定向接线已提交。精确树 `f67740af04a6a480074d88251a4a66a8b1af53aa` 干净导出累计 3886 项 / 53.05 秒及残留/语法/wheel 通过，随后仅补文档。真实复测 `candidate_01` 301.109626 秒 / CPU 299.605092 秒、192021 次候选、67899 次完整评价；前 341 次接受与原九级逐值一致，新增 28 次均为链内前移。晚交仍 11 单 / 288.89 吨，仅两单提前约 0.636 和 11.601 小时，累计负担 1297489.840268→1294569.295364（约 -0.225%）；欠重仍 1 条 / 41.01 吨，宽差/虚拟/链数不变，双审计通过但正式质量与性能仍为 BEST_EFFORT。搜索时限截断，按计划未重复，不称更大动作已充分探索或订单不可提前。详细逐单证据见 [执行报告](docs/implementation/evidence/apsgo_v7_urgent_order_search/README.md)。未调目标顺序、预算、规则库/配置/前端或部署；下一步搜索机会分配及交期前移第 3/4 位仍待讨论确认，不自动实施。以下“下一阶段/未实施”均是历史记录。

- 临期订单搜索开始实施（2026-09-11，实施前 `8942d18`）：用户授权按新计划持续推进，阶段 0 已冻结当前九级基线、旧七级参考和输入身份；原九级 11 单/288.89 吨晚交、1 条/41.01 吨欠重不改写。下一阶段为原订单紧迫性排序，阶段 2、4、6 集中验证，各阶段独立中文提交。下方“仅文档/待授权”为历史状态，最新执行以临期专项计划第 9 节及证据为准；不调整目标顺序、预算、规则库或部署。

- 临期订单定向搜索计划入口（2026-09-11，编写前 `96c6475`）：用户已确认先增强急单前移/交换、允许带已有欠重继续交期精修，再根据复测决定是否调整目标顺序。本轮仅新增[临期订单定向调整实施计划](docs/implementation/apsgo_v7_urgent_order_search_implementation_plan.md) v0.1 并更新本文件，沿用 `codex/delivery-objective-optimization`；代码阶段 0～6 均未开始，须后续实施指令。保留九级原顺序，交期仍第 5、6 项；新增/扩展精修每次接受必须零禁止、欠重链数与缺口分别不增加、完整九级严格改善，来源/期锁/拆单/双审计不放松。只复用原动作和阶段骨架，补链内前移及原订单紧迫性，不写死 31 单、不恢复借用比例、不合并旧阶段或新增回放/依赖。新计划第 3 节仅增补原交期设计的排序和精修保护，不批量同步旧文档；原九级仍为 11 单晚交/288.89 吨且欠重 1 条/41.01 吨的 BEST_EFFORT。将交期前移第 3、4 位及欠重容忍、正式验收修改均待真实复测后再次确认，不自动实施。阶段说明/依据与依赖/执行记录/提交表齐备，测试集中阶段 2、4、6；只同步本计划、本文件和后续必要证据，正式配置/数据库/前端/部署/合并/推送不在本轮范围。

- 交期专项后端收口（2026-09-11，实施前 `3dfbee8`）：阶段 0～5 后端功能、阶段 6 对照及阶段 7 源码回归已执行，设计 v0.3/计划 v0.4。最终精确树 `79275e7cd7a7522513b112971c3ea8c67e3aa4ab` 导出累计 3869 项通过，54.27 秒，残留/语法/Python wheel 通过；仅文档随后补记录，生产源码与对照一致。旧七级 33.96 秒、原方案精确不变；新九级两次约 207.90/208.19 秒且有序方案/评价/轨迹/计数/报告一致。本月晚交 24 单/844.82 吨→11 单/288.89 吨，但仍欠重 1 条/41.01 吨，宽差 11726→12151，旧欠最后完成晚约 1.08 小时，整体 **BEST_EFFORT**，不可声称零欠重/性能门通过或替换正式版本。主要耗时来自整链交期移位反复完整评价；自然结束后因欠重跳过序列精修。详见交期专项阶段 6、7 证据；后续流程优化需讨论，不自动放宽门槛或增加额度。用户确认虚拟速度100，前端/HTTP/正式库/Windows适配延后；本轮没有部署、合并、推送或服务启停。下面历史“下一项/未实施”按当时状态解释。

- 交期后端进度（2026-09-11）：阶段 0～4 已分别提交 `0138939`、`dc1da4b`、`2e3f5ff`、`7030e4b`、`857f931`；用户另授权清理两个失效并行文件，`a141b03` 已提交且架构 81 项通过。搜索/审计集中 1092 项通过，原七级空扩展指纹投影已闭合；阶段 5 后端数据准备、身份读回、只读报告已实现，15 项与真实 531 单预准备通过。入口仍为交期专项计划，下一项真实旧七级/新九级对照；不宣称已改善现场交期。后端 `delivery-backend-v1` 仅用于 Python 重放，HTTP/前端/活动规则库保持原状；虚拟速度显式 100 米/分钟。原表/配置/数据库哈希不变。

- 交期专项最新授权（2026-09-11，实施前 `e9004e8`）：用户确认虚拟材统一 100 米/分钟，并明确先完成后端算法、暂不考虑适配。设计 v0.2、计划 v0.3；阶段 0 按已有源文件哈希、六月一日起点和上述速度冻结，不再等待客户端选择。继续后端计时/九级评价/搜索/审计/重放及真实对照；HTTP v2、Web/C#、规则页面、正式库启用和 Windows 适配延后，不作为后端收口阻塞，也不能标为已完成。旧服务配置/数据库/接口不改；阶段独立中文提交，阶段 1～2 / 3～4 集中验证。下方待确认入口保留历史含义。

- 当前交期专项阶段 0（2026-09-11，实施前 `73e93f2d3336120d7154c59af7639f727875a9ae`）：用户已授权按计划持续实施，当前在 `codex/delivery-objective-optimization` 开始前置核对，计划 v0.2；[核对记录](docs/implementation/evidence/apsgo_v7_delivery_objective_optimization/stage_00_precheck/README.md)。原数据库改动已由用户在 `73e93f2` 提交，当前只读保护，不再将其称为未提交改动。此前 GQGA4 结果表 533 个真实片段对应旧参考全部 531 个原订单，宽厚及分片总重量一致，交期/真实炉区速度齐备；当前活动规则版本 14 与参考版本 6 除版本/指纹外业务配置一致，27 个原型相同但全部缺速度。虚拟速度来源/数值和首个 Web 或 C# 客户端仍待用户确认，因此阶段 0 未完成，不进入阶段 1、不启用交期、不重跑正式求解。此次只记录前置检查并独立提交，不改生产/配置/库/原表/外部仓库；下方“仅创建文档”和数据库未提交为上一轮历史状态。

- 当前交期优化文档入口（2026-09-11，基线 `web-integrate@db79384eb0e57bdece4706251ab9704cbe628b98`）：按用户要求从当前分支建立 `codex/delivery-objective-optimization`，本轮仅新增[交期优化专项设计](docs/design/apsgo_v7_delivery_objective_optimization_design.md)与[实施计划](docs/implementation/apsgo_v7_delivery_objective_optimization_implementation_plan.md) v0.1，并更新本文件；代码阶段 0～7 均未开始。目标为显式生产开始时间、交期当天 24 点、在原前四级之后加入“新增晚交原订单吨位”和“累计等待与延期吨·小时”，宽差/虚拟重量/链数后移；旧欠单按起排后等待计分，拆单按原订单最后片段完成，虚拟材占时不计真实晚交吨位。保留既有阶段骨架/工艺/资源/预算及独立审计，不新建求解框架或资源池。v2 契约、规则激活与选定客户端接入按专项文档实施，虚拟速度来源和客户端范围需在阶段 0 明确，不用零工时伪造准交。当前 `data/apsgo_v7_rules.sqlite3` 用户未提交改动保留、不暂存；本轮不改生产/配置/数据库/外部仓库，不部署、合并或推送。
- 交期专项验证安排（用户最新要求，2026-09-11）：不要求每个子步骤、每个阶段提交都跑业务测试或重复全量回归。阶段 1～2 在“计时与评分”检查点合并验证，阶段 3～4 在“搜索与审计”检查点合并验证，接口/真实对照/最终回归按计划集中执行；关键异常随时做最小定位。暂未验证提交必须写明待验证检查点，不可部署或冒充通过；阶段独立提交、必要正确性测试与最终审计保留。文档提交只检查文档、差异范围和用户文件保护，不跑业务 pytest / 完整求解；本专项覆盖下方旧记录中的每提交共享树/干净树重复累计要求，不改历史业务/性能门槛。今后新增实施计划均须包含阶段说明、设计依据/依赖/计划提交/状态总表、实际执行记录和提交情况表。

- 依赖安装方式补充（2026-09-09，实施前`edbc5a6`）：按用户要求，新增4项依赖进入原`release/requirements-build.txt`，与PyInstaller共5条固定版本；安装统一`pip install -r release/requirements-build.txt`。只取消“整个文件必须单行”，仍要求PyInstaller声明唯一并核验原全部精确版本；3个运行库与pyproject保持一致。设计/计划v0.8，原阶段5历史单行记录保留；本次不安装环境、不改版本/算法/配置/数据库，最终验证见独立提交正文，Windows实包仍待验。

- 当前NumPy／Numba阶段5（2026-09-09，实施前 `111ac67`）：阶段0～4已分别提交，阶段4共享/最终精确树各3873项通过。设计/计划v0.7；本阶段仅发布脚本、发布测试/说明和专项文档，生产算法、规则、预算、YAML和SQLite不改。增加NumPy2.2.6/Numba0.65.1/llvmlite0.47.0/hooks2026.6元数据核验及清单，保留原PyInstaller单行依赖/目录式构建和源码禁入，不自动安装、不全量收集。既有可写目录冒烟之后，在临时规则库执行单桥/双桥首次/重复/重启共6次请求，核验实际编译日志、顺序/原型/重量及双审计；Windows访问权限将自有程序副本设为不可写，外部配置/库/诊断可写，保留小证据后只清理程序副本。[阶段5记录](docs/implementation/evidence/apsgo_v7_numpy_numba_virtual_bridge/stage_05_release/README.md)和提交正文记录最终本机检查；源码TestClient/模拟权限命令不代表Windows实测。当前macOS无法构建或运行Windows EXE，阶段5整体及阶段6待Windows检查、构建和真实订单证据，不代签关闭历史Windows/20对门，不部署、停服、合并或推送。下方旧“下一阶段”均按其当时状态解释。

- 当前NumPy／Numba阶段4（2026-09-09，实施前 `491e901`）：阶段3双树各3756项通过并提交。设计/计划v0.6；本机五对全部有序业务精确一致，完整耗时中位数81.105801→32.275849秒（减少60.21%）、CPU81.066752→32.131365秒、首轮36.520444→13.134452秒，均完成200000/3697/54及双审计。实际编译冷成本包含；同进程第二次30.429171秒、重启32.199898秒独立列示。首轮物化455702→10549，实际语义编码463448→7802，无Python桥接回退。原正式180/10输入31.255488秒，原质量/双审计通过；最新310/10输入31.892006秒完成20万次，不与旧未知源码Windows119809次混比。本项不改生产/配置/库，首个补充profile摘要格式失败保留，仅导出修正后另目录重试，不重跑冷配对；[阶段4证据](docs/implementation/evidence/apsgo_v7_numpy_numba_virtual_bridge/stage_04_comparison/README.md)与提交正文记录最终双树。提交后持续阶段5发布源码适配；Windows及历史20对门仍待验，不部署、停服、合并或推送。

- 当前NumPy／Numba阶段3（2026-09-09，实施前 `1994ee3`）：阶段2双树各3713项通过并提交。设计/计划v0.5；唯一bridge入口在原直接连接/数量上限后接入数值路径，原Python循环原样提取作回退，只物化胜出节点。目录按工厂复用，锚点每次准备；静态不支持不增加预算轮询，单次锚点回退不污染目录，取消整体丢弃，程序异常不吞。每工厂至多三条真实执行日志，help/规则GET/直接连接不加载数值模块。专项233项通过，原自定义17查询/49轮询保持，语法树及独立复核通过；[阶段3证据](docs/implementation/evidence/apsgo_v7_numpy_numba_virtual_bridge/stage_03_bridge_integration/README.md)及提交正文记录最终双树验证。规则、预算、配置和库不改；独立提交后持续阶段4完整对照，不提前宣称性能/Windows验收。

- 当前NumPy／Numba阶段2（2026-09-08，实施前 `d5ff583`）：阶段1双树各3628项通过并提交。设计/计划v0.4；只在原私有模块加入四种判边、三点评分和共用64组合扫描，两个薄包装负责预算/数组边界；同边规则不短路、边间短路，原序/同分/双桥中间边双计分与异常回退保持。未建额外连接缓存。两次独立真实nopython探针选桥与原物化字段一致，首次129原型扫描含JIT1.652684/1.760753秒，复调约1.396/1.474毫秒，不是完整求解提速。正式bridge尚未接线，YAML/SQLite/规则/发布不改；[阶段2证据](docs/implementation/evidence/apsgo_v7_numpy_numba_virtual_bridge/stage_02_numeric_kernels/README.md)和提交正文记录专项/累计与原始测量。提交后持续阶段3唯一入口接线；不关闭完整性能或Windows门禁。

- 当前NumPy／Numba阶段1（2026-09-08，实施前 `b659866`）：阶段0已提交，双树各3560项通过。设计/计划v0.3；新增私有 `_bridge_numeric.py`，准备绑定问题/规则集/上下文的只读原型和本次锚点数组；缺失掩码、规则顺序、原始Decimal温区及64行分块停止均保留。未知类型/重复边规则/不安全投影保守回退，不提前物化。数值桥接边均含生成型虚拟材，软硬规则只读原虚拟桥接开关，不建无用牌号编码。精确依赖NumPy2.2.6/Numba0.65.1/llvmlite0.47.0已声明，只有此模块有外部库导入例外；其余核心架构不放宽。正式bridge、规则、预算、YAML、SQLite及发布脚本不改；[阶段1证据](docs/implementation/evidence/apsgo_v7_numpy_numba_virtual_bridge/stage_01_numeric_inputs/README.md)及提交正文记录验证。独立提交后持续进入阶段2，不把数据准备视为内核/性能/Windows验收；下方旧入口为历史。

- 当前NumPy／Numba阶段0（2026-09-08，实施前 `7fff997`）：用户已要求按专项计划持续实施，设计/计划v0.2，第11节三项为执行依据，不在小步骤之间重复询问。明确源码完整Python基线86.983092秒、CPU86.589640秒，200000/3697/54、质量 `(0,0,0,0,11726,660,23)`；完整有序方案/评价/轨迹/缓存与旧明确源码样本相同，双审计通过。900/10、最新310/10及原正式180/10输入分别冻结；指定Conda内保留NumPy2.2.6并安装Numba0.65.1、llvmlite0.47.0，未来hooks锁2026.6。两次独立编译小探针通过，首次调用0.275383/0.181187秒，只证可行性，不代表正式内核性能。新增两个薄验证工具，无生产/配置/库/发布变更；[阶段0证据](docs/implementation/evidence/apsgo_v7_numpy_numba_virtual_bridge/stage_00_baseline/README.md)，最终共享/暂存树累计见提交正文。独立提交后继续阶段1私有数值适配；Windows条件不足时暂停，旧平台门禁不自动关闭。下方“仅文档/待确认”是历史状态。

- 当前 NumPy／Numba 虚拟桥接文档入口（2026-09-08，编写前 `d55dadbcc2610b0cd8ef29362084f40ff3929706`）：用户只要求先创建设计与实施计划，新增[专项设计](docs/design/apsgo_v7_numpy_numba_virtual_bridge_design.md)和[实施计划](docs/implementation/apsgo_v7_numpy_numba_virtual_bridge_implementation_plan.md) v0.1，仅同步两份与本文件，阶段 0～6 均未开始。提案为私有数值目录和分块单/双桥选择，先选原型再用原方法物化胜出节点；唯一入口、规则配置、遍历/同分顺序、温区、业务计数、评价/采纳及双审计不改。待确认三项：仅私有模块增加 NumPy/Numba 且首批四种边规则单实例组合、64组合块间停止与真实缓存统计变化、首次实际使用时JIT计入原搜索时限且不设磁盘缓存/启动预热。未知规则/不支持形状回落原Python，编译/依赖错误不得静默回退；同工作量业务须一致，不要求伪造旧缓存或轮询次数。下一步获确认和代码授权后进入阶段0，当前未安装依赖、改生产/测试/发布代码或配置/库。最新 `b5abc4aa…` 为310/10、119809次检查、304.744141秒、CPU301.218750秒，零禁止/零欠重且双审计通过，不与旧900/10全20万次混作同工作量，`unversioned` 仍不能关闭Windows版本门禁。共享累计3560项通过（102.88秒）；最终精确暂存树验证见提交正文。沿用当前性能分支，不部署、停服、合并或推送，旧Windows及历史20对验收边界保留。

- 当前未变链评价复用阶段 3（2026-09-08，实施前 `5279885`）：阶段 0～2 已分别由 `1bc5bf3`、`be9fd7c`、`5279885` 提交，阶段 2 双树各 3522 项通过。设计/计划 v0.5；5 对完整测量的 10 次有序业务结果完全一致，完整经过时间中位数 131.241016→83.180424 秒（减少 36.62%）、CPU 131.129282→83.078666 秒，首轮 66.527548→37.251942 秒（减少 44.01%）。首轮探针完整链实际 4136/潜在 51937 次、节点 85225/1235956 次，全局逐候选重算；条目去重最大 31 仅为本次首轮观察，不是字节内存或全流程硬上限。原正式 180/10 输入单次 72.170514 秒，200000/3603/58、质量 `(0,0,0,0,10912,600,22)`、原质量门和双审计通过；不与现场 900/10 的 `(0,0,0,0,11726,660,23)` 混作同输入。新增 38 项工具测试，最终共享/暂存树累计见提交正文；证据在 `docs/implementation/evidence/apsgo_v7_unchanged_chain_evaluation_reuse/stage_03_comparison/README.md`。阶段 3 不改生产；没有 NumPy/Numba、配置/库/依赖/规则变化。下一步阶段 4 等待 Windows 明确提交的 EXE、发布清单、匹配规则库/配置和现场诊断；本机测量不替代 Windows 或历史 20 对门槛，不部署、停服、合并或推送。下方旧阶段入口均为历史记录。

- 当前未变链评价复用阶段 2（2026-09-08，实施前 `be9fd7c`）：阶段 0 `1bc5bf3`、阶段 1 `be9fd7c` 已独立提交；阶段 1 最终共享/暂存树各 3494 项通过。设计/计划 v0.4，生产仅评价及邻域两个文件，唯一完整候选入口已接入仅当前方案复用。原始链和节点贡献按序重组，全局每次重算；同对象才命中，首次冷算、拒绝不保留、提交成功后引用提升，绑定变化或未知规则/规则集回落。新增 28 项，评价/审计聚焦 154、候选生命周期相关 269 项通过，独立复核及 Ruff 通过；累计与最终暂存树见本阶段提交正文，证据见 `docs/implementation/evidence/apsgo_v7_unchanged_chain_evaluation_reuse/stage_02_search_reuse/README.md`。提交后持续进入阶段 3：同输入完整有序对照、五对完整计时、函数分析及正式质量；目前不宣称性能收益或 Windows 验收。不改规则、配置、库、预算、依赖或算子，不部署、合并或推送。

- 当前未变链评价复用阶段 1（2026-09-08，实施前 `1bc5bf3`）：阶段 0 已独立提交。设计/计划 v0.3，评价模块已提取唯一共用组装；公开入口和搜索仍无缓存，原始贡献、顺序和数值公式不变。新增 9 项用例，评价/数值/规则分派聚焦 130 项通过，20 种内置及派生规则已独立核验；未知规则集子类也回落原评价。累计和精确暂存树结果见本阶段提交正文，证据见 `docs/implementation/evidence/apsgo_v7_unchanged_chain_evaluation_reuse/stage_01_shared_evaluation/README.md`。提交后持续进入阶段 2 唯一候选入口接线；尚未完成复用收益或 Windows 验证。

- 当前未变链评价复用阶段 0（2026-09-08，实施前 `70c8494`）：用户已授权按专项计划持续实施，各阶段独立验证与中文 `#feat`/`#fix` 提交，仅新增决策或 Windows 条件不足时暂停。设计/计划 v0.2；最新 900/10/200000 输入已冻结，工具增加完整有序评价快照。生产与 `590faed` 相同，完整观测 131.571310 秒、CPU 131.422517 秒，200000/3697/54、质量 `(0,0,0,0,11726,660,23)`、双审计及现场业务指纹一致；工具 8 项通过。累计及精确暂存树结果见本阶段提交正文，证据见 `docs/implementation/evidence/apsgo_v7_unchanged_chain_evaluation_reuse/stage_00_baseline/README.md`。阶段 0 提交后持续进入阶段 1，不把基线当复用收益；不改依赖、配置、库、规则/算子语义、V3/V6/C#，不部署、合并或推送。下条“仅文档”是此前状态，Windows 待验边界保留。

- 当前未变链评价复用文档入口（2026-09-08，编写前 `590faede8ff77efb9bf988569bda9e030fbe8aa1`）：用户仅授权研究并输出文档；新增[专项设计](docs/design/apsgo_v7_unchanged_chain_evaluation_reuse_design.md) v0.1、[实施计划](docs/implementation/apsgo_v7_unchanged_chain_evaluation_reuse_implementation_plan.md) v0.1，本轮只同步这两份与本文件，阶段 0～4 均未实施。提案为同次搜索、仅当前已接受方案、同一不可变链对象的评价复用，保存完整链和节点原始贡献及链摘要；首次候选冷算，拒绝不缓存，原提交成功后才提升，资源/全局聚合/最终独立审计继续重算。未核验自定义规则回落原评价，不改规则基类、API、YAML、SQLite、依赖或各算子，不扩大到 NumPy/Numba。新现场归档 `02b74678...7z` 为 900/10/200000，625.184367 秒、CPU 621.125 秒、200000/3697/54、零禁止零欠重且双审计通过；不同于旧 900/30 输入，且包版本仍 `unversioned`，不是本项提速或正式 Windows 验收。共享累计 3484 项通过（140.11 秒），最终文档/精确暂存树验证见提交正文；下一步须获确认后进入本计划阶段 0，前一专项 Windows 门禁不自动关闭。

- 当前性能优化本机完成边界（2026-09-08，阶段 2 实施前 `6b1311b`）：阶段 0 `3315925`、阶段 1 `6b1311b` 已分别提交；计划 v0.4 的阶段 2 实测和正式质量回归完成，最终提交验证见提交正文。[阶段 2 证据](docs/implementation/evidence/apsgo_v7_solver_performance_optimization/stage_02_comparison/README.md) 与统计摘要保存五对首轮、四次完整运行及原文件身份。首轮中位数 103.713202→66.526065 秒（减少 35.855741%），完整第一组 203.647481→131.456331 秒；所有固定工作量方案/轨迹/计数/资源/业务指纹一致，双审计及逐单守恒通过。现场绑定请求原 900/30/200000，结果 `(0,0,0,0,11726,660,23)`；历史正式输入原 180/10/200000，单次 120.205827 秒且原质量门通过，结果 `(0,0,0,0,10912,600,22)`，不能混作同输入，也不替代历史 20 对性能门。生产只保留已提交的同次桥接节点复用，没有新增依赖、改规则/YAML/SQLite/预算；完整评价成为下一候选热点，仍须另行确认。下一入口为阶段 3 Windows 实包：当前 macOS 无法代验，现场归档缺匹配数据库快照，已列明 Python 冻结请求重放与 EXE 独立测试说明。等待 Windows 条件，不自动新增重放接口、部署、停服、合并或推送；下方旧“下一步阶段 0/1”均是历史入口。

- 当前性能优化阶段 1（2026-09-08，实施前 `3315925`）：阶段 0 已独立提交。计划 v0.3 的节点复用已在 `VirtualFactory.bridge()` 实现，仅增加调用内两个位置的映射，第一位置复用单桥遍历值、第二位置按需创建；两个原型位置仍有不同编号，不跨调用复用。旧代码测试先暴露重复创建，修改后 275 项聚焦通过：小例 12→6 次创建，选桥、17 次边查询、4/13/13 缓存统计及 49 次预算检查不变；新增取消/超时、温区与编号测试，独立静态复核和 Ruff 通过。累计及最终暂存树结果见提交正文；[阶段 1 证据](docs/implementation/evidence/apsgo_v7_solver_performance_optimization/stage_01_bridge_reuse/README.md) 已记录。未改规则、YAML、SQLite、预算、正式编号/采纳及审计，不新增依赖；提交后持续进入阶段 2 实际复测，不提前宣称速度收益或 Windows 通过。

- 当前性能优化阶段 0（2026-09-08，实施前 `3c81a69`）：用户已授权按专项计划持续实施。计划 v0.2 的输入冻结与测量工具已实现，复用公开求解入口、精确 JSON 和既有身份辅助；读取同一份字节进行校验和保存，首轮截取不进入拆单/审计，完整模式保留双审计。真实首轮 102.523736 秒，64226 次检查/2263 次评价/34 次采纳，原方案与轨迹一致；7 项工具测试及 Ruff 通过，累计与精确暂存树结果见提交正文。[阶段 0 证据](docs/implementation/evidence/apsgo_v7_solver_performance_optimization/stage_00_baseline/README.md) 保存身份、原始样本路径和恢复命令。生产、规则、预算、YAML、SQLite 均未改；提交后继续阶段 1 节点复用，不把本次首轮观测当作完整质量或性能验收。以下“仅文档/须另授权”是此前状态，条件性 NumPy/Numba 等和 Windows 操作边界仍保留。

- 当前性能优化文档入口（2026-09-08，来源 `codex/rule-setting-api-integration@ec3e0a79f0a74675ad478d2011a8aad9136f8b78`）：已从当前本地提交创建 `codex/solver-performance-optimization`，本轮仅新增[求解性能优化专项设计](docs/design/apsgo_v7_solver_performance_optimization_design.md) v0.1、[专项实施计划](docs/implementation/apsgo_v7_solver_performance_optimization_implementation_plan.md) v0.1 并更新本文件；代码阶段 0～3 均未开始。首项是 `VirtualFactory.bridge()` 同次调用内按原型及两个编号位置按需复用节点，保留规则、枚举/同分顺序、锚点温区、边查询、预算检查、正式编号及双审计。固定工作量下方案/轨迹/计数与公开边缓存统计须一致，限时运行因截断变化可产生不同结果，不能混作等价性证明。NumPy / Numba、完整评价复用、CSR 和多进程仅为测量后另行确认的候选，不自动安装或实施，不新增性能门槛。
- 本专项下一入口为阶段 0“冻结对照输入与测量入口”，须另获代码实施授权；现场 900 秒总时限/30 秒收尾与仓内 310 秒/10 秒配置分开保存，不覆盖 YAML 或正式库，不以 `unversioned` Windows 包作为明确源码基线。临时分析资料仅是线索，先冻结可复现输入和工具；只同步两份专项文档、本文件及后续本项证据，不改 V6、V3、C#、主分支或远端。运行/测试沿用 Conda `aps_3.10.18`，独立验证及中文 `#feat`/`#fix` 提交；本次共享累计 3463 项通过（193.31 秒），最终精确暂存树结果见提交正文，不代表任何优化已实现或 Windows 已验收。

- 当前 CPU 计时实施记录（2026-09-08，实施前 `0ce9c2e`）：用户已授权按计划持续推进。[求解日志与诊断数据落盘实施计划](docs/implementation/apsgo_v7_solver_diagnostics_implementation_plan.md) v0.7 的阶段 5 已接入请求入口双时钟、累计进程 CPU、平均使用核数及按系统逻辑处理器数换算的百分比估计；复用原日志和摘要，不新增依赖、配置、采集线程或核心字段。未知采样为缺失，不猜核数、不从中断后补建起点；取消后仍记录原工作线程收尾，摘要不含自身完整写盘，后续全局结束日志再采样。前置请求错误日志也隔离外部处理器故障，不能替换原 HTTP 错误。54 项诊断专项、471 项服务/发布/架构、共享树累计 3463 项通过（199.49 秒），原生 Ruff 通过；最终精确暂存树同范围累计及干净导出检查见本阶段提交正文。生产修改仅服务 `app.py` 和 `diagnostics.py`，算法、规则、预算、指纹、HTTP、YAML、正式库、C#、V3/V6 不改；未运行现场服务或重跑 531 单/20 对性能。[阶段 5 证据](docs/implementation/evidence/apsgo_v7_solver_diagnostics/stage_05_cpu_timing/README.md) 已列 Windows 复测步骤，阶段 6 仍缺 Windows 实包证据，不能以 macOS 测试代替或据此确定现场慢因。以下“仅文档/待实施”是此前历史状态。

- 当前 CPU 计时文档入口（2026-09-08，补充前 `9d6a8fd`）：用户仅授权补充文档，[求解日志与诊断数据落盘实施计划](docs/implementation/apsgo_v7_solver_diagnostics_implementation_plan.md) 已升至 v0.6，新增第 8 节及阶段 5“CPU 计时接入与回归测试”、阶段 6“Windows 实包复测与结果分析”；两阶段尚未实施。借鉴 V3 标准库双时钟，复用 V7 现有请求上下文、日志和摘要，不另建采集系统。CPU 起点与请求经过时间起点一致，累计 CPU 不是阶段 CPU，也不是求解线程独占时间；摘要不包含自身完整写盘，后续全局工作线程结束日志才反映该段收尾，二者不强求数值相同。不新增等待时间推断、配置、依赖或核心结果字段，不改算法、规则、HTTP、预算、指纹、数据库、C#、V3/V6；只修改本文件及该计划。后续须另按用户指令进入代码实施，Windows 与旧验收状态不因本文更新而自动通过。

- 当前求解诊断本地收口（2026-09-08，实施前 `080f8b2`）：阶段 1～3 分别提交 `6d7f9ee`、`c8b6873`、`080f8b2`。阶段 4 真实 531 单、活动规则 5 的关闭/开启对照通过：200.208208 / 200.945054 秒；均 200000 次检查、3697 次完整评价、54 次采纳、23 链、零禁止及零欠重，双审计/来源守恒与稳定响应/轨迹身份一致。开启产生 2080284 字节诊断资料、566 行人工候选，全部时间与行数据校验通过，数据库未改。证据在 `docs/implementation/evidence/apsgo_v7_solver_diagnostics/README.md`，最终累计见本阶段提交正文。没有重跑 20 对性能验收或放宽门槛；当前 macOS 不替代 Windows 实包验证，现场版本 6 的不可发布原因须以新日志和输入定位。新版模板默认启用，旧现场 YAML 需人工补诊断块，不能覆盖现场库。
- 当前算法过程日志入口（2026-09-08）：阶段 3 已补齐应用与核心阶段的实时开始、结束、截断、跳过、异常，以及正式提交后的采纳记录。标准库日志辅助只处理观测，不增加运行预算检查、规则评价或第二条求解入口；受控拆单重放明确标记且不重复累计指标。16 项专用、423 项聚焦通过，最终暂存树累计以本阶段提交记录为准；下一步为 531 单关闭/开启诊断对照及使用说明，不能以 macOS 验证替代 Windows 实包验证。
- 当前诊断数据落盘入口（2026-09-08，实施前 `6d7f9ee`）：阶段 1 已提交，干净导出累计 3392 项通过。阶段 2 以工作线程为单次诊断生命周期，保存原始/绑定输入、实际响应、最终搜索候选及双审计、人工 CSV 和独立日志；同请求重复不覆盖，诊断失败不改变 HTTP 及求解结果。最后关闭单次日志后写摘要，全局结束行计入收尾耗时；目录根为 YAML 相对路径，正式数据库及算法决策不变。本项最终回归见提交正文；后续持续推进阶段 3 与阶段 4，Windows 现场实包仍需单独验证。
- 当前求解诊断实施入口（2026-09-08，实施前 `dfc8521`）：用户已授权按专项计划持续实施。阶段 1 的可选 YAML、每物理行毫秒时间、轮转日志、请求耗时及不可发布失败摘要已接线，聚焦 129 项通过；最终暂存树验证以提交正文为准。根 `/diagnostics/` 加入忽略并保留用户原末尾空行；阶段 2 输入/候选文件和阶段 3 算法过程日志尚未实施。规则、预算、HTTP 发布语义及正式数据库未改，下一项为“阶段 2：保存输入与最终候选”。
- 当前求解诊断文档入口（2026-09-08，编写前 `6cd7a46825afe18696930abcc8b460f9935d9af5`）：用户要求先形成[求解日志与诊断数据落盘实施计划](docs/implementation/apsgo_v7_solver_diagnostics_implementation_plan.md)。本轮只新增该计划并更新本入口，阶段 1～4 尚未实施；每行日志包含本地日期/时间/毫秒，求解另记录阶段与累计耗时，复用绑定输入、最终候选、违规及双审计，保存逐请求证据。不可发布候选只供诊断，不改变 HTTP 发布语义；不打印逐候选检查，不修改规则、算法决策、300 秒搜索或 200000 次上限。新 YAML 的可选 `diagnostics` 块、重复请求独立目录、线程取消收尾和写盘失败隔离按该计划实施，每阶段独立验证并中文 `#feat`/`#fix` 提交。本次保留 `.gitignore` 末尾空行的用户改动且不纳入文档提交，不改 YAML、数据库、C#、V3 或 V6；下一代码入口是“阶段 1：统一时间日志与配置”。
- 当前 Windows 发布源树门禁修正入口（2026-09-08，修正前 `0fb9814ca0d5feb7369786a892c54327fd72b0c0`）：用户确认将根目录 `/.vscode/` 加入 `.gitignore`，与既有 `/.idea/` 一样视为不参与发布的本地编辑器目录。Windows 正式构建不再强制使用分离提交状态，干净普通分支和分离提交状态均可；完整 40 位提交号仍是权威身份，清单记录实际本地分支名或 `null`。全部已跟踪改动、其他未忽略的未跟踪文件、SQLite 辅助文件及配置/数据库/包级错误仍被拒绝，构建前后复核分支、提交、配置和数据库身份。发布专项 55 项、共享累计 3353 项通过；共享残留检查仍只因 V7 已不存在的 8 个 V6 历史稳定残留失败，不为本次重建。本次只修源树入口及相应测试、文档，不改业务、数据库、发布内容或 Windows 验收门槛；当前 macOS 仍不能替代 Windows `-CheckOnly`/`-Clean` 实测。
- 当前 Windows 打包修复入口（2026-09-08，修复前 `49ad41baf4bdaa0c991dcce98b1eedc68c4e4ba1`）：首次 Windows `-CheckOnly` 实测已通过操作系统、64 位进程和 Conda 环境定位，但在 Python 运行时检查处失败；日志中的 `version: ..join(...)` 证明 Windows PowerShell 调用原生 `python.exe -c` 时丢失了多行脚本内的双引号。根因修复将运行时检查、PyInstaller 版本检查和数据库种子备份三段多行 Python 统一改为经标准输入交给 `python.exe -`，避免只修首个报错后在后两段再次失败；不改构建输入、依赖版本、数据库、业务代码或发布流程。发布专项 52 项、共享累计 3350 项通过；当前 macOS 无 PowerShell，修复提交后仍须在 Windows x64 的干净 Git 工作树重新执行 `-CheckOnly` 和 `-Clean`，未重跑前不得称 Windows 门禁通过。详见[阶段 2 源码与首次 Windows 检查证据](docs/implementation/evidence/apsgo_v7_windows_exe_release_packaging/stage_02_windows_onedir_build_source/README.md)。
- 当前发布实施入口（2026-09-08，阶段 4 实施前 `b2f683b0d569154f44892b29070e09b43199f295`）：阶段 4“建立发布清单与包级冒烟”源码已实现。清单固定应用/项目/提交/构建依赖、源库与种子逻辑身份、规则/原型/软硬钢字典身份、验证状态及除清单自身外的逐文件 SHA-256，严格拒绝非规范 JSON、未知字段、现场文件、sidecar、源码、测试、缓存、日志、链接和 Windows reparse point；PowerShell 只在临时副本规则 GET→同内容 POST→GET→停止→重启→GET 全部通过后把 `smoke` 从 `pending` 封为 `pass`。冒烟临时现场 YAML 固定 `127.0.0.1` 动态端口，正式 `0.0.0.0:8001` 模板字节不变；回环禁用代理，保存前后稳定业务内容及停服后的完整数据库身份闭合，停止失败只按本次启动 PID 进程树应急清理。发布专项 51 项、共享累计 3349 项通过；最终精确暂存树结果以本阶段提交正文为准。当前 macOS ARM64 未生成或运行 Windows EXE，阶段 2～4 Windows 门禁仍开放；下一步必须在 Windows x64 干净 Git 工作树完成真实构建和本阶段实包冒烟，再进入阶段 5 的 531 单与局域网验收。证据见[阶段 4 发布清单与包级冒烟](docs/implementation/evidence/apsgo_v7_windows_exe_release_packaging/stage_04_manifest_and_package_smoke/README.md)。
- 当前发布实施入口（2026-09-08，阶段 3 实施前 `d2d4f9d47eca4a7b88c71175f4f88e6c0f416a52`）：阶段 3“建立启停与规则数据库保护”源码已实现。构建复用现有 SQLite Backup API 生成种子，`seed` 验证比对完整逻辑内容与活动业务身份，拒绝 sidecar、链接和同一文件；长时构建结束前重新校验源 Git/YAML/数据库身份及包内种子。启动脚本独立、原子初始化缺失的现场 YAML/运行库，已有文件绝不覆盖；停止脚本只匹配自身目录 EXE 绝对路径并容忍进程已退出竞态。37 项发布专项及真实种子对照通过，最终累计回归以本阶段提交正文为准。当前 macOS ARM64 未执行 BAT/PowerShell 或生成 EXE；阶段 2～3 Windows 门禁仍开放。下一源码入口为阶段 4“建立发布清单与包级冒烟”。证据见[阶段 3 启停与数据库保护](docs/implementation/evidence/apsgo_v7_windows_exe_release_packaging/stage_03_start_stop_and_database_protection/README.md)。
- 当前发布实施入口（2026-09-08，阶段 2 源码实施前 `4bebd5f0e4a21668e923760fd16ec094cf90469a`）：阶段 2“建立 Windows 目录式 EXE 构建”的源码部分已实现。新增最小冻结入口、薄 BAT、唯一 PowerShell 编排和固定构建依赖；首次 Windows 候选为 `PyInstaller==6.22.2`，只表示官方兼容候选，实包通过前不得称已验证版本。构建固定 Windows x64、Conda `aps_3.10.18`、Python 3.10.18、`onedir + console + noupx` 和 Uvicorn 子模块，不自动安装、不扫描整个 Conda DLL/PYD、不复制 V3 多进程或其他产线资源。正式构建须来自干净 Git 工作树，普通分支或分离提交状态均可；`-CheckOnly` 在任何项目写入前退出，`-Clean` 只处理 `release/build` 与 `release/dist/APSGoV7` 并拒绝重解析点。源码专项 26 项和入口帮助通过，最终累计结果以本阶段提交正文为准；当前 macOS ARM64 未生成 Windows EXE，阶段 2 Windows 构建门禁仍未关闭。证据见[阶段 2 源码记录](docs/implementation/evidence/apsgo_v7_windows_exe_release_packaging/stage_02_windows_onedir_build_source/README.md)。
- 当前发布实施入口（2026-09-08，阶段 1 实施前 `656f9dc70b23b96fb214fe8513f021f5a8e5ac6a`）：阶段 1“建立发布输入与数据库验证”已实现，新增 `release/verify_release.py` 及 `tests/release/test_release_packaging.py`。验证器提供源码与发布包两种模式，使用 SQLite 强制只读冻结连接，复用既有配置、schema、活动规则、虚拟原型和字典校验；不调用初始化、迁移、保存、恢复或备份入口，不修改生产服务、数据库、YAML、规则、算法或 HTTP 契约。21 项专项回归通过，最终累计结果以本阶段提交正文为准；证据见[阶段 1 验证器记录](docs/implementation/evidence/apsgo_v7_windows_exe_release_packaging/stage_01_source_and_database_validator/README.md)。
- 当前发布实施入口（2026-09-08，阶段 0 实施前 `90c78d2f460fd24add0f078508fdbb14cbc486dd`）：阶段 0“固定发布输入与数据库跟踪边界”已完成，用户已有的 `.gitignore` 修改按确认语义纳入本阶段，主数据库允许受 Git 跟踪，`-journal`、`-shm`、`-wal` 继续忽略并禁止进入发布包。README 已改为当前口径，数据库、YAML、生产代码和业务契约未改，未生成 Windows EXE。权威入口为[Windows EXE 发布打包详细设计](docs/design/apsgo_v7_windows_exe_release_packaging_design.md)、[实施计划](docs/implementation/apsgo_v7_windows_exe_release_packaging_implementation_plan.md)与[阶段 0 证据](docs/implementation/evidence/apsgo_v7_windows_exe_release_packaging/stage_00_release_input_baseline/README.md)。
- 首版发布采用 Windows x64 原生构建、PyInstaller 目录式控制台程序、现有 YAML 和单 Uvicorn worker；不复制 V3 的全 Conda DLL/PYD 扫描、多进程入口和其他产线资源。现有 YAML 在发布包中使用模板文件名，首次启动才生成现场 YAML，已有现场配置永不覆盖；Git 跟踪数据库是唯一发布基准，构建通过现有 SQLite Backup API 生成不同文件名的种子，首次启动才创建可写运行库，已有运行库永不覆盖。当前现场只读快照为 schema 2、活动版本 5、17 条规则（16 启用）、27 个虚拟原型、230 条字典；这些值只作漂移证据，每次构建必须重新读取，禁止硬编码。
- 下一门禁是在本阶段源码提交的 Windows x64 干净 Git 工作树中，显式安装固定候选并依次执行 `-CheckOnly`、`-Clean` 和 EXE `--help`；普通分支可直接使用。通过后另交证据关闭阶段 2。若暂时没有 Windows 环境，只能继续准备后续发布源码，不能把 macOS 检查冒充实包验收。最终 Windows EXE 还必须在无 Python 的目标机完成规则读写重启、真实 GQGA4 531 单、局域网、升级和回退验收。
- 当前正式库迁移（2026-09-07，实施前 `377ebc2`）：用户确认服务无人访问并允许停机，备份目录为 `/Users/miles/dev/dev-py/APSGOV7-bak`。正式 `data/apsgo_v7_rules.sqlite3` 已从 schema v1、活动版本 1 迁至 schema v2、活动版本 3；版本 2 导入 V3 GQGA4 字典 230 条（软钢 131、硬钢 99），版本 3 复用保存并启用事务，只给 27 个既有虚拟原型合并 `hot_roll_grade=SPHC`，17 条规则、其他原型字段和历史版本均保留。迁移前备份为 `apsgo_v7_rules_before_schema_v2_20260907_201925.sqlite3`，SHA-256 `bc9088995333c79c8cddb626d9951af1283861d278319259784f5f86baaa8bf6`，权限 600；SQLite Backup API 临时恢复文件与备份字节相同，完整性、外键、结构和逐表内容通过。迁移后正式库 SHA-256 `8659e4a63f496e40ce33baa39531e068f307cae82eb6d48cf101c47206860de5`，规则指纹 `fd313421b74603e10a942d60f05fffc1b58d41f41677b1579ad14c8ec1863607`，字典指纹 `d292d5efb53ee541f4d3900b2295ababfecffb1912fd0bafe4cb8bbb7b90ec14`。服务已用 Conda `aps_3.10.18` 和正式 YAML 恢复，监听 `0.0.0.0:8001`；`127.0.0.1`、`192.168.4.42` 的规则 GET 均返回 200 和活动版本 3。阶段 9 证据在 `docs/implementation/evidence/apsgo_v7_month_scheduling_and_grade_preparation/stage_09_production_database_migration/README.md`；阶段 8 尚缺 Windows Debug/Release、Designer、真实页面和失败回滚运行验证。
- [历史信息] 搜索预算复测（2026-09-07，实施前 `d98ff45`）：默认 YAML 使用 `total_time_limit_seconds=310`、`finalization_reserve_seconds=10`，即 300 秒实际搜索，候选检查上限仍为 200000；C# 提交 `ae8587f` 将等待超时提高到 370 秒。验收工具提交 `b902f0d` 支持直接读取当前 YAML；独立 `run_06` 在 222.596230 秒达到 200000 次候选上限，HTTP 200、零禁止、零欠重、双审计与 531 来源/29333.91 吨守恒通过，质量 `(0,0,0,0,11726,660,23)`、23 条链、两类拆单各 1 次。相较历史 `run_05`，链间宽差减少 15、虚拟材增加 20 吨，按七级顺序整体更优；搜索未用满 300 秒，这是一轮功能复测，不代替新性能门槛。该轮使用正式库的临时副本；当时正式库未迁移，后续状态以上一条为准。历史 180 秒门槛、20 对样本及 `run_01`～`run_05` 原样保留。
- [历史信息] 阶段 8 Python/真实 HTTP 子项通过：受控拆单边界重建已提交 `ab31244`，虚拟原型热轧牌号契约已提交 `c8d2ba8`，报告活动版本补正已提交 `fccd89e`；C# 新建原型补正为 `5c3bff8`。最终 `run_05` 使用真实 C# 531 单、V3 的 230 条字典、正式 V7 规则库临时副本及 20 万次/180 秒策略，HTTP 200，`success/publishable=true`；140529 次候选检查、3536 次完整评价、53 次接受后得到 23 条链、同期间及未来借入归还拆单各 1 次。零禁止违规、零欠重、双审计、531 个来源和 29333.91 吨真实重量守恒均通过；565 行含 32 个虚拟材，全部 `hot_roll_grade=SPHC`、`soft_hard_class=null`，虚拟重量 640 吨、占比 2.1351902371%。核心 170.218809 秒、外层 170.840363 秒，因时间额度停止但已有可发布方案；这是单样本，不代替 20 对性能。阶段 8 尚缺 Windows Debug/Release、Designer、真实页面和失败回滚运行验证。
- [历史信息] 当前专项进度（2026-09-07，阶段 8 拆单边界重建待提交）：真实 C# 531 单的 `run_01` 在第 2 次未来借入归还拆单移除 `0002002073-000010` 后，把其左右各 2 个旧 `edge_bridge` 拼成 4 连虚拟材，最终审计正确拒绝发布。提交 `009bff1` 的拒绝式保护在 `run_02` 消除了 4 连，但也拒绝了该笔必要拆单，最终留下 `0002002073-000010` 的 600 吨 IF 窄钢连续真实重量违规，仍为 `complete_not_publishable`。当前根因修复只删除被拆父订单左右紧邻、用途为 `EDGE_BRIDGE` 且无拆单分区关联的失效旧桥，再复用 `VirtualFactory.bridge()` 重建剩余真实端点的唯一边界；非桥接虚拟材阻止本次拆单，候选授权独立重算并逐值核验新桥，虚拟序号单调续接。拆单专项 83 项、完整搜索 589 项、共享树累计 3298 项通过；首轮精确暂存树干净导出残留门禁、3298 项累计、编译及 wheel 构建通过，最终暂存树按同范围复验后提交。同请求 `run_03` 尚未执行。历史冻结订单与实际请求按来源逐项业务字段一致，只有跨计划期全局交织顺序不同；HTTP 继续保留前端相对顺序。正式 V7 数据库、三个源数据库和 C# 工作树未因本轮写入。
- 当前专项进度（2026-09-07，阶段 7 实施完成）：C# 仓库 `codex/v7-rule-client-integration` 已提交 `7546dc0 #feat 接入GQGA4月计划V7求解回写`，精确树 `1579b381a4522ce03566f3abea6fe6694ab68385`。GQGA4 命令使用 V7 专属规则、求解及回写服务，在后台按所选版本/产线读取整批“配置规则”记录并按 `RollSeq/Id` 稳定排序；完整结果、来源重量、拆单共同谱系和每分区 `拆片数-1` 个隔离材均核验后，才以短事务替换求解步骤数据。取消边界位于事务之前，失败不推进页面步骤；共享空选择入口和 GQGA4/四镀锌结构化告警计数已接线，V3 及其他产线路径不变。阶段 7 验证脚本 125 项静态契约通过，并在 C# `SchedDatas.db` 临时副本逐字回读 1054 字符/2522 字节的 `Warnings`，源库 SHA-256 前后均为 `6a6e60f...e309`；V7 共享树累计 3295 项通过，耗时 181.27 秒，首轮精确暂存树导出同范围 3295 项通过，耗时 184.59 秒。证据见 [阶段 7 C# 回写](docs/implementation/evidence/apsgo_v7_month_scheduling_and_grade_preparation/stage_07_csharp_writeback/README.md)。当前仅剩 C# 提交外的 `licenses.licx` 和未跟踪 `.gitignore`；Windows Debug/Release、Designer、真实页面与失败回滚运行验证仍是未关闭门禁。阶段 0～7 已完成，下一项为阶段 8“真实数据与前后端联调”；正式 V7 目标库迁移仍在阶段 9，生产包不包含牌号字典 JSON。
- 当前专项进度（2026-09-07，阶段 6 实施完成）：C# 仓库 `codex/v7-rule-client-integration` 已提交 `ac5f6b7 #feat 建立月计划V7专属求解客户端`，精确树 `8836dc1dfd18c069ef7dd26f4c2920c8b7b7cf22`。新增 `ApsgoV7Configuration` 与 `ApsgoV7SchedulingApiClient`，现有规则客户端只复用独立 `PipelineV7ApiBaseUrl`，不会被月计划路由或超时键连带阻断；保留用户地址 `http://192.168.4.42:8001`，月计划路径固定为 `/api/v1/scheduling/GQGA4/default/month/solve`，客户端超时 240 秒。请求/响应/结果行及谱系字段与 Python 契约静态逐项一致，区分 422 错误对象和 `failed/input_invalid` 完整结果，核对请求标识及活动规则版本；网络不自动重试。C# 提交未包含 `licenses.licx`、根 `.gitignore`，未改 V3 类、页面命令或业务数据；当前 macOS 无 .NET Framework 编译器，Windows 构建和运行仍为未关闭门禁。下一项为阶段 7“接入 C# GQGA4 求解服务和原子回写”。
- 当前专项进度（2026-09-07，阶段 5 实施前 `9ff4782`）：软硬钢准备与月计划接入阶段 5 已将阶段 4 传输契约接入现有 FastAPI 宿主，新增 `POST /api/v1/scheduling/GQGA4/default/month/solve`；原规则 GET/POST、256 KiB 上限及错误体保持，新求解路由独立使用 8 MiB/413 和非 JSON/415。YAML `monthly_solve` 严格加载六项并复用 `SolverPolicy`；活动版本在同一只读事务内核对，409 时搜索调用为零。每应用一个非阻塞求解占用，忙时 429；工作线程退出才释放，请求断开经线程安全事件传给既有取消接口，规则查询/保存不受占用；Uvicorn 启动固定单 worker。真正无字典为 503，字典明细/指纹半损坏为 500；`failed/input_invalid` 保留完整诊断并返回 422。113 项聚焦、322 项服务与架构、3295 项仓内累计及真实 TCP 回环通过，共享累计耗时 190.30 秒；精确树和干净导出结果以本项提交为准。正式规则数据库与 C# 用户工作树未改。下一项为阶段 6“建立 C# V7 专属配置和客户端”。
- 当前专项进度（2026-09-07，阶段 4 实施前 `5a5f66c`）：软硬钢准备与月计划接入阶段 4 已实现严格月计划 JSON 请求与发布结果传输契约。请求固定根/期/订单字段，拒绝重复键、未知字段、非有限或布尔数值、空集合、错误期序、重复来源和虚拟输入；显式期序排序但保持订单相对顺序，并由服务端派生身份与实际过渡材角色。外部语义请求、字典补齐前服务输入和绑定后核心请求三层身份分开，绑定结果固定业务规则版本及评分声明。结果只读双审计 release，保持发布顺序，返回固定完整行、拆单/虚拟谱系、权威违规与纯告警投影；无产出计划期不造占位行，非发布结果 rows 为空。正式 release 只允许无违规或链重欠下限允许偏差，禁止违规不能包装为可发布结果。25 项传输与绑定、295 项服务与架构、3268 项仓内累计通过，共享累计耗时 182.11 秒；精确树及干净导出结果以本项提交记录为准。HTTP、YAML 求解策略、正式规则数据库和 C# 用户工作树未改。下一项为阶段 5“接入 HTTP 与 YAML 求解策略”，其中活动版本不一致必须在搜索开始前返回 409。
- 当前专项进度（2026-09-07，阶段 3 实施前 `107b34b`）：软硬钢准备与月计划接入阶段 3 已实现同一只读事务内冻结活动规则、虚拟原型和软硬钢字典；`bind_gqga4_scheduling_task()` 对所有服务层调用统一执行纯订单补齐，核心请求只接收补齐后的不可变订单。绑定任务与结果保留活动版本、规则指纹、字典指纹、准备报告及最终请求五项关联身份；成功报告只记录命中/缺失，已有值冲突通过定位诊断在求解前失败。绑定完成后关闭数据库读取，搜索、边缓存和最终无缓存审计不再查库。7 项绑定专用、203 项服务层、74 项架构及 3250 项仓内累计通过；共享累计耗时 184.44 秒，干净导出结果以本项提交记录为准。正式规则数据库和 C# 用户工作树未写入，下一项为阶段 4“月计划请求与结果转换”。
- 当前专项进度（2026-09-07，阶段 2 实施前 `ca64e10`）：软硬钢准备与月计划接入阶段 2 已实现 schema v2、规则版本字典指纹和不可变字典明细，普通规则保存同事务继承活动字典，历史规则恢复沿用操作前活动字典。首次初始化显式读取 V3 SQLite；旧库迁移先用 SQLite Backup API 固化 V3 一致性快照，再用一个写事务完成 schema、向前规则版本、230 条字典、激活与回读。正式 V3 主文件 SHA-256 仍为 `2c4e44c...cce7e`，实际导入快照 SHA-256 为 `f4164be...25d1f`；临时 V7 副本活动版本 1→2、230 条启用（软 131/硬 99），字典指纹 `d292d5ef...ec14`，备份恢复字节一致。普通保存不能使用保留的迁移主体；失败重试仅复用严格 schema v1 且三张规则表内容完全一致的备份。正式 `data/apsgo_v7_rules.sqlite3` 保持 schema v1 和原 SHA-256，未写入；没有生成字典 JSON。18 项迁移专用、201 项服务层及 3248 项仓内累计通过；共享累计耗时 181.79 秒，干净导出结果以本项提交记录为准。下一项为阶段 3“绑定求解输入与数据准备报告”。
- 当前专项进度（2026-09-07，阶段 1 实施前 `a254c59`）：软硬钢准备与月计划接入阶段 1 已实现 `apsgo_v7_service.grade_dictionary` 的不可变字典条目/快照、全字段稳定指纹、启用项精确查找、订单纯拼接及准备报告。牌号仅按去空白转大写精确匹配；普通真实材和实际过渡材均补齐，未命中或停用写 `None` 并报告，已有非空分类不一致、非文本、空牌号及生成型虚拟输入聚合诊断后整体失败。生产代码不读取 SQLite、V3、tests 或包内 JSON，不修改原订单；13 项专用、87 项专用加架构及 3203 项仓内累计通过。下一项为阶段 2“数据库升级与快照生命周期”，只在临时库实施和验证 schema v1→v2，正式目标库仍留到阶段 9。
- 当前专项进度（2026-09-07，阶段 0 实施前 `f44f371`）：软硬钢准备与月计划接入的阶段 0 已完成，证据见 [原始输入与接入基线](docs/implementation/evidence/apsgo_v7_month_scheduling_and_grade_preparation/stage_00_baseline/README.md)。只读脚本冻结 V3 数据库及相关源码、531 输入和冻结问题身份；GQGA4 230 条字典全部启用，软钢 131/硬钢 99，无规范化重复；V3 生产拼接与冻结问题 531/531 一致，525 软/4 硬/2 空，61 条实际过渡材均为软钢。C# 已确认按版本读取整批“配置规则”、当前 531 输入无虚拟/重复/预拆片，历史结果含拆片和虚拟输出；真实运行库、Windows 构建及长告警 JSON 留待后续。未复制 230 行字典，未改真实数据库、生产代码或 C#；下一项为阶段 1“建立字典快照与纯拼接”。
- 当前实施入口（2026-09-07，实施前 `dcc4c9b`）：用户确认不生成 `src/apsgo_v7_service/data/gqga4_grade_dictionary.json`，字典只通过显式 SQLite 到 SQLite 的初始化/迁移命令写入 V7 数据库；生产包不保存第二份字典，运行时不依赖 V3 路径。先同步专项设计和计划，再从阶段 0 持续实施，每阶段独立验证并使用中文 `#feat`/`#fix` 提交；仅业务语义、真实数据库操作或外部环境门禁需要用户确认时暂停。
- 当前文档入口（2026-09-07，代码基线 `072a6d0`）：用户要求先输出 V7 求解前软硬钢准备及月计划求解接入的设计和实施计划。新增 [专项详细设计](docs/design/apsgo_v7_month_scheduling_and_grade_preparation_design.md) 与 [专项实施计划](docs/implementation/apsgo_v7_month_scheduling_and_grade_preparation_implementation_plan.md)，当前仅文档，阶段 0～9 尚未实施。范围包含 V3 GQGA4 230 行字典迁入 V7、求解前统一拼接、规则/字典/原型同事务冻结、新 V7 月计划 HTTP 入口、C# 专属配置/客户端/求解服务与原子回写；现有核心规则、缓存、算法、评分和门槛继续使用。单牌号未命中保留 `None` 并按现有热轧牌号策略兜底；两条 `HC220YD+Z-GL` 不猜别名，原生产拼接与冻结 531 单分类逐单一致。旧版本字典身份不回填，迁移用目标当前活动规则建立向前新版本；规则恢复保留当前字典。下一代码入口是“阶段 0：冻结原始输入与接入基线”，本轮不修改生产代码、C# 或数据库。
- 用户最新 C# 边界（2026-09-07）：V7 专属配置读取 `SchedApp/App.config` 的 `PipelineV7ApiBaseUrl`，保留用户已修改的值及 `ApsgoV7RuleApiClient` 读取行为；新求解服务只切换 GQGA4，V3 配置、客户端和服务不改。保留按选中记录定位版本后读取全部“配置规则”步骤记录、独立“计算最晚日期”及手动查询“排程结果”的业务流程。本条覆盖下方旧记录中 V7 继续使用 `BACKEND_ALGORITHM_URL` 的目标态约定；旧实施证据保留历史含义。当前 C# 未提交的 `App.config`、`ApsgoV7RuleApiClient.cs`、`Properties/licenses.licx` 和未跟踪 `.gitignore` 属于用户，实施时精确保护。
- 运行与测试环境（用户确认，2026-09-07）：本项目的服务启动、命令行脚本、测试和 `compileall` 统一使用 Conda 环境 `aps_3.10.18`；命令中的 `python` 均指 `/Users/miles/anaconda3/envs/aps_3.10.18/bin/python`（或 `conda run -n aps_3.10.18 python`），不得使用系统 Python 或其他 Conda 环境代替。若该环境缺少执行所需依赖，必须先明确报告，不得静默切换环境并把结果记录为正式验证。
- [历史信息] 局域网监听实施（2026-09-07，实施前 `0ec86f8`）：用户明确要求其他电脑能够访问规则服务，并确认 `listen_host` 可以填写 `0.0.0.0`，本次不把登录鉴权作为前置条件。默认监听为 `0.0.0.0:8001`；配置加载器只允许 `127.0.0.1` 或 `0.0.0.0`，不扩展任意主机名、其他 IPv4 或 IPv6 地址。当时 C# 尚未切换专属地址键；当前以 `PipelineV7ApiBaseUrl` 为准。设计 v0.14、计划 v0.16；专项 64 项、完整服务 143 项、共享累计 3190 项通过。当时真实进程显示 `TCP *:8001 (LISTEN)`，并通过本机局域网地址 `192.168.4.42:8001` 成功回读版本 1、17 条规则和 27 个虚拟原型；第二台电脑尚未实测。首轮精确暂存树 `15c6065357b1ab0a2e775e08a59b5f136b212c3f` 的干净导出残留门禁、3190 项累计、`compileall`、构建和 wheel 安装均通过。
- [历史信息] 规则服务 YAML 配置补正（2026-09-07，补正前提交 `6301149`）：受跟踪入口改为 `config/apsgo_v7_service.yaml`；后续月计划接入在根级增加 `monthly_solve`，当前 C# 使用 `PipelineV7ApiBaseUrl`。生产依赖 PyYAML 使用安全加载器，并严格拒绝危险标签、非法 YAML、重复/非文本/缺失/未知键和非法值；相对数据库路径仍按配置文件目录解析。初始化器与服务继续支持 `--config` 且不读取 `APSGO_V7_RULE_DB_PATH`；恢复命令仍只接受显式既有数据库绝对 `--database-path`。设计 v0.13、计划 v0.15 和阶段 12 证据已同步；专项 103 项、完整服务 144 项、共享累计 3191 项通过，首轮精确暂存树的干净导出累计 3191 项通过，真实 Uvicorn、`compileall`、sdist、wheel、临时安装和三个命令入口均通过。当时默认数据库重复初始化前后 SHA-256 均为 `af15c62d5920dee93da0bc1dda9ab519d04829c142532025d43f7837737342d0`；当前正式库状态以上方阶段 9 记录为准。
- [历史信息] 阶段 12 补正前 JSON 方案（2026-09-07，阶段 12 实施前 `85b1583`）：补正前方案已完成验证。V7 服务运行参数当时统一迁入受跟踪的 `config/apsgo_v7_service.json`，恰好包含 `database_path`、`database_timeout_seconds`、`listen_host`、`listen_port`。默认数据库值为 `../data/apsgo_v7_rules.sqlite3`，相对路径按配置文件目录解析为绝对路径；非法配置在数据库操作和 Uvicorn 启动前失败。初始化器与服务支持 `--config`，不再读取 `APSGO_V7_RULE_DB_PATH`；底层存储和求解绑定显式接收数据库路径。恢复命令仍必须显式指定既有数据库绝对 `--database-path`，不读取服务配置。`listen_host` 严格限定 `127.0.0.1`；C# 保留 `SchedApp/App.config` 的 `BACKEND_ALGORITHM_URL`。运行期 SQLite 文件族继续忽略。本阶段未改数据库 schema、活动规则内容、求解器、HTTP 契约或 C# 工程；历史环境变量记录保留当时语义。设计 v0.12、计划 v0.14 和 [阶段 12 证据](docs/implementation/evidence/apsgo_v7_rule_setting_api/stage_12_service_configuration/README.md)已同步。配置聚焦 101、完整服务 142、共享及干净导出累计各 3189 项通过；真实 Uvicorn、默认数据库重复初始化与哈希不变、`compileall`、sdist、wheel 安装及三个控制台入口验证通过。共享残留检查仍因冻结清单中的旧 V6 缺失残留失败，不为本阶段重建；干净导出残留门禁通过。Windows 构建、Designer 和真实页面交互仍为整个规则设置专项的独立未关闭门禁。
- 当前规则设置接口收口入口（2026-09-07，阶段 11 实施前 `d9e90b7`）：阶段 11 的后端真实回环脚本已由 `b57f10d #feat 增加规则接口真实联调验收` 独立提交；真实 Uvicorn + 临时 SQLite 覆盖链重、厚度有序数组、虚拟原型小数与关闭/恢复、409、422、即时/历史幂等重放及操作标识内容冲突，HTTP 闭环结束时为 5 个版本/85 条规则。历史规则受控恢复按设计 v0.11、计划 v0.13 实现：命令必须显式指定既有数据库绝对路径，并接收历史来源版本、保存 UUID 和操作前活动版本；复用完整历史快照校验及正常保存事务，只创建向前新版本、不修改历史行，审计主体记录来源。当前活动版本不得伪装成恢复来源，同一操作标识还须匹配原执行主体；被后续版本替代的重放不重新激活且以非零退出码提示。真实脚本恢复后为 6 个版本/102 条规则，并完成 1 个版本/17 条规则的 SQLite 备份恢复；默认数据库主文件及 WAL/SHM 前后均不存在。后端关键矩阵 204 项、恢复相关 62 项、架构 74 项、共享候选工作树累计 3167 项、最终精确树累计/构建/绑定复核及 C# 阶段 8～10 三组静态验证均通过。当前 macOS 无 .NET Framework 4.7.2、MSBuild 和 DevExpress 20.1.3，因此 Windows 构建、Designer 与真实页面交互是唯一未关闭门禁；本专项不得提前标记完成。证据见 [阶段 11 联调与验收记录](docs/implementation/evidence/apsgo_v7_rule_setting_api/stage_11_integration_acceptance/README.md)。
- 当前规则设置接口实施入口（2026-09-07，V7 实施前基线 `574f75e`）：开发分支为 `codex/rule-setting-api-integration`，权威文档为 [月计划规则设置接口详细设计 v0.10](docs/design/apsgo_v7_rule_setting_api_design.md) 与 [月计划规则设置接口实施计划 v0.12](docs/implementation/apsgo_v7_rule_setting_api_implementation_plan.md)。阶段 0～9 的提交保持原记录；阶段 10“改造保存并启用流程”已由 C# 提交 `4b7668c #feat 接入GQGA4规则保存并启用` 完成，精确树为 `ccb9562eb43eaf26bddcd3b31a81fa181f1b4174`。页面以完整 17 条活动规则为底，只覆盖可见字段，并用一次 V7 POST 原子保存和启用；`409`、`422`、网络结果不确定及被替代的幂等重放均有独立处理，只有保存版本仍为活动版本时返回 `DialogResult.OK`。旧 V3 目录、表单规范、预览、草稿和单独启用路径已从 GQGA4 页面移除，其他产线的 `PipelineV3ApiClient` 不改。虚拟材料关闭时提交空集合；空活动集合显示为未启用并预置标准 3 宽 × 9 厚 × 20 吨候选；已有组合保留标识、牌号、温区和规则属性，新增组合使用固定 `virtual_sphc:{width}x{thickness}`、`SPHC`、空温区和空属性；不可无损投影的非空快照隐藏编辑器并原样回传。阶段 8～10 静态检查已在共享 C# 工作树和精确树导出通过，用户未跟踪 `.gitignore` 字节未改；当前 macOS 没有 .NET Framework/MSBuild/C# 编译器/DevExpress 构建环境，不声称 Windows 编译或真实页面通过。下一阶段为“完成联调与验收”，重点关闭 Windows 构建、页面交互和真实 GET→编辑→POST→GET 闭环。共享 V7 残留检查仍因旧 V6 稳定残留清单要求已不存在的 `.claude`、旧 `dist/apsgo-*` 和 `src/apsgo.egg-info` 而失败，不得为通过检查重建或提交这些旧残留；干净导出检查仍是正式门禁。证据见 [阶段 10 保存并启用验证](docs/implementation/evidence/apsgo_v7_rule_setting_api/stage_10_gqga4_save_and_activate/README.md)。
- 用户已确认物理架构：在 APSGOV7 内新增独立 `apsgo_v7_service` FastAPI 服务包，监听地址允许 `127.0.0.1` 或 `0.0.0.0`，默认端口为 `8001`；C# 使用 V7 专属 `PipelineV7ApiBaseUrl`，V3 地址配置不改。V7 使用独立 SQLite 文件和标准库 `sqlite3`，不复用 V3 数据库、不同时实现 MySQL；首期无登录鉴权，审计主体取服务端进程身份，用户已确认非回环监听不以鉴权为前置条件。`apsgo_v7_service` 可单向依赖 `apsgo_scheduler`，后者继续只包含 `api/core/app` 且无 Web、数据库和具体产线字面依赖。
- 已确认接口固定为 `GET /api/v1/rule-sets/GQGA4/default/month/getActiveRules` 与 `POST /api/v1/rule-sets/GQGA4/default/month/setActiveRules`。不保留保存草稿和单独启用：页面一次提交完整 17 条规则可编辑快照，后端在单事务中逐条保存、注入固定七级评分与允许偏差、复用 V7 指纹和加载器编译完整 `RuleSetSpec` JSON并切换活动版本；虚拟材料原型随版本保存但不伪装成规则。求解任务启动时只读一次活动编译快照，搜索过程不查规则数据库；不兼容 V3 规则数据库、DSL 或运行时，首期只适配 GQGA4 月计划页面。
- 保存接口必须同时实现 `save_operation_id` 幂等和 `expected_active_version_id` 乐观并发控制，并先检查幂等记录、再检查活动版本。逐条规则记录用于管理，完整编译 JSON用于求解；历史版本不可修改，事务失败不得产生半版本或改变活动指针。生产种子样本为 17 条规则、16 条启用、`forbid_consecutive_reverse_width` 一条停用、7 项评分和允许偏差 `chain_weight_below_minimum`，样本指纹 `d963206b01c0d439303c1d6ae7d7374e20eaa0777801d12c0bebc89bfe6349c0`；当前正式活动版本 3 的规则指纹为 `fd313421b74603e10a942d60f05fffc1b58d41f41677b1579ad14c8ec1863607`。数据库初始化和回读必须通过生产编译路径重新计算，不直接信任记录值。
- `request_hash` 必须基于阶段 2 按 GQGA4 模板归一参数类型并按固定 17 条顺序生成的页面快照；摘要包含规则集路径身份、期望活动版本、规则、虚拟原型和去空白备注，不包含 `save_operation_id`。禁止对原始请求文本或阶段 1 尚未类型化的数据对象直接求摘要。数据库回读还必须核对版本号、规则集身份、指纹及连续规则顺序均与编译快照一致。
- 当前专项入口（2026-09-05，实施前 `3564277`）：按 [专项设计](docs/design/apsgo_v7_inter_chain_width_optimization_design.md) 与 [专项实施计划](docs/implementation/apsgo_v7_inter_chain_width_optimization_implementation_plan.md) 持续实施，逐项验证并独立提交，仅业务决策、设计变化或验收口径需要确认时暂停。步骤 0～6 已分别提交 `4a02466`、`cddfa3a`、`5e5eeb8`、`eaaf1db`、`12dc232`、`6902be0`、`3564277`，步骤 6 双树各 170 项证据聚焦、50 项绘图及 2947 项累计通过；步骤 7 专项验收现已完成，最终提交验证见下条及提交正文。以下各步未接线说明为当时的历史边界。
- 步骤 7 专项验收完成：三次固定输入运行均为 200000 次额度停止，稳定结果/轨迹/计数相同、宽差 10912。首批 20 对受合盖休眠干扰，完整保留；用户授权的保持唤醒整组复测无休眠记录，新实现中位数 171.498582 秒、第 95 百分位 171.586066 秒，参考漂移均小于 20%，原 180 秒门槛通过。正式 20 次均零禁止/零欠重、10912/600 吨虚拟/22 链，但全部搜索时间停止、检查 166591～171789 次，不宣称自然收敛或每次用满额度。证据见 `step_07_acceptance/`；临时防空闲休眠已随命令退出，不改持久电源配置。共享累计、完整集成及绘图补验通过，最终暂存树同范围验证见本项提交正文；用户已授权将验收提交 `b693fae` 合入本地 `main`，不关闭旧功能 21 的其他范围。
- 步骤 5 在原受控拆单及其至多一次旧搜索重放后、核心审计前增加一次集中精修入口；仅启用宽差目标、当前零违规且没有真实停止时调用并记录阶段耗时。只在进入边界清除自然完成标记，复用同一状态、上下文、缓存、工厂和预算；不回流拆单。整链候选复用原同期间位置与序列构造，保持旧搜索枚举/计数，同时受纯调序和宽差两道保护；完整 GQGA4 收益仍待步骤 6。
- 步骤 6 实测（代码 `6902be0`）：两次独立正式配置运行宽差 11218→10912，22 链、零禁止/零欠重、双审计与守恒通过；虚拟由 320→600 吨，符合宽差优先和 5% 规则。首跑/观察服务分别 167.609114 / 167.886101 秒，200000 次检查后额度停止；语义结果及轨迹相同。原 50 次接受/549 节点保持，新增 7 次节点移动、1 次交换和 14 个桥；四类均实际访问，但本次连续段与切链无接受。新报告与独立原始产物在 `docs/implementation/evidence/apsgo_v7_inter_chain_width_optimization/step_06_real_run/`；本步已提交，后续确定性/性能结论见上方步骤 7，不宣称自然收敛或自动合并。
- 步骤 4 的切链保持全部订单/虚拟节点与原重量，前后两段一次完成联合安置并接受；不新增接口桥或订单拆分。轻量原始位置先扣检查再做真实节点、链重、期序筛选，非法跨期位置不重排成重复候选；每个切点最多 `n×(n+1)` 个原始描述，不能混称完整评价。后段身份确定性派生并处理碰撞，不使用全局编号。
- 步骤 3 只增加段范围描述，完整两链形成与接受复用步骤 2；段内节点原序不反转，已有虚拟节点保留，支持 1 对多及首尾对中段交换，排除整链和纯虚拟段动作。真实枚举测试与原累计回归分开记录，尚未进行新算法 GQGA4 收益验收。
- 步骤 2 使用真实动作描述、共享分批扫描和唯一完整候选接受器；规范化所属期后先按已有规则筛宽差，再连接两链新接口。原始欠重可能由必要接口桥补足，不能在桥接前全部排除；最终必须零违规。宽差预筛不是另一个评价器，不删除旧虚拟节点、不新建缓存或状态；连续段、切链及正式接线仍按后续步骤实施。
- 步骤 1 保留唯一 `try_complete_candidate`，新增默认关闭的宽差专项保护；校验全部旧节点保全、新增内部连接桥按现有工厂物化一致、无任何违规、目标前缀不变及宽差严格降低。新增私有分批扫描骨架只处理轻量动作描述，共享原额度、未改善续扫、接受后全部重建，扫描器不清除任何停止标记。原生产流程仍未调用新骨架，真实节点/段/切链动作在后续步骤实现，不把安全测试当作 GQGA4 收益。
- 本次方案保留原构造、固定修复、受控拆单及其至多一次旧搜索重放，在它们完成后、最终审计前增加集中宽差精修。仅合规方案进入；新增节点/连续段移动与交换、切链与两段联合安置，复用纯整链调序，按小到大分批扫描、改善后重新派生候选。切链不是订单重量拆分，不增加第二轮受控拆单，不重置预算，不新建评分器或全局状态。
- 前置质量保持、宽差严格降低、全部规则及旧节点保全是新候选边界；必要链内接口桥仍用现有工厂，拒绝不消耗正式编号。公共流程接线按步骤 5 验证。固定基线 22 条链内容、方向和期归属时，11218 mm 已达整链调序下界；该结论不是允许改变结构时的全排程最优性证明。
- `5de1d1d` 当时仅更新上述两份专项文档、原 V6 两份文档的衔接说明和本文件，随后获得代码实施授权。原规则/初始构造/授权/审计继续有效，原记录不整体作废；扩展范围以设计第 11 节为准。旧文件中的“唯一执行入口”仅适用于其原实施范围。该提交当时保持 `main` 和 V6 原仓库不变；新证据独立保存，不覆盖历史报告。
- 步骤 0 基线：`main@bb2a31b` 独立导出后完整 GQGA4 实跑，七级 `(0,0,0,0,11218,320,22)`、94518 次检查、2627 次完整评价、50 次接受、2 次同期间拆单，109.671247 秒单次公共服务后自然结束；正式质量门、双审计和守恒通过，与历史结构结果一致。证据在 `docs/implementation/evidence/apsgo_v7_inter_chain_width_optimization/step_00_baseline/`；不是新算法收益或 20 次性能验收。新 3 项合成反例明确区分旧生成能力缺口和手工完整候选可接受性，后续实际生成断言在对应步骤落实。
- 当前仓库：`/Users/miles/dev/dev-py/APSGOV7`；实际主分支名为 `main`，用户于 2026-09-05 授权后已从 `bb2a31b` 快进至验收提交 `b693fae`，开发分支 `codex/inter-chain-width-optimization` 保留。仓库最初从 V6 已提交分支复制并建立独立 Git 历史；本次合入不推送远端，不改动 V6 原仓库。
- 迁入来源：`/Users/miles/dev/dev-py/APSGOV6` 的 `codex/solverpy-path-cover-clean`，提交 `6536acd7586c8c6687e0e9842831a3124a25907f`，源树 `8837bb833af9a8cb05fcf63a02eadf77a5e78cbd`。只复制源提交中的 286 个受跟踪文件；不复制 `.git`、IDE、缓存及未跟踪旧实现残留。V6 提交历史仍在原仓库，以下历史 SHA 不保证能在 V7 直接解析。
- `bb2a31b` 迁移提交的范围仅为仓库与分支准备：当时仅本文件增加 V7 入口说明，其他生产代码、测试、工具、规则配置、设计、实施记录及结果报告保持源提交字节不变。后续变更以本节最新专项记录为准；包名仍为 `apsgo_scheduler`，不因目录名称变更重命名包、批量替换历史 V6 文档或重新生成历史证据。
- 实施前约定链间宽差搜索只在上述开发分支推进，不直接改 `main`；新的首尾调整、链切分与联合重排动作已逐项验证并以中文 `#feat` / `#fix` 提交，现按用户授权合入 `main`。分支建立本身仍不代表功能完成，完成依据是步骤 0～7 的验收记录。
- 已确认评分顺序保持：禁止违规数、禁止严重度、欠重链数、欠重缺口、相邻链首尾宽差总和、虚拟重量、非空链数；跨大辊期边界计入宽差。不设链数上限，仍遵守业务规则、来源覆盖与重量守恒；不把总宽差改成平均值或最大值，也不通过调整预算或验收门槛掩盖算法差异。
- 原工具 `tools/check_workspace_residuals.py --verify` 的共享目录模式要求 V6 历史未跟踪稳定残留存在，不适用于已剥离残留的 V7 Git 工作目录。不得为通过该模式复制旧残留、改冻结清单或绕过保护；V7 开工先核对 Git 状态和受跟踪路径，再对当前提交的干净导出运行原检查器；提交前对精确白名单暂存后的干净导出运行同一检查及累计回归。只有干净导出检查通过时才能记录为 `clean_export` 通过，不得声称 V7 工作目录的旧共享模式通过。此条覆盖以下仅面向 V6 原共享目录的残留检查操作说明。
- 提交前仍使用 `git write-tree` 和 `git archive` 导出到新临时目录；仓内累计范围现为 `tests/architecture tests/api tests/app tests/core tests/service`，使用 Conda `aps_3.10.18`、`PYTHONDONTWRITEBYTECODE=1` 和 `pytest -p no:cacheprovider`。构建及 `compileall` 只在干净导出中执行。初始迁入白名单为源提交的精确文件列表；后续只暂存本项文件，不使用 `git add .` / `git add -A`。
- 原参考脚本及 V3 归档属于外部历史证据，本次不复制、不改写其绝对路径；需要调用这些外部参考时须单独核验其可用性，不能把仓内累计回归称为外部参考核验或完整 GQGA4 性能验收。
- 以下为随源码保留的 V6 历史工作记录。其中旧分支、旧“当前入口”及原目录操作约定以本节的 V7 迁入边界为准；业务规则与已确认决策仍按最近有效记录执行。

# V6 来源分支历史工作约定

- 当前执行记录（2026-09-04，实施前 `c2d87bc`）：22.5 目标顺序/链数门槛修订已提交；按用户要求进入 22.6，使用正式七级配置完整重跑 GQGA4，并更新独立 `report` 报告。实际七级 `(0,0,0,0,11218,320,22)`，94518 次候选检查、2627 次完整评价、50 次接受及 2 次同期间拆单，单次公共服务 103.485174 秒后自然结束；零禁止、零欠重、来源守恒及双审计通过，不设链数上限。按指标名称比较，宽差、虚拟重量与链数均与 22.4 相同，不宣称优先级调整已改善质量或性能。计划 v0.62 第 8.23.11 节及 `docs/implementation/evidence/solverpy_path_cover_local_search/function_22_inter_chain_width_gap/step_22_6_priority_rerun/README.md` 记录实际验证；新报告入口为同目录 `report/report.html`，保留旧结果/旧图。只更新报告读取的目标位置适配及对应测试，不改生产、输入、规则、预算或门槛；本项不包含 20 对性能或完整功能 21 验收。

- 当前实施入口（2026-09-04，实施前 `495e4bf`）：用户确认将七级评分原第 5、7 项互换，现顺序为禁止违规数、禁止严重度、欠重链数、欠重缺口、相邻链首尾宽差、虚拟重量、非空链数；指标公式和严格逐级比较不变。用户同时取消“最多 22 条链”验收上限，链数仅作末级优化目标；零欠重、零禁止、来源覆盖守恒、虚拟比例 5%、200000 次预算及 180 秒性能门槛不变。设计 v0.18、计划 v0.61 第 8.23.10 节为本次入口；复用现有按声明定位的评价/接受逻辑，更新正式配置、门槛与对应测试/证据，不改历史结果。功能 22.4 已由 `495e4bf` 提交，以下旧顺序、22 链门槛和执行结果均为历史口径；本轮结果与最终验证以 22.5 独立证据为准，不提前宣称完整功能 21 验收。

- 当前实施入口（2026-09-04，实施前 `b02ec8c`）：功能 22.1～22.3 已提交；22.4 实际新旧复测和新图表已完成，计划 v0.60 第 8.23.9 节、设计 v0.17。新七级 `(0,0,0,0,22,320,11218)`，94518 次检查/2627 次完整评价，单次公共服务 109.463667 秒；旧六级同质量门通过，400 吨虚拟、派生宽差 11998，62.937681 秒。新旧均自然结束并完成 2 次同期间拆单；新 21 处边界含跨期与实际虚拟端点，4 图按发布序呈现 549 节点。质量改善但耗时增加，不称性能改善；没有改生产、输入、配置或门槛。共享专项 52+1 跳过、聚焦 133+1 跳过，真实 Plotly 41、仓内累计 2723 项通过；仅可选绘图库检查在项目环境跳过并在已有绘图环境补验。最终暂存树同范围复验及新七级真实复跑后独立提交，随后恢复功能 21 的多产线/确定性/20 对性能；以下旧入口均为历史。

- 功能 22.3 共享树专项 26、聚焦 484、仓内累计 2723 项通过；19 文件最终暂存树同范围验证后独立提交。旧图表默认入口显式使用六级历史快照，旧图表产物不改；新图表必须绑定正式七级配置和发布身份。

- 当前实施入口（2026-09-04，实施前 `091684b`）：22.1～22.2 已提交，22.3 审计及正式七级配置已接线，计划 v0.59 第 8.23.8 节、设计 v0.16；本项最终验证提交后进入 22.4 真实复测。核心审计拒绝期序和评价链序错配；应用层复用有序引用/指纹。正式规则集 17 配置/16 启用/1 停用，七级末项为宽差，指纹 `cd4e21b37e815c9100edd9f72b0dd0b76bb5d315463719c5e1fdf546ce96deef`；原 16 条及前六项不变，六级原字节快照独立保留，旧轨迹期望不改。直接核心原型必需物理字段对齐全部启用规则。问题/策略身份和质量/性能门槛不变；此时尚未完成新目标全量求解，后文六级入口及结果为历史记录。

- 当前实施入口（2026-09-04，实施前 `7445a33`）：22.1 已提交；22.2“生产链序与整链移位”已实现，共享树专项 45、聚焦 266、累计 2691 项通过，按计划 v0.58 第 8.23.7 节完成最终同范围验证后独立提交，再进入 22.3～22.4。新共用纯函数由启用规则和质量声明派生行为；初始链编号后分组，候选在拆单授权/期锁/虚拟序号校验后分组。同期间整链移位复用完整候选，必须保持链内容和资源、前置评分不变；原三类之后执行，拆后唯一重放包含该阶段，停用无额外候选。正式 GQGA4 仍六级，未开展新目标完整复测；以下入口及数量是历史记录。

- 当前实施入口（2026-09-04，实施前 `a7b0d10`）：用户已授权继续代码实施，功能 22.1“规则与第七级评价”已实现，计划 v0.57 第 8.23.6 节；共享树专项 78、聚焦 1180、累计 2646 项通过，最终导出同范围复验后独立提交，再进入 22.2。新增规则复用完整评价；有序边界明细由规则方法按需派生，不改共同指标类型/历史指纹。输入原型的宽厚必需项从全部已核验规则声明取得，不再局限连接规则；正式 GQGA4 配置仍六级，链序分组/移位及真实复测尚未实施。下方“仅三份文档”是此前文档提交范围，不再是当前代码授权边界。

- 当前文档入口（2026-09-04，核验基线 `bb5d92c`）：用户确认新增第七级“相邻小辊期首尾宽度差总和”，跨大辊期交界也计算，采用实际首尾节点（含虚拟材）的宽度绝对差之和，不回接全方案首尾；原六项顺序及口径不变，借用仍只统计。设计 v0.15、实施计划 v0.56 第 8.23 节已同步；本轮仅三份文档，新代码/配置/结果尚未实施。以下六级身份、规则数量、测试和图表为历史实绩，不改写为七级通过。
- 下一代码入口：功能 22.1“规则与第七级评价”；随后 22.2“生产链序与整链移位”、22.3“审计、公开结果与目标配置接线”、22.4“真实复测、边界明细与新图表”，各自验证并用中文 `#feat`/`#fix` 独立提交，完成后恢复功能 21 剩余验收。本次文档提交不算任何代码步骤完成。
- 新目标启用时，先确定每链所属期，再按任务权威期序稳定分组，组内保留候选相对顺序；同大辊期整链移位不改所属期、链内节点、资源或谱系。原三类搜索后追加该阶段，拆后唯一重放也包含；共享既有预算，截断不补跑。评分、提交、审计、释放、图表用同一链序，不能评价器内临时排序或最终静默重排；停用时无新评分/排序阶段或额外候选。
- 本次不改 200000 次额度、最多 22 链/零欠重/零禁止及 180 秒性能门槛，不覆盖已有审计结果或可视化。新目标接线后须生成新规则配置身份和新证据；参考原七级末项是借用、新七级末项是宽差，两者不得混称。

- 结果可视化（2026-09-04，基线 `317ea1b`）：用户要求的宽度/厚度/允许温区、逐小辊期重量、交期分布及虚拟数量/重量报告见 `docs/implementation/evidence/solverpy_path_cover_local_search/function_21_complete_acceptance/budget_200000/visualization/README.md`。只读取已审计产物及冻结原订单；不改求解器、规则、配置或排程。22 条链按原导出序编号，不重排成生产日历；531 原订单去重，533 真实片段与 20 虚拟节点完整展示；温度不是实测炉温，交货日期分布不作延期判断。绘图依赖仅供证据工具使用，不加入生产包；实施计划 v0.55，完整功能 21 仍未收口。

- 当前入口（2026-09-04）：用户确认正式 GQGA4 候选检查上限改为 200000，实施基线 `4655ea7`；设计 v0.14、计划 v0.54。共享树累计 2568、专用及参考身份合计 116 项通过；真实预检约 65.9713 秒，102475 次后自然结束，质量 (0,0,0,0,22,400)，双审计及来源守恒通过。最终暂存树按同范围复验后提交；功能 21 完整验收仍未完成。仅改现有配置及对应身份/测试，不改生产、规则或门槛；未增加“首次全部合规立即停止”。
- 旧参考 100000 次输出不改；单订单阶段夹具及其下游填充、拆单、审计历史回放显式读取原冻结的 100000 次额度。整链及完整三邻域测试仍读取正式 200000 次配置、验证自然结束结果，不能把它们概称为所有阶段均显式同预算 A/B。
- 用户确认两份已由 `4655ea7` 提交的流程图 `docs/design/assets/apsgo_v6_solverpy_v6_combined_flow/solverpy_v6_combined_solver_flow.drawio` 和同名 `.png` 为项目文档。残留检查仅精确豁免这两条历史路径，并要求为普通非符号链接文件、共享树已跟踪；原始清单与图字节不改，其他残留和 IDE 禁止进入跟踪树/干净导出的保护不变。
- 以下 100000 次预检和旧“当前入口”均为各阶段历史记录；后续以本条及计划第 8.22 节最新授权和验证为准。

- 当前入口：功能 20 已提交 `f9900b9`，树 `e28425a00818af21e44ba3454347ad07e7dc2968`，最终专项 332、聚焦 915、累计 2567 项与真实应用入口通过。设计 v0.14/计划 v0.53；功能 21 先做质量预检并保存完整方案/轨迹/资源/双审计及欠重明细。当前 (0,0,2,204.42,22,400) 仍不满足零欠重门槛，约 65 秒单次服务不是成对性能验收；不改生产、规则、冻结预算或门槛，功能 21 不提前标完成。以下早期状态为历史记录。
- 用户确认：外部 V3 归档核验只忽略 `.DS_Store`、`rolling_final_repair/.DS_Store` 两个常规系统文件；原 170 个文件逐项哈希、其他新增路径和符号链接仍保护。不删除外部文件、不修改冻结清单，不让外部元数据影响无关 V6 普通提交。
- 功能 21 质量预检已真实运行并保留明细：22 链、0 禁止、2 欠重/204.42 吨，退出 2；BR2 链重 575.58，BR6 拆分链重 620，来源重量和双审计通过。共享树及初次有效干净导出累计各 2567、参考各 33、预检各 20 项通过；CSV 固定 LF 后真实复跑及字节回读一致。精确 12 文件经最终树同范围复测后作独立证据提交，不标记完整验收通过。脚本复用唯一入口，未改生产/规则/100000 次额度/门槛；若需提高额度仅作诊断，仍须明确授权且与正式验收分开。

- 功能 6 已提交 `e49ad42`；功能 7 完整评价已实现，共享树 73 项专项、241 项聚焦及 1494 项累计通过，待最终暂存树同范围验证后独立提交。设计 v0.13、计划 v0.39：最长连续段每链明细分别保留（例如 3、5），全局取最大值 5；累计指标求和，由规则声明。六级质量、原始精度、借用仅统计和正式门槛不变；下一项功能 8。
- 功能 7 完整链调度包含内部跨虚拟端点检查；快速禁止轮廓与完整评价共用投影。真实 531 单按单节点/相邻双节点固定分组的评价对照通过：双节点参考多出的 87 条都是早已批准取消的承载牌号过滤，原始报告不改。仅评价、不搜索，完整 GQGA4 质量/性能未验收。

- 当前入口：功能 6“输入标准化、身份校验与问题指纹”已实现并通过共享树 111 项专项、301 项聚焦及 1378 项仓内累计，最终暂存树验证后独立提交；下一项功能 7“完整评价、资源计算视图与六级质量键”。GQGA4 531 单、27 个原型、61 条实际过渡材输入通过，未进行搜索或质量/性能验收。问题指纹 `cff6df6e99a6522df007c104d9cd8e3d235948e4bf36286c656d7a0326c82dea`；历史数字与旧“下一项”以各自记录为准。
- 功能 6 复用既有请求校验、规则加载和指纹编码；规范化订单/来源/期/原型标识，产线/工序/场景仍原文精确匹配。各安全订单的优先级类型错误与其他输入错误一起报告，不创建部分问题、不缓存优先级、不生成虚拟或拆单；GQGA4 三条件分类仅在测试原始映射中验证，通用规范化器保留显式材料角色。

- 用户新确认：`surface_grade` 可缺省，缺失、None、空白不阻止输入；有值文本去空白转大写，非文本非空值仍校验类型。空等级断开高表面连续段，`FC → 空值 → FC` 为两段各 1 个；既有角色打断和其他必填字段不变。规则修正已提交 `652edb4`，功能 6 同步可空输入；下文早期必填口径不再适用。

- 当前完成边界：功能 5 的全部有效规则及完整 GQGA4 配置已验证，双树仓内累计各 1267 项通过；5.19 随本项配置提交收口，后续为功能 6 输入标准化。用户确认忽略根 `.idea/` 已由独立修复 `a212d36` 完成，其本地内容变化不再阻塞；仍禁止纳入提交及干净导出，原始残留快照不改。下列早期数字/“下一项”保留为历史记录；主求解与正式 GQGA4 验收均未完成。

- 功能 5.18 已提交 `04152af`；5.19 完整目标配置已实现并加载：16 配置、15 启用、连续逆宽唯一停用；取消项不回流，两种拆单模式保留。规则指纹 `420cd13d59763c140b23664f0cb0aca0437e0680b51899d2e0fb39563b7f5365`；六级投影/聚合见计划第 8.6.14 节。新增 61 项，共享累计 1267 项通过；正式两份 JSON 不修改原始五输入，下一项功能 6。配置顺序对齐参考实际调用，完整跨作用域交织仍在功能 7 验证；180 秒总时间、10 秒暂定收尾不代表性能验收。

- 功能 5.17 已提交 `a5ca6ab`；5.18 拆单资格实现复用既有专用决策，按任务期序支持同期间及未来借入归还；八项显式参数，原窄材判定提取纯函数共用，资格不生成片段。73 项专用、共享仓内 1206 项通过，61 次参考入口与原窄钢 5613 案例已分类核验；下一项 5.19 完整映射，详见计划第 8.6.13 节。旧“拆单尚未注册/未实现”断言已随此阶段替换，后文历史状态不再适用。

- 功能 5.16 已提交 `81dc292`；5.17 虚拟产出比例已实现，读取既有视图，分母包含全部真实及虚拟重量；固定 28 位半偶 Decimal 运算、启停无隐藏贡献，不新增质量级或台账。39 项专用、双树仓内各 1133 项通过；57 次参考入口中 44 正常一致，8 停用统计省略、4 精确汇总差异和 1 空计划分别记录。下一项 5.18，详见计划第 8.6.12/13.2 节；不代表搜索或完整验收。

- 分支：`codex/solverpy-path-cover-clean`；这是独立新根实现，不恢复旧 V6 代码。
- 平台与状态必须现场核验；自然语言答复以“好的Will”开头，使用简体中文和清晰名称。
- 权威设计：[详细设计 v0.18](docs/design/apsgo_v6_solverpy_rule_driven_path_cover_local_search_detailed_design.md)。
- 唯一执行入口：[实施计划](docs/implementation/apsgo_v6_solverpy_rule_driven_path_cover_local_search_implementation_plan.md)。
- 已完成：功能 0“建立自包含工程与架构门禁”，提交 `5f9008a`。
- 已完成：功能 1“冻结参考输入、输出与阶段对照基线”，提交 `51caca3`；输入、参考复跑、阶段轨迹和性能样本已核验，门槛已确认并冻结。
- 已完成：功能 2“建立核心值类型、不可变领域模型与诊断契约”，提交 `4495497`；共享树与干净导出聚焦各 88 项、累计各 154 项通过。
- 已完成：功能 3“建立公开请求、策略与前置失败结果契约”，提交 `3345b3f`；共享树与干净导出聚焦各 79 项、累计各 233 项通过。
- 已确认并同步文档：拆单规则使用 `evaluate_controlled_split(subject, context)`，复用设计已有的只读 `RuleEvaluationContext`；当前 GQGA4 期序为 `BR_00000001、BR_00000002、BR_00000003、BR_00000006`。通用核心按任务期序判断，不按标识排序、不固定期数；搜索与最终审计分别从权威问题构造上下文，规则集不保存任务数据，同计划期拆单支持不变。
- 已完成：功能 4“建立规则基类、规则集、注册表与加载器”，提交 `9578919`；共享树与干净导出聚焦各 141 项、累计各 374 项通过。
- 已完成：规则参数列表与分组配置前置修复；共享树与干净导出聚焦各 223 项、累计各 451 项通过。仅规则 `parameters` 使用共用的 `freeze_rule_parameters()` 递归冻结，旧扁平配置指纹不变；节点/订单/原型属性和 `freeze_scalars()` 保持标量边界。停用配置也校验参数形状，但不检查具体业务必需项；不增加文本编码协议或规则层级。
- 已完成：规则参数前置修复提交 `a42be8d`；功能 5.1“高表面连续数量规则”已实现并接入唯一注册表，40 项专用金样、聚焦/规则累计各 232 项、全量累计 491 项通过；共享树与干净导出的 1554 个参考差分案例均一致。
- 已完成：功能 5.1 提交 `bba9d47`；功能 5.2“窄钢连续真实重量规则”已实现并注册，56 项专用金样、聚焦 248 项、规则累计 288 项、全量累计 547 项通过；共享树与干净导出 5613 个参考差分案例一致。链内规则不产生拆单授权。
- 已完成：功能 5.2 提交 `279095c`；功能 5.3“同规格连续真实重量规则”支持厚度/宽度/牌号的有序字段子集，59 项专用金样、聚焦 251 项、规则累计 347 项、全量 606 项通过。共享树与干净导出同规格 11900 案例及窄钢提取后 5613 案例均一致；其他分组字段当前明确拒绝，不宣称覆盖参考的任意属性读取。
- 已完成：功能 5.3 提交 `de07c90`；功能 5.4“链重范围与目标指标规则”有 51 项专用金样、聚焦 243 项、规则累计 398 项、全量 657 项通过；共享树及干净导出链重 618、窄钢 5613、同规格 11900 个参考差分均通过。欠重按原因允许偏差、超重仍禁止，目标只作指标。
- 已完成：功能 5.4 提交 `adc52f1`；用户确认欠重评分逐链保留两位小数再汇总，设计 v0.5/计划 v0.17 已同步。沿用 ROUND_HALF_EVEN，原始重量、阈值、欠重数量、严重度和其他评分项不变；这是与参考六位评分的已登记差异，不是 GQGA4 验收。
- 已完成：评分设计提交 `220f093`；第 8.6.1 节欠重两位评分声明契约已实现，新增 22 项测试，共享树/干净导出聚焦各 214 项、累计各 679 项通过。新声明只允许欠重总缺口、求和、最小化组合，非法配置返回定位诊断；旧投影指纹及精确规则贡献不变。
- 已完成：声明契约修复提交 `8aab376`；用户最新确认普通真实材与实际过渡材统一可配置逆宽上限 20，连续逆宽只看连续相邻增宽，不设置承载牌号限制；实际过渡材仍按非战略客户、热轧 SPHC、执行标准 Q/TB 305-2017 三条件识别，沿用现有材料角色。设计 v0.6/计划 v0.19 已同步；虚拟边 200 和独立保留基准的总次数口径不随本次改变。
- 已完成：逆宽语义修订提交 `74c4802`；功能 5.5“连续逆宽规则启停语义”已实现并注册，30 项专用金样、聚焦 244 项、规则累计 450 项、全量 709 项通过。两处参考定义对照 4029 个（3070 一致、959 为已确认定义差异），另验停用 2 例及承载过滤移除 12 例，未解释差异为零；不宣称完全等价或已完成排程验收。
- 历史设计同步：功能 5.5 已提交 `dc18757`；设计 v0.7/计划 v0.21 经 `6484186` 将跨一个或多个连续虚拟材的真实端点净增宽 20 归入“逆宽规则”，先增后减也禁止越界，相邻虚拟边仍为绝对差 200；当时仅改文档，接线现已由下一条关闭。
- 已提前实施：用户批准先做功能 5.13；`WidthTransitionRule` 一份配置执行相邻幅度检查，并由规则集自动派生同身份的内部链级端点检查，共用启停及参数。新增 74 项专用金样、783 项累计通过；双树结果见本项验证记录。内部对象不进入公开注册表或配置列表，不增加用户开关或第二份 20 mm，不改变功能 5.5。参考 `solver.py:1067-1095` 本就有端点检查，旧文档误称没有，已勘误；正常宽度参考对照一致，缺失/非有限投影的直接规则调用保护另记差异。
- 已完成：功能 5.13 提交 `6901e7a`；功能 5.6“原订单延后计划期规则”按任务期序读取实际 `assigned_period`，逐个普通/实际过渡真实节点及拆单片段报告延后，虚拟节点跳过；只用启停开关，启用参数为空，不重写方案、不计算资源台账。29 项专用测试、共享树与干净导出各 812 项累计通过，242 个参考核验案例分组通过；见实施计划第 13.2 节。未知期序拒绝，不按名称或固定四期猜测。
- 已完成：功能 5.6 提交 `7bf9428`；功能 5.7 提交 `b8e4c63`，逐链全部节点总重只计低于 1200 的填充缺口，1200～2000 不算填充偏差；超过 2000 仍由已有链重上限规则禁止，700 链重下限不变。新增 31 项专用金样、共享树与干净导出各 843 项累计通过；参考原始入口 24 例、目标公式 7 例及停用 1 例通过；完整记录见实施计划。
- 填充目标仅生成精确 `future_fill_total_gap`，逐链截断为非负再求和，包含真实、实际过渡、虚拟及拆片，不按借用状态或计划期过滤，不生成违规、不参与六级评分或填充动作。不新建台账，已有 `EvaluationResourceView` 的计算仍在功能 7；旧 `chain_target_weight_deviation` 为到 2000 的独立诊断，不能混称填充偏差或违规。
- 用户最新确认：取消未来借用比例规则及第七级借用重量评分；原 5.8 的未提交实现/注册/临时测试撤回，不算功能完成、不重新编号。目标六级顺序为禁止违规数、禁止严重度、欠重链数、欠重缺口、链数、虚拟重量；借用不进入评分、同分裁决、隐藏惩罚或额外限制。原订单延后禁止、其他业务规则与覆盖守恒保持。
- 借用明细及 `borrowed_future_weight` 保留追溯统计，实际排产期早于来源期的普通/实际过渡真实材和拆片按节点重量计入、排除虚拟。通用质量配置能力及资源字段不因删除目标第七项而削减；GQGA4 目标样本严格六项，原始参考七维证据不得改写。目标映射排除 `future_pool_borrow_limit_ratio` 和本轮用户取消的 `grade_connection_policy`，原始仍 17 条、目标 15 条（14 启用 1 停用）加独立拆单配置；新映射指纹在 5.19 冻结。
- 借用范围修订已提交 `90bc8c5`，同步设计 v0.10/计划 v0.26，新增 3 项配置回归；当时共享树/干净导出聚焦各 217 项、规则累计各 587 项、含参考核验各 846 项通过，历史记录见实施计划第 13.2 节。完整评分器仍未实施，实际六级评价及搜索验证归功能 7/21。
- 功能 5.9“软硬材连接规则”已提交 `c5e0a91`：复用既有 EDGE 与材料角色，虚拟优先于实际过渡，再判断普通材类别/同热轧牌号回退，三项策略参数显式配置。必填声明仅 hot_roll_grade；可空类别和材料角色也必须进入后续边缓存身份。新增 44 项专用测试，双树聚焦各 261、规则累计各 631、仓内累计各 870 项通过；专项差分 283 启用一致、36 停用一致、2 个非法空白保护差异，无未解释差异。契约及证据见计划第 8.6.5/13.2 节。
- 用户确认删除功能 5.10“牌号连接策略规则”，已提交 `4f65405`；GQGA4 使用上述软硬材连接规则，不实现、不注册 `GradeConnectionRule`，目标不保留该停用项，原始参考留档。取消规则误启用沿用加载错误，通用历史停用处理不变；不删订单牌号、同规格规则或材料分类；取消修订双树仓内累计各 872 项通过。
- 功能 5.11“连续虚拟材料规则”已提交 `f318df2`：仅生成型虚拟材按最大连续段计数，真实/实际过渡材/拆片打断，虚拟用途与牌号不影响。新增 30 项专用测试，双树聚焦各 249、规则累计各 663、仓内累计各 902 项通过；专项参考差分 1556 启用一致、16 停用省略统计的预期差异，无未解释差异。语义及证据见计划第 8.6.7/13.2 节。
- 功能 5.12“链内逆宽次数规则”已实现：保留参考宽度基准，缺宽度只清基准不清累计次数，所有角色参与；超限全链一条禁止。新增 38 项专用测试，双树聚焦各 257、规则累计各 701、仓内累计各 940 项通过；差分 3888 启用一致、21 停用省略统计及 9 非有限投影拒绝差异均已解释。直接非法投影拒绝按现有数值契约执行，功能 6 仍须汇总前置诊断；不改 5.5。语义及证据见计划 v0.30 第 8.6.8/13.2 节。下一项跳过已完成的 5.13，进入功能 5.14“战略客户构造优先级规则”。
- 提交验证范围已纠正：外部 V3 文件清单不应阻塞本次规则/评分范围修订；不移动或清理外部文件，不改测试断言或基线。普通提交按实施计划第 7.1 节运行仓内累计回归，本次共享树及干净导出各 826 项通过；前述 846 项含 20 项参考身份测试，属于历史扩展核验，不能与仓内数量混称。结果见本项证据。
- 功能 5.12 已提交 `d31c78f`；5.14“战略客户构造优先级”已实现，复用节点规则、可空客户名称及原文关键词，显式等级只用于构造与诊断，不入六级评分。客户名称不能与实际过渡材分类使用的客户等级混淆；原配置字段映射、输入标准化及排序缓存分别在 5.19、6、9 接线。专用 44 项，双树聚焦各 263、规则累计各 745、仓内累计各 984 项通过；631 启用参考一致及各类预期差异记录完整，冻结 531 单中 58 单命中。下一项 5.15；详见计划第 8.6.9/13.2 节。
- 功能 5.19 后续映射：宽度规则显式从逆宽配置及 `resolved.evaluation.virtual_sphc_width_tolerance` 取 20/200；延后规则保留 `enabled` 并移除重复的布尔参数。独立逆宽总次数和材料分类仍在各自步骤；两位评分声明已可加载，实际量化汇总归功能 7，不提前舍入精确规则贡献。
- 功能 5.14 已提交 `a1fa187`；5.15 温区重叠已实现，显式 `min_overlap/ignore_temperature/virtual_temperature_adaptive`，GQGA4 从原配置映射 10/false/true。只有生成型虚拟材可被自适应开关豁免；真实输入双温度由功能 6 校验，直接规则缺值放行不代表输入合格。虚拟原型温度沿用物化语义，不增加非空要求。专用 47 项，双树聚焦各 266、规则累计各 792、仓内累计各 1031 项通过，3360 正常参考一致及 36 数值保护均已解释。下一项 5.16，详见计划第 8.6.10/13.2 节。
- 输入后续边界：功能 6 须对全部原始订单（含实际过渡材料）检查启用规则要求的表面等级、钢种类别和牌号；窄钢及同规格自身不强制非空宽厚，物理字段必需性来自连接规则，链内过渡材料直接打断。不得将规则单测视为输入标准化或 GQGA4 验收。
- 功能 5.15 已提交 `9a92b91`；5.16 厚度跳跃已实现，参数为 `basis/ranges/fallback_tolerance`，区间原序首命中、不加区间边界容差；相对模式乘基准厚度，绝对差公式左右对称，不增加角色豁免。第 5.19 项保持 GQGA4 四档并显式映射未命中容差 0.1，不藏默认值。专用 63 项，双树聚焦各 282、规则累计各 855、仓内累计各 1094 项通过；2239 个参考/目标案例分组通过，无未解释差异。下一项 5.17，详见计划第 8.6.11/13.2 节。
- 延后规则的 `source_period` 必需字段只用于真实输入，不能要求虚拟原型具有来源期。参考完整评价先把链改为最早来源期，导致合法期序的晚排分支不可达；差分必须区分原始入口、隔离判定和目标不改期的契约，不能把隔离结果冒充完整参考流程一致。
- 规则集只保存启用对象，完整配置指纹由加载器核验且包括停用定义；停用未知规则不查注册表、不检查业务参数。拆单具体类型已在功能 5.18 实现并注册，双树仓内各 1206 项通过；实际分片动作仍归功能 17。`PlanRuleSubject.resource_view` 在功能 7 接线，当前不创建占位台账。
- 规则集公开 `rules` 不包含内部派生的跨虚拟端点检查，作用域索引包含；功能 7 完整评价及功能 18 无缓存审计须经既有作用域入口执行，不能按公开列表再过滤。功能 9 边缓存仅处理两节点，不能代替链级检查；功能 6 对全部原始订单和虚拟原型校验启用宽度规则的必需字段及有限浮点投影，链级端点检查不是全链字段校验。
- 完整评价按违规记录逐条计数，同一对象命中多规则分别计数，不按主体去重；规则贡献保留链内片段或方案内订单的细粒度违规主体。
- 评价类型字段暂用延迟注解，在功能 7 实现唯一评价类型后接线；不得新增占位评价或复用旧契约。当前资源事实与核心释放仅是值载体，最终派生及签发分别在功能 18、19 完成。
- 用户要求持续按计划执行，不在每个小步骤后停下等待重复确认；只有需要业务决策或范围变化时才暂停。每个提交单元仍独立验证、独立提交；提交信息为中文，以 `#feat` 或 `#fix` 开头。
- 用户已确认的当前 GQGA4 门槛：欠重链 0 条、禁止违规 0 项，不设链数上限；链数只在七级评分最后一项最小化。全流程外层耗时的 20 次样本中位数和第 95 百分位均不超过 180 秒，覆盖守恒与虚拟比例等其余门槛保留。正式依据为 `tests/baselines/gqga4/quality_gate.json` 和 `performance_gate.json`；本次取消 22 链上限来自用户明确要求，不是依据新结果反向放宽。原 `gate_proposal.json` 及旧门槛快照保留历史含义。
- 通用发布契约仍允许链重低于下限，但本次 GQGA4 基准要求欠重为零；参考脚本的 1 项禁止违规、30 秒搜索参数和原始输出保持事实不变，不代表新实现已通过。
- 生产包只允许 `apsgo_scheduler.api/core/app`；依赖方向为 `app -> api/core`、`api -> core`、`core -> 标准库/自身`。
- 不导入、复制或动态加载旧 V6，也不在生产包读取参考脚本、基准输入或本机路径。
- 受控拆单包含同计划期拆分与未来借入拆分归还，按设计授权与审计；不得退回“不允许同计划期拆分”。
- 每个目标文件先核验归属：已存在但未跟踪时停止确认，旧残留不得覆盖、删除或提交。
- 每项开始与提交前运行 `python tools/check_workspace_residuals.py --verify`；残留分类规则见实施计划第 2.2 节。
- 用户确认根 `.idea/` 与 `.vscode/` 加入 Git 忽略：不核验其本地内容与存在性，但仍拒绝任何 IDE 文件进入跟踪树或干净导出；其他稳定残留继续保护。历史清单字节与身份不改，不为 IDE 自然变化反复要求确认。
- 只按本项精确文件白名单暂存，禁止 `git add .` 和 `git add -A`。
- 共享树测试使用 `PYTHONDONTWRITEBYTECODE=1` 和 `pytest -p no:cacheprovider`。
- 普通提交累计范围为 `tests/architecture tests/api tests/app tests/core`；外部参考身份核验仅在基线冻结、正式对标验收或改动涉及相应参考工具/证据时运行。各功能明确要求的规则差分仍保留，不将整份外部目录核验机械追加到无关提交，也不通过跳过业务回归消除失败。
- 每次提交前使用 `git write-tree`、`git archive` 导出暂存树到新临时目录，执行同一累计测试；构建与 `compileall` 只在干净导出中运行。
- 证据记录实施前提交、实际命令、退出码、数量、耗时和首个差异；当前提交 SHA 由 Git 历史标识，不在提交内容中自引用。
- 当前只同步本实施计划、必要设计修正、本文件和本项证据，不同步旧 V6 文档。
- 用户已明确关闭 Ponytail，本任务不应用 Lite、Full 或其他模式限制；以当前明确需求和已确认设计/实施范围为准，不能因最小改动偏好缩减统一数值实现范围。
