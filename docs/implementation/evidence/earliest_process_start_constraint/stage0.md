# 阶段 0：输入与工作边界冻结

- 日期：2026-09-17；实施前后端 `6699d53`，新分支 `codex/earliest-process-start-constraint`；前端 `master@4afb74c`。
- 用户已授权按计划持续实施，正式写库/启用、服务操作仍须单独确认。Ponytail 关闭。
- 后端已有数据库差异，当前 SHA256 `7c5cacab006b9d9e0036e51b16acfcd9b376f7ae84f52b57c484ed314895e036`；不写库、不提交。前端已有 6 文件保持，清单见实施计划第 1.1 节。
- 服务配置 SHA256 `04cb4998cd3d8100a4f7f2ff9c62e10111ced4ae8f827df841507fa535c2f0c0`；种子 590531、40 万候选、310 秒总时限/10 秒收尾保持。
- 旧物理输入 `tests/baselines/gqga4/inputs/input_orders.csv` SHA256 `d178cc19ac6ef2807962beb110333f8702c8e2eeec153f9b0f94236913060941`。旧代码对照可从 `6699d53` 干净导出，调用既有月计划 v2 入口；不向旧代码发送新字段。
- 现有冻结输入不能作为逐单最早时间的来源；前端 `earliestDates.coating` 才是本功能输入，真实新下界快照尚待取得。不得以虚构时间开展真实对照；阶段 6 实际数据验收保持待验证，不阻塞前置代码阶段。
- 新增 `tests/baselines/earliest_process_start/cases.json`：9 个纯时间边界小例，包含等号、完成不早但开始过早、1 毫秒、虚拟、拆片、换序、不可修复和负偏移；用于后续内核测试，不冒充完整物理可行排程。
- 验证：JSON 解析和独立整数累计核对 9 例；无完整求解。精确树核验结果见提交正文。
- 残留检查 `python tools/check_workspace_residuals.py --verify` 退出 1，仍是既有 8 个旧 V6 文件缺失；不修改清单或重建旧产物。

下一步：阶段 1 公共时间输入及数值列；阶段 0 的真实数据缺口由阶段 6 前补齐，不能标为已冻结实际下界。
