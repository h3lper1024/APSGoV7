# 阶段 6：集中回归与交接

最新状态（2026-09-17）：用户已提供真实 CSV，并指定 6 月 1 日零点开始。三组 40 万候选对照完成，停用组保持旧业务/轨迹；启用组提前节点 27→6，仍不可发布，阶段 6 不关闭、阶段 7 不进入。完整九项目标、6 单时间和逐节点 CSV 见 [真实对照记录](real_20260601/README.md)。以下“待输入”是此前代码回归交接时的状态。

## 当前完成边界

- 后端分支 `codex/earliest-process-start-constraint`，前端提交 `74235f4`。实现包含输入、统一毫秒时钟、硬规则、搜索修复、独立审核、v3 接口和 Web 投影；九项目标、正式预算和无等待语义不变。
- 阶段 0 的 9 个手算时间见证、阶段 1～5 专项记录分别保留。新加公共求解入口 3 例：修复后双审核发布、全部未就绪时不可发布且无等待、停用时改变下界不改变方案和评分。
- 真实输入缺少逐单下界，已向用户请求当前订单列表导出；阶段 6 **不能关闭真实数据对照验收**。不得给旧冻结输入统一补日期后冒充真实通过。
- 阶段 7 正式 SQLite 备份/启用尚未执行，未启动/停止/重启用户服务。开发浏览器使用的独立 5197/5198 服务在验证后已关闭。

## 集中验证记录

验证在 Conda `aps_3.10.18`，`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src` 下进行。共享树与精确提交树分别运行：

```text
pytest -q -p no:cacheprovider tests/architecture tests/api tests/app tests/core tests/service
```

为防止 Numba 首次编译耗尽旧小例 12 秒预算，同一解释器先以 `tests.core.test_numeric_boundary_audit.numeric_request()` 的零候选副本预热，仅该副本使用 300 秒总时限和 10 秒收尾；随后原测试预算和断言不变。实际命令为调用 `solve_request()` 后再调用 `pytest.main()`。这不是提高正式求解时限，也不把预热时间计为测试或性能收益。

公共入口 3 例开发诊断在 `NUMBA_DISABLE_JIT=1` 下通过（0.55 秒）；最终必须以实际编译模式的累计结果为准，不将该诊断替代编译验收。

共享累计实际结果为 **4594 通过、1 失败，310.35 秒，退出 1**；不是一次全绿。唯一失败是架构检查把违规明细第 3 列（节点位置）的索引误认为旧基线常量。只精确允许 `_numeric_refinement_scan.build_scan()` 的 `early[:, 3]`，添加其他函数/模块/常量仍拒绝的反例；修复后架构 **98 项通过，1.27 秒**。原有禁止历史源码和依赖边界不放宽。

收口复核补齐有下界节点报告的 `chain_id`、`assigned_period`，对应设计要求的人工定位；不改变时间或评分，缺省旧输入的报告不变。最终精确暂存树按上述完整范围实际编译复验，结果/耗时/树身份写入本阶段提交正文，以该次为最终累计结论。

## 用户现场验证顺序

1. 后端切到本专项分支并使用配套前端；旧服务不识别 v3，请由用户自行重启更新后的服务，本轮未替用户操作。
2. 在订单当前步骤执行既有“计算最早日期”，确认每个待排真实单均有“最早连镀时间”。当前步骤全量导出/原始列功能沿用已有修改，本项不改导出规则。
3. 正式启用之前，提供这批订单当前步骤导出文件（包含来源订单编号、最早连镀时间及完整物理字段），并明确本次排产开始时间，用于固定输入对照。
4. 真实验收按旧原规则、新停用、新启用三组保持同输入、开始时间、种子、40 万候选及现有时限。旧实现不接收新字段，统一下界只用于其离线统计；记录九项评分、提前明细、交期、资源、额度、耗时及双审核。
5. 对照评审并得到用户单独授权后，才备份正式库到 `APSGOV7-bak` 并通过既有事务新建活动版本。现在查询旧活动版本不会自动出现新规则，这是刻意保留的启用边界。
6. 已启用后，提前开工即使只提前 1 毫秒也不能回写。失败应查看 `diagnostic_violations` 和 `delivery_report.kind=diagnostic_candidate_not_publishable`；未找到可行解不等于证明数据无解，不应自动改开始时间或关闭规则。

## 文件保护

后端正式数据库摘要 `7c5cacab006b9d9e0036e51b16acfcd9b376f7ae84f52b57c484ed314895e036`、配置摘要 `04cb4998cd3d8100a4f7f2ff9c62e10111ced4ae8f827df841507fa535c2f0c0` 与实施开始一致，数据库既有未提交差异未纳入提交。前端原 6 个未提交文件摘要一致、未纳入本项提交；具体清单见计划第 1.1 节。

共享残留检查仍仅报原有 8 个旧 V6 残留缺失，不修改冻结清单。精确导出残留检查单独执行，不将上述旧环境问题改写为新功能失败。
