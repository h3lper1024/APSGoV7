# APSGo V7 NumPy 数值搜索阶段 6 证据

## 1. 阶段 6.1 范围

- 实施前提交：`ab4969e`。
- 代码提交：`690de02`（`#feat 建立热点数值候选有序批次`）。
- 精确代码树：`87ecf37729e15c0ea2d15019a5229c47d05716ec`。
- 当前只建立**有界串行批次**：主线程按既有顺序最多预取 8 条候选描述，再逐条调用原完整数值候选入口；没有线程池、Numba 并行或新的评价后端。
- 批次大小是内部试验常量，不增加 HTTP、规则或求解配置项。规则、九级评分、40 万逻辑额度、动作族顺序、每原单 4 个提案、每动作族 64 个提案和首个改善接受均不变。

## 2. 关键控制边界

1. 候选描述来自同一只读方案代次；拒绝候选继续复用当前方案和既有候选私有变化链、资源及评价工作区。
2. 动作族来源游标只在描述实际消费后推进。批次预取不能把下一轮搜索起点越过已接受候选。
3. 主线程严格按原顺序消费；首个改善被接受后，同批剩余描述属于旧方案代次，立即作废，不扣逻辑候选额度。
4. 取消、时间或额度停止发生在消费前时，未消费描述不调用候选入口、不提交方案、资源或正式编号，并分别记录已准备、已消费、拒绝、接受、旧代次、取消和停止数量。
5. 动态桥接以及一个结构配方内部的第二修复变体继续使用原串行候选事务。本阶段没有把它们伪装成可并行工作，也没有改变其额外候选计费。

## 3. 有界行为检查

| 检查 | 结果 |
|---|---|
| 批次首项接受 | 只消费 1 次；来源游标停在接受项；同批后 2 条记为旧代次且不执行 |
| 消费后取消 | 首项拒绝后触发取消；后 2 条不消费，停止原因为 `user_cancelled` |
| 逐条 1 项与批次 8 项 | 1000 次额度、8 次接受样本的方案、九级评分、接受轨迹、完整评价数、资源序号和停止原因完全一致 |
| 候选批次上限 | 实际最大值为 8；参数只在内部测试入口覆盖，不进入外部契约 |

聚焦测试：

- `tests/core/test_numeric_refinement.py`：15 项通过。
- `tests/core/test_numeric_refinement.py tests/core/test_numeric_search.py tests/core/test_numeric_evaluation.py`：35 项通过。

## 4. 冻结真实请求对照

在改动前提交 `ab4969e` 的精确导出和当前代码上，使用同一冻结请求、种子、数值标准及 20000 次候选上限各运行一次。该低额度结果仍含禁止违规，仅用于验证候选控制行为，**不是质量验收样本**。

| 项目 | 旧逐条入口 | 8 条描述批次 | 结论 |
|---|---:|---:|---|
| 经过时间 | 3.045050 秒 | 3.079705 秒 | 单样本正常波动；不宣称提速 |
| 候选检查 | 20000 | 20000 | 一致 |
| 完整评价 | 218 | 218 | 一致 |
| 接受动作 | 13 | 13 | 一致 |
| 最终链数 | 21 | 21 | 一致 |
| 九级评分 | `(2,170.3,2,775.89,564.2136111111111111111111111,2048223.745972222222222222222,11224,120,21)` | 同左 | 一致 |
| 结果/确定性/轨迹指纹 | 已核对 | 已核对 | 全部一致 |
| 停止原因 | `candidate_limit_reached` | `candidate_limit_reached` | 一致 |

## 5. 回归与工作区边界

- 共享树累计：3869 项通过，42.18 秒。
- 精确暂存树导出累计：3869 项通过，43.40 秒。
- `git diff --check` 通过；目标源码和测试可编译。
- Conda `aps_3.10.18` 当前未安装 `ruff`，因此没有把未执行的 Ruff 检查记为通过。
- 共享残留检查仍只报历史 8 个缺失路径及汇总，不属于本阶段改动；没有恢复、删除或提交这些路径。
- 两份前序实施记录改动及两份未跟踪后端技术说明保持原状；正式 YAML、SQLite、服务、前端、发布、部署、分支合并和推送均未修改。

## 6. 结论与下一步

阶段 6.1 的正确性门已关闭：批次边界、来源游标、首改善、逻辑额度、取消和旧方案描述作废均保持串行语义。当前实现仍是单线程，单次 20000 次对照也没有显示速度收益。

下一项 6.2 才会在此边界内试点同一数值评价内核的 1/2/4/8 线程执行，并继续由主线程按描述原序决定接受；若完整同机对照没有整体收益，则保留当前数值串行实现。
