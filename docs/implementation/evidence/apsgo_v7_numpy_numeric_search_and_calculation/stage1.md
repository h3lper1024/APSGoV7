# 阶段 1：权威数值结构执行记录

## 1. 子项 1.1：单位、四舍五入、范围和身份

- 实施前 `0a7d3c4`；新增私有输入边界模块 `core/_numeric_units.py`，集中精确倍率选择/转换/还原、有理数约分、整数范围检查、四舍五入、毫秒/交期、稳定拆片工时分配及新单位身份。
- Python 大整数只在输入准备证明和比值舍入中使用，已检查的结果才能进入原生数组；本项没有把这些边界函数当作已完成的 Numba 内核。后续数值内核的中间乘积仍须在运算前证明。
- 所有量化点按四舍五入；缺值在数组阶段用有效位，不把物理空值转零。原工时直接从请求转换，不通过旧每吨工时反推。
- 新标准 `integer_physical_ms_half_up_v1` 和单位身份已定义，但未切换旧 `SolverPolicy`、请求/规则声明或正式求解入口；阶段 2～4 统一接线，不能提前宣称新请求已可运行。
- 新增 `tests/core/test_numeric_units.py`，包含精度上下文不变性、浮点/非法输入、巨大指数拒绝、正负中点、严重度六位、欠重两位、范围/前缀溢出、拆片零毫秒/守恒、毫秒交期边界及起排高精度小数。
- 实际命令：`PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -q -p no:cacheprovider tests/core/test_numeric_units.py tests/architecture/test_package_dependencies.py tests/architecture/test_clean_room_boundaries.py::test_production_tree_is_self_contained`。
- 共享结果：42 项通过，0.56 秒，退出 0；没有运行完整求解或累计回归。精确暂存树同范围验证记录在本项提交正文。第一阶段集中检查仍待 1.2 后执行。

## 2. 子项 1.2：字段列与完整片段位置

- 实施前 `19479aa`；新增 `_numeric_state.py`：权威节点列、原单表、原型行、拆分组表、按规则独立的分类/优先级数组，以及节点序列/链偏移/链稳定编号/原单全部片段和最后生产位置。
- 原单初始重量、工时与节点列共享只读内存；不再额外保留参与计算的 Decimal/浮点列。字符串仅保存在边界身份/分类映射；数值列全部为连续、只读的原生整数/布尔数组。
- 倍率覆盖物理输入、虚拟原型及已知宽厚温/重量阈值；列值和物理差值、原单累计重量/毫秒及重量乘时间的现有规模范围检查已实现。动态候选的实际规模、中间比例/严重度乘积在规则内核及候选阶段继续事前证明，不能把当前规模的检查当作全程完成。
- 原型模板不能直接当正式虚拟节点放入方案；所有拆片位置完整保留，检查原单重量/工时守恒。动态资源授权、正式编号提交和候选私有工作区的实际搜索接线仍归阶段 3，本项不宣称已实现拆单搜索。
- 精确增加 `_numeric_state` 的 NumPy 架构许可，附错误模块/其他依赖反例；没有放开整个核心层或新增依赖。
- 集中命令：`PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -q -p no:cacheprovider tests/core/test_numeric_units.py tests/core/test_numeric_state.py tests/architecture/test_package_dependencies.py tests/architecture/test_clean_room_boundaries.py`。
- 共享检查 108 项通过，0.80 秒，退出 0；包括新单位、数组、全部片段、错误索引/原型、规范化、范围及架构。精确树同范围结果见提交正文；没有进行全仓累计或完整求解。

## 3. 真实数据转换画像（只准备，不搜索）

采用阶段 0 冻结请求，经现有加载/归一化取得物理字段；原工时直接从请求传入 `NumericTask.build()`，不从旧派生每吨工时反推。

| 项目 | 实际结果 |
|---|---|
| 数值任务身份 | `9ad4247f71f130a8746cedd33389f98ee9419453227161bf02491106340bf8de` |
| 节点目录 | 531 原单＋27 原型，共 558 行；原型未进入方案 |
| 倍率 | 宽度 1、厚度 100、温度 1、重量 100；全部原单物理值与重量还原逐项一致 |
| 原单工时合计 | 2274024785 毫秒 |
| 原单工时量化差 | 相对原请求小时精确有理数求和，合计约 +1.6360702614860985 毫秒；未拆原单理论界 265.5 毫秒。不是零误差，不含未来虚拟或拆分前缀误差 |
| 旧欠原单 | 96 单，按相对交期小于等于零 |
| 预计算命中 | 战略优先 58、普通窄钢 205、高表面 25；同规格分组单独数组 |
| 节点数组字节 | 92070 字节，仅节点列，不含全部任务/边界映射或进程内存 |
| 准备单次耗时 | 约 0.0235 秒，仅数组准备，不是完整排程或正式性能验收 |
| 位置检查 | 人工单节点链序列包含全部 531 原单，片段/最后位置与原序一致；不是求解生成的初始解 |

实际检查通过 Conda Python 内联调用 `load_request`、`load_rule_set`、`normalize_input`、`NumericTask.build`、`from_ticks`、`NumericPlan.build`，并以 `Fraction` 重算原始工时求和；退出 0。

## 4. 当前边界与下一项

阶段 1 数据基础及集中检查完成，下一项 2.1 规则编译与数值判定。新数组尚未替代生产搜索状态，正式运行仍为旧实现，不混合数值；公开新声明、全量评价、动态候选、独立审核分别在后续步骤接线。配置、SQLite、原诊断、前序脏文档和用户技术说明不变。
