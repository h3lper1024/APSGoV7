# 阶段 3：完整数值串行搜索执行记录

## 1. 子项 3.1：数值连接、路径覆盖与初始方案

- 实施前提交 `72b3d31`。新增私有数值构造模块，直接读取 `NumericTask` 和 `NumericRuleProgram`；构图过程中不恢复订单对象、不调用旧规则对象、不写正式资源或编号。
- 构造图使用 `ordered_rows`、`adjacency_offsets`、`adjacency_rows` 三个只读 `int64` 数组。`adjacency_offsets + adjacency_rows` 是压缩邻接表：每个节点的可连接后继在一个连续区间内，避免用节点标识字典承载搜索图。
- 主排序仍为宽度降序、构造优先级、厚度、最低温度；后继排序仍为厚度差、重量降序、构造优先级。区别是物理量和权重直接比较精确整数，不再投影为浮点数，也不再保留构造方向的 `1e-9` 隐式余量。
- 最大匹配继续采用分层广度遍历和非递归增广路径，主次序及后继次序稳定；结果用整数后继数组和路径偏移表达。初始方案继续只做直接追加或切链，不生成虚拟材、不拆单、不消耗候选额度；链重上限和禁止轮廓使用已编译整数规则，最后执行一次全量九级评价。
- `NumericPlan` 新增结构身份，覆盖任务、节点序、链边界、链标识、所属期及由这些结构派生的节点/原单位置数组，不把方案代次混入结构身份；全量评价同时绑定该身份，避免相同代次或错误派生位置误配另一方案。

## 2. 小型确定性检查

- 两个可连接真实订单形成一条路径和一条初始链；链重达到下限，九级末项为 1。
- 链级禁止轮廓恶化或追加后超过链重上限时，在当前节点前切链，节点不丢失。
- 四节点图验证增广路径会重接早期选择，得到最大匹配而非停在局部最大匹配。
- 搜索开始前取消时只返回未签名的部分图；部分图不能进入路径覆盖。
- 跨任务规则程序、图中不存在的路径边、错误数组形状和不一致身份均明确拒绝。

## 3. 冻结 531 单真实构造

使用 `diagnostics/critical_delivery_search_and_bridge_reclamation/combined_01/prepared_request.json`，只显式替换为已经确认的新九级数值投影；订单、规则参数、任务期序、种子 `590531` 和工时不变。运行不写输出目录、不修改正式 YAML 或 SQLite。

| 阶段 | 结果 | 本次本体耗时 |
|---|---|---:|
| 数值构图 | 检查 140715 条前向边，允许 32536 条；图身份 `1a54067986a284484f2a2903bf09fdb16ed0dfc72d193895dcdcd93bfc7fce60` | 2.023122 秒 |
| 最小路径覆盖 | 最大匹配 514 条边，形成 17 条覆盖路径；身份 `7ffa8fd12e18b63ced44bb0bbc043b085a72aa7e50ac44bee26321fcbecaf81a` | 0.545932 秒 |
| 初始方案 | 切分为 30 条链；方案身份 `a2a5e50c3e7ffebd199a130870e96cbea2bf04fde8fcf77f88d7265c4987b23c` | 0.109666 秒 |
| 初始九级整数值 | `(2, 170300000, 14, 473672, 2072548, 689748150853, 11133, 0, 30)`；16 条违规记录 | 包含在初始方案耗时中 |

候选检查数为 0，停止原因为空。以上约 2.68 秒只覆盖数值构图、匹配和初始方案，不含输入准备、局部搜索、动态资源、双审计和结果装配，不能作为一分钟目标结果。

对同一冻结请求额外运行旧构造入口：主节点排序完全一致，允许边数量及无序边集合均一致；同一个左节点内的后继次序存在变化。根因是旧入口把厚度和重量转成浮点后会产生并列，新入口按权威整数精确排序。路径覆盖仍为 17 条，但具体匹配和初始切链变化为 30 条对旧 31 条，属于已确认新数值标准允许的搜索轨迹变化，不是规则可连接性漂移。

## 4. 集中验证

共享树集中覆盖规则、旧图/构造回归、新数值单位/状态/规则/评价/构造、规则加载和架构门禁：最终 1519 项通过，5.57 秒，退出 0。新模块及相关文件 Ruff 和 `git diff --check` 通过。

源码、测试及阶段文档暂存树为 `097085bdcd5089a0c4afa734ee7490f7049568a6`，精确导出到 `/tmp/apsgo-numeric-construction.Gx8n4q`；同范围 1519 项通过，6.50 秒，退出 0，干净导出残留检查、`compileall` 和 Ruff 均通过。随后只补录本段验证结果，源码和测试字节不再变化；提交身份以 Git 历史为准。

## 5. 当前边界与下一项

### 5.1 子项 3.2：候选编辑、唯一接受与静态首轮前缀

- 候选描述绑定任务、当前方案指纹、方案代次、已消费的逻辑候选序号，以及稳定的源/目标链 ID；真实节点移动还绑定稳定节点行。接受后旧代次描述立即失效，不能套到新方案位置上。
- 整链直接合并保留原追加、前置、各位置插入及允许的链内反转枚举顺序；真实节点移动仍只面向欠重目标链，并检查源链不欠重、目标链不超重及三处直接连接边。整链调序保留按有效交期枚举、同计划期内原绝对位置插入的语义。
- 所有可评价候选只构建 `NumericPlan`，共用一次完整数值评价和唯一严格九级比较；只有完整评价结束且共享预算仍有效时才原子发布下一代方案、评价与接受轨迹。拒绝、取消、额度耗尽和陈旧候选不修改当前状态。
- 暂时无法直接连接的整链边不会被永久删除。当时计入 `deferred_bridge_candidate_count`；子项 3.3 接线后改名为 `bridge_required_candidate_count`，表示需要尝试桥接的候选总数，包含最终桥接成功的候选。整链调序实现已通过独立检查，但完整首轮将在 3.3 加入虚拟填充后按原顺序编排；本项不会提前改变首轮阶段顺序。

小型静态首轮前缀从两条 100 吨链开始，九级值由 `(0, 0, 2, 20000, 0, 0, 100, 0, 2)` 改善为 `(0, 0, 0, 0, 0, 0, 0, 0, 1)`：消费 2 个逻辑候选、完整评价 1 个、接受 `whole_chain_prepend` 1 次、登记待桥接候选 1 个；最终方案身份 `dbc9f4769eb1dd876c82686b6783aa88b6b658b31b7ac4b38c02363444b17d9e`，阶段检查点身份 `0521c2c9fe15a7d96c81b5acf2bcb014b29f44a52e1da6801de801f41dffdd8d`，停止原因为空。

集中检查在 3.1 范围加入数值搜索专项，最终共 1525 项通过，5.97 秒，退出 0；相关 Ruff 和 `git diff --check` 通过。覆盖首改善整链合并、真实节点移动、旧欠整链提前、陈旧代次、额度前停止、双向不可直连桥接延后，以及静态首轮动作顺序。

源码、测试及阶段文档暂存树为 `920afb9adc5135843e4f33136f1ad5e64cc94c55`，精确导出到 `/tmp/apsgo-numeric-search.FPf7wb`；同范围 1525 项通过，6.71 秒，退出 0，干净导出残留检查、`compileall` 和 Ruff 均通过。随后仅补录本段验证结果，源码和测试字节不再变化；提交身份以 Git 历史为准。

### 5.2 当前边界与下一项

### 5.2 子项 3.3：动态材料、受控拆分与单次重放

- `NumericTask` 的节点身份覆盖全部静态行，并可由 `NumericDynamicNode` 候选私有扩展。动态行从原订单或虚拟原型模板复制权威物理量、文本分类和派生规则列，仅显式覆盖重量、毫秒工时、材料角色、资源/来源、所属期、用途、拆分谱系和正式接受序号。
- 每次候选扩展都会生成新的任务身份；已编译规则参数和九项目标只重新绑定该身份，不读取旧对象规则，也不修改当前已接受状态。完整候选经唯一 `commit()` 校验任务、规则、评分、方案、评价和代次一致后，才同时发布目录、方案、评价、虚拟材序号与拆分序号。
- 整链不可直连位置按既有顺序尝试一个或两个虚拟桥接节点；单桥优先，双桥按原平滑度公式稳定选择。所有试探原型在私有临时目录评价，只有选中的正式节点进入候选；拒绝、取消或无解都不扩大当前任务。
- 欠重填充按欠重链、插入位置和原型输入顺序逐候选检查，锚点温区按现有最小/最大语义派生。只有连接、链重、虚拟比例和完整九级评价均满足接受条件时发布节点并推进正式序号。
- 受控拆分继续使用已编译拆分资格，覆盖同计划期拆分和未来借入归还两种模式。片重按最大片重切分并检查最小片重，父单总毫秒按固定片重比例和稳定片段顺序分配；拆片总重量和总工时必须等于父单。普通相邻桥接材先移除，存续供体边界按需修复，各片间强制选择受保护分隔材并受数量/重量上限约束。
- 拆单候选仍需完整九级评价并严格改善。一次或多次拆单被接受后，只调用一次既有完整首轮顺序“整链调整 → 真实节点移动 → 虚拟填充 → 整链调序”；没有建立另一套修复或评分流程。阶段停止原因为 `LOCAL_SEARCH_COMPLETE`（局部搜索自然结束），额度或取消仍保留原停止原因。

小型检查覆盖：桥接成功后只发布所选动态行；禁用桥接时拒绝候选不增加行或序号；欠重填充只推进接受的虚拟材序号；同期间和未来借入归还拆分分别生成模式 0/1，600 吨父单形成 500/100 吨片段，片段毫秒之和等于父单且只使用一个分隔材、只重放一次。

共享树累计范围为 `tests/architecture tests/api tests/app tests/core tests/service`：4195 项通过，55.12 秒，退出 0。相关文件 Ruff 和 `git diff --check` 通过。

源码、测试和初版阶段文档暂存树为 `16af14967b7fbaca8cde2305be41ce2edae66184`，精确导出到 `/tmp/apsgo-numeric-dynamic.23WQGT`；干净导出残留检查通过，同范围 4195 项通过，55.34 秒，退出 0，`compileall` 和 Ruff 均通过。随后只补录本段验证结果，源码和测试字节不再变化；最终文档树与实际提交身份以 Git 历史为准。

### 5.3 当前边界与下一项

子项 3.3 完成，下一项为 3.4“接通后置精修和完整串行主流程”。当前数值搜索仍是隔离入口，尚未替换正式 `solver.py`；冻结 531 单也尚未运行完整数值搜索。后置节点/片段移动交换、切链、普通连接材回收、4/64 轮转、独立双审计、完整 GQGA4 质量与一分钟目标均未验收。
