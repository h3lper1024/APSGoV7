# NumPy 数值搜索阶段 5 实施证据

## 步骤 5.1：冻结结构精修热点与计数边界

- 日期：2026-09-15
- 实施前提交：`5c5b9dc`
- 范围：只增加结构精修汇总计数、单次生成器推进计时和 `_layout()` 聚合计时；不改变候选顺序、通道归属、额度扣除、评价、接受或规则。
- 未执行：40 万次完整排程，原因是步骤 5.2～5.4 尚未完成。

### 有界样本

使用 8 个真实订单、2 条链和 100 次逻辑候选额度执行后置结构精修，结果如下：

| 指标 | 实际值 |
|---|---:|
| 原始结构组合 | 251 |
| 当前方案代次内首次出现的组合 | 183 |
| 重点/常规通道过滤 | 150 |
| 逻辑候选额度消费 | 100 |
| 完整候选评价 | 57 |
| 完整 `NumericPlan` 构造 | 57 |
| 接受 | 0 |
| `_layout()` 调用 | 492 |
| `_layout()` 聚合耗时 | 0.001757 秒 |
| 生成器单次推进最长耗时 | 0.001901 秒 |

这个样本只用于证明计数边界，不代表真实 GQGA4 的耗时比例。它确认了两个实现事实：原始组合和通道过滤可能明显多于正式消费额度；当前每个进入完整评价的候选都会先构造完整方案，而生成及过滤还会反复恢复完整 Python 链容器。

### 验证

```bash
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider tests/core/test_numeric_refinement.py
```

结果：5 项通过。`py_compile` 与 `git diff --check` 同时通过。共享残留检查仍只报告已登记的 8 个历史缺失路径及汇总项，退出 1，不记为本步骤通过项，也未恢复或删除这些文件。

### 结论与下一步

步骤 5.1 完成。下一步 5.2 建立按当前方案代次复用的只读数值索引，候选生成、重点/常规通道判定和快速过滤不得再调用完整 `_layout()`。

## 步骤 5.2：按方案代次建立只读数值索引

- 日期：2026-09-15
- 实施前提交：`f6a4084`
- 范围：只改结构精修的候选生成、排序、通道归属和普通连接材扫描的数据读取；候选动作、顺序、逻辑额度扣除点、完整评价和接受条件保持。

### 实现结果

每个当前方案代次建立一次只读数值索引，包含：

- 每条链的 `node_rows` NumPy 视图和链长；
- 稳定链标识到链下标的只读定位；
- 每个原订单全部片段的链下标与链内位置；
- 当前重点原订单在全局节点序列上的累计计数，用于常数时间的区间归属判断。

生成与过滤路径不再调用 `_layout()`，`_recipe_real_sources()` 也只读取上述索引。相同 8 单/2 链/100 次额度样本中，原始组合、通道过滤、候选消费、完整评价和接受计数与步骤 5.1 一致；`_layout()` 调用由 492 次降至 200 次。剩余 200 次均来自候选执行阶段的完整 Python 布局恢复，留给步骤 5.4 消除。

### 验证

```bash
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider tests/core/test_numeric_refinement.py
```

结果：6 项通过。专项测试用失败桩替换 `_layout()`，仍能生成同序节点配方并完成重点通道过滤，证明该路径不再依赖完整 Python 链布局。步骤 5.2 完成，下一步 5.3 处理重复组合、唯一通道归属以及额度、时间与取消检查位置。

## 步骤 5.3 历史实现：生成后去重及额度与取消边界

- 日期：2026-09-15
- 实施前提交：`0e86c35`
- 范围：在当前方案代次内按完整结构配方去重；不改变动作内容、重点优先、动作族轮转或九级接受条件。

### 实现结果

原始配方先用数值区间累计判定唯一的重点或常规通道，只有通道匹配且本代次首次出现的配方才会送到 `_scan_family()`。逻辑候选额度在这个唯一配方即将执行结构和规则检查时消费。长目标位置扫描及普通连接材扫描会轮询同一 `SolveRuntimeBudget`，因此时间到达或用户取消可在生成内部终止。

6 单全部位于同一链的切链见证得到：

| 指标 | 实际值 |
|---|---:|
| 原始切链组合 | 60 |
| 重复过滤 | 50 |
| 唯一送出 | 10 |
| 唯一送出中的重复 | 0 |

取消见证在目标位置扫描的第一次轮询即得到 `user_cancelled`，逻辑候选消费保持 0。没有新增生成额度或动作族专属额度。

### 验证

```bash
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider tests/core/test_numeric_refinement.py
```

结果：7 项通过。这一实现随后被设计 v0.14 取代：它仍然先按原订单重复生成链级候选，再通过集合过滤，且允许候选轨迹变化，不能作为当前步骤 5.3 的完成证据。以上数据只保留历史含义。

## 步骤 5.3 回修：按结构归属直接唯一生成

- 日期：2026-09-16
- 实施前提交：`812a2ea`
- 范围：只修正后置结构精修的候选生成归属；动作集合、重点/常规通道、每原订单 4 个候选、每动作族 64 个候选、单一逻辑额度、首改善接受和九级评分不变。

### 实现结果

- 节点移动与节点交换继续由唯一节点位置生成。
- 片段移动与片段交换以“优先级最高、既有遍历顺序最先”的位置作为区间唯一拥有者；同一原订单拆成多个片段时，同一区间也只生成一次。
- 切链和同期间整链调序各链只建立一个结构生成器。多个原订单通道仍按既有轮转节奏共同推进该生成器，因此保留原候选首次出现顺序，但不会再次构造相同候选。
- 删除生产路径中的跨来源 `seen` 集合、字典去重和 `duplicate_filtered` 诊断。测试可以检查身份无重复，但生产正确性不依赖事后过滤。
- 长位置扫描、连接材料回收、时间和取消轮询、候选额度消费点保持不变。

6 个原订单同处一条链的切链见证由历史“生成 60 个、过滤 50 个”改为直接生成 10 个；5 个切分位置分别对应 2 种结果链位置，没有重复身份。包含一个原订单两个拆分片段的区间见证同样没有重复候选。

### 同标准行为等价

使用实施前提交 `812a2ea` 的独立 Git 导出作为参考，不在生产包保留旧生成器或兼容开关：

| 对照 | 实际结果 |
|---|---|
| 8 单、2 链、候选自然穷尽 | 新旧均消费 202 个规范候选，候选身份及顺序逐项一致，停止原因为 `local_search_complete` |
| 12 单、3 链、1000 次额度且存在接受动作 | 新旧均在序号 6、7、28、58、117、544、625、629 接受，共 8 次；停止原因为 `candidate_limit_reached` |
| 上述有接受样本最终结果 | 节点顺序、链偏移、链标识、所属期及九级评分逐项一致；最终九级评分为 `(0,0,0,0,0,0,30,0,4)` |
| 覆盖评价正式物化边界 | 接受 8 次、正式方案物化 8 次；拒绝候选不正式物化，步骤 5.4 边界保持 |

### 验证

```bash
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider tests/core/test_numeric_refinement.py -q
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider tests/core/test_numeric_*.py -q
```

结果：专项 10 项、数值专项 93 项通过。独立 Git 导出对照脚本另行逐项核对候选、额度、接受轨迹和最终方案并通过；该脚本只作本次验证，不进入生产包。步骤 5.3 回修完成，步骤 5.4 的回修后复验同时关闭。未运行 40 万次完整 GQGA4 排程，下一项为步骤 5.5。

## 步骤 5.4：受影响链覆盖与接受后正式物化

- 日期：2026-09-16
- 实施前提交：`0b277e8`
- 范围：只改变后置结构精修候选的内存表示与正式方案构造时机；动作集合、候选额度、通道轮转、规则和九级评分不变。

结构候选保存当前未变化链的只读 NumPy 视图，只为变化链建立私有整数序列。覆盖评价直接执行现有链规则、方案规则、资源汇总和毫秒交期计算，不建立全局节点位置、原订单片段位置或正式方案指纹。只有覆盖评价严格改善当前九级评分时，才构造下一代正式 `NumericPlan`；正式评价必须与覆盖评价的九级评分、违规、规则结果、资源重量、逐链事实及交期数组完全一致，随后才允许原子提交。

100 次有界拒绝样本实际消费 100 次逻辑候选、执行 62 次完整覆盖评价、接受 0 次，正式方案构造 0 次，`_layout()` 调用 0 次。普通连接材回收接受见证中，接受 1 次、正式方案构造 1 次，且覆盖与正式评价一致。

### 验证

```bash
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider tests/core/test_numeric_*.py -q
```

结果：原实现时共享树数值专项 91 项、精确暂存树核心专项 52 项通过。步骤 5.3 回修后又完成 93 项数值专项和有接受行为的同标准 Git 导出对照：接受 8 次仅正式物化 8 次，候选顺序、额度、接受轨迹和最终结果与回修前一致。覆盖评价与等价正式方案差分、拒绝候选不物化、接受后正式复核均有见证。步骤 5.4 的回修后复验完成；下一步 5.5 执行固定 40 万次完整串行质量、重复性与热点对照。

## 步骤 5.5 预检：结构归属索引共享与完整运行失败证据

- 日期：2026-09-16
- 实施前提交：`293c697`
- 范围：同一次节点或片段动作族扫描只构造一次位置优先级和区间拥有者索引，供各原订单流共享；不改候选集合、候选顺序、额度、规则或九级评价。

固定 10 万次请求完成 100000 次候选检查、788 次完整评价、301 次接受和 21 条链，九级评分为 `(2,170.3,1,225.49,569.3580555555555555555555556,1485629.458033333333333333333,11171,320,21)`，与历史同标准结果逐项一致；经过时间由 14.428400 秒降为 13.756 秒。数值专项 94 项通过。

随后固定 40 万次首轮在 3501.088924 秒后结束，但结果为失败，不能登记为正式质量或性能样本：搜索消费 400000 次候选、执行 88118 次完整评价并接受 481 次；最终审计发现 `split_target_period_mismatch` 2 项，未来借入拆分组 `split-000002` 的授权目标期为 `BR_00000006`，两个片段却被后置精修移入 `BR_00000002`。数值搜索阶段本身为 3497.949 秒。该结果同时证明后半程候选扩展任务触发了大量全方案评价，单独共享结构归属索引不足以完成性能收口。

下一步先在数值候选统一接受边界保护拆片授权目标期，再修复仅追加候选资源时未变化链被重复全量评价的问题。两项修复分别验证、分别提交；之后重新执行固定 40 万次正式对照。

### 拆片授权目标期保护

实施前提交为 `e74f15d`。数值后置结构候选和首轮/拆单准备候选在调用完整评价前统一检查所有已调度拆片；任一片段所在链的计划期与拆分组授权目标期不一致时直接拒绝，不消费额外额度、不执行完整评价或提交。

首版曾把同一检查放进通用 `NumericPlan.build()`；20 万次探针在 111455 次候选检查处证明，第二次未来借入拆分会先构造归还事务的临时布局，过早校验使合法候选被误报为 `input_invalid`。纠正后通用构造器只负责结构守恒，两个候选事务入口负责提交前期锁。未来借入拆分见证验证最终片段均处于授权目标期，后置精修越期见证仍在评价前拒绝；精确暂存树数值专项 95 项通过。该修复不改变拆单资格、目标期决策、候选顺序、九级评分或 40 万次额度；下一项仍为候选扩展任务的未变化链评价复用。

### 只追加候选任务的未变化链评价复用

实施前提交为 `5f644a5`。动态资源任务记录由 `extend_numeric_task()` 产生的可信祖先身份；当候选任务只是在当前任务尾部追加虚拟连接材或拆片、编译规则参数和九级目标保持一致时，现有增量评价可继续复用未变化链规则结果与链事实。变化链、当前任务的节点指标、交期时钟和方案级规则仍重新计算；非只追加任务继续走权威全量评价。

小型差分见证中，两条链只有第一条加入新虚拟节点，增量评价只调用第一条链的规则计算，第二条链复用；权威全量评价调用两条链，最终规则、资源、交期、节点指标和九级评分逐项一致。数值专项 96 项通过。

固定 12 万次探针在 13.446534 秒完成：120000 次候选检查、1051 次完整评价、368 次接受、22 条链；同期间拆单和未来借入归还各接受 1 次，核心审计与应用审计均通过，质量键为 `(0,0,2,126,567.6777777777777777777777778,1527446.025652777777777777778,11331,360,22)`。这只是中间探针，不是 40 万次验收。

固定 20 万次探针运行超过 7 分钟后人工中止，没有响应、审核或质量结果。实时采样与中断栈均落在 `_family_stream.source_stream()` 的 `lane_filtered`：结构候选已经唯一生成，但仍先生成完整候选，再分别为重点通道和常规通道过滤，稀疏重点通道会扫描大量不属于自己的组合且不消费逻辑候选额度。下一步应在候选生成时直接限定通道归属；该中止运行不登记为性能样本。

### 重点与常规通道直接生成

实施前提交为 `7598408`。节点移动在扫描目标位置前根据被移动节点归属通道；节点交换根据两端节点共同决定。片段移动根据来源片段决定，片段交换根据两端片段共同决定；切链和整链调序根据整条来源链决定。未归属当前通道的结构不会进入深层位置/区间枚举，外层删除 `lane_filtered` 事后过滤。`critical_lane=None` 仍保留无通道的测试/诊断枚举，不进入生产求解流程。

修改前后的固定 12 万次探针对照完全一致：结果指纹、确定性运行指纹、轨迹指纹、候选/完整评价/接受/拆单/链数全部计数、九级评分、核心审计和应用审计逐项相同；经过时间分别为 13.446534 和 13.794469 秒，不宣称该短样本有稳定性能改善。

相同固定输入的 20 万次运行在 109.666584 秒完成，CPU 109.662382 秒；200000 次候选、19550 次完整评价、469 次接受、22 条链，同期间及未来借入归还拆单各 1 次。质量键为 `(0,0,0,0,541.9522222222222222222222222,1182507.53935,11611,360,22)`，核心和应用双审计通过。此前同边界超过 7 分钟仍停在生成后过滤并人工中止，因此通道直接生成解决了已确认热点；当前 109.67 秒仍超过一分钟目标，也不是 40 万次正式对照，下一步继续定位剩余热点。

### 动作族链序排序复用

实施前提交为 `c769458`。15 万次带剖析样本显示，`_source_recipe_stream()` 会在每个原订单流开始时计算链交期顺序，即使当前动作族完全不读取该结果；同一方案代次内相同排序因此被调用 18834 次，累计约 6.25 秒。现只有片段动作族建立排序，并在该动作族开始时计算一次后传给全部原订单流。单独调用片段配方流的测试/诊断入口仍按需计算，不新增缓存、配置或第二套排序语义。

相同固定请求和 15 万次候选额度的修改前后带剖析结果如下：

| 指标 | 修改前 | 修改后 |
|---|---:|---:|
| 经过时间 | 65.854874 秒 | 59.250986 秒 |
| CPU 时间 | 65.852647 秒 | 59.082988 秒 |
| 候选检查 | 150000 | 150000 |
| 完整评价 | 5469 | 5469 |
| 接受动作 | 460 | 460 |
| 最终链数 | 22 | 22 |
| 两类拆单 | 各 1 次 | 各 1 次 |

结果指纹、九级评分 `(0,0,0,0,541.9522222222222222222222222,1183921.162958333333333333333,11611,360,22)`、核心审核和应用审核逐项一致。带剖析经过时间下降 6.603888 秒，约 10.0%；`_chain_order()` 已退出修改后的累计热点榜。专项 12 项、数值专项 96 项、共享树累计 3863 项通过。累计检查同时补齐 `_numeric_refinement` 和 `_numeric_resources` 作为 NumPy 数值内核模块的架构门禁登记，不放宽其他生产依赖。共享残留检查仍只报告已登记的 8 个历史缺失路径及汇总项，不作为通过项，也未恢复、删除或提交这些路径。

本项只消除不变排序的重复计算。修改后主要热点仍为结构修复候选、虚拟桥/候选资源扩展及交期评价；40 万次正式对照继续暂停，不能用本次 15 万次样本宣称一分钟目标完成。
