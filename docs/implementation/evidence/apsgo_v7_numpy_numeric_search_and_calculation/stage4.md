# 阶段 4：增量复用、独立审核与边界接线执行记录

## 1. 子项 4.1：同标准数值增量与缓存复用

- 实施前提交：`a0d5c03`。
- 候选评价仍调用第 2.2 阶段建立的同一数值规则、资源、时钟和九级汇总函数，不引入旧对象评价或另一套近似评分。
- `NumericChainFacts` 以只读 `int64` 数组保存逐链总重量、工时、真实材重量、虚拟材重量和未来借用重量。稳定链标识、所属期和有序节点行完全相同时复用；任一条件变化即重算该链。
- 未变链规则结果按相同条件复用；链换序时仅重定位违规记录的链下标。连续段、逆宽基准和跨虚拟端点等链级规则不会拼接局部中间值，变化链始终整链重扫。
- 全局真实材、虚拟材、借用及虚拟比例从最新逐链摘要汇总；计划期延后和相邻链首尾宽差仍由同一方案级规则入口读取当前结构，避免缓存旧链序。
- 交期时钟先识别最长相同节点前缀和后缀：前缀完成时刻可直接复用；后缀仅在节点序列相同且进入后缀前的累计工时也相同时复用，否则从最早变化位置继续累计。全部原单最后片段位置由新 `NumericPlan` 重新建立，再计算完成、晚交和等待负担。
- 同一任务与规则下，节点优先级统计直接复用。任务对象、规则程序或九级目标身份变化时保守执行完整的新数值评价；不会回落旧求解器。旧评价的方案身份、代次、链数、节点完成数组或原单完成数组不匹配时明确拒绝。
- 本阶段没有增加“前缀快速拒绝”；因此不存在未计算列按零参与接受。完整候选仍在唯一接受点按完整九级数值比较。

## 2. 专项差分检查

执行命令：

```bash
PYTHONPATH=src PYTHONDONTWRITEBYTECODE=1 \
  /Users/miles/anaconda3/envs/aps_3.10.18/bin/python \
  -m pytest -p no:cacheprovider \
  tests/core/test_numeric_incremental_evaluation.py -q --tb=short
```

结果：`4 passed in 0.19s`。

| 场景 | 实际增量行为 | 与完整重算 |
|---|---|---|
| 两条链只换生产顺序 | 链规则结果重算 0 条，违规链下标随新顺序重定位 | 全部违规、指标、资源、时钟和九级相同 |
| 三条链中节点由第一条移入第二条 | 仅重算变化的 2 条链，第三条链复用 | 全部结果相同；完整入口会重算 3 条链 |
| 任务对象身份变化 | 不使用旧链缓存，完整执行新数值评价 | 全部结果相同 |
| 旧完成时刻数组与旧方案节点数不符 | 在复用入口拒绝 | 不进入候选接受 |

## 3. 共享树累计检查

执行命令：

```bash
PYTHONPATH=src PYTHONDONTWRITEBYTECODE=1 \
  /Users/miles/anaconda3/envs/aps_3.10.18/bin/python \
  -m pytest -p no:cacheprovider \
  tests/architecture tests/api tests/app tests/core tests/service -q --tb=short
```

结果：`4203 passed in 51.27s`。

相关源码与测试通过 `ruff check`，`git diff --check` 通过。共享工作区残留检查仍只报告历史 8 个稳定残留缺失及汇总，共 9 项错误；本阶段未恢复、删除或提交这些历史路径。

首次精确暂存树为 `c7de044519ba55d71c38938ff12e352d497c9656`，导出目录 `/tmp/apsgo-numeric-incremental.sX4zKc`。干净导出残留检查、`compileall`、相关 Ruff 均通过；同范围累计复验为 `4203 passed in 52.01s`。补入本段证据后将再次生成最终暂存树并执行同范围复验，最终树和结果记录在提交正文；提交身份以 Git 历史为准。

## 4. 完成边界

- 子项 4.1 的代码和共享树检查完成，提交为 `94b19ae`。
- 子项 4.2 已接通显式新数值身份、独立核心/应用双审计、公开结果、交期报告及后端运行身份；共享树累计检查为 `4209 passed in 57.30s`。
- 正式 YAML、SQLite 与活动规则版本没有迁移；旧请求继续按旧身份执行。只有声明 `integer_physical_ms_half_up_v1` 且使用新投影的请求进入新数值流程，不兼容声明返回 `numeric_standard_incompatible`，不回落旧计算。
- 本阶段没有运行 531 单完整搜索、40 万候选性能对照、Windows 或一分钟验收；不能据此宣称总体提速、正式质量通过或可部署。

## 5. 子项 4.2 独立审计、输出与后端接线

### 5.1 实际实现

- 数值核心审计从原始问题、原始总工时、受控拆分组与最终行序重建新的只读数组；不读取搜索链摘要或交期缓存，重新执行规则编译、全量评价、来源/重量/工时/谱系和资源事实检查。
- 数值方案只在最终边界恢复 `SchedulePlan`、`PlanEvaluation`、违规明细、接受轨迹及拆分/虚拟谱系；公开链序与数值评价链序一致，不另行排序。
- 公共服务按请求的数值身份选择流程并在运行清单写入 `numeric-path-cover-local-search-v1`。旧身份调用保持原四参数路径，避免未迁移活动规则被自动切换。
- 数值交期报告按整数毫秒重建原单和虚拟材工时；拆单片段按稳定片号分配父单总毫秒，输出逐节点毫秒、逐单完成时间、晚交分类和数值转换摘要。半秒边界按四舍五入记为一秒。
- 新身份配旧投影或不完整九级声明时，前置返回带字段位置的 `numeric_standard_incompatible`，不调用旧求解器兜底。

### 5.2 检查记录

| 范围 | 结果 |
|---|---|
| 数值边界与独立审计专项 | 6 项通过；覆盖公开值恢复、搜索评价篡改、动态拆分工时、端到端双审计、半秒四舍五入及不兼容声明 |
| 交期报告回归 | 40 项通过；旧报告形状及历史精度不因新分支改变 |
| 架构与资源审计归属 | 103 项通过；NumPy 依赖只精确开放给数值审计，资源事实仍由最终审计边界派生 |
| 共享树累计 | `4209 passed in 57.30s` |

首次累计检查暴露一项架构错误：数值审计直接调用资源事实私有派生函数。修正为由 `final_audit` 提供审计专用入口后，同一私有计算仍只有最终审计模块调用；未放宽原测试。共享残留检查仍只报告历史 8 个稳定残留缺失及汇总，共 9 项；正式 YAML、SQLite、用户既有脏文档和未跟踪技术说明哈希保持。

精确暂存树 `778bfa0ef061acb1bcb6a9b09e8906b7fa5451d7` 导出到 `/tmp/apsgo-v7-numeric-stage42.yCznEV` 后，干净残留检查、`compileall` 及本次相关 Ruff 检查通过，同范围累计为 `4209 passed in 58.13s`。全源码 Ruff 仍可检出 16 处本项未修改旧文件的导入排序问题，不在本提交顺手格式化；本次修改文件均通过。
