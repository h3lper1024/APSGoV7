# 阶段 2：数值规则与全量评价执行记录

## 1. 子项 2.1：规则参数编译与数值判定

- 实施前 `c930fd3`。新增私有规则内核 `core/_numeric_rules.py`，把现有规则对象一次编译为任务绑定的整数参数；搜索侧规则判断只读取 `NumericTask`、`NumericPlan` 和编译结果，不在每次判断中查询参数字典或恢复领域对象。
- 编译器显式覆盖当前注册表的全部 20 类具体规则；冻结 GQGA4 请求实际启用 17 条规则，全部编译，零静默跳过。任务、规则集、编译程序和方案分别携带身份，跨任务组合明确拒绝。
- 已实现边级、链级、静态方案级与受控拆单资格判定。覆盖宽度、厚度、温区、软硬材、连续段、链重、逆宽、跨连续虚拟材真实端点、期序、虚拟比例、链间宽差、填充和拆单资格。交期规则参数已编译，但生产时钟及九级评分留在 2.2，不能把 2.1 称为完整评价完成。
- 所有边界直接按整数比较，不引入隐式宽限；相对厚度使用交叉乘法，厚度区间保持配置原序首命中。欠重违规保留为允许偏差，超重和其他禁用项保留为禁止违规。动态虚拟材、拆片和已接受方案的全量验证依赖阶段 3 的候选物化，本项不伪造动态运行结果。

## 2. 真实输入静态扫描

冻结请求经现有加载和归一化后构建数值任务与规则程序。为只检查全部规则路径而不冒充求解质量，按来源期和原始行序建立 531 条单节点链，未放入原型、未拆单、未搜索。

| 项目 | 实际结果 |
|---|---|
| 数值任务身份 | `9ad4247f71f130a8746cedd33389f98ee9419453227161bf02491106340bf8de` |
| 数值规则程序身份 | `3a1d92c4a648adf0adcf6d452e214a46b61256c31b8d98fbd51db08f52c42276` |
| 编译结果 | 17 条启用规则，531 条单节点链 |
| 判定输出 | 533 条规则违规记录、5314 条规则指标记录 |
| 判定耗时 | 0.026837 秒；仅静态扫描，不是候选搜索或正式性能结果 |

首次内联扫描命令遗漏 `PYTHONPATH=src`，在导入阶段以 `ModuleNotFoundError: apsgo_scheduler` 退出，未进入业务计算；补齐项目包路径后得到上表结果。

## 3. 集中验证

共享树命令：

```bash
PYTHONPATH=src PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -q -p no:cacheprovider \
  tests/core/rules \
  tests/core/test_numeric_units.py \
  tests/core/test_numeric_state.py \
  tests/core/test_numeric_rules.py \
  tests/app/test_gqga4_rule_set_mapping.py \
  tests/architecture/test_package_dependencies.py \
  tests/architecture/test_clean_room_boundaries.py
```

- 共享树最终复验：1174 项通过，1.50 秒，退出 0。
- Ruff：本项五个 Python 文件检查通过；自动修正 1 处导入顺序。
- 首次精确导出发现测试错误依赖未跟踪的 `diagnostics` 请求，因文件缺失为 1173 项通过、1 项失败；测试随后改为仓内自包含的全部 20 类编译样例，真实 531 单只保留为独立扫描证据。最终精确暂存树导出、相同测试及残留检查记录在本项提交正文。
- 正式配置 SHA-256 仍为 `04cb4998cd3d8100a4f7f2ff9c62e10111ced4ae8f827df841507fa535c2f0c0`；SQLite SHA-256 仍为 `72a60e32fea0d2d55f883a8b743175aa5e8fd94e779388db6ae8c8c26ec2bd6a`。

## 4. 当前边界与下一项

### 4.1 子项 2.2 实现结果

- 实施前提交 `11792e0`。新增不依赖搜索缓存的全量数值评价：逐链执行已编译规则，方案级汇总资源与规则，生产序累计 `int64` 毫秒，并按每个原订单最后一个实际片段的位置确定完成时刻。
- 旧欠订单在起排时冻结；交期边界相等不算晚交。等待毫秒先按四舍五入转整秒，再乘一次原订单整数重量；拆片不能重复增加原单交期评分。旧欠全部完成用时、累计等待与延期负担均由同一生产时钟派生。
- 九级目标使用独立的新数值投影名称，严格要求已确认九项全集，实际比较顺序由规则声明取得。旧活动配置继续由现有加载器读取，以保持当前生产服务不被未完成的新搜索中断；新数值评价编译器拒绝旧投影，因此没有将旧名字静默解释为新算法。
- 全量结果绑定任务、规则程序、评分程序、方案代次；所有数组只读，跨任务方案、残缺九级目标和形状错误均前置拒绝。正式规则 SQLite、YAML 和生产求解入口均未修改。

### 4.2 冻结输入固定方案重评

继续使用冻结 531 单，但为避免把未完成的动态虚拟材和拆片转换伪装成正式结果，只按来源期和原输入次序建立 531 条单节点链。将同一规则含义显式换成新数值投影后进行一次全量重评，不搜索、不接受候选。

| 项目 | 实际结果 |
|---|---|
| 数值任务身份 | `aa1d8827e6b944c7b7b2afa323d2eb6da02a96391e5bf0819b5b37629b1f9a5b` |
| 规则程序身份 | `7939161ae9d175e81bc2d3043e4b0784c999cf83f2251c1264ee7d691796469a` |
| 九级程序身份 | `d21292965136f79d861b84b6d1dff8df82cd09ac6e406351d5dc0538ca53101a` |
| 固定方案 | 531 条单节点链；533 条违规记录，其中绝大多数为允许的链重欠下限；该方案不是质量基线 |
| 九级整数值 | `(2, 170300000, 531, 34236609, 498750, 149486673200, 108716, 0, 531)` |
| 新增晚交 | 0 单；仅说明该固定次序下的时钟计算结果 |
| 全量评价耗时 | 0.023755 秒；不含输入准备、搜索、动态动作、审核和输出，不是性能门槛结果 |

### 4.3 阶段 2 集中检查

在子项 2.1 命令基础上加入 `test_numeric_evaluation.py`，并补充规则加载和交期秒级准备回归。首次集中检查为 1308 项通过、1 项失败：旧测试把 `NumericProjection` 固定为 3 个值，并用与新投影不兼容的欠重指标构造所有投影。该测试按每种投影各自合法的指标组合修正，生产逻辑不回退。

- 最终共享树：1310 项通过，2.23 秒，退出 0。
- 新评价模块及相关规则、架构和测试 Ruff 检查通过；`git diff --check` 通过。
- 覆盖毫秒生产序、交期边界等号、无旧欠、旧欠清空、逐原单等待、链间宽差、两位欠重、跨任务身份和不完整九级声明拒绝。
- 源码/测试暂存树 `87aa584fc27be4d1a5eb01cb88246d3de639ed9f` 精确导出到 `/tmp/apsgo-numeric-evaluation.WoK739`；同范围 1310 项通过，2.73 秒，残留检查、`compileall` 和 Ruff 均通过。随后仅补录本证据，源码和测试字节不变，不为文档记录重复运行同一批测试。

## 5. 当前边界与下一项

阶段 2 已完成，下一项进入 3.1“数值化连接、遍历与初始构造”。生产求解仍未接入新内核；当前固定方案不是正式排程，也不代表完整 GQGA4 质量门槛、一分钟目标、Windows 发布包或正式配置迁移已经通过。动态虚拟材和拆片的数值物化及其原单最后片段重评归阶段 3。
