# 阶段4：完整等价、冷启动与正式质量

实施前 `491e901757d98eb4101d17c3803cdedc98623c9d`。本阶段不改生产；仅增加两个复用现有工具的观察入口、相应测试及文档。结果来自macOS ARM64、指定Conda Python3.10.18，不能代替Windows实包验收。

## 1. 冻结与比较边界

- 旧源码：`7fff99791ab22725401580685598b52fc1dec205`，生产SHA `e11bdea69285f3c260353eb54560a6769bbe8f1198098a13d22a038aa0f77602`，导出 `/tmp/apsgo-v7-numeric-baseline.TOCMRO`。
- 新源码：上述实施前提交，树 `c628626055c057b60ae4d9adad1f4d051c15214c`，生产SHA `8474993426b8710323814b6bef88ce43ee7b1e7551059f86e5382bda35dfa405`，导出 `/tmp/apsgo-v7-numeric-stage3.DGQBQE`。仅两个批准的桥接文件不同。
- 输入为阶段0冻结的900/10/200000、种子590531；实际字节SHA `11bdd096717414a25b14e7a219b860e6e84953f17e9c02bbf8e298da9266829c`。NumPy2.2.6、Numba0.65.1、llvmlite0.47.0不变。
- 原观察工具SHA `822239430ee9c06a4d0db87d5da9ea039b219b660737089fe819292ab08cc581`。复用旧完整比较和工作量检查，只追加初始、首轮最终、全程最终三处缓存的hits/misses/entries共9个数值叶豁免；类型、位置、新字段、运行清单及全部业务内容继续比较。
- 逐样本前后检查源码、输入、工具、辅助脚本和依赖；独立进程交替执行，不并行测试，以 `caffeinate -is` 保持运行期间唤醒，不改永久电源设置。未观察到休眠或需要弃用整组的异常中断。

全部10份的初始/首轮/最终有序方案、评价、轨迹、资源、评分、计数、停止及双审计精确一致。首轮64226次检查/2263次完整评价/34次采纳，自然结束；最终200000/3697/54，停止原因为候选额度用尽。质量 `(0,0,0,0,11726,660,23)`，零禁止、零欠重，保留两次拆单（同期间1次、未来借入归还1次）及来源守恒。

## 2. 五对独立冷进程

下列完整调用包含同样观察开销；每个新进程真实包含首次导入/编译，不是热内核计时。外层另计启动至退出，包含工具准备及产物写入。

| 对次／执行顺序 | 旧完整秒 | 新完整秒 | 旧CPU秒 | 新CPU秒 |
|---|---:|---:|---:|---:|
| 1 旧→新 | 80.311575 | 32.974840 | 80.289260 | 32.689509 |
| 2 新→旧 | 81.359855 | 32.043927 | 81.348209 | 31.988565 |
| 3 旧→新 | 80.927723 | 32.275849 | 80.905180 | 32.068920 |
| 4 新→旧 | 81.105801 | 32.224467 | 81.066752 | 32.175460 |
| 5 旧→新 | 81.399115 | 32.391288 | 81.379099 | 32.131365 |

| 中位数／秒 | 旧 | 新 | 减少 |
|---|---:|---:|---:|
| 完整公开调用 | 81.105801 | 32.275849 | 60.21% |
| 完整CPU时间 | 81.066752 | 32.131365 | 60.36% |
| 首轮局部搜索 | 36.520444 | 13.134452 | 64.04% |
| 进程启动至退出 | 81.742445 | 32.978497 | 59.66% |

新5份日志各恰好start、single-ready、double-ready三事件；两模式均为真实nopython执行，首个单桥扫描无解、双桥扫描有解。旧5份没有数值事件。最终实际边缓存从2685638命中/206379未命中/206379条目，变为133643/123929/123929；没有为等价伪造统计。**5对专项不是历史20对性能门。**

## 3. 同进程与重启

| 独立补充场景 | 运行前有编译签名 | 完整秒 | CPU秒 |
|---|---|---:|---:|
| 同进程第一次请求 | 否 | 32.574896 | 32.306076 |
| 同进程第二次请求 | 是 | 30.429171 | 30.425108 |
| 随后新进程重启请求 | 否 | 32.199898 | 31.897730 |

每次通过原工具重新读取并构造完整请求、重新执行公开求解，目录按新工厂构建；三次均重新出现有界执行事件，结果保持同工作量。阶段3绑定隔离测试和本次源码复核共同确认只复用进程内编译代码，不共享上次问题、规则或原型目录。此三次单独列示，不混入冷进程中位数。

## 4. 函数次数与剩余成本

旧分析复用 `diagnostics/unchanged_chain_evaluation_reuse/stage_03_profile/`：源码逐文件、请求字节、原工具以及完整初始/首轮快照与阶段0一致。旧探针只增加候选评价计数，不调用节点物化或语义编码；仅复用下面次数，不能比较旧分析的耗时。新分析同为首轮64226/2263/34；除明确缓存统计外，有序业务完全相同。

| 首轮调用 | 旧 | 新 |
|---|---:|---:|
| 桥接入口 | 9283 | 9283 |
| 节点物化／Node初始化 | 455702 | 10549 |
| 其中桥接物化 | 447930 | 2777 |
| 其中重量填充物化 | 7772 | 7772 |
| 原Python桥接回退 | 原主路径 | 0 |
| 语义指纹查询（含对象缓存命中） | 2779402 | 140002 |
| 实际语义指纹编码 | 463448 | 7802 |

新探针观察211669个原生块，合计3.266909秒、CPU3.145145秒；首块含JIT为2.640844秒，后续最长块68.208微秒。块观察覆盖进入首轮前至首轮结束的实际块调用，只计原生调用，不含数组准备/模块导入；cProfile本身仅覆盖首轮。观察开销和编译分析开销都存在，不用这些时间计算新旧收益，也不承诺硬实时最大块时长。

剩余主要成本为完整候选评价、资源视图和Decimal重量汇总；累计时间存在嵌套，不能相加。本阶段不据此扩大优化范围。

首次补充分析 `stage_04_profile/` 的求解、快照和pstats已完成，但摘要将普通float交给精确JSON编码器导致退出1，留下空摘要；原文件和错误日志全部保留。仅修正补充工具在导出时转Decimal，在新目录 `stage_04_profile_retry_01/` 重跑分析并退出0；生产、五对样本、冷热请求未重跑或改写。

## 5. 原正式质量及最新现场输入

- 原正式180/10输入：按原 `quality_precheck.py`、原质量门与原外部参考校验执行；31.255488秒，200000/3603/58，质量 `(0,0,0,0,10912,600,22)`，零禁止/零欠重、完整531单及29333.91吨守恒，两次同期间拆单，双审计与全部原质量检查通过。门槛、规则及输入身份未改；不是20对性能验收。
- 最新310/10输入：31.892006秒、CPU31.839215秒，完成200000/3697/54，实际是额度截断而非时间截断；质量 `(0,0,0,0,11726,660,23)`，双审计通过。与现场原119809次的未知源码Windows样本不是同工作量，不报告跨机器提速比。
- 两输入的请求身份分别沿用阶段0冻结值，不把正式22链与现场23链混称为同输入质量变化。

## 6. 原始产物、命令与验证

原始目录统一位于仓库忽略目录 `diagnostics/numpy_numba_virtual_bridge/`，不覆盖历史样本。`stage_04_cold_pairs/` 内每对均有old/new原请求、身份、完整measurement、日志、开始/结束摘要及逐对比较；`summary.json`逐个绑定测量/身份/log哈希。父导出清单为 `stage_04_exports.json`。

| 文件（相对上述原始目录） | SHA-256 |
|---|---|
| `stage_04_cold_pairs/summary.json` | `b5c5656b1c2e37eb24d641f38a6049293ea0bf49193aa15e42b24f4122583b6b` |
| `stage_04_cold_pairs/cohort.json` | `fdd6b1993e6d2b0088a8f3766c61959231bd4e4ec8476015667e54b53fe1722b` |
| `stage_04_cold_pairs/expected_sources.json` | `1944b9d5ed9a529a321b1013577b6f78d38424992473e12d7e6d5ef5b614b049` |
| `stage_04_exports.json` | `ba2924a12d4221cdd0ea4b2c5aa555d5d0e0871e47725de35e58b1dda1f30497` |
| `stage_04_warm/probe_summary.json` | `2125e94c85700cd2772bd10cc1e63c291837f971b7fe9cfd4ce7a40d378b37fa` |
| `stage_04_restart/probe_summary.json` | `64860bf64bbc970ba47696df5f9f7a716343f2ee5827c8cbf2ce60c234ec70da` |
| `stage_04_profile_retry_01/probe_summary.json` | `d2adc9b2b3e77bbea671b85d9444ff05891272d74b5c65765d74c11ceae0035d` |
| `stage_04_profile_retry_01/first_search/first_search.pstats` | `0b11b7fe8fc943971e4d5fd71cff8a23829b4845d58327a6d9479a19619e7c11` |
| `stage_04_formal_quality/quality_report.json` | `c70a99d1396ffcee0e08da8241fc29c007404ea9bce7e40b178fb3ad391b41e8` |
| `stage_04_latest/measurement.json` | `bdf82a82a37b762bad6b19cd4446d6bc7c1df4783b8960a92fca55581a2e3c15` |

复用旧pstats SHA `4190e87764d2f5035399fc7460ad31daae4515af72508459b6c4e042b29fed0b`。所有原始输入、规则、预算和两个保护文件均保持冻结字节；实际源码与产物经独立只读复核。

```bash
cd /Users/miles/dev/dev-py/APSGOV7
PYTHONDONTWRITEBYTECODE=1 caffeinate -is /Users/miles/anaconda3/envs/aps_3.10.18/bin/python docs/implementation/evidence/apsgo_v7_numpy_numba_virtual_bridge/stage_04_comparison/run_pairs.py --old-root /tmp/apsgo-v7-numeric-baseline.TOCMRO --new-root /tmp/apsgo-v7-numeric-stage3.DGQBQE --prepared-request diagnostics/unchanged_chain_evaluation_reuse/frozen/20260908_205419_4a73iqo7/prepared_request.json --output diagnostics/numpy_numba_virtual_bridge/stage_04_cold_pairs
```

补充工具 `probe_bridge.py` 使用 `--code-root`、`--prepared-request`、`--output-dir`，模式依次为 `warm`、`restart`、`profile`；实际成功输出为上述同名目录及profile_retry_01。正式质量命令指定原脚本、`--code-repository /Users/miles/dev/dev-py/APSGOV7 --code-revision 491e901757d98eb4101d17c3803cdedc98623c9d`；最新输入使用原观察工具 `--scope full`，没有预算覆盖参数。上述输出均为已存在证据，重跑必须新目录，不能原地再次执行覆盖。

比较工具117项合成测试通过，覆盖精确叶子豁免、身份、工作量、错误与实际模式证据。共享/精确暂存树最终累计、Ruff、diff及原残留检查结果见本阶段提交正文。**本机完整收益和原质量已验证；提交后进入阶段5发布源码适配，Windows实际构建、只读运行和真实订单仍待验。**
