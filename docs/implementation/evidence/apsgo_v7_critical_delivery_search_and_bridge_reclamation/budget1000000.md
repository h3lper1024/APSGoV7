# 组合方案：100 万次与 40 万次候选额度对照

## 1. 授权与运行条件

用户要求对关键候选调度＋连接虚拟材回收组合单独提高候选额度后测试。执行基线 `codex/delivery-objective-optimization@6554106`，开始前工作区干净；macOS / Darwin、Conda `aps_3.10.18` / Python 3.10.18。

只改变 `policy.candidate_check_limit`：400000→1000000。生产代码与 40 万次运行 `66cf04e` 相同，种子 590531、九级顺序/秒级精度、工时、规则、起排 `2026-06-01T00:00:00+08:00`、虚拟速度 100 米/分钟不变，搜索 9999 秒＋收尾 10 秒；正式服务配置与数据库不改。

复用已有独立输入 `diagnostics/backlog_priority_search/input_budget1000000_search9999.json`，SHA-256 `cb4e1fe98520c4923dd9424e1988afd2bb4aab94e708158342b5beecf08b0d9e`。用 `load_request()`、`prepare()` 和 `dataclasses.replace()` 逐值证明来源请求和实际秒级请求都只差候选上限，不重新拼装订单或活动规则。

对照为 `diagnostics/critical_delivery_search_and_bridge_reclamation/combined_01`，其既有重复样本为 `combined_repeat_02`；新样本使用同目录下 `combined_1000000_01`。23 份既有运行产物哈希均已核验，不覆盖旧结果。

## 2. 实际命令

```sh
PYTHONDONTWRITEBYTECODE=1 /usr/bin/caffeinate -i \
  /Users/miles/anaconda3/envs/aps_3.10.18/bin/python tools/run_delivery_comparison.py \
  --prepared-request diagnostics/backlog_priority_search/input_budget1000000_search9999.json \
  --timing-source diagnostics/delivery_objective/timing_source.json \
  --start 2026-06-01T00:00:00+08:00 --virtual-speed 100 \
  --variant delivery-backlog-seconds --observe-search-opportunities \
  --output-dir diagnostics/critical_delivery_search_and_bridge_reclamation/combined_1000000_01
```

运行完成，入口退出 0，实际用满 1000000 次后以 `candidate_limit_reached` 停止；没有触及 9999 秒搜索时限。独立结果与原结果均保留，不同预算不称同条件复跑；本次单样本不是正式性能验收或最优性证明。

## 3. 检查与执行记录

- 运行前输入差异和生产代码一致性断言退出 0；23 份旧产物哈希通过。首次预检把 `prepare()` 返回的二元组误当成请求，退出 1、未写文件；按实际接口解包后上述预检通过，不涉及算法故障。
- 共享残留检查仅历史八项 V6 缺失及汇总，退出 1；不恢复或删除旧残留。本次不改生产、工具或测试，最终按文档/证据提交口径核对保护哈希与精确导出。
- 完整比较产物已写出；比较过程重新计算全部 531 原单日期、完整评价及方案身份，核心/应用双审计和来源守恒通过；额外无搜索缓存核心复审两组均通过。比较进程已结束，终端摘要曾截断；收口另行回读并验证落盘结果，不以截断输出代替结果。
- 当前状态：本次独立试验完成；未改变生产代码、工具、测试、正式配置或数据库。最终静态检查、保护哈希及精确导出结果见本项提交正文。
- 收口只读复算再次通过：全部比较字段及 531 单明细与已落盘结果相同、两组独立核心审计通过，退出 0。首次回读断言忘记沿用已明确标记的“不同预算” `scope`，仅该标签不一致而退出 1；补上原标签后通过，不修改原始结果或比较逻辑，不重跑求解。
- 共享静态检查退出 0：四文件 UTF-8 无 BOM、11 处本地链接、预算及计数闭合通过；九项保护哈希、23 份旧产物、两组 12 份结果产物及原始比较摘要/531 单明细哈希全部通过，生产代码相对 `66cf04e` 无差异。

## 4. 结果与取舍

**增加额度显著提前旧欠清空，但不保证本月晚交单数减少。** 96 单旧欠、5386.03 吨的范围保持不变；其原本已经欠交，不能与本月新增晚交混为一类。

| 指标 | 组合 40 万次 | 组合 100 万次 |
|---|---:|---:|
| 旧欠全部完成时间（2026 年） | 6 月 15 日 03:35:31 | 6 月 11 日 03:35:40 |
| 旧欠按重量加权平均等待 / 小时 | 92.93 | 81.04 |
| 旧欠完成 90% 重量时间 | 6 月 8 日 18:21:23 | 6 月 8 日 00:59:22 |
| 本月晚交原单数 / 吨 | 14 / 685.33 | 18 / 762.02 |
| 等待及晚交加权负担 / 吨·小时（评分值） | 636816.43 | 519392.32 |
| 链间宽差总和 / 毫米 | 14954 | 14919 |
| 虚拟材数量 / 吨 | 41 / 820 | 44 / 880 |
| 小辊期数 | 26 | 27 |
| 禁止违规 / 欠重链 | 0 / 0 | 0 / 0 |
| 候选检查次数 | 400000 | 1000000 |
| 完整候选评价次数 | 172312 | 433269 |
| 接受次数 | 804 | 1090 |
| 外层实际耗时 / 秒 | 1214.57 | 3297.38 |
| 进程 CPU 时间 / 秒 | 1210.56 | 3285.31 |
| 停止原因 | 候选额度用完 | 候选额度用完 |
| 来源守恒、双审计、独立核心复审 | 通过 | 通过 |

旧欠清空提前约 96 小时，加权等待均值降低 11.89 小时；加权负担降低 18.44%。相应代价是本月晚交增加 4 单、76.69 吨，虚拟材增加 60 吨、小辊期增加 1 个；耗时为原来的 2.71 倍，多约 34.71 分钟。按当前严格逐级九项评分，旧欠清空是更靠前的目标；晚交单数本就只作统计，因此上述变化不表示评分器发生逆向接受。不能将“当前评分更优”写成“所有业务指标更优”。

耗时主要增加在后置精修：首轮局部搜索 155.97→154.68 秒，拆单及重放 251.83→248.38 秒，链间宽差及交期结构精修 802.29→2889.60 秒。百万次 CPU/实际耗时约 99.63%，这是约一个逻辑核心的计算量比例，不是整机 CPU 利用率或多核并行率。

## 5. 本月交期与全部原单变化

| 交货日期 | 该交期原单数 | 40 万次晚交单数 / 吨 | 100 万次晚交单数 / 吨 |
|---|---:|---:|---:|
| 6 月 5 日 | 6 | 3 / 341.37 | 5 / 380.34 |
| 6 月 10 日 | 20 | 10 / 319.96 | 13 / 381.68 |
| 6 月 15 日 | 5 | 1 / 24.00 | 0 / 0.00 |
| 6 月 30 日 | 404 | 0 / 0.00 | 0 / 0.00 |

本月有 3 单由晚交变为按期、7 单由按期变为晚交，净增 4 单。全部原单完成时间对比：旧欠 50 单提前、46 单后移；其余 435 单中 297 单提前、138 单后移。因此“旧欠整体更早”不等于每张旧欠订单都更早。原单按最后一个片段完成时间统计，拆单不重复计数。

由晚交恢复按期：`0030122066-000020`、`0030121432-000010`、`0030120940-000120`。

由按期变为晚交：`0030121690-000040`、`0030121690-000010`、`0030121346-000020`、`0030121196-000020`、`0030120757-000040`、`0030120745-000010`、`0030120974-000030`。

18 单最新晚交日期、重量和延期小时详见[机器摘要](budget1000000_comparison.json)的 `new_late_orders`；全部 531 单新旧明细保留在仓内诊断目录 `diagnostics/critical_delivery_search_and_bridge_reclamation/compare_combined_1000000_01/order_changes.json`。交期边界仍为当天 24 点，评分仍按秒，不更改原始工时精度。

## 6. 搜索路径、预算与回收核对

两组生产源码摘要一致，为 `1c33e3047ffa324a4270401ec07b2155da13916e88d63eeb00389dedb7e00494`；实际请求仅候选上限不同。首轮完整快照、精修入口快照一致，前 804 次接受完整一致。新增第 805 次接受发生在第 400303 次检查，为单节点移动；不是起始输入或早期搜索发生偏移。未在百万次运行内部额外保存“恰好 40 万次”的完整方案快照，不将共同轨迹冒充该快照。

比较复用 `tools.compare_backlog_search_runs.compare()`，仅在本次进程用临时验证器先断言两侧额度恰为 400000/1000000，再把右侧额度归一为 400000 交给原请求一致性检查；其他字段逐值比较，生产工具文件未修改。实际 `identical_request=false`，另记 `request_equal_except_candidate_limit=true`；没有关闭原工具的身份或审计检查。

| 精修入口之后的检查归属 | 40 万次 | 100 万次 |
|---|---:|---:|
| 连接材独立清理 | 2122 | 8238 |
| 重点候选 | 108764 | 416642 |
| 常规候选 | 95574 | 381580 |
| 合计 | 206460 | 806460 |

入口已消耗 193540 次；上述合计与各自全程额度精确闭合。额外 600000 次带来 286 次接受，不能将候选检查次数等同于完整评价次数。

精修入口虚拟材为 23 个/460 吨。40 万次过程删除 50 个/1000 吨、新增 68 个/1360 吨，最终 41 个/820 吨；百万次删除 119 个/2380 吨、新增 140 个/2800 吨，最终 44 个/880 吨。两组库存分别满足 `460-1000+1360=820`、`460-2380+2800=880`；生成序号上界分别 27→95、27→167，没有复用删除编号。这里是接受过程的累计增删，不把回收 2380 吨称为净节省。

## 7. 证据与交接

提交的机器摘要保存两组产物 SHA-256、源码身份、计数/阶段时间、交期分组、回收核对及新增晚交明细；大体量原始请求、方案、日志、观察记录和全部原单变化保留原诊断目录。旧 23 份运行产物和计划第 2.2 节九项保护材料复核，不覆盖旧对照。

本次只完成指定的一次百万次试验及与既有 40 万次的对比，没有新增算法、修改优先级、扩大正式配置或运行第二个百万次样本。按 Ponytail Lite 复用现有入口及比较器，不增加仅为一次试验服务的生产开关或测试框架。后续是否采用该额度需根据旧欠、晚交及耗时的取舍决定。
