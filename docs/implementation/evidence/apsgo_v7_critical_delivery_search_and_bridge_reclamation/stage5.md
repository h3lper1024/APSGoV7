# 阶段 5：最终回归与交接

实施前 `728f65c`，分支 `codex/delivery-objective-optimization`，macOS / Conda `aps_3.10.18`。生产实现为阶段 1 的 `fe6c54d` 和阶段 3 的 `66cf04e`，本阶段不改生产代码，不追加调参或完整求解。

## 1. 完成范围与采用边界

已实现关键/常规候选交替、接受后重新定位，以及普通连接虚拟材的独立清理和随结构调整回收；保持原有完整接受、规则与双审计。仍只由既有显式秒级交期模式启用；原七级、默认交期九级和历史交期优先九级保持。

本专项三组及组合重复均用 400000 次候选上限，搜索 9999 秒及收尾 10 秒。正式 YAML、SQLite、原订单/工时及历史结果不改。每个实际候选版本从共用额度扣费；没有额外“免费”候选。

结果详见[阶段 4 对照](stage4.md)：组合旧欠 6 月 15 日 03:35 清空、本月 14 单 / 685.33 吨晚交、820 吨虚拟和 26 个小辊期，两次业务状态一致，零禁止/零欠重、来源守恒与双审计通过。

- **实现正确**：功能与重复核验通过；完整累计回归结果见第 2 节。
- **实测收益**：比原代码同 40 万次旧欠清空提前约 6.90 天，但本月晚交多 2 单 / 31.43 吨、虚拟多 240 吨、链数多 1，单次运行约 20 分钟。比仅调度旧欠再提前约 32.87 小时、本月晚交少 1 单，但平均等待和耗时增加，虚拟重量不降。
- **是否采用**：按当前确认的严格九级评分，组合优于两组对照；建议保留为该试验模式的实现结果。没有证据支持“全部指标更优”或“已经最优”，不自动启用到正式配置。若用户更看重本月晚交数或虚拟重量，应另行讨论目标/流程，不在此收口中暗改。

未验证 Windows/EXE、HTTP/C# 现场、正式成对性能验收；未启用规则、重启服务、部署、合并或推送。未运行新的初始构造、并行或评分试验。

## 2. 累计回归与架构检查误报修正

首次共享累计退出 1：4072 项通过、1 项失败，耗时 58.75 秒。唯一失败为架构扫描把任何数字 `3` 都认成旧基线“3 条违规”，误拦本专项已确认的三节点短片段比较及三类关键动作数量；并非排程违规或算法测试失败。

仅修正 `tests/architecture/test_clean_room_boundaries.py`：限定生产模块、函数名和完整语法树表达式，只识别 `_critical_recipe` 的两个三节点跨度比较和 `_scan_critical_families` 的 `(3, 6)` 家族数量。仍拒绝其他模块、其他函数、同函数无关的 `benchmark = 3`、旧基线 `37`；原违规样例全部保留，新增两个带正反例的检查。没有删除全局基线数字保护，也没有改生产数字、求解流程或实测产物。

```sh
PYTHONDONTWRITEBYTECODE=1 /usr/bin/caffeinate -i \
  /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -q -p no:cacheprovider \
  tests/architecture tests/api tests/app tests/core tests/service
```

修正后共享累计退出 0：**4075 项通过 / 58.57 秒**。精确暂存树通过 `git write-tree` / `git archive` 导出到新临时目录，执行上面同范围累计回归及残留检查；实际树身份、导出路径、数量、退出码和耗时随本阶段提交正文记录，只有通过才提交，不用更小范围替代。

## 3. 保护与追踪

九项冻结保护材料按本专项计划第 2.2 节逐项复核；正式配置、SQLite、百万次原输入/工时/三份主产物及两份旧专项文档不能被新证据覆盖。40 万次派生请求沿用已冻结独立路径。

共享残留检查仍仅历史八项 V6 稳定残留缺失及汇总，单独记录、不恢复旧残留；精确导出检查不能包含这些本地残留。中文提交按阶段独立保留，阶段 4 为 `728f65c`；本阶段身份以 Git 提交为准，不在正文自引用。

如需恢复，完整回到专项前逻辑以 `896dba5` 为对照，只去掉回收以阶段 2 的 `82106e3` 为对照；必须另行获得恢复指令，核对用户工作区并用明确反向提交，不执行破坏性重置、不删除证据。
