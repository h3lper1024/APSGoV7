# 首批性能样本的休眠影响与复测授权

## 已确认事实

首批 `performance/acceptance_report.json` 的 42 行包含双方各 1 次预热及 20 对正式样本，计时统计公式返回 `pass`：新实现中位数 171.370401334 秒，第 95 百分位 172.196414500 秒，最大值 172.539290541 秒。参考中位数 21.927131708 秒、第 95 百分位 23.132397208 秒，控制漂移分别约 7.09%、2.44%；参考最大单次 30.945888292 秒保留，不把最大值改为门槛。

但开始/结束 UTC 为 `2026-09-04T09:08:17.176035+00:00`、`2026-09-04T14:00:06.428531+00:00`，相隔 17509.252496 秒；42 个子进程记录的计时合计 4049.461647 秒，差额约 13459.790849 秒。不能把这个差额直接分摊到某个求解样本。

只读检查命令：

```bash
pmset -g log | rg '2026-09-04 (1[7-9]|2[0-2]):.*(Entering Sleep|Wake from|DarkWake from)'
```

以下为该命令输出中的关键事件摘录；仅保留时间和事件，省略无关电量及设备细节，不冒充完整原始日志：

| 本地时间（UTC+8） | 系统事件 |
|---|---|
| 2026-09-04 18:06:43 | `Entering Sleep state due to 'Clamshell Sleep'` |
| 2026-09-04 18:10:06 | 再次 `Clamshell Sleep` |
| 2026-09-04 18:11:29～22:31:18 | 多次维护睡眠、后台唤醒后再睡眠 |
| 2026-09-04 22:00:06 | 后台唤醒，随后再次睡眠；首批台账在该时段结束 |
| 2026-09-04 22:33:27 | `Wake from Deep Idle`，原因包含打开屏盖及输入活动 |

系统日志证明休眠与首批性能采样重叠。原计时沿用冻结工具的 `perf_counter()`；台账未记录每个新实现子进程的 UTC 起止，不能精确分解睡眠、样本外身份核验及其他间隔的贡献，也不据此断言算法本身耗时数小时。

## 对首批结论的限制

- 原始 42 行和每个新实现的六份产物全部保留，原报告 `pass` 不改写。
- 该 `pass` 是既有公式对记录值的结果，不作为正常持续唤醒环境下正式性能收口的依据。
- 正式新实现样本中，10 次额度停止、10 次搜索时间停止；18 次宽差 10912，另两次为 11137、11102，全部优于原 11218，且正式质量门通过。不能宣称 20 次结果完全相同。
- 前面的独立三次确定性运行均未时间截断，原验证结论保持，不以受干扰样本替换它们。

## 复测授权与边界

用户已明确确认：保持电脑唤醒、不合盖，允许重新完整运行 20 对性能样本；不是从首批挑选样本补足。新目录固定为 `performance_awake_rerun/`，双方重新各预热一次，再按原顺序运行 20 对。

复测使用原运行器、同一代码导出、输入、种子、规则、200000 次候选上限及 180 秒时限，不改统计公式或门槛。使用本机 `caffeinate -i` 在该命令存续期间临时阻止系统空闲休眠，命令退出即解除；不修改持久电源配置，不声称能防止合盖休眠。启动前已核对本机手册；2026-09-04 22:38 的供电状态为电池，因此不假设交流电专用选项生效。

复测后再次核对其实际时间段的系统休眠事件，独立记录新结论。首批受干扰记录、后续授权及复测记录均保留。

## 已完成的保持唤醒复测

- 实际命令在原运行器前添加 `/usr/bin/caffeinate -i`，未改变运行器文件、子进程参数或门槛。启动时 `pmset -g assertions` 确认本次 `caffeinate` 为该 Python 命令持有 `PreventUserIdleSystemSleep`；运行约 35 分钟时仍存在。退出后该临时进程已结束，不改动用户其他防休眠程序。
- 新批开始/结束为 `2026-09-04T14:40:55.567731+00:00`、`2026-09-04T15:48:59.657510+00:00`，即本地 22:40:55～23:48:59。总历时 4084.089779 秒，子进程计时合计 4080.322844333 秒，差额 3.766934667 秒；不再出现首批的长时间未覆盖间隔。
- 对该实际时间段执行下述系统日志检查，退出 0，结果为 `sleep_events_in_rerun=0`。这是该日志范围内未检出休眠事件，不将防空闲休眠工具泛称为防止所有类型休眠。
- 新批 42 行完整，双方各 1 次预热、20 对正式样本；参考输出全部一致，质量及数值性能门通过。具体统计见 [验收记录](README.md)，首批不并入、不替换、不删除。

```bash
pmset -g log | awk 'substr($0,1,19) >= "2026-09-04 22:40:55" && substr($0,1,19) <= "2026-09-04 23:48:59" && /Entering Sleep/ {print; count++} END {print "sleep_events_in_rerun=" count+0}'
```
