# 阶段 6：最终回归与专项收口

2026-09-14，macOS / Darwin，Conda `aps_3.10.18` / Python 3.10.18。实施前 `codex/delivery-objective-optimization@adb5377`，工作区干净；生产代码为 `bf2dbf8`。本阶段只更新专项文档与本记录，不改代码、规则、配置、数据库或求解结果。

## 收口结论

**实现正确、重复性通过，但综合业务收益有取舍，暂不直接替换正式方案。**

秒级时间评分和欠重优先顺序按确认语义实现：复用原计时、完整候选及双审计，不增加新框架、依赖、隐藏晚交评分或另一套搜索流程。两次百万次运行均零禁止、零欠重，26 链、1220 吨虚拟、宽差 14406；全部 531 单日期、1408 次接受、最终方案/评价与计数完全一致。

相对历史交期优先百万次结果，154 条欠重被消除，虚拟/链数/宽差降低，但旧欠清空从 6 月 6 日推至 6 月 12 日，本月晚交从 5 单 / 378.36 吨增至 15 单 / 677.06 吨，耗时从约 27.7 分钟增至约 47.2 / 46.5 分钟。不能把质量条件通过等同于所有业务目标更优。完整明细和身份见[阶段 5.2](stage5_2.md)及[机器摘要](stage5_2_comparison.json)。

## 验证记录

| 检查 | 本次证据 / 状态 |
|---|---|
| 阶段 5.1 功能与集中验证 | 提交 `bf2dbf8`；相关 114 项；共享/精确导出累计各 4023 项；第 47 次历史极小时间差样例被新完整候选拒绝 |
| 阶段 5.2 完整对照 | 提交 `adb5377`；两次运行和两次比较均退出 0；精确导出累计 4023 项 / 58.47 秒 |
| 阶段 6 共享树累计回归 | 新运行 4023 项 / 57.43 秒通过，退出 0；不是复用前阶段数字 |
| 阶段 6 最终精确暂存树 | 干净导出执行同范围累计回归及残留/静态检查；具体树、路径、退出码和数量由本阶段提交正文记录，只有通过才提交收口 |
| 输出独立验证 | 两次核心审计独立回算与原记录相同；报告自身及请求、规则、问题、方案绑定一致；1408 次接受严格改善，清空首次改善最小 46 秒 |
| 保护核验 | 对照机器摘要列出的 34 个输入/历史/新产物哈希；正式 YAML、SQLite、原文件以及 `bf2dbf8` 后生产源码均不变 |
| 共享残留 | 仍只有原八项旧 V6 稳定文件缺失；保留原始清单，不恢复无关文件，不把历史缺失当作本次代码失败 |
| 提交范围 | 仅 `AGENTS.md`、本专项设计/计划及本记录；UTF-8 无 BOM、LF、链接、围栏、差异和精确文件清单检查 |

本阶段累计命令：

```sh
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -q -p no:cacheprovider tests/architecture tests/api tests/app tests/core tests/service
```

同命令也在 `git write-tree` / `git archive` 生成的最终临时目录执行；不对普通文档小步骤重复新排程，不新增性能样本。

## 尚未关闭的正式采用事项

| 事项 | 实际边界 |
|---|---|
| 正式质量 / 业务取舍 | 本次数据的零禁止、零欠重、来源守恒和虚拟比例条件通过；本月仍 15 单晚交，旧欠清空并非最早，不称最优 |
| 正式性能验收 | 两次为百万次/9999+10 秒后端试验，不是正式 20 对性能门；耗时没有改善，未改变正式门槛或时限 |
| Windows / 打包 / 前端 | 本轮只在 macOS 项目环境验证；没有新增 Windows 打包、现场或 C#/HTTP 适配验收 |
| 活动规则与部署 | 新模式显式通过 `--variant delivery-backlog-seconds` 选择；默认及历史规则语义保留。没有改正式 YAML/SQLite、启用活动规则、重启服务、合并或推送 |

阶段 0～6 的本专项实施与证据到本次独立提交完成收口。上述业务和部署取舍单独决策，不以“继续实施”为由自动扩大范围或启动下一轮调参。
