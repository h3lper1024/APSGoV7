# 阶段 2 修正：直接可连接的插入位置优先

2026-09-14，实施前 `de9015f`，macOS / Conda `aps_3.10.18`。用户已明确确认本次调整。**两条冻结样例的搜索机会检查已通过，候选构造和核心审计保持；本次不是完整 GQGA4 重跑，新九级评分尚未实施。**

## 1. 实现范围

仅改变“启用交期且有旧欠”时的跨链单节点插入位置顺序。沿用现有连接缓存，检查插入处的两个有向接口；首尾空位只检查存在的一侧。直接可连接的空位先给出，其他空位暂存后仍全部给出，两组内稳定保留原先的访问顺序和轮转起点。生成器按需查询，不建立第二份矩阵或完整候选缓存。

4 提案/原单、64 提案/动作类、目标链轮转、移动/交换交替、原九级评分及原欠重保护不变。移出断口、链级规则、桥接、完整评价、实际候选扣费及无缓存审计仍走原代码。无旧欠或无交期时不增加本次排序查询。直接可连接不是完整候选可接受的保证。

## 2. 机会对照

| 原订单 | 原九级旧枚举 | 未修正的小批轮转 | 本次节点动作提案位次 | 本次五类共同轮转中的出现计数 |
|---|---:|---:|---:|---:|
| `0002002009-000020`，8.14 吨 | 120667 | 430854 | **29741** | **96966** |
| `0030117283-000020`，3.44 吨 | 165345 | 406737 | **6389** | **25881** |

原单初始排名仍为第 2 / 5；只调整插入位置。两例在节点类中分别比原枚举提前约 75.35%、96.14%，且五类共同轮转时均在原 400000 次额度内出现。前一次失败记录 [stage2.md](stage2.md) 保留，不覆盖或反向改写为通过。

复现命令（已有目录不可覆盖，重复时换新的末尾序号）：

```sh
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python tools/replay_backlog_search_witnesses.py \
  --baseline-manifest docs/implementation/evidence/apsgo_v7_urgent_order_search/restored_search9999_comparison.json \
  --output-dir diagnostics/backlog_priority_search/stage2_direct_first_opportunities_03 --opportunities

PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python tools/replay_backlog_search_witnesses.py \
  --baseline-manifest docs/implementation/evidence/apsgo_v7_urgent_order_search/restored_search9999_comparison.json \
  --output-dir diagnostics/backlog_priority_search/stage2_direct_first_witnesses_02
```

机会工具先核对原五产物及输入保护哈希，再使用相同冻结方案和原规则自动枚举；订单号只用于观察命中，不参与生产排序。节点类单独观察用于比较提案位次；五类扫描另从原状态开始，共扣 400000 次，观察回调始终返回拒绝，不执行候选构造或桥接，原方案指纹不变。**这是不改变方案的机会诊断，不是完整实际搜索轨迹或新的发布结果。**

独立样例模式分别从同一原方案构造两次完整移动。两次仍各花 1 次实际候选检查、均接受且核心审计通过；原单完成时间、完整质量、计划指纹和审计摘要与阶段 0 精确一致，不是将两次移动叠加成一个方案。公共服务/应用发布审计由阶段 3 全流程复测验证，本次不冒称已通过。

最终独立样例 `_02` 退出 0，经过 1.065039 秒 / CPU 1.038903 秒。两种最终诊断均绑定生产源码摘要 `ba52bbbb5d41d80636fd0928f68d5e370230e82ca5d290531aedf80504998d5d`；原五产物和冻结输入哈希由工具逐项校验。小型产物留在 `diagnostics/backlog_priority_search/`，不整包提交：

| 相对此诊断根目录的文件 | SHA-256 |
|---|---|
| `stage2_direct_first_opportunities_03/opportunities.json` | `0636213fb99bbad9e69b179c11ecb460d5a52e0ee3af34a30df9b308ec0c1d61` |
| `stage2_direct_first_witnesses_02/summary.json` | `37675ff45097052698c145f1f1aff616cb5856391b0989b7af12492cefa924f8` |

## 3. 枚举开销及重复性

最终机会观察 `_03` 退出 0，`opportunity_gate_passed=true`；整个工具 12.375746 秒 / CPU 12.304431 秒。其中：

| 观察部分 | 经过时间 / 秒 | CPU / 秒 | 连接缓存命中 / 未命中 |
|---|---:|---:|---:|
| 节点类枚举到两条样例 | 5.008288 | 4.987991 | 96025 / 206618 |
| 五类动作共同枚举 400000 次 | 6.906477 | 6.879723 | 113029 / 216747 |

两部分分别新建空连接缓存，不是连续共享同一个已预热缓存；“未命中”会实际执行启用的连接规则。表中是**包含预排序的整个枚举观察耗时**，不是单独排序函数耗时，也不含候选完整评价。新增计算真实存在，不能把缓存查询说成零成本，或据此声称整体求解提速。正式流程的缓存预热程度与这里不同，实际质量和总耗时由阶段 3 对照。

`_01` / `_02` 的独立重复得到相同提案位次、出现计数、动作分配、停止原因和未变方案；`_03` 在动作名称修正后再次得到相同结果。计时不同，不要求运行元数据字节一致。五类提案分配与前一轮相同，只是提案顺序变化；不靠额外额度命中样例。

## 4. 回归、保护与交接

新增 13 项检查覆盖真实有向规则、首尾、全直接/全非直接、两组稳定及完整覆盖、重新定位、缓存复用、取消和时间截止。首次专项 67 项 / 0.75 秒，扩展相关 880 项 / 26.89 秒通过。

首次累计检查（不含服务）发现上一轮动作数字编号触发旧架构检查：内部常量 `3` 被判作基准数字；实际 1 失败 / 3549 通过 / 39.78 秒。此数字是动作分支编号，并非生产订单数据，但此前只跑相关测试，未发现该门禁冲突。本次用明确的五类动作名称和元组解包替代数字分支/下标，未修改架构检查、业务顺序或目标。修正后架构及相关专项 143 项 / 1.26 秒通过；机会 `_03` 也验证顺序未变。

完整累计命令：

```sh
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider \
  tests/architecture tests/api tests/app tests/core tests/service -q
```

共享树最终累计 **3909 项通过 / 51.06 秒，退出 0**；精确暂存树导出同范围复验结果在本次提交正文记录。共享残留检查仍只报告既知 8 个 V6 历史文件缺失；不重建残留、不改旧清单。SQLite / YAML 与原输入、原结果受保护且未改动。

下一步为阶段 3：保持原九级和相同预算，完整对照搜索结果。正式配置、数据库、前端、服务启停、部署、合并与推送均未操作；本阶段不决定正式采用，也不把原有欠重结果改写成质量验收通过。
