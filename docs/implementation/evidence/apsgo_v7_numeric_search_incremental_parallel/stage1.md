# 第一阶段：数据结构与原序候选验证

## 范围与身份

- 阶段 0：`80d1bc1`；1.1 数值目录：`0f91f9e`；1.2 描述/整数连接：`dde6609`。
- 集中验证在 `dde6609` 上补充动态虚拟/重复身份/回落用例及批量已知位置实际计数，不改变业务计算。
- 首次测量源为精确暂存树 `a583244b88c60feb3dedc93a7f3ae22d998a025f`，导出 `/private/tmp/apsgo-v7-numeric-stage13.ZAXvGe`。后续记录更新不改变这份测量源码。
- 本轮仅阶段 0～1。未实施资源/交期增量、按需评价、候选并行或新评分。

## 实现边界

| 内容 | 实际实现 | 保留的权威入口 |
|---|---|---|
| 数值目录 | 只读连续 NumPy 物理字段数组、存在/可投影标记、角色/来源期/原单/原型整数列，精确重量及原工时对象 | 不支持投影的合法未用字段仍保留精确值；不改变任何规则公式 |
| 当前方案 | 展平整数行、链偏移、节点/链位置、原单全部拆片位置；任务原始行固定，新增虚拟/拆片仅占当前私有动态行 | `SchedulePlan` 与唯一 `commit_accepted`；接受前准备新视图，提交后发布，失败/拒绝不提升 |
| 轻量候选 | 原整链/节点/连续块/链内移动元组绑定代次、逻辑序号、入口保护及实际链/节点，检查范围后还原 | 原动作函数构造完整候选并完整评价；不提前拒绝分数，不越过资源修复 |
| 连接查询 | 单份完整语义缓存使用整数有向键；短期数组仅批量读取已有决定，未知原游标查询 | 原规则对象、全部声明语义字段、真实/虚拟角色及谱系；不使用构造图缺边作拒绝 |
| 回收/拆单 | 新候选动态行不修改固定目录或当前方案 | 切链、虚拟回收、拆单及桥接生成仍走既有串行守卫及独立审计 |

数值视图不保存一份额外可提交状态，不复制 `ChainSummary`，不新建平方连接矩阵。第一阶段未减少完整评价次数，布局准备成本计入完整运行。

## 集中检查

| 检查 | 已取得结果 |
|---|---|
| 1.1 薄检查 | 共享与精确导出各 4 项通过 |
| 1.2 相关检查 | 共享与精确导出各 159 项通过 |
| 1.3 数值/描述补充 | 18 项通过；包含合法大数回落、只读、原单全部拆片、动态虚拟隔离、失效、非法/重复身份、真实有向连接、原直接/延后组和取消轮询 |
| 原版/新版有序生成 | 同一原完整方案，五个家族每轮取 256 项、保留游标连续两轮，共 2560 提案；序列摘要 `430b2e2543b66aa82d4480102d412063da472fd16d7fc695bd5860149cbbeca9` 相同，逻辑扣费均 0 |
| 共享累计 | 首次 4094 通过 / 1 失败，56.95 秒；批量统计口径修正后 4095 项通过 / 54.15 秒 |
| 精确导出累计 | 4095 项通过 / 55.34 秒；`clean_export` 残留检查、`compileall -q src` 通过；[机器摘要](stage1_checks.json) 包含 JUnit 文件摘要与实际源码变化 |
| 静态检查 | `git diff --check` 通过；环境无 Ruff，未安装依赖、未宣称 Ruff 通过；编译仅在干净导出执行 |

首轮 1.2 架构失败为误将三位置配方长度认作旧基线数字，只精确豁免指定模块/类/方法/完整表达式并补反例。累计唯一失败则是旧测试要求重复查询必增“逐边缓存命中”；现改为验证重复全部命中“批量已知位置数”，同时保留顺序相同、不新增缺失、不扣额度。不是删除正确性反例或修改排程门槛。

累计命令（均使用 Conda `aps_3.10.18`）：

```bash
PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -p no:cacheprovider \
  tests/architecture tests/api tests/app tests/core tests/service
```

有界提案材料在 `diagnostics/numeric_search_incremental_parallel/stage1_original_recipes.json`、`stage1_numeric_recipes.json`；来自 `_backlog_iterators`，原/新各自独立 `load_case`，用 `islice(stream, 256)`，两轮共享原五组游标，不进行候选执行。这是生成器顺序对照，不冒充 40 万次完整求解。

## 完整测量（同机、同绑定请求）

| 样本 | 源码 | 完整公共求解秒 | 进程 CPU 秒 | 状态 |
|---|---|---:|---:|---|
| 历史组合 40 万次 | 历史冻结运行 | 1214.566601 | 1210.557494 | 仅历史参考，不用于本轮精确加速比 |
| 本轮未优化对照 | `7964d9d` 干净导出，与 `400b8ce` 生产相同 | 1126.143692 | 1122.865433 | 完成；三份阶段方案/评价/轨迹、计数/上界及逐单精确日期与历史一致，双审计通过 |
| 第一阶段数值版 | 上述精确暂存树 | 1123.220659 | 1120.566784 | 完成；与现场原版逐项等价，双审计通过 |
| 第一阶段重复 | 同上 | 1152.119645 | 1148.513229 | 完成；与首次及原版业务等价，双审计通过 |

未优化对照阶段：首轮 146.005332 秒、拆单与重放 237.898714 秒、后置精修 738.031979 秒。控制运行期间未同时执行另一求解或累计回归；有编辑和少量短检查活动，不将单次同机对照称正式五样本性能验收。

首个数值版阶段：首轮 147.931088 秒、拆单与重放 227.308459 秒、后置精修 743.677974 秒。总耗时仅少 2.923033 秒（约 0.26%），目前应视为基本持平，不宣称明显加速。

重复样本阶段：首轮 155.820347 秒、拆单与重放 237.943105 秒、后置精修 753.891133 秒。两次新版平均 1137.670152 秒，比本轮单次原版多约 1.02%；**没有证明稳定提速，也远未达到一分钟**。这是第一阶段数据基础的真实结果，不靠改候选额度或放宽目标制造性能改善。全部计时、源码/产物摘要和三个对照结果见 [stage1_measurements.json](stage1_measurements.json)。

首个数值版实际：400000 候选、172312 完整评价、804 接受、2 次拆单（同期间/未来借用归还各 1）。零禁止/零欠重，26 链、820 吨虚拟、14954 毫米宽差；与原首轮、精修入口和最终方案/完整评价/轨迹、生成上界及全部原单精确日期一致。原逐边缓存命中 1314904→495952，缺失/条目均保持 203843；这是已知边转入批量读取，并非业务可连接性变少。

布局准备累计 0.500419 秒，805 次视图准备（初次＋804 次接受），最大目录 576 行；语义目录 812 行。轻量配方还原 244283 次，完整入口内方案构造 180032 次；批量窥看 869438 个位置，其中 784918 个已知。按真实工作量分列，不据此宣称其他热点已经消除。

重复样本上述工作量全部一致，布局准备 0.535594 秒；有序方案、完整评价、全部接受轨迹、虚拟序号、两种拆单次数、531 个原单精确完成值及位置均一致，三个对照均无首个差异。两个数值样本与当前源码 SHA-256 均为 `153e50f5621a930d8f27d36219fb8b2ee5b3ecc1f70287be1280f4d35fff8170`。

所有新大产物位于 `diagnostics/numeric_search_incremental_parallel/`，从既有 `prepared_request.json` 回读，不读取活动数据库。完整命令沿计划第 7.1 节 `tools/profile_solver_search.py --scope full`，三个独立目录为 `stage0_control_full_01`、`stage1_full_01`、`stage1_repeat_01`。输出不可覆盖。

## 统计口径与停止边界

- 原逻辑候选、实际完整评价、接受计数保持原定义；第一阶段预期与原 400000 / 172312 / 804 完全一致，实际运行后核验。
- `numeric_layout` 独立记录：视图准备次数/耗时、最大目录行数、轻量配方还原次数、完整候选入口内方案构造次数、语义行数、批量窥看位置数/已知位置数。
- “完整入口构造次数”只统计 `try_complete_candidate` 内新方案及稳定分组新建对象，不冒称所有模块内全部 Python 对象分配次数；未测全进程分配总量或峰值 RSS。
- 批量窥看是实际读取，可能尚未消费到对应候选；与逐边 `hit_count` 分列，不伪装逻辑候选或虚构命中。
- 第一阶段实现、集中验证及完整重复对照已完成；按用户要求停在 1.3，不进入第二阶段，不宣称达到一分钟或 Windows 实包性能目标。

## 提交检查与结论

最终收口只补充本专项设计/计划/AGENTS 和真实证据，不再改变已测生产、测试、工具、配置。精确暂存树再次导出，逐路径与 `a583244` 对照源码/测试/工具相同，复用已执行的 4095 项导出结果；不因仅写入结果再机械运行第三次累计回归。另核验七个保护摘要、UTF-8/链接、`git diff --check` 和原干净导出残留检查，结果随收口提交正文。

**功能等价与重复性通过；稳定提速未证实；本轮授权工作完成。** 后续缓存、按需完整评价和并行仍未实现、未测试，需要下一轮明确指令。
