# 回退版本：40 万次 / 300 秒对照

2026-09-12，macOS，Conda `aps_3.10.18`，代码 `4162a88`；源码/测试/工具与 11 单结果对应的 `b1ab8af` 相同。仅提高候选上限，仍为欠重第 3/4 位、交期第 5/6 位；不是已经撤回的交期优先方案。

## 执行范围

用户授权候选上限 400000、搜索时限 300 秒。保持既有 10 秒收尾预留，即总策略 310 秒。直接复用 `diagnostics/urgent_order_search/input_budget400000.json`，经 `load_request()` 回读确认仅候选上限变化；最终准备请求与原 `candidate_01` 再逐字段比较，除了 `policy.candidate_check_limit`，所有业务字段相同。

```sh
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python tools/run_delivery_comparison.py \
  --prepared-request diagnostics/urgent_order_search/input_budget400000.json \
  --timing-source diagnostics/delivery_objective/timing_source.json \
  --start 2026-06-01T00:00:00+08:00 --virtual-speed 100 \
  --variant delivery --output-dir diagnostics/urgent_order_search/restored_delivery_400000_01
```

实际退出 0，状态 `publishable_with_allowed_deviation`，双审计通过。源码、工具、默认配置、正式规则库及历史结果未修改。本轮无源码变更，不重复上一轮 3886 项回归或构建。

## 实际结果

| 指标 | 11 单基线：20 万上限 | 回退版本：40 万上限 |
|---|---:|---:|
| 本月晚交单数 / 吨 | 11 / 288.89 | 11 / 288.89 |
| 累计等待与延期 / 吨·小时 | 1294569.295364 | 1294569.295364 |
| 欠重链数 / 缺口吨 | 1 / 41.01 | 1 / 41.01 |
| 禁止违规数 | 0 | 0 |
| 虚拟吨 / 链数 / 链间宽差 | 540 / 23 / 12151 | 540 / 23 / 12151 |
| 实际候选检查 | 192021 | **192551** |
| 完整评价 / 接受 | 67899 / 369 | 68269 / 369 |
| 公共入口经过时间 / 秒 | 301.109626 | 301.167615 |
| 进程 CPU / 秒 | 299.605092 | 300.710441 |
| 停止原因 | 搜索时间上限 | 搜索时间上限 |
| 核心与应用审计 | 均通过 | 均通过 |

**最终有序方案、完整评价、九级质量、全部采纳轨迹逐值相同。**531 原订单完成日期、全节点时间、交期摘要也相同；完整报告身份随请求策略改变，不宣称整个文件逐字相同。相等项与两组文件哈希见 [机器可读对照](restored_budget400000_comparison.json)。真实源订单及 29333.91 吨守恒由独立审计确认。

上月 96 单/5386.03 吨最后完成仍为 `2026-06-26 07:21:25.873870+08:00`，最大实际晚交 607.357187 小时；生产结束仍为 `2026-06-27 17:12:53.827950+08:00`。本月交期 6 月 5/10/15 日仍分别晚交 2/8/1 单，月底交期无晚交。不是只比较晚交数量，而是实际全部逐单日期相同。

## 阶段与原因

| 阶段 | 原基线秒数 | 本轮秒数 |
|---|---:|---:|
| 首轮局部搜索 | 116.248870 | 115.333057 |
| 拆单及一次回放 | 82.168636 | 81.151830 |
| 后置定向精修 | 98.898308 | 100.841274 |

两组均完成同样的 2 次拆单（同期间拆单 1 次、未来借入归还拆单 1 次），精修前均为 164646 次候选、49844 次完整评价、341 次接受及相同方案。随后同样接受 28 次链内前移，没有新的跨链或片段接受。本轮多检查 530 次、完整评价多 370 次，但没有产生额外接受。

**本次实际受时间而非候选上限限制。**原基线没有用完 20 万次，本轮同样在不到 20 万次时就触及 300 秒，40 万额度没有发挥作用。两次实际计数略有差异与墙钟截断相容；不能把微小计时差或同一最终方案解释为更多预算永远无效，也不能称其他动作已遍历完。

仍有 `initial-000030` 链重 658.99 吨、低于 700 吨下限，缺口 41.01 吨。**正式零欠重门仍未通过，整体 BEST_EFFORT**；单次不代替历史性能门或确定性重复验收。

## 记录与保护

新目录保留 `input_binding.json`、`prepared_request.json`、`execution.log`、`measurement.json` 和 `delivery_report.json`；本报告仅读结果，不改排程。冻结文件、规则库和 YAML 哈希一致，生产/测试/工具与 `b1ab8af` 差异为空。仅本计划、AGENTS 和本专项证据提交，未部署、合并、推送或再增加预算。后续是否提高时间或调整阶段，需要用户另行决定。
