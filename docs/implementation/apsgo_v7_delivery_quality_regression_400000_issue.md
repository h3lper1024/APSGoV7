# APSGo V7：40 万次求解交期质量退步问题记录

## 1. 状态与范围

- 登记日期：2026-09-16。
- 状态：**现象已确认，根因待定位，尚未修复、尚未关闭**。
- 登记基线：`codex/delivery-objective-optimization@fcce4e6`；macOS arm64。
- 用户指出：当前结果的上月欠交最晚完成日期、本月晚交情况，均比此前采用的 40 万次组合结果差，要求另建文档记录。
- 本轮只回读已有产物并登记，不修改算法、目标优先级、候选额度、正式配置或数据库，不重新运行求解，不同步其他设计/实施文档。

**结论：当前数值路径存在相对历史 40 万次业务基线的交期质量退步。耗时、虚拟材和链间宽差的改善不能代替交期质量达标，也不能用本轮数值内核改造前后等价来关闭这一问题。**

## 2. 两份结果与可比范围

| 对照项 | 历史 40 万次结果（用户截图） | 当前 40 万次结果 |
|---|---|---|
| 方案 | 关键交期调度＋连接虚拟材回收组合 | NumPy 数值搜索，阶段 7.4 编译评价内核 |
| 代码定位 | 历史证据登记生产提交 `66cf04e` | 阶段 7.4 提交 `fcce4e6`；运行于该提交前同一生产源码工作树 |
| 结果目录 | `diagnostics/critical_delivery_search_and_bridge_reclamation/combined_01/` | `diagnostics/numpy_numeric_search_and_calculation/stage7_native_400k_01/run_02/` |
| 结果指纹 | `33faa1839cf2c10e01fe06fbbbf5ccf096da2e844005436819366ece455c292e` | `f1f7678e13998a8945a5c887aa91388e6276a0fbcc1e21989e5666a8b881072a` |
| 数值标准 | 原浮点/Decimal 路径，交期评分为秒级半偶舍入 | 精确定点整数、毫秒时钟，交期评分为秒级四舍五入 |

两份实际准备请求已逐字段回读比较：差异只在数值标准、九项评分的数值投影声明以及由此变化的规则指纹；原订单、原型、规则业务参数、目标顺序、候选额度、种子、输入工时和起排条件一致。两份问题指纹均为 `e391e275a2285adebbc2ca9d7a7f95d9a6d27def3cfb6fd795562b8ec4b7f993`。

共同业务条件：531 个原订单，其中旧欠 96 单、5386.03 吨；起排为 `2026-06-01T00:00:00+08:00`，虚拟材速度 100 米/分钟，种子 590531，400000 次候选检查，9999 秒搜索＋10 秒收尾。两次均以候选额度用完停止。

因此可以比较业务结果，但**不是完全相同数值标准下的表示等价试验**。已确认的数值标准变化不代表用户接受交期退步；不能未经定位就把以下差距归因于舍入，也不能笼统归因于 NumPy/Numba。

## 3. 已确认的结果差距

时间均为 2026 年、中国标准时间；表内显示值作适度小数截取，原始精度保留在诊断产物。

| 指标 | 历史 40 万次 | 当前 40 万次 | 变化 |
|---|---:|---:|---|
| 上月欠交全部完成日期 | 6 月 15 日 03:35:31.512811 | 6 月 23 日 13:13:24.885 | **推迟 8 天 9 小时 37 分 53.372189 秒** |
| 本月新增晚交原单数 | 14 单 | 24 单 | **增加 10 单** |
| 本月新增晚交原单重量 | 685.33 吨 | 1182.86 吨 | **增加 497.53 吨** |
| 累计等待与延期负担 | 636816.4265 吨·小时 | 1181514.0349 吨·小时 | 恶化；两侧数值评分口径差异另行记录，不作同标准差分结论 |
| 禁止违规数 / 欠重链数 | 0 / 0 | 0 / 0 | 均满足当前约束，不代表交期同样达标 |
| 虚拟材重量 | 820 吨 | 360 吨 | 减少 460 吨，不能抵消前两项交期退步 |
| 小辊期数量 | 26 条 | 22 条 | 减少 4 条 |
| 链间宽差总和 | 14954 毫米 | 11606 毫米 | 减少 3348 毫米 |
| 完整经过时间 | 1214.566601 秒，约 20.24 分钟 | 同进程第二次 138.965682 秒，约 2.32 分钟；首次 154.426290 秒 | 变快，但不是保持历史交期质量的无损提速证明 |
| 核心/应用审计 | 通过 | 通过 | 审计通过证明规则与结果一致性，不证明达到历史业务质量 |

截图中的旧欠按重量加权平均等待为 92.93 小时，历史证据为 92.925353 小时；本轮未重算当前同名统计，不能把当前第 6 项“累计等待与延期负担”当作该平均值。

## 4. 修正“结果保持不变”的比较范围

阶段 7 验证比较的是：**已完成数值迁移的 Python 整数评价路径 → 相同数值标准的 Numba 编译评价内核**。

该对照的旧版来自 `a03fdf5` 的生产源码，完整耗时 233.390746 秒；新版为 154.426290/138.965682 秒。两侧订单结果、九项评分、接受轨迹、计数和双审计确实一致，但其旧欠清空和本月晚交已经分别是 6 月 23 日、24 单/1182.86 吨。

**该结论不能扩展成“当前结果与用户之前认可的 6 月 15 日、14 单/685.33 吨的历史结果一致”。**交期退步在阶段 7 编译内核替换之前已经存在；本轮仅确认这一时间边界，没有定位首次引入退步的提交或算法原因。

现有产物还显示两侧搜索路径不同：初始链数 31→30，完整候选评价 172312→78775，接受次数 804→486。它们是后续排查线索，不是根因结论；同为 40 万次也不能据此认为访问了相同候选。

## 5. 后续定位与关闭要求（待执行，不构成本轮实施）

1. 固定两份原始输入、结果和已有代码身份，按初始构造、首轮搜索、拆分/唯一重放、后置结构精修逐阶段寻找首个分歧。
2. 分开核查已确认的精度/舍入变化，以及候选集合、顺序、额度消费、评分比较或资源动作的非预期变化；不能用“数值化允许差异”跳过解释。
3. 对发生退步的旧欠订单及由按期转为晚交的本月订单，列出位置、所属链、完成日期和造成变化的接受动作。
4. 修复后用同一输入和 40 万次额度复测，独立报告交期、违规/欠重、来源与拆分守恒、虚拟材、链数、宽差及耗时。历史 40 万次结果仍是业务质量对照，不能只对比已经退步的中间数值版本。
5. 未恢复历史交期水平、未解释根因，或业务取舍未经用户确认前，保持本问题未关闭；不能以单元测试、同标准内核等价或耗时下降替代关闭条件。

## 6. 证据与本次执行记录

- 历史结果：[组合方案阶段 4 对照](evidence/apsgo_v7_critical_delivery_search_and_bridge_reclamation/stage4.md)、[100 万与 40 万次对照](evidence/apsgo_v7_critical_delivery_search_and_bridge_reclamation/budget1000000.md)。截图对应后者的 40 万次列，不是 100 万次列。
- 历史原始交期：[combined_01/delivery_report.json](../../diagnostics/critical_delivery_search_and_bridge_reclamation/combined_01/delivery_report.json)。
- 当前原始交期：[stage7_native_400k_01/run_02/delivery_report.json](../../diagnostics/numpy_numeric_search_and_calculation/stage7_native_400k_01/run_02/delivery_report.json)。
- 阶段 7 的同标准等价范围：[阶段 7 证据](evidence/apsgo_v7_numpy_numeric_search_and_calculation/stage7.md)。
- 2026-09-16：Conda `aps_3.10.18` 只读解析两份准备请求、测量与交期报告，逐字段核对输入差异并计算日期、重量差值；未运行新排程、未修改原始结果。仅新增本问题记录，静态检查和独立提交身份见 Git 历史。
