# APSGo V7 求解日志与诊断数据落盘实施计划

## 1. 当前状态与目标

| 项目 | 内容 |
|---|---|
| 版本 / 日期 | v0.2 / 2026-09-08 |
| 状态 | 用户已授权持续实施；阶段 1 源码完成，最终提交验证见提交记录；阶段 2～4 待实施 |
| 仓库 / 分支 | `/Users/miles/dev/dev-py/APSGOV7` / `codex/rule-setting-api-integration` |
| 编写前提交 | `6cd7a46825afe18696930abcc8b460f9935d9af5` |
| 当前核验环境 | macOS ARM64，Darwin 27.0.0；Conda `aps_3.10.18`，Python 3.10.18 |
| 当前工作树 | `.gitignore` 有用户增加的末尾空行；本次文档提交保留且不纳入 |
| 本次交付 | 按阶段实施日志、诊断落盘与验证；正式数据库及 Windows 现场不自动修改 |

用户在 Windows 上收到 `complete_not_publishable`，现有日志只能看出求解结束且不能发布，不能直接定位失败规则和订单。本专项要让一次运行留下可定位原因的日志、输入和最终候选，支持人工检查。

现有 Windows 日志说明规则从活动版本 5 保存为版本 6，随后求解不可发布；**这些信息不能证明具体根因**。本专项先补齐诊断能力，复测后按真实明细判断，不预设为此前的连续虚拟材或窄钢问题。

沿用现有[月计划接入设计](../design/apsgo_v7_month_scheduling_and_grade_preparation_design.md)与[Windows 发布实施计划](apsgo_v7_windows_exe_release_packaging_implementation_plan.md)。本专项只增加可观测数据，不修改规则、搜索顺序、七级评分、接口字段、300 秒搜索预算或 200000 次候选上限；不修改 C#、V3 或 V6 仓库。

## 2. 当前代码缺口与复用位置

以下定位以编写前提交为准，实施时按函数名再次核对。

| 位置 | 当前事实 | 本次处理 |
|---|---|---|
| [`app.py`](../../src/apsgo_v7_service/app.py)，`_log_month_solve`、`run_server` | 求解日志仅含请求、版本、结果码和 HTTP 状态；Uvicorn 默认格式无日期时间 | 统一格式，补开始、结束、耗时与失败摘要 |
| [`configuration.py`](../../src/apsgo_v7_service/configuration.py) | 顶层配置键严格匹配 | 增加可选诊断块，兼容已有 YAML |
| [`scheduling.py`](../../src/apsgo_v7_service/scheduling.py)，`solve_gqga4_scheduling_task` | 绑定后具有完整规则、虚拟原型、补齐后的订单、策略与身份 | 在已有绑定位置保存输入，不重新查库或再次绑定 |
| [`month_scheduling.py`](../../src/apsgo_v7_service/month_scheduling.py)，`month_solve_response_data` | 无正式发布结果时，响应中的质量、违规和结果行均为空 | HTTP 响应保持原契约；独立诊断读取内存中的候选 |
| [`api/result.py`](../../src/apsgo_scheduler/api/result.py)，`SchedulingResult` | 保留 `diagnostic_candidate`、问题、双审计、计数及阶段耗时 | 直接用于诊断 JSON，不重新执行算法或规则评价 |
| [`core/solver.py`](../../src/apsgo_scheduler/core/solver.py)，`solve` | 已测量构图、初始构造、搜索、拆单、宽差优化及审计耗时 | 各阶段及时打印开始/结束摘要，复用已有计数和评价 |
| [`core/neighborhoods.py`](../../src/apsgo_scheduler/core/neighborhoods.py)，`try_complete_candidate` | 已有采纳动作记录，只有正式采纳后才修改方案 | 成功提交方案后打印该条采纳记录，拒绝候选不打印 |

`solve_request(request, cancellation)` 的两参数入口保持。核心只使用标准库日志，诊断文件由服务层管理；不建立第二条求解入口、事件总线或新的审计模型。

## 3. 日志与文件口径

### 3.1 每行时间及耗时

- **每一行控制台与文件日志**均打印本机日期、时间、毫秒与时区偏移，包含 Uvicorn 启停、规则 GET/POST、求解、警告及异常堆栈的各物理行。换行消息也要补时间前缀。
- 日志时间用于定位发生时刻；耗时使用单调计时器计算，单位为秒，避免调整系统时间导致负耗时。
- 配置读取发生在 Uvicorn 启动之前，因此先建立带时间的基础控制台日志；配置加载失败也必须能定位。加载成功后再启用文件处理器。
- 求解请求从进入路由开始计时。请求相关日志携带 `elapsed_seconds`（从该请求开始的累计耗时）；阶段结束行另带 `stage_seconds`（本阶段耗时）。服务启停等无请求日志不伪造请求耗时。
- 最终总耗时统计到本次响应内容与诊断收尾完成，不声称包含网络发送或 C# 回写耗时。客户端断开时单独记录断开及后台工作线程实际结束。
- 同一格式用于控制台和文件；日志内容中的外部换行、控制字符应转义，防止伪装成其他记录。异常堆栈保留定位信息。

示例仅说明字段与格式，数值不是实测：

```text
2026-09-08 15:26:31.428+0800 INFO month_solve_started request_id=... order_count=531 elapsed_seconds=0.002
2026-09-08 15:26:33.561+0800 INFO solver_stage_finished request_id=... stage=initial_solution stage_name=初始方案构造 stage_seconds=0.321 elapsed_seconds=2.135
2026-09-08 15:26:34.007+0800 INFO solver_move_accepted request_id=... sequence=1 action_name=... quality_before=... quality_after=... elapsed_seconds=2.581
2026-09-08 15:30:11.608+0800 WARNING month_solve_finished request_id=... status=complete_not_publishable publishable=false stop_reason=... elapsed_seconds=220.182 diagnostic_directory=...
```

### 3.2 打印哪些内容

| 时点 | 日志内容 |
|---|---|
| 服务启动 | 监听地址、端口、诊断是否开启、成功启用的输出目录；可用的构建/版本身份 |
| 规则查询或保存 | 保留现有操作、活动版本、指纹和结果码；使用统一时间格式 |
| 求解开始 / 输入绑定完成 | 请求标识、订单/计划期数量、预期与实际规则版本、规则/字典/最终请求指纹、种子和本次预算、补齐统计 |
| 算法阶段开始 / 结束 | 英文阶段键及中文名称；阶段完成、截断或异常状态；已有链数、质量值、检查及采纳计数、阶段与累计耗时 |
| 结构调整被采纳 | 序号、动作名称、涉及链/来源订单、调整前后质量值、当前候选检查数、累计耗时；不能在候选提交前记录为已采纳 |
| 求解结束 | 求解状态、停止原因、可发布性、双审计结果/失败码、带目标名称的质量值、违规和问题计数、候选/完整评价/采纳/拆单计数、总耗时、诊断目录 |
| 不可发布或异常 | 逐条输出最终禁止违规的规则、原因码、对象和中文说明，以及其他失败问题；相同诊断避免重复打印；保留异常类型和堆栈 |

初始方案构造完成时，记录已有初始评价和链数。后续阶段跳过、提前退出或超时，不得打印成正常完成。阶段与采纳日志在运行中输出，不等求解结束后才批量打印。

日志只记录上述摘要和失败定位，不打印整份订单/规则 JSON，也不逐条打印最多 200000 次候选检查。阶段内部受控拆单后的原搜索重放，标明所属阶段及重放事实，不能重复累计外层耗时。

`core_solve` 总耗时已包含核心细阶段，阶段字典不能简单求和作为请求总耗时；累计时间始终从请求入口独立测量。`complete_not_publishable` 也可能源于收尾超时或其他审计失败，不能统一记录为“违反规则”。

### 3.3 每次求解的文件

启用落盘后，目录结构为 `diagnostics/runs/<request_id>/<运行时间_唯一后缀>/`。同一请求标识再次提交时建立新目录，不覆盖或混入上一轮。目录名只使用服务端规范化标识和生成的后缀，不使用订单号或外部任意路径。

| 文件 | 内容与写入时点 |
|---|---|
| `request.json` | 请求解析通过且获得求解执行权后，保存实际收到的 JSON 正文，保留原订单相对顺序 |
| `prepared_request.json` | 一次绑定完成后立即保存：补齐后的订单、权威期序、规则和评分声明、虚拟原型、策略、准备报告、活动版本与各项指纹；这是实际交给算法的输入 |
| `solve.log` | 本次请求的阶段与采纳日志，使用同一时间格式实时追加；包含初始构造与后续调整的摘要，便于单独发回排查 |
| `response.json` | 实际生成的 HTTP 响应正文；不可发布时仍保持公开响应的空 `rows` 语义。没有生成响应时不伪造该文件 |
| `diagnostic_summary.json` | 状态、停止原因、版本/指纹、策略身份、问题、双审计、运行计数与耗时；存在候选时还保存候选完整计划、质量、各链评价和全部违规，明确其是否获准发布 |
| `candidate_rows.csv` | 存在完整候选时生成，按原方案链序及链内顺序展开，供人工查看；标明“诊断候选”及实际发布状态 |

“中间数据”首版固定为绑定后的输入、初始评价摘要、各阶段摘要和采纳记录；不保存每个拒绝候选或每次调整后的全量方案副本。初始评价、阶段摘要复用当时已算出的值，不为落盘增加完整评价。

数据细节：

- 优先从 `diagnostic_candidate.plan/search_evaluation` 取候选，正式发布评价单独标注。候选搜索评价不能冒充最终无缓存审计结果；没有候选时写明 `candidate=null`，不生成虚假的 CSV 行。
- 非发布 `SchedulingResult` 没有完整的独立审计评价；此时保存搜索评价、审计报告和审计产生的 `issues`，不能承诺从结果对象还原独立审计的全量质量与违规列表。
- CSV 至少包含计划期、小辊期标识及序号、节点序号、订单/节点标识、重量、宽厚、温区、牌号、材料角色与拆单来源；使用既有行转换和谱系字段。完整违规以 JSON 为准，不依赖面向正式发布的告警投影才能生成诊断行。
- JSON 保留数值精度，复用 `dumps_exact_json`；不先转为浮点数。代码、文档、日志与 JSON 使用 UTF-8 无 BOM。CSV 供 Excel 通过“数据导入”按 UTF-8 打开，订单号指定文本；文本字段防止被解释为公式，原值仍在 JSON 保留。
- `prepared_request.json` 保存本次用到的规则、原型及补齐结果，字典保留指纹和准备报告即可；不为本专项再导出整个 SQLite 库或第二份字典。
- 原始请求、准备输入和阶段日志逐步写入；最终文件逐个用同目录临时文件写完再替换。整个运行目录不声称具备跨文件事务，最后的摘要应记录已写文件及写入失败；进程被强杀时允许保留不完整记录。
- 单文件写入、序列化或 CSV 投影失败只影响诊断，明确记录 `diagnostic_write_failed` 和文件名，其余能写的文件继续保存；不改变求解状态、候选采纳、HTTP 错误语义或发布判定。日志处理器异常也不能传播回搜索。
- 客户端断开后，后台工作线程仍按原取消机制结束；本次诊断记录保持到实际退出，再关闭文件。解析前错误和拒绝执行的 429 请求至少有带时间的错误日志，不为任意非法请求保存全量正文。
- 当前成功响应在求解工作线程返回后序列化；实施时将响应构造和本次诊断收尾纳入该工作线程的生命周期，完成后再向 HTTP 层交付响应内容。响应序列化失败继续使用原 HTTP 错误映射，不能作为“诊断写盘失败”被吞掉；仅诊断文件的序列化和写入错误允许隔离。

## 4. 配置、发布及最小改动范围

计划在现有 YAML 新增一个可选块：

```yaml
diagnostics:
  enabled: true
  output_directory: ../diagnostics
```

新模板显式启用。旧 YAML 没有该块时，继续输出带时间的控制台日志，文件落盘关闭；`enabled: false` 同样关闭文件落盘，不取消控制台诊断摘要。块存在时严格校验布尔值、非空目录和未知键。

目录相对 **YAML 所在目录** 解析，Windows 发布包中对应 `<发布根目录>\diagnostics`；不依赖启动盘符、当前工作目录或 PyInstaller 内部目录。已有现场 YAML 不被升级脚本覆盖，升级时由部署人员补入配置块。

共享文件 `diagnostics/service.log` 采用标准库 `RotatingFileHandler`（按文件大小轮换日志），固定 10 MiB、保留 3 个历史文件。逐请求目录先人工归档清理，不增加定时清理或保留策略配置。显式启用但启动时无法创建目录，应先用带时间的控制台错误清楚报告并停止启动；运行途中写盘失败按第 3.3 节隔离。

| 改动位置 | 计划修改 |
|---|---|
| `src/apsgo_v7_service/configuration.py`、`config/apsgo_v7_service.yaml` | 可选诊断配置及配置文件相对目录 |
| 新增 `src/apsgo_v7_service/diagnostics.py` | 集中实现标准日志配置、单次运行文件与已有值对象的输出；普通函数和必要标准处理器即可 |
| `src/apsgo_v7_service/app.py`、`scheduling.py` | 请求计时、绑定输入、结果及失败分支接线；后台线程退出前完成其诊断收尾 |
| `src/apsgo_v7_service/month_scheduling.py` | 提取可共用的只读行转换，支持未获发布签发的候选；公开响应原样保持 |
| `src/apsgo_scheduler/core/solver.py`、`neighborhoods.py` | 少量阶段、异常与正式采纳日志；只依赖标准库，不在核心直接写文件 |
| `.gitignore`、`release/README.md`、必要的 `README.md` 使用说明 | 添加根 `/diagnostics/` 忽略、现场启用方法和人工定位步骤；保留用户已有修改 |
| `tests/service`、相关 `tests/core` 和 `tests/release` | 复用现有夹具，覆盖不可发布、时间、文件、并发取消与打包路径 |

Uvicorn 和算法日志应共用同一组处理器配置，明确路由算法包日志，避免只配置 `uvicorn` 而丢失核心日志。通过 `uvicorn.run(log_config=...)` 接入；不在启动前临时添加随后会被覆盖的文件处理器。单次请求信息使用标准库上下文机制关联到实际工作线程，完成后释放，不以全局“当前请求”变量串接。

构建脚本已复制整个 YAML，无需新增依赖或专门的数据收集步骤。日志和运行目录不进入干净发布包；在发布临时副本启动后才生成，包级校验仍区分原始发布文件与现场运行产物。

## 5. 实施步骤与提交

按表顺序实施，每项验证后独立提交，提交信息为中文并以 `#feat` 或 `#fix` 开头。阶段名称应与编号一并说明。

| 阶段 | 实施内容 | 完成标准 | 建议提交 | 状态 |
|---:|---|---|---|---|
| 0：形成实施计划 | 本文、当前代码核对和 `AGENTS.md` 导航 | 内容、路径和边界复核完成；只有两份文档进入提交 | `#feat 增加V7求解诊断实施计划` | 本次文档交付 |
| 1：统一时间日志与配置 | 新旧 YAML、目录解析、控制台格式、共享轮转日志、请求开始/结束摘要 | 启停、规则、求解、异常各行都有时间；不可发布日志直接显示真实失败原因；关闭与旧配置兼容 | `#feat 增加V7带时间的求解诊断日志` | 源码完成，提交验证见记录 |
| 2：保存输入与最终候选 | 运行目录、原始/绑定输入、最终响应、诊断摘要、候选 CSV、单次日志 | 成功及不可发布均有完整证据；重复请求不覆盖；断开和写盘失败不影响既有求解语义 | `#feat 增加V7求解输入与候选数据落盘` | 待实施 |
| 3：补齐算法过程日志 | 构图到审计各阶段，以及唯一候选接受位置的采纳记录 | 运行中可看到进度与前后质量；采纳数与原轨迹计数一致；不增加候选检查和完整评价 | `#feat 增加V7算法阶段与采纳过程日志` | 待实施 |
| 4：复测与使用说明收口 | 固定工作量开关对照、真实 GQGA4、Windows 新旧配置与人工审核 | 自动化证据完成；Windows 实包能生成可读日志及候选；实际失败可由规则/对象定位，尚未具备现场输入时明确保留待查 | `#feat 完成V7求解诊断验证与使用说明` | 待实施 |

阶段 2、3 的 `diagnostics.py` 共用一套请求上下文和处理器，不分别建立采集系统。阶段 4 若缺 Windows 环境，先完成源码侧验收并记录待验证项目，不以 macOS 结果宣称 Windows 已通过。

## 6. 验证、保护与回退

最小验证清单：

1. **配置与时间**：旧 YAML、启用/关闭、非法配置、中文及空格目录、启动目录变化；毫秒前缀覆盖普通行及堆栈每行；时间戳与累计耗时区分，日志不重复、文件实际发生轮转。
2. **真实结果对象**：复用现有测试构造成功、禁止违规不可发布、输入失败和取消；逐项核对候选质量/违规/链及订单、双审计、计数和 CSV 重量顺序。搜索评价和审计不一致时分别保留诊断，不能写成已发布。
3. **运行生命周期**：相同请求标识两次执行、规则查询与求解并行、客户端断开后线程收尾、目录不可写/写盘失败/序列化失败；日志与文件不串请求，错误不覆盖求解返回值。取消后不存在的响应或阶段不伪造。
4. **算法行为**：在固定输入/种子/候选工作量、时间预算不触发的测试中，对比关闭与开启诊断的状态、方案、质量、来源守恒、稳定指纹、采纳轨迹与计数；时间字段可不同。业务返回除原有观测耗时外应一致。
5. **真实耗时观察**：相同 531 单、相同规则快照和当前 300 秒/200000 次策略，各运行一轮关闭及开启诊断，报告总耗时、实际检查数、停止原因、日志行数和文件大小。若时间截断导致探索量不同，分开解释，不声称逐字确定性或正式 20 对性能验收；不擅自放宽既有门槛。
6. **Windows 发布验证**：重新构建，在临时发布副本验证新配置开启、旧配置缺块、非发布目录启动、不可发布明细和 CSV 的 Excel 导入。沿用现有规则保存/重启检查，核对业务未回归；没有 Windows 实测证据则保持该项待验。

自动化测试及运行统一使用 Conda `aps_3.10.18`。专项测试放入 `tests/service/test_solver_diagnostics.py`，其余扩展已有测试；完整代码提交的累计命令为：

```shell
conda run -n aps_3.10.18 env PYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider tests/architecture tests/api tests/app tests/core tests/service tests/release
```

每阶段记录输入提交、实际命令、退出码、测试数量、耗时与首个差异。精确文件白名单暂存后，按项目约定用 `git write-tree`、`git archive` 导出，在导出目录检查残留和执行对应回归；不得为 V7 补回 V6 历史残留。文档阶段只验证文档与归属，不能记为诊断功能测试通过。

测试用规则库临时副本及独立诊断目录，不修改正式数据库或已有历史结果。首版不做数据库变更，也不自动启动/停止用户的现场服务。诊断文件含订单业务数据，仅保存到本地指定目录，不自动上传或随发布包分发。

快速关闭文件输出：将现场 `diagnostics.enabled` 改为 `false` 后重启，已有证据保留。若回退到本专项之前的程序，先从现场 YAML 移除新增 `diagnostics` 块（旧加载器不接受该键），再按原发布说明恢复整套程序；保留运行数据库、原配置备份及诊断目录。

人工排查顺序：通过最终日志找到本次目录 → 读 `diagnostic_summary.json` 的停止原因/审计失败/违规对象 → 用 `candidate_rows.csv` 定位链和订单 → 对照 `prepared_request.json` 的真实规则参数、软硬钢拼接结果与预算 → 查看 `solve.log` 的初始状态和采纳过程。CSV 是诊断资料，不得直接当作 C# 可回写的正式结果。

## 7. 执行记录

| 日期 | 阶段 | 实际结果 | 下一步 |
|---|---|---|---|
| 2026-09-08 | 0：形成实施计划 | 已核对当前分支、提交、Python 版本、日志入口、候选/响应边界及 YAML/发布路径；编写前提交的干净导出残留检查为 `pass/clean_export`。本次仅文档，代码与 Windows 诊断功能均未实施 | 按用户后续指令进入阶段 1：统一时间日志与配置 |
| 2026-09-08 | 1：统一时间日志与配置 | 实施前 `dfc8521`；可选 YAML、每物理行毫秒时间、日志轮转、实际不可发布原因、入口异常已接线。聚焦 129 项通过（2.35 秒，退出 0），最终暂存树同范围及累计结果见本阶段提交；项目环境未安装可选 Ruff，未新增依赖。实际单个超重订单在输入阶段被拒绝，因此不可发布测试使用 600 吨 IF 窄钢和零搜索预算，核验真实最终审计失败；未修改业务门槛 | 阶段 2：保存输入与最终候选 |
