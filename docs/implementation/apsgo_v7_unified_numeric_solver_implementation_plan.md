# APSGo V7 统一数值计算层实施计划

## 1. 当前入口与范围

| 项目 | 内容 |
|---|---|
| 版本 / 日期 | v0.22 / 2026-09-17 |
| 设计依据 | [统一数值计算层设计 v0.1](../design/apsgo_v7_unified_numeric_solver_design.md)；本文“设计第几节”均指该文档 |
| 编写前基线 | `/Users/miles/dev/dev-py/APSGOV7`，`codex/delivery-objective-optimization@39a50f8` |
| 代码与设计检查点 | 最近代码为原 8.1 `fd5a5f7`；`64eb929` 是局部扩围文档，`39a50f8` 是统一设计与停止节点文档 |
| 环境实查 | macOS Darwin 27.0.0 arm64、zsh；Conda `aps_3.10.18`，Python 3.10.18、NumPy 2.2.6、Numba 0.65.1 |
| 当前授权与状态 | 新 8.2.3 热点筛查完成：256 个真实描述全部结果相同；串行批量 0.146220 秒，单条 0.200562 秒，并行 1/2/4/8 均未胜出；精确树验证见提交正文 |
| 后续代码入口 | 新 8.3“完成端到端采用决策”：不扩大无收益的线程组；验证有收益的串行批量组织能否改善真实窗口和完整求解，默认尚不切换 |
| 实施目标 | 一套公共数值数据、链操作、修复、私有资源、候选计算和评价，全阶段接入；先完成统一串行再试点并行 |
| 当前范围之外 | 规则/九级优先级/搜索算法调整、正式 YAML/SQLite 迁移、API/C#、Windows 打包与现场验收、新依赖、历史交期退步修复 |
| 工作方式 | 不应用 Ponytail 限制；可作必要架构改造。按完整功能单元提交，必要验证集中进行，不逐次编辑重复全量测试 |

本文是后续统一数值改造的实施入口。[原 NumPy 实施计划](apsgo_v7_numpy_numeric_search_and_calculation_implementation_plan.md)的已完成阶段和第 9.3 节检查点保留，原 8.2.1、8.2.2、8.3、9.1 继续暂停；**新阶段编号不继承旧阶段的完成状态**。

## 2. 固定约定与保护清单

### 2.1 不得随实现改变的边界

- 依设计第 8～9 节保留单位、四舍五入、整数毫秒累计、评分秒精度、交期当天 24 点、片重工时守恒、规则和九级顺序；不再次设计数值口径。
- 保留阶段和动作次序、直接位置优先、桥接枚举及同分顺序、每原单 4 次推进、重点/常规各 64 次轮转、两种修复原扣额点、首个改善接受和唯一拆后重放。
- 原结构拥有者直接生成候选，不先生成重复候选再过滤；随机源、随机调用次数和稳定排序不变。
- Python 保留调度、额度、取消、原序消费、接受与边界映射；高频搜索/修复/资源/评价直接读写原生数组。不同阶段调用公共实现，不能只加一个统一名称后继续分发到旧对象路径。
- 同数值标准、同工作量且不触发超时时要求候选、逻辑计数、接受、资源身份与最终结果一致。物理预计算、复制等诊断计数如变化须独立解释，不得放宽正式业务计数和公开结果对照。
- 新增实现诊断保存在实验侧证据中，不借计数变化修改公开返回字段、隐藏接受差异或扩大结果比较的忽略清单。
- 新数值语义内部最终只保留一套实现；历史公开语义路由不在本轮退役，数值失败不允许回退旧求解器。

### 2.2 输入和质量基线

| 项目 | 冻结值与处理 |
|---|---|
| 原请求 | `diagnostics/critical_delivery_search_and_bridge_reclamation/combined_01/prepared_request.json` |
| SHA-256 | `b94d979b3d5d6545a0b7bac9a01b49b89f4feda0582914a4ff07d4d35088ae6b`，本次只读核验一致 |
| 样本与预算 | 原有序 531 单、27 原型、任务期序、规则参数、种子 `590531`、400000 次逻辑候选；样本数量不得写成生产固定容量 |
| 时间条件 | 2026-06-01 00:00 起排、虚拟速度 100 米/分钟；实验 9999 秒搜索＋10 秒收尾；正式 YAML 300 秒＋10 秒不修改 |
| 新语义请求 | 复用 `tools/profile_numeric_solver.py` 的 `derive_numeric_request()`；只作既定数值声明与身份转换，阶段 0 冻结转换后字节及指纹，不从活动 SQLite 重组 |
| 同标准代码对照 | `39a50f8` 的生产代码，阶段 0 记录源码摘要并核对与设计基线 `64eb929` 的生产源码相同；不拿旧 Decimal 路径充当本轮等价基线 |
| 当前质量约束 | 零禁止、零欠重、启用规则和来源/重量/工时/谱系/期锁/双审计通过；不恢复最多 22 条链限制 |
| 独立历史问题 | [40 万次交期退步](apsgo_v7_delivery_quality_regression_400000_issue.md)继续未关闭；当前实现等价不代表恢复历史 6 月 15 日旧欠清空、14 单本月晚交的质量 |

阶段 0 已完成独立全流程运行，见[阶段 0 证据](evidence/apsgo_v7_unified_numeric_solver/stage0.md)。已有 7.4 全量仅作完整返回校验参考，8.1 局部窗口不冒充本次全流程基线。

### 2.3 既有文件保护

以下六项本次实查摘要如下，实施前/提交前复核；不修改、不暂存四份用户文件，不自动迁移配置或数据库。

| 路径 | SHA-256 |
|---|---|
| `docs/implementation/apsgo_v7_numeric_search_incremental_parallel_implementation_plan.md` | `c44cc7ca7ac32d606aefb3470b6b562d107df07843e7f318b6479f739f5ea4cf` |
| `docs/implementation/evidence/apsgo_v7_numeric_search_incremental_parallel/stage3.md` | `155bbfdd6ca663959a8ddd18ca60d0939000aba9f0f0d10a59d1db597ef055c3` |
| `docs/apsgo_v7_backend_technical_guide.md` | `7b9d0599802b11f2c8aee49ec4f57be532529c655b22648925ffd784081a15a9` |
| `docs/apsgo_v7_backend_technical_guide.html` | `5f1cd4126648ffc13a7767775f61a52c2316a672e2b63a01993bad02aa1668e2` |
| `config/apsgo_v7_service.yaml` | `04cb4998cd3d8100a4f7f2ff9c62e10111ced4ae8f827df841507fa535c2f0c0` |
| `data/apsgo_v7_rules.sqlite3` | `72a60e32fea0d2d55f883a8b743175aa5e8fd94e779388db6ae8c8c26ec2bd6a` |

原阶段 8 证据、独立交期问题和已有诊断目录保留原字节。若用户后续主动修改保护项，先核对变更来源与影响，不覆盖用户改动，也不为通过检查擅自更新摘要。

共享工作区残留检查目前仍报告 8 个历史缺失路径及汇总共 9 项：`.claude/settings.local.json`、两份 `dist/apsgo-0.1.0.dev0` 制品和五份 `src/apsgo.egg-info` 元数据。它是已登记的旧状态，不记为通过，不据此恢复、删除或提交无关文件；新增差异须定位。干净导出检查仍须通过。

## 3. 修改范围与测试入口

| 范围 | 文件或模块 | 工作性质 |
|---|---|---|
| 公共数据与单位 | `core/_numeric_state.py`、`_numeric_units.py` | 复用单位和基础列，增加共享视图、描述、私有缓冲和容量契约；不改数值公式 |
| 公共链操作 | 拟新增 `core/_numeric_chain_ops.py` | 实现数值读取、片段、插删移动交换、切合链、换序及变化链输出 |
| 动态资源 | `core/_numeric_resources.py` | 统一边/桥接、私有资源列及拆片派生；正式扩展退出拒绝路径 |
| 候选计算 | 拟新增 `core/_numeric_candidate_kernel.py` | 公共单次尝试计算入口，不发布状态、不反向依赖阶段控制 |
| 规则与评价 | `core/_numeric_kernel.py`、`_numeric_rules.py`、`_numeric_evaluation.py` | 适配公共视图，保留权威公式、增量核验及同原语明细 |
| 全流程接线 | `core/_numeric_construction.py`、`_numeric_search.py`、`_numeric_refinement.py` | 专有数值枚举与公共操作接线，保留阶段控制，移除原内部对象准备路径 |
| 批量 | `core/_numeric_batch.py` | 从数值描述进入同候选内核，不接收完整业务候选再打包 |
| 发布与审核 | `core/_numeric_solver.py`、`_numeric_boundary.py`、`_numeric_audit.py` | 接受后物化、独立重建、输出兼容；不改变公开协议 |
| 测试与工具 | 现有 `tests/core/test_numeric_*.py`、架构/相关应用测试及 `tools/profile_numeric_solver.py`、`verify_numeric_kernel_migration.py`、`verify_numeric_serial_batch.py` | 复用测量和对照能力，按实际缺口补共享原语/全阶段轨迹见证 |

表内 `core/` 指 `src/apsgo_scheduler/core/`。新模块只在承载实际功能时创建；如调整文件归属，应记录理由和调用方向，不改变公共职责。不把新旧差异迁移为生产兼容分支；冻结参考留在测试/诊断侧。

工具限制：`verify_numeric_search.py` 当前读取旧画像中的 `first_search` 等结构，不能直接假定适配新完整数值结果；7.1 已将 `verify_numeric_serial_batch.py` 和全流程追踪改挂公共消费入口，禁止零采样假通过。优先复用现有 `compare_runs()` 的完整公开结果对照与 `first_difference()`，不使用 `--allow-evaluation-count-change` 掩盖本轮逻辑变化。

## 4. 阶段与计划提交总表

每行是一个可独立提交的功能单元，不代表每行都跑全量测试；验证范围见第 6 节。计划提交信息不是实际提交记录。

| 步骤 | 完整名称 | 主要设计依据 | 依赖 | 计划提交信息 | 当前状态 |
|---|---|---|---|---|---|
| 0 | 冻结全流程基线与覆盖清单 | 第 1～2、8、13～15 节 | 实施授权、保护检查完成 | `#feat 冻结统一数值实现全流程基线` | 已完成；完整 40 万次、同标准公开结果一致、双审计通过 |
| 1.1 | 建立共用描述、方案视图和私有工作区 | 第 3～4、6.3 节 | 0 | `#feat 建立统一数值状态与候选工作区` | 已完成；数据契约可实际调用，主流程尚未接线 |
| 1.2 | 建立共用链操作与代次索引 | 第 5、9.3 节 | 1.1 | `#feat 统一数值链操作与方案索引` | 已完成；阶段 1 集中验证通过，未接主流程 |
| 2.1 | 统一边判定与数值桥接修复 | 第 6.1～6.2、9.1 节 | 阶段 1 | `#feat 统一边判定与私有数值桥接` | 已完成；2,124 组真实桥接字段/结果一致，未接主流程 |
| 2.2 | 统一拆片与资源事件派生 | 第 6.2～6.3、9.2 节 | 2.1 | `#feat 统一拆片派生与候选资源记录` | 已完成；两类拆片/分隔材/事件对照一致，未接主流程 |
| 3.1 | 接通公共视图与权威评价内核 | 第 5.1、9 节 | 阶段 2 | `#feat 接通共享数值视图与权威评价` | 已完成；正式/私有读同公式，候选准备尚未替换 |
| 3.2 | 建立统一单次候选计算入口 | 第 7.1～7.2、8 节 | 3.1 | `#feat 建立统一候选计算内核` | 已完成；原 14 类动作、私有修复与评价可调用，未接主流程 |
| 3.3 | 统一原序消费和接受发布 | 第 7.3～8、9.3 节 | 3.2 | `#feat 统一候选消费与原子发布` | 已完成；小例身份/轨迹/错误/扣额/原子性闭环，未接主流程 |
| 4.1 | 接通有序连接图与路径覆盖 | 第 3、10 节 | 阶段 3 | `#feat 接通公共数值图与路径覆盖` | 已完成；有序数组/原遍历接线，真实 531 单与冻结基线字节一致 |
| 4.2 | 接通初始链数值构造 | 第 5、7.2、10 节 | 4.1 | `#feat 接通统一数值初始链构造` | 已完成；原追加/截链政策、数组切片及真实初始结果一致 |
| 5.1 | 接通首轮四类局部搜索 | 第 7～8、10 节 | 阶段 4 | `#feat 将首轮搜索接入统一候选内核` | 已完成；四类动作共用候选，真实 110964 次原序检查/361 次接受一致 |
| 5.2 | 接通两类拆单和唯一重放 | 第 6～8、10 节 | 5.1 | `#feat 将拆单与重放接入统一数值流程` | 已完成；真实两类各 1 次、唯一重放及 134226 次原序检查一致 |
| 6.1 | 统一精修描述生成与结构归属 | 第 4、7.1、8、10 节 | 阶段 5 | `#feat 统一精修数值描述与候选游标` | 已完成；真实窗口 19564 个生成提案及 2 万次逻辑检查顺序一致 |
| 6.2 | 接通精修与连接材回收 | 第 6～8、10 节 | 6.1 | `#feat 将精修与回收接入统一候选内核` | 已完成；旧准备退出调用，真实窗口逻辑/身份一致 |
| 7.1 | 完成全阶段审核与热路径检查 | 第 9～11、13.1 节 | 阶段 6 | `#fix 收紧全流程数值接线与独立审核` | 已完成；累计 4078 项、真实单条/8 条窗口及双审计通过 |
| 7.2 | 完成统一串行全量结果与耗时对照 | 第 13 节 | 7.1 | `#feat 记录统一数值串行完整对照` | 已完成；8 次完整结果一致；新/旧热中位数 158.34/153.32 秒，未提速；热点及内存问题已定位 |
| 7.3 | 修复原生生成器提前终止的资源释放 | 第 8、13～14 节；7.2 实测问题 | 7.2 | `#fix 修复数值生成器提前终止的数组释放` | 已完成；提前弃用 1000 组净保留归零；真实窗口及完整结果一致 |
| 8.1 | 建立从描述开始的共用数值批次 | 第 7～8、12 节 | 7.2 正确性及 7.3 释放验证通过 | `#feat 建立统一候选数值批次` | 已完成；同序结果保持，工作区代次复用，尚未并行 |
| 8.2 | 试点同内核候选修复构造评价并行 | 第 12～13 节 | 新 8.1 等价通过 | 按以下三个工程单元提交 | 试点完成；没有并行收益，不登记并行提速成功 |
| 8.2.1 | 将共用拼接修复及资源写入移入有界原生步骤 | 第 5、10、12 节 | 新 8.1 | `#feat 编译共用片段修复与资源写入` | 已完成；197 项必要回归、真实窗口字节一致；非整候选并行完成 |
| 8.2.2 | 接通整候选原生计算与同序串行闭环 | 第 10～12 节 | 新 8.2.1 | `#feat 接通整候选原生计算` | 已完成；205 项必要回归及真实窗口字节一致；线程试验尚未开始 |
| 8.2.3 | 试点同内核多线程并完成热点筛查 | 第 12～13 节 | 新 8.2.2 | `#feat 试点统一候选内核并行` | 已完成；6 种模式各预热及 5 热样本，256 个描述结果逐项一致 |
| 8.3 | 完成端到端采用决策 | 第 12～14 节 | 新 8.2 分级试验结论 | `#feat 记录统一候选串并行采用结论` | 未开始 |
| 9.1 | 清理替代路径并完成交付记录 | 第 11、13～14 节 | 新 8.3 | `#fix 收口统一数值实现与验收记录` | 未开始 |

代码接线阶段按此依赖顺序进行，不在首轮或拆单仍有对象候选路径时先开展并行。新计划步骤必须带完整名称，尤其新 8.1/8.2 不能与旧计划已完成或暂停的同号步骤混称。

## 5. 各阶段实施说明

### 5.1 阶段 0：冻结全流程基线与覆盖清单

**目标：**固定实际调用链、数据与行为参照，后续能定位首个差异，而不是仅比较最终九项分数。

1. 在新临时目录导出基线源码，冻结实际树、生产文件摘要、原请求及派生数值请求；不切换或覆盖共享工作区，不把四份用户文件带入基线。
2. 对构图、路径覆盖、初始链、首轮、两类拆分、唯一重放、精修和回收建立覆盖表，逐项列出公共操作、当前实现、阶段检查政策、扣额点、随机使用、资源编号与旧路径退出位置。
3. 复用测量工具，补必要的阶段快照及候选有序摘要。候选轨迹采用流式摘要加有界样本，差异时缩小窗口重放，不为 40 万次逐项保存完整方案或明细。
4. 建立有接受/有拒绝、直接连接/单材/双材、两类拆片、跨虚拟端点、空等级、取消与容量的必要小样例；正常执行真实前缀取得精修窗口，不伪造恢复状态。
5. 完成一次同标准 40 万次基线运行，保存公开结果、交期、阶段身份、逻辑计数及双审计；这次有诊断的运行不作为正式性能样本。

**完成标准：**冻结请求转换无额外变化，全阶段覆盖清单完整，原始基线可重放，业务结果与已知检查点可解释。基线本身不能完成或出现新规则错误时先定位，不能带着未知失败进入改造。

### 5.2 阶段 1：共用数据、链操作和索引

**1.1：**在现有 `NumericTask`、`NumericPlan` 基础上实现数值描述、统一方案读取视图、私有字段尾部、链映射和状态码；明确索引域、有效长度、容量、只读性与版本失效。保留已确认单位，不复制第二套任务字段。

**1.2：**实现公共链操作模块，覆盖片段读取、反向、拼接、插删、移动交换、切合链和换序；索引按方案代次建立，未变链指向原数组，变化链才写私有缓冲。高频操作不先生成元组/列表，也不先拼全方案数组。

实际新增数值模块时同步现有架构保护的精确依赖登记，说明 NumPy/Numba 与内部数值码的用途；不整体放宽生产代码的旧项目导入、本机路径、产线常量或测试依赖限制。

**集中验证：**数据类型、容量重试、共享区不变、跨候选不串写、空/单节点链、链增删换序和只变两链的引用见证。阶段结束时证明这些公共能力真实可调用，不把类型定义或空模块算完成；主求解暂未接线。

### 5.3 阶段 2：连接修复与资源原语

**2.1：**边判定读取公共基础列与私有尾部，复用规则表；收敛原型快速掩码与通用边逻辑。桥接采用原直接/单材/双材顺序、原型枚举与平滑同分规则，输出数值资源与变化链，不扩展整任务或生成正式指纹。

**2.2：**实现同期间与未来借入归还所需的拆片字段、工时分配、分隔材、目标期和扩展事件记录。它是资源派生能力，不在此新增拆单动作或改变授权政策；正式资源身份留给接受边界。

**集中验证：**对冻结真实原型/边界及小样例比对资源参数、选择顺序和失败结果；保留失败桥接候选，不只验成功成品。验证片重/工时守恒、期锁信息完整、拒绝资源零发布、整数超界与容量重试，禁用旧节点对象构造/整任务扩展的失败桩下仍可计算。

### 5.4 阶段 3：权威评价、统一候选和发布

**3.1：**将现有编译评价内核接入公共方案/资源读取，正式方案作为零覆盖特例；同一内核承担规则、链事实、交期和九级评分，未变链复用必须验证任务/规则/期/节点有效性。明细从同原语按需生成，内部合法数组不重复转元组验证。

**3.2：**实现 `compute_candidate_attempt` 公共入口，按数值描述组合链编辑、修复、私有资源和原阶段检查，再调用完整评价。阶段策略用明确输入表达，不能新增全候选零违规前置拦截，不能内部回调阶段对象实现。

**3.3：**统一消费与发布适配，保留原扣额、修复变体、错误生效和首改善；接受时确认扩展事件和资源编号、构造正式方案、同内核复核后一次提交。拒绝、取消或发布失败不得残留状态和缓存。

**集中验证：**同一固定方案的全量/覆盖/缓存有无一致；紧凑汇总与明细一致；两变体扣额、后续错误延迟、接受后后缀作废、版本过期与容量重算均有见证。公共事务在小样例中闭环后，才开始各阶段主流程接线。

### 5.5 阶段 4：连接图、路径覆盖和初始构造

**4.1：**构图直接写有序邻接数组与偏移，使用公共边判定；广度优先队列、深度优先栈及邻接游标数值化。保留原排序、随机打散、分层和增广顺序，不换算法、不引入 SciPy。一次性的随机序列准备可以留在 Python 边界，不能换随机源。

**4.2：**初始链拼接与截链改用公共链写入和规则原语，最终构造一次正式初始方案；不强套首改善候选事务，不新增链重等构造约束，不改变逻辑额度。

**集中验证：**有序邻接、匹配结果、路径、初始链顺序/身份与基线逐项一致；真实 531 单仅运行到初始方案即可，不在本阶段重复全量搜索。验证队列/栈容量和有界取消。

### 5.6 阶段 5：首轮、拆单及重放

**5.1：**整链调整、真实节点移动、虚拟填充和整链调序都生成共用数值描述，调用统一候选入口；保留各动作的筛选条件和原扣额点。首轮拒绝路径不再经过 `_layout()`、完整方案构造或整任务资源扩展。

**5.2：**两类受控拆单通过共同私有资源和变化链完成，事务后检查授权目标期；接受后执行原唯一一次首轮重放，直接使用已改造入口，不留下旧对象版重放。

公共准备结果在原拆单扣额点暂停、扣额后由同一候选入口续算；未评价结果不能消费/发布。扩容重试和准备失败不扣额度，期锁拒绝保留原扣额但不算完整评价。验证恢复时的身份/描述/策略/代次，不能通过移动扣额点简化接线。

**集中验证：**有直接/桥接/填充接受的小样例及真实正常前缀，比较首轮、两类拆单、重放结束的候选计数、接受顺序、方案/资源身份和评分。此时后置精修尚未接线，必须明确记录，不能称全流程完成。正式状态载体暂与后续现有模块保持可交接，不通过每候选还原对象做兼容。

### 5.7 阶段 6：后置精修和连接材回收

**6.1：**将结构拥有者、位置/区间枚举、通道判定及候选游标接入共用数值索引和描述格式；按原阶段算法生成，直接唯一归属。深层扫描分段返回，不在 Python 生成全部候选后去重或筛选。

实际采用阶段专属 `_numeric_refinement_scan.py`，借用权威数组、平铺归属列和实际编译生成器的原生续算状态；深扫每 256 步返回取消检查，不占提案轮转/额度。6.1 保留到旧准备的纯元数据过渡映射，6.2 必须移除该映射及生产调用，不能把它作为最终统一实现。

**6.2：**精修及回收的链编辑、清理后/原样修复变体、桥接、私有资源与评价转入统一候选入口；保留保护分隔材、期序、原代次失效和调用顺序。此阶段先逐条数值计算，原有最多 8 条描述的有界预取及原序消费语义保留；正式诊断若依赖原物理预计算计数，在阶段 0 明确区分，不把物理次数冒充逻辑额度。

移除新数值路径对 `PreparedRefinementCandidate`、旧 `_prepare_repaired_parts()` 对象准备与完整候选再打包的依赖；只保留测试侧冻结对照，不把旧实现当运行时备用。新的数值批量调用在阶段 8 单独接通，不能用原 8.1 已通过替代本轮检查。

**集中验证：**结构穷尽小样例与真实精修入口后新增 20000 次窗口，比较生成、修复、消费、接受、身份和双审计；窗口累计计数不清零，原请求上限不改。此窗口是精修验证，不代表完整 40 万次验收。

### 5.8 阶段 7：统一串行闭环和完整对照

**7.1：**接通最终数值输出及独立审核；逐一核验阶段 0 覆盖表。新增或收紧架构/调用见证：所有搜索动作进入公共候选内核，公共原语不反向依赖阶段；拒绝路径对象构造、全方案物化、整任务扩展和正式指纹计数为零。未改公开语义仍保留原入口，不做活动规则切换。

**7.2：**先做一次完整 40 万次同标准结果对照，再按第 7 节安排无重型探针的完整性能样本；可将第一个合格正式样本兼作完整质量样本，避免重复运行。保留全部九项分数、订单顺序、资源编号、逻辑计数、轨迹/结果身份、交期报告和双审计；只排除实测时间，不放宽业务结果。

**7.3（根据本轮实測增加的修复单元）：**已在当前环境复现原生生成器中途弃用保留数组，而正常耗尽可释放；当前原生对象没有 `close()`。为全部六类原生生成器增加私有终止标记和 Python 生命周期边界，在每个已返回项后的第一个恢复位置检查标记并立即返回，释放时只恢复至该出口，不扫描剩余候选、不扣额、不接受。保留原生成算法和原序；不是耗尽剩余空间来掩盖泄漏，也不重写为另一套候选算法。验证完整/提前关闭/取消/错误及接受后作废，弱引用和原生分配统计归零；真实有序窗口和一次完整同标准结果保持。属于本轮实现问题的正常修复，不扩展业务或依赖。后续批次先复用有界缓冲和阶段不变参数，处理剖析所见的准备/桥接/重复校验成本，再试点并行。

**完成标准：**全阶段真实调用证据、数值无对象编译见证、必要累计回归、完整同标准行为等价和串行耗时报告均具备。未达一分钟不伪称通过性能目标；有行为差异不得进入批量/并行。一致但较基线明显变慢时先定位串行热点，不用并行掩盖额外工作。

### 5.9 阶段 8：共用批次与并行试点

**新 8.1：**从数值描述开始分配有界私有工作区，批量串行调用同一候选计算入口；不先生成 Python 完整候选。保持修复变体依赖及实际消费扣额，支持容量缩批、错误延迟和接受后作废；批次 1 与批次 8 作同序对照。旧批量适配只在所有调用迁移后删除。

**新 8.2：**在后置精修的独立结构描述外层试点 Numba 并行，计算包含链编辑、桥接/资源和完整评价，不仅评价尾端。先验证实际无对象编译及共享只读/私有写区，再对 1/2/4/8 线程测试；主线程原序消费，线程不能直接提交状态。同一描述内存在依赖的变体不强行并行。

该项按三个可验证工程单元提交：8.2.1 先把共用片段拼接、桥接扫描完成后的资源写入连成有界原生步骤；8.2.2 用相同原语接通整个候选的准备与评价，移除对应 Python 热循环；8.2.3 再在独立候选外层试验实际多线程。原生步骤以数值游标续算，主线程保留取消检查；不把两种修复、拆单准备前后扣额或主线程接受改成无序任务。三个单元只细分工程依赖，不扩大算法范围、不更换原语公式、不新增测试全量频率。

**新 8.3：**分开报告公共串行改造、批量组织和线程并行的收益；根据热点筛查、真实窗口和完整端到端结果决定采用。无阶段收益的线程组不扩大到 40 万次；有阶段收益也须完整对照后才能采用。线程选择只服务本次内部实验，不新增 HTTP、规则库或服务配置项。

新 8.2.3 已见整候选原生批量串行快于逐条协调，而线程组全部更慢。因此后续仅比较这两种**同内核串行组织**：各预热及 3 个真实 2 万窗口，确认完整结果一致且阶段收益不被波动抵消，再扩大到各 3 个完整热样本。没有收益的线程组不再跑窗口或全量；这不是改候选顺序、规则或额度，也不是并行成功。

**完成标准：**采用串行或并行的依据明确，均保持统一数值入口。并行试验无收益也可完成“试验与决策”，但不得登记“并行提速成功”；未执行的完整并行组写“不执行及理由”，不能写已通过。

### 5.10 阶段 9：清理与交付

删除已无调用的旧内部候选准备/包装、重复规则快路径与过时缓存；保留有用冻结参考和独立历史公开入口。同步诊断工具挂接位置、当前设计实现边界、本文执行表及 `AGENTS.md`。

完成最终必要累计回归与完整覆盖表复核；清理若改变生产执行路径，至少补一个完整同标准结果见证，不因已有性能结论省略正确性。只有计时相关路径变化才重做受影响性能组，不机械重跑全部组。输出最终采用方式、耗时、峰值内存、剩余热点与未关闭质量问题；本阶段不包含部署、数据库启用或 Windows 验收。

## 6. 必要验证与提交纪律

### 6.1 集中验证矩阵

| 检查点 | 必要验证 | 不要求重复执行 |
|---|---|---|
| 阶段 0 | 来源/请求派生、轨迹工具小样例、真实全流程基线；若基础回归状态未知，先作必要健康检查 | 不测线程、不跑多组性能 |
| 阶段 1～2 | 相关状态/单位/链原语/资源规则测试，实际数组与编译见证；新增模块依赖检查 | 不跑完整 40 万次 |
| 阶段 3 | 全量/变化链/私有资源评价、增量缓存、明细与原子提交测试 | 不因每个接口完成就跑全仓 |
| 阶段 4 | 构造专项、有序图/路径对照及真实初始构造 | 不运行后续搜索 |
| 阶段 5 | 首轮、两类拆单、唯一重放专项及真实前缀对照 | 不重复全套性能样本 |
| 阶段 6 | 描述结构归属穷尽样例、精修/回收测试及一次 20000 次窗口等价 | 不把窗口扩成多次完整求解 |
| 阶段 7 | 架构、全部数值专项、相关应用/接口/双审计及必要累计回归；完整结果与性能 | 不为逐行修改重复累计回归 |
| 新阶段 8 | 批次、错误/额度/取消与隔离；同内核串并行、真实窗口、通过分级门后完整组 | 不运行无收益依据的多组全量并行 |
| 阶段 9 | 最终必要累计回归、工具与全覆盖核验；生产路径变化后的完整正确性见证 | 计时路径不变时复用已合格性能样本 |

每个提交单元都有相关快验或复用已有验证的明确理由；集中检查点通过前不标该阶段整体完成。架构、公共数值类型或发布边界改变时必须运行对应检查，不把“不必每一步测试”解释为可以跳过正确性。

可复用的现有测试组：

- 数据：`tests/core/test_numeric_units.py`、`test_numeric_state.py`。
- 构造/搜索：`test_numeric_construction.py`、`test_numeric_search.py`、`test_numeric_refinement.py`。
- 评价/审核：`test_numeric_rules.py`、`test_numeric_evaluation.py`、`test_numeric_incremental_evaluation.py`、`test_numeric_kernel_migration.py`、`test_numeric_boundary_audit.py`。
- 批量：`test_numeric_batch.py`；应用测量：`tests/app/test_numeric_solver_measurement.py`、`test_numeric_bridge_measurement.py`。
- 架构：`tests/architecture`；必要累计范围为 `tests/architecture tests/api tests/app tests/core`。新增共享链/候选内核见证随实际功能添加，不能用现有测试数量代替新范围覆盖。

### 6.2 环境与命令

以下是后续实施命令，不是本次已执行测试；在 V7 仓库根目录运行。执行记录保存实际完整命令、退出码、数量、耗时和首个差异。

```bash
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python tools/check_workspace_residuals.py --verify

PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider tests/core/test_numeric_units.py tests/core/test_numeric_state.py tests/architecture

PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider tests/architecture tests/api tests/app tests/core
```

完整测量可复用已存在的入口；下面的 `--repeat 4` 表示先记录一次预热/首次运行，再取后续三次作为热运行候选样本，不能把四次全部当冷启动。目录必须不存在；当前工具对已存在目录直接拒绝。

```bash
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python tools/profile_numeric_solver.py \
  --prepared-request diagnostics/critical_delivery_search_and_bridge_reclamation/combined_01/prepared_request.json \
  --candidate-check-limit 400000 \
  --repeat 4 \
  --output-dir diagnostics/unified_numeric_solver/stage7_serial_01
```

运行前核对派生请求、代码树和计时模式；阶段 0/7 如扩展工具收集全阶段信息，沿用现有入口并记录新增参数的实际帮助信息，不能引用未实现参数。当前工具的 `--profile-first` 只用于单独有界诊断，不用于正式性能组。

### 6.3 精确提交范围

实施前和提交前运行残留检查；新路径已存在但未跟踪时先辨明归属，不覆盖用户文件。每项只暂存精确白名单，禁止 `git add .` / `git add -A`；中文信息以 `#feat` 或 `#fix` 开头。

用 `git write-tree` 和 `git archive` 导出精确暂存树到新 `mktemp -d` 目录，复验该功能单元实际选择的同一必要范围。只在干净导出中构建/编译；全仓回归仅在上述累计检查点执行，不机械加到每次提交。共享树与导出树的差异必须解释，不能只验共享未提交内容。

文档提交只检查 UTF-8 无 BOM、链接、表格、围栏、阶段/版本/入口、保护摘要、暂存范围与 `git diff --check`，不运行算法测试。当前提交 SHA 不写进自己的提交内容；实际身份由 Git 历史及下一次记录确认。

## 7. 实验分级与采用标准

| 级别 | 样本与次数 | 进入条件 / 结论边界 |
|---|---|---|
| 小例和固定输入 | 覆盖第 6 节必要边界；数值结果逐项核对 | 出现差异先定位，不扩大运行 |
| 阶段顺序见证 | 每个真实阶段边界一次；精修入口后新增 20000 次逻辑窗口 | 从正常前缀进入，不清零计数，不把人工窗口截断当完整排程 |
| 统一串行完整对照 | 当前同标准基线与新统一串行各 3 个合格热运行；各自首次/预热另报 | 阶段 7 一次集中执行；同树同模式已有合格样本可复用，但旧 7.4 秒数不能直接代替当前机器基线 |
| 批次正确性 | 批次 1/8 各一次相同窗口；含接受、拒绝、错误、容量和取消 | 首先证明同序等价，附加逐项核对的时间不能用作提速证据 |
| 并行热点筛查 | 同一批 256 条真实数值描述及全部原修复尝试；新单条串行、批量串行、并行 1/2/4/8 各预热一次、测量 5 次 | 失败描述也保留，缺失边界用小例补；计入描述准备/修复/资源/评价，不只计评分 |
| 并行阶段收益 | 批量串行与热点筛查有收益的一个线程组合，各 3 个同入口 20000 次窗口 | 计入消费、作废和接受，实际并行调用必须大于零；无收益不扩大 |
| 完整采用对照 | 候选并行通过阶段门后，只对一个候选线程组合补 3 个 40 万次热运行，与同条件统一串行比较 | 同标准结果与逻辑路径一致、完整中位数降低且波动不抵消收益才采用；否则采用串行 |
| 最终冷启动 | 最终采用实现一次新进程、相关编译缓存未命中的完整运行 | 在临时目录隔离缓存，不删除用户缓存；计入导入、编译和全流程，不能拿热运行替代 |

这是本专项分级工程验证，不扩展为历史 20 对性能验收，也不以 3 次样本宣称统计显著或第 95 百分位达标。每组记录实际源码树、输入/规则/标准身份、机器负载、线程数、启动/缓存方式、墙钟与 CPU 时间、峰值内存；休眠或明显干扰的样本保留并标注，不据其作采用结论。

完整后端计时覆盖输入准备到最终审核和返回对象构造；另外记录进程启动/导入、HTTP 与诊断写盘，不偷偷省略成本。现有测量入口不能完整覆盖的边界，阶段 0 明确并补测。正确性探针、剖析与正式计时分离。

一分钟目标分别报告：冷启动是否小于等于 60 秒、热运行三次中位数及每次是否小于等于 60 秒。未达目标如实记录，不降低 40 万额度、不把 9999 秒改为 60 秒截断、不改变规则或关闭审核；CPU 消耗上升但经过时间下降时同时报告。

## 8. 停止、恢复与收口边界

- 同标准行为、资源身份、守恒或审核发生差异：停在最近已验证单元，冻结首个差异，不修改期望值放行；已知历史质量退步仍单独跟踪。
- 新增依赖、算法/业务变化、公开契约、活动规则切换或前端/Windows 范围：说明原因和影响，等用户确认，不视为数值实现的附带授权。
- 长任务超过预计进度时先检查计数、阶段和当前栈，有界定位；主动说明当前工作，不连续重复无上限全量运行。中断样本保存状态，不写成通过。
- 需恢复时，以第 10 节最近验证提交导出独立目录复现；共享工作区优先修复提交。需要撤回代码须按精确提交和影响确认，不用 `reset --hard`、不覆盖用户文件、不删除诊断目录。
- 无数据库/服务部署变更，因此本计划没有隐含停机或库迁移步骤。所有步骤完成只表示本机实现和所列验证完成，不代表已部署、已通过 Windows 打包或已恢复历史交期质量。

## 9. 全阶段接入验收表

此表在阶段 0 定位现有调用方，在实施时逐项填入实际函数、必要验证和提交。**所有行验收前，不得称“全流程统一数值化完成”。**

当前调用方、各阶段检查/扣额/随机/资源和旧热路径退出点已在[阶段 0 覆盖清单](evidence/apsgo_v7_unified_numeric_solver/stage0.md#3-全流程调用行为和退出清单)逐项冻结。后续各行的“未接入”指尚未接入新公共层，不是没有现有数值实现。

| 环节 | 统一能力 / 检查点 | 负责步骤 | 当前状态 |
|---|---|---|---|
| 基础字段与规则依赖 | 一份权威值、私有尾部字段齐全、无回查业务对象 | 1.1、2、3.1 | 已接入；正式/私有双段数组与全部依赖字段共用，专项及累计见阶段 1～3、7 |
| 连接图与路径覆盖 | 公共边规则、有序邻接、数值队列/栈与游标 | 4.1 | 已接入；真实有序图、匹配、路径和身份与基线一致 |
| 初始链构造 | 公共链写入，原构造约束与结果保持 | 4.2 | 已接入；真实 30 条初始链的数组/身份/评分与冻结基线一致 |
| 首轮四类搜索 | 统一描述、资源、候选计算与消费 | 5.1 | 已接入；拒绝不构造正式资源/方案，真实各阶段及接受/扣额摘要一致 |
| 两类拆单 | 私有资源、授权期锁、守恒、原子候选 | 5.2 | 已接入；准备/扣额/期锁边界保持，真实两类各 1 次及资源身份一致 |
| 唯一重放 | 复用已统一首轮入口，不多跑 | 5.2 | 已接入；仅 1 次，真实累计检查/评价/接受及完整状态一致 |
| 后置精修与回收 | 同一个候选入口、结构唯一归属和原序消费 | 6.1～6.2 | 已接入；描述直达公共私有计算、原序消费发布，旧准备/索引/打包不在生产调用 |
| 规则/资源/交期/九级 | 共用视图与既有权威公式、增量核验 | 3.1、7.1 | 已接入；公共消费逐项重算真实 5560 个候选一致；完整 40 万结果待 7.2 |
| 发布、输出与独立审核 | 仅接受后物化、公开结果一致、独立重建 | 3.3、7.1 | 已接入；旧入口失败桩、调用链及真实窗口双审计通过；完整质量待 7.2 |
| 单条/批量/并行 | 共用同一候选入口、私有写区与原序接受 | 新 8.1～8.3 | 单条/批量有序对照通过；整候选原生调度/并行及采用待新 8.2～8.3 |

## 10. 执行记录与提交情况

### 10.1 实际执行记录

| 日期 / 项目 | 实际工作 | 验证与证据 | 状态 / 下一步 |
|---|---|---|---|
| 2026-09-16 / 计划编写 | 核对 `39a50f8`、统一设计、当前工具参数、环境和保护摘要；编制公共层优先的阶段、验证、实验及提交安排 | 本轮只创建本文并同步文档入口；没有执行阶段 0，没有新算法测试或求解结果 | 仅文档；后续授权实施时从阶段 0 开始 |
| 2026-09-16 / 文档核验 | 使用 Conda `aps_3.10.18` 检查四份文档的编码、围栏、表格、链接、步骤表/提交表及保护范围 | UTF-8 无 BOM、122 个本地链接、40 张表、20 个提交单元的两表状态一致，`git diff --check` 通过；7 项保护摘要和两份历史证据不变，设计与旧计划仅补入口 | 共享检查通过；残留检查仍为原 8 项缺失及汇总、退出 1，不记通过。精确导出复验与提交身份记录在 Git 正文；无算法测试或求解 |
| 2026-09-16 / 阶段 0 | 从 `2539ab0` 冻结生产源码、派生请求；补有界有序摘要及阶段快照，干净导出完成一次 40 万次基线 | 400000 候选、78775 完整评价、486 接受、两种拆分各 1、唯一重放；完整返回除实测时间外匹配 7.4 检查点，双审计通过。诊断 189.3497125 秒，不作正式性能；命令、覆盖和小例见 `stage0.md`，精确树复验见提交正文 | 阶段 0 完成，下一项 1.1；正式配置/库、用户文件和历史问题不动 |
| 2026-09-16 / 1.1 | 从 `52c5e82` 建立统一描述、原方案/变化链视图、私有列与容量/代次契约；复用动作及状态码 | 共享 156 项/26.16 秒，实际 Numba 无对象读取、私有隔离、容量和失效保护通过；完整命令见 `stage1.md`，精确树复验见提交正文 | 1.1 完成；下一项 1.2，主流程未接线 |
| 2026-09-16 / 1.2 | 从 `44c426c` 建立数组链片段/反向/插删/移动交换/切合链/换序及代次索引 | 共享 165 项/26.73 秒，穷尽小例与序列代数一致、实际编译、未变引用与失败零修改；命令见 `stage1.md`，精确树见提交正文 | 阶段 1 完成；下一项 2.1，未接主流程、不宣称提速 |
| 2026-09-16 / 阶段 2 前置修复 | 冻结源码复现非自适应温区无单材解时 `best` 未初始化；用户确认允许修复，增加一次初始化 | 仅恢复原双材枚举及无解返回；独立回归见 [阶段 2 证据](evidence/apsgo_v7_unified_numeric_solver/stage2.md)，实际测试/精确树见提交正文 | 独立修复；不算 2.1 完成，不改 GQGA4 自适应路径 |
| 2026-09-16 / 2.1 | 统一权威边公式，原型掩码共用；私有原型扫描、温区派生、严格同分顺序与有界续算 | 冻结 531 单/27 原型的 2,124 组成功及失败逐项一致，全部新节点/派生列字节一致；集中和精确树验证见 `stage2.md`/提交正文 | 2.1 完成；下一项 2.2，主求解尚未接私有资源，不宣称提速 |
| 2026-09-16 / 2.2 | 共用模板字段写入；私有拆片与分隔材、精确累计工时分配、授权期与资源事件记录 | 两/三片、两种拆分模式、原函数失败桩和大整数精确分配通过；真实 5 个资源案例与冻结原实现全部字段/事件字节一致。集中/导出见 `stage2.md` 和提交正文 | 阶段 2 完成；下一项 3.1，未接候选主流程或发布 |
| 2026-09-16 / 3.1 | 正式数组与基础/私有列共用读取，正式平铺方案作为零变化视图调用同一完整内核；私有评价无全布局拼接 | 正式冻结参考、零变化/两变链缓存、单/双虚拟及拆片工时/全部明细对照、失效边界与失败桩；集中和精确树见 `stage3.md`/提交正文 | 3.1 完成；下一项 3.2，不算候选统一接线完成 |
| 2026-09-16 / 3.2 | 从 `2996e37` 建立统一单次候选入口；14 类动作以数值片段/元数据组合公共链与资源操作，保留修复变体及显式阶段检查政策 | 整链保序/反向、节点片段、切链换序、填充、两类拆分、清理/原样修复、回收与原路径对照；拒绝路径禁用正式构造见证；集中/精确树见 `stage3.md` 和提交正文 | 3.2 完成；下一项 3.3 原序消费与发布，不算全流程接线或并行完成 |
| 2026-09-16 / 3.3 | 从 `93b2e25` 建立原序消费、错误延迟、接受后资源事件物化/正式方案/同内核明细复核与唯一提交 | 共享数值/架构集中 310 项通过，134.32 秒；新旧接受指纹/祖先/轨迹一致，失败/取消/后缀作废与第二变体原扣额通过。精确树见 `stage3.md`/提交正文 | 阶段 3 完成；下一项 4.1，尚未开展主流程切换或新完整性能 |
| 2026-09-16 / 4.1 | 从 `1463fd8` 将有序邻接、稳定排序、广度分层队列、深度增广栈及路径输出数值化；保留原随机源/次数与顺序 | 真实 531 单新旧图/路径 JSON 字节一致：140715 检查、32536 允许、17 路径、候选计数 0；共享必要 132 项/53.62 秒，精确树见 `stage4.md`/提交正文 | 4.1 完成，下一项 4.2；未重跑全搜索或性能组 |
| 2026-09-16 / 4.2 | 从 `74e2d12` 将原初始追加/截链按路径切片编译，共用链绑定、期排序、最终一次平铺及链规则 | 真实 531 单从图到 30 初始链的顺序/编号/期/评分/身份字节一致、候选额度 0；共享必要 151 项/64.48 秒，精确树与冷编译诊断见 `stage4.md`/提交正文 | 阶段 4 完成；下一项 5.1，不宣称已提速或完整搜索通过 |
| 2026-09-16 / 5.1 | 从 `d7f9715` 接通四类首轮搜索，共用候选与发布；权威数组预检/代次元数据，移除主循环的对象候选准备 | 真实扣额摘要、完整接受和阶段状态字节一致：110964 检查、848 完整评价、361 接受；共享必要 175 项/114.76 秒，精确树见 `stage5.md`/提交正文 | 5.1 完成；下一项 5.2，冷诊断不算性能组或全流程通过 |
| 2026-09-16 / 5.2 | 从 `39d956c` 接通私有拆单准备、原扣额后续算及唯一公共首轮重放 | 真实完整前缀字节一致：134226 检查/1128 完整评价/442 接受，两类拆单各 1、重放 1；必要集中及精确树见 `stage5.md`/提交正文 | 阶段 5 完成，下一项 6.1；22 链、零禁止/欠重是精修前中间结果，不作全流程/性能结论 |
| 2026-09-16 / 6.1 | 从 `d8de4dd` 接入编译数值枚举、平铺归属和有界续算，保留共享链游标及 4/64 原序轮转 | 正常前缀后 2 万次窗口与冻结 JSON 字节一致；19564 个生成提案全流摘要相同，新增 5560 完整评价/18 接受；共享必要 187 项/120.56 秒，精确树见 `stage6.md`/提交正文 | 6.1 完成，下一项 6.2；准备路径尚未公共接线，不称完整质量或性能通过 |
| 2026-09-16 / 6.2 | 从 `f631653` 撤除旧准备与元数据适配，全部精修/回收直达公共候选；私有工作区原序消费、原修复变体扣额保持 | 真实 2 万次窗口与冻结 JSON 字节一致；共享必要 219 项/121.49 秒，精确树见 `stage6.md`/提交正文；物理预计算 5601、消费 5560、作废 41 单列，不冒充逻辑计数 | 阶段 6 完成，下一项 7.1；未跑新完整质量/性能组 |
| 2026-09-17 / 7.1 | 从 `6b457b9` 核验公共入口与独立双审计，增加旧准备失败桩/传递调用检查；探针迁移至公共消费，拒绝不为诊断构造对象 | 共享累计 4078 项/215.43 秒；真实单条和 8 条窗口各 5560 次完整消费，逐项数值、接受轨迹和完整公开结果一致且双审计通过；[阶段 7 证据](evidence/apsgo_v7_unified_numeric_solver/stage7.md)，精确树见提交正文 | 7.1 完成；下一项 7.2 完整 40 万次及无重型探针性能组；不称并行已完成 |
| 2026-09-17 / 7.2 | 从 `927f650` 完成全流程追踪、两侧首次及各 3 热样本；再预热后剖析固定 2 万次窗口，核验数组生命周期 | 全部完整返回/轨迹一致、双审计通过；新/旧热中位数 158.343551/153.324088 秒，新慢 3.27%，进程峰值 2667446272/769359872 字节。准备/桥接为主要热点；提前终止 1000 个原生生成器保留 1000 组数组，受控返回小例可归零。新增工具保护 2 项通过 | 7.2 测量与诊断完成；新增 7.3 先修复释放；未达一分钟、不进入并行、不关闭历史交期问题 |

| 2026-09-17 / 7.3 | 从 `e1af523` 为六类原生扫描接入暂停点立即退出和共同生命周期管理，补齐取消/错误/接受作废覆盖 | 共享必要 250＋2 项通过；实际 1000 次提前弃用的存活数组/净原生分配/净管理块归零；真实 2 万次窗口字节一致，完整 40 万次返回除时间外一致且双审计通过。单次首次 248.236088 秒不是热性能结论；[阶段 7 证据](evidence/apsgo_v7_unified_numeric_solver/stage7.md)，精确树见提交正文 | 阶段 7 收口，下一项新 8.1；历史交期及一分钟目标未解决 |

| 2026-09-17 / 新 8.1 | 从 `e8959ee` 将描述变体、私有槽复用、代次评价参数和错误捕获接入公共批次，保持原序消费与原子发布 | 共享必要 216＋9 项通过；真实 5560 个单条/8 条实际消费逐项相等，旧 7.1 轨迹字节相等且双审计通过。8 条工作区分配 223 次，原同窗口剖析 36747 次；不将带验证的计时冒充性能。[阶段 8 证据](evidence/apsgo_v7_unified_numeric_solver/stage8.md)，精确树见提交正文 | 新 8.1 完成，下一项新 8.2 整候选原生计算和并行；一分钟/交期问题未关闭 |

| 日期 / 步骤 | 实际改动 | 验证与证据 | 当前状态 / 下一步 |
|---|---|---|---|
| 2026-09-17 / 新 8.2.1 | 从 `57a4543` 编译共用片段拼接及资源写入，以数值游标有界续算；细分后续整候选接线与线程试验 | 共享必要 197 项/134.17 秒通过，真实前缀及 2 万次窗口与冻结 JSON 字节一致；两类拆单/唯一重放保持。精确树同范围见提交正文，[阶段 8 证据](evidence/apsgo_v7_unified_numeric_solver/stage8.md) | 新 8.2.1 完成；下一项整候选原生计算，尚未并行、不宣称提速 |
| 2026-09-17 / 新 8.2.2 | 从 `64ea669` 将普通候选链编辑、修复、私有资源和同一评价内核接成有界原生入口；移除对应旧 Python 准备分支，复用只读代次参数及私有输出 | 共享必要 205 项/220.08 秒通过；同进程随后真实前缀及 2 万窗口与冻结 JSON 字节一致。完整原生入口/不回退/输出复用和独立评分见证通过；精确树同范围见提交正文，[阶段 8 证据](evidence/apsgo_v7_unified_numeric_solver/stage8.md) | 新 8.2.2 完成；下一项实际线程筛查；无并行或性能采用结论 |
| 2026-09-17 / 新 8.2.3 | 从 `2e8e331` 接入共享只读输入、候选独立缓冲及同描述变体依赖；整候选串行/原生线程调用同一有界步骤，主线程保留取消、扩容和原序消费 | 必要业务 78 项及架构 97 项通过；实际 256 描述、33 批、3 个正常前缀代次，6 模式各 5 热样本全部输出相同。串行批量 0.146220 秒优于单条 0.200562 秒；并行 1/2/4/8 为 0.205066/0.255566/0.304550/0.364021 秒。精确树见提交正文 | 试点完成；不采用本版并行、不扩大线程组；下一项验证串行组织端到端收益 |

实施时在此追加实际改动、完整命令、退出码、测试数量、耗时、证据路径、首个差异、偏差及下一步；不得把计划描述复制成“已完成”。

### 10.2 实施与提交登记

| 步骤 | 实施前提交 | 实际提交 | 实际验证 / 证据 | 当前状态 |
|---|---|---|---|---|
| 本计划文档 | `39a50f8` | 以本项 Git 历史为准 | 文档与保护检查见第 10.1 节，精确导出复验见提交正文 | 文档已完成；不算阶段 0 完成 |
| 0 | `2539ab0` | `52c5e82` | [全流程基线与覆盖](evidence/apsgo_v7_unified_numeric_solver/stage0.md)，双树各 155 项，完整比较退出 0 | 已完成 |
| 1.1 | `52c5e82` | `44c426c` | [共用数据与工作区](evidence/apsgo_v7_unified_numeric_solver/stage1.md)，双树各 156 项 | 已完成 |
| 1.2 | `44c426c` | `473596c` | [公共链操作与索引](evidence/apsgo_v7_unified_numeric_solver/stage1.md#12-公共链操作与代次索引)，双树各 165 项 | 已完成 |
| 2.1 | `795a107`（前置修复） | `6f64c98` | [私有桥接与真实原型对照](evidence/apsgo_v7_unified_numeric_solver/stage2.md)，双树各 196 项 | 已完成 |
| 2.2 | `6f64c98` | `1519292` | [私有拆片与分隔材](evidence/apsgo_v7_unified_numeric_solver/stage2.md)，双树各 205 项 | 已完成 |
| 3.1 | `1519292` | `2996e37` | [公共视图与权威评价](evidence/apsgo_v7_unified_numeric_solver/stage3.md)，双树各 264 项 | 已完成 |
| 3.2 | `2996e37` | `93b2e25` | [统一候选计算](evidence/apsgo_v7_unified_numeric_solver/stage3.md#32-统一单次候选计算)，双树各 151 项 | 已完成 |
| 3.3 | `93b2e25` | `1463fd8` | [原序消费与发布](evidence/apsgo_v7_unified_numeric_solver/stage3.md#33-原序消费与原子发布)，双树各 310 项 | 已完成 |
| 4.1 | `1463fd8` | `74e2d12` | [有序连接图与路径覆盖](evidence/apsgo_v7_unified_numeric_solver/stage4.md)，双树各 132 项 | 已完成 |
| 4.2 | `74e2d12` | `d7f9715` | [初始链构造](evidence/apsgo_v7_unified_numeric_solver/stage4.md#42-初始链数值构造)，双树各 151 项 | 已完成 |
| 5.1 | `d7f9715` | `39d956c` | [首轮公共候选接线](evidence/apsgo_v7_unified_numeric_solver/stage5.md)，精确树结果见提交正文 | 已完成 |
| 5.2 | `39d956c` | `d8de4dd` | [两类拆单及唯一重放](evidence/apsgo_v7_unified_numeric_solver/stage5.md)，精确树结果见提交正文 | 已完成 |
| 6.1 | `d8de4dd` | `f631653` | [精修数值描述与归属](evidence/apsgo_v7_unified_numeric_solver/stage6.md)，精确树结果见提交正文 | 已完成 |
| 6.2 | `f631653` | 以本项 Git 历史为准 | [精修与回收公共接线](evidence/apsgo_v7_unified_numeric_solver/stage6.md#62-精修与回收公共候选接线)，精确树结果见提交正文 | 已完成 |
| 7.1 | `6b457b9` | `927f650` | [全流程边界与窗口](evidence/apsgo_v7_unified_numeric_solver/stage7.md)，精确导出累计 4078 项/212.72 秒 | 已完成 |
| 7.2 | `927f650` | `e1af523` | [完整结果、两组性能及有界热点](evidence/apsgo_v7_unified_numeric_solver/stage7.md)，精确树见提交正文 | 已完成；性能未改善，释放修复见 7.3 |
| 7.3 | `e1af523` | `e8959ee` | [生命周期与完整结果复测](evidence/apsgo_v7_unified_numeric_solver/stage7.md#73-原生生成器提前终止释放)，精确树见提交正文 | 已完成 |
| 8.1 | `e8959ee` | 本项提交，身份由 Git 历史记录 | [公共批次及真实有序窗口](evidence/apsgo_v7_unified_numeric_solver/stage8.md)，精确树见提交正文 | 已完成；未并行 |
| 8.2 | 从 `57a4543` 开始 | 按三个工程单元记录 | 三个单元完成 | 试点完成，无并行提速 |
| 8.2.1 | `57a4543` | 本项 Git 历史及提交正文 | 共享 197 项/134.17 秒；精确树同范围见提交正文 | 已完成；真实前缀和 2 万窗口字节一致 |
| 8.2.2 | `64ea669` | 本项 Git 历史及提交正文 | 共享 205 项/220.08 秒；精确树同范围见提交正文 | 已完成；普通结构整候选原生计算及真实同序窗口 |
| 8.2.3 | `2e8e331` | 本项 Git 历史及提交正文 | 共享必要业务 78＋架构 97 项；精确树同 175 项见提交正文；真实 256 描述热点筛查 | 已完成；默认尚未切换 |
| 8.3 | 待执行时记录 | 无 | 未执行 | 未开始 |
| 9.1 | 待执行时记录 | 无 | 未执行 | 未开始 |

后续小型证据目录拟用 `docs/implementation/evidence/apsgo_v7_unified_numeric_solver/`，实际运行产物拟用 `diagnostics/unified_numeric_solver/`；只在真实执行对应阶段时建立，不先生成空证据，不覆盖既有诊断。每个运行目录须是新目录，记录真实输入、代码和执行条件。

## 11. 文档修订记录

2026-09-17 / v0.22：记录整候选同内核真实线程筛查及无并行收益；下一项只扩大有收益的串行批量组织，默认采用仍需实际窗口/完整结果验证。原实验分级及同标准边界不变。

2026-09-17 / v0.21：记录整候选有界原生入口、代次只读输入和私有控制/输出缓冲、原错误与取消边界；新 8.2.2 验证完成，下一项线程试验。拆单原扣额边界及整个算法不变。

2026-09-17 / v0.20：将新 8.2 细分为有界原生修复、整候选接线和线程筛查三个工程单元；记录首单元实际验证。范围、算法、实验门槛与完整测试频率不变。

v0.19（2026-09-17）：登记新 8.1 代次内缓冲/参数复用、225 项必要范围、真实单条/8 条对照及工作区分配统计；进入整候选原生调度和并行试点，不提前称已编译整个候选或已提速。

v0.18（2026-09-17）：登记 7.3 六类原生生成器释放、252 项必要回归及真实窗口/完整结果对照；进入新 8.1，不把生命周期修复当作整体提速。

| 版本 / 日期 | 修改内容 | 代码状态 |
|---|---|---|
| v0.1 / 2026-09-16 | 根据统一数值设计建立阶段说明、依赖、必要验证、分级实验、执行记录和提交表；保留旧执行检查点及历史问题 | 所有新代码步骤未开始 |
| v0.2 / 2026-09-16 | 持续实施授权及阶段 0 全流程基线、真实调用覆盖、诊断/业务计数边界 | 阶段 0 完成；下一项 1.1，新公共层未接线 |
| v0.3 / 2026-09-16 | 共用描述、方案读取及私有缓冲契约、状态和动作权威归属 | 1.1 完成；下一项 1.2，未接主流程 |
| v0.4 / 2026-09-16 | 公共编译链操作、代次索引、穷尽小例及精确架构保护 | 阶段 1 完成；下一项 2.1，未接主流程 |
| v0.5 / 2026-09-16 | 公共边公式、私有桥接和真实原型对照；用户确认的旧非自适应错误单独修复 | 2.1 完成；下一项 2.2，未接私有资源主流程 |
| v0.6 / 2026-09-16 | 私有拆片、分隔材、工时精确分配和扩展事件；真实资源对照与边界验证 | 阶段 2 完成；下一项 3.1，未接主流程 |
| v0.7 / 2026-09-16 | 公共视图的零拷贝字段/链读取、统一完整评价与缓存保护 | 3.1 完成；下一项 3.2，候选准备未替换 |
| v0.8 / 2026-09-16 | 统一单次候选、原动作数值组合、显式阶段政策与资源/修复对照 | 3.2 完成；下一项 3.3，主流程未切换 |
| v0.9 / 2026-09-16 | 原序消费/第二变体扣额/错误延迟、按原事件正式物化、同内核复核及失败零发布 | 阶段 3 小例闭环；下一项 4.1，主流程未切换 |
| v0.10 / 2026-09-16 | 接通数值有序邻接、稳定排序、队列/栈遍历及真实图/匹配/路径对照 | 4.1 完成；下一项 4.2，搜索未切换 |
| v0.11 / 2026-09-16 | 初始链原构造政策编译、公共切片/一次物化、取消与数值保护及真实初始结果一致 | 阶段 4 完成；下一项 5.1，搜索未切换 |
| v0.12 / 2026-09-16 | 首轮四类动作共用候选与发布，原直接预检/扣额和真实阶段/接受身份一致 | 5.1 完成；下一项 5.2，拆单和精修未切换 |
| v0.13 / 2026-09-16 | 两类拆单公共准备/原扣额后续算、期锁和唯一重放接线，真实完整前缀一致 | 阶段 5 完成；下一项 6.1，精修与完整性能待验证 |
| v0.14 / 2026-09-16 | 精修原生描述/平铺归属/有界续算及真实 2 万次有序窗口通过 | 6.1 完成；下一项 6.2，元数据过渡边界待移除 |
| v0.15 / 2026-09-16 | 精修/回收公共候选与原序消费接线，撤除生产旧准备；真实窗口业务一致、物理计数单列 | 阶段 6 完成；下一项 7.1，完整验收尚未完成 |
| v0.16 / 2026-09-17 | 全流程公共调用与独立审核见证；追踪入口改挂公共消费，单条/8 条窗口及累计回归通过 | 7.1 完成；下一项 7.2，完整质量/性能尚未完成 |
| v0.17 / 2026-09-17 | 完整同标准结果/轨迹及新旧各三次热样本；有界剖析定位准备成本和生成器数组保留，增加 7.3 修复单元 | 7.2 完成但未提速；先修复释放再进入新 8.1，不启动并行 |
