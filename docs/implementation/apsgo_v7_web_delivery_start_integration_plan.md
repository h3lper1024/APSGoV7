# Web 月计划开始时间与交期目标接入实施计划

## 1. 已确认范围

2026-09-17 用户批准前后端完整接入。后端基线 `5d41729`，前端
`APSGoWeb/master@0fe9b0b`。前端已有未提交的表格、导入及工序最早日期改动，
本项保留；只提交本项新增差异，不代为提交已有改动。Ponytail 关闭。

求解弹窗选择北京时间，首次建议计划月 1 日 00:00，可修改；必须显式提交。
求解请求增加开始时间、逐单交期与两种速度。真实订单优先有效炉区速度，再取
备用工艺速度；虚拟材使用已确认的 100 米/分钟。禁止缺失值静默当零工时。
交期截止仍为当天 24 点；拆单完成仍取最后片段，沿用数值求解的毫秒时钟及秒级评分。

## 2. 接口与规则边界

- 保持求解 URL，新增显式 `v7-month-solve-v2` 请求版本；v1 历史请求不伪装支持交期。
- v2 顶层 `schedule_start_at` 为带时区的时间文本；订单增加 `due_date`、
  `furnace_speed_mpm`、`process_speed_mpm`。开始时间归一为北京时间。
- v2 仅使用已经启用交期的九级规则版本及已验证数值求解，不临时隐式改库。
  顺序为禁止数、禁止严重度、欠重数、欠重缺口、旧欠清空、等待/延期负担、宽差、虚拟重、链数。
- 新规则模板只扩展交期及数值投影；旧历史版本继续可读。保存/启用继续使用原有事务、
  乐观版本检查、幂等标识和字典继承。升级创建新版本，不修改历史记录。
- 返回实际开始时间、交期报告和基于同一已审核节点时钟的最晚日期；第四步只回填
  求解返回的日期，不另选起算日期、不重算另一条连镀时钟。前序工序沿用现有配置倒推。
- 活动库升级前用 SQLite 在线备份保存到 APSGOV7-bak；验证副本后才操作实际库。
  正式 8001 服务属于用户运行进程，不擅自中断；如切换需重启，届时请求确认。
- 不改算法搜索、目标排序、40 万额度、300 秒搜索配置；不进行整组性能实验。

## 3. 阶段与提交表

| 阶段 | 完整名称 | 依据/依赖 | 计划提交信息 | 状态/提交 |
|---|---|---|---|---|
| 1 | 后端开始时间、九级规则和权威日期接线 | 第 1～2 节 | `#feat 接通月计划开始时间与交期求解` | 已完成：`bc54444`；精确树 488 项通过 |
| 2 | Web 求解时间选择与日期回填 | 阶段 1 接口 | `#feat 接入求解开始时间选择和交期日期` | 已完成：前端 `299d398`；保留用户原有工作 |
| 3 | 备份、规则升级与手动验证交接 | 阶段 1～2；用户最新授权仅备份与启用 | `#feat 备份规则库并启用九项目标` | 正式库备份及版本 15 启用完成；服务启动和测试交由用户，本项提交身份见 Git 历史 |

## 4. 必要验证

阶段 1 集中验证时间/时区/交期/速度错误、规则新旧回读与保存、实际数值求解、
报告和日期一致；阶段 2 验证请求投影、取消/重复提交/禁用、日期不可另改及构建。
提交前精确暂存树同范围验证。最终使用一次真实 531 单完整求解验证双审计与九级目标、
选定开始时间和逐节点日期一致；测试使用数据库副本和隔离应用实例，前端精确树浏览器测试用独立端口。

## 5. 执行记录

- 准备：macOS，Python 使用 Conda `aps_3.10.18`。8001 当前由用户 Python 服务占用；
  前端 5173 已运行。后端工作树干净，前端已有差异与 App.vue 原内容已存临时保护副本。
- 接口、日期和前端已经实现；真实 531 单经 HTTP 路由测试完成，双审核通过，551 节点
  日期同源一致。300 秒搜索截断于 382925 次检查，结果不作质量改善结论。
- 集中回归、精确树和浏览器记录见[专项证据](evidence/web_delivery_start_integration.md)及两库提交正文。
- 2026-09-17 用户确认仅执行数据库备份和九项目标启用，服务由用户启动，测试由用户进行。
  已通过既有升级工具备份正式库并新建活动版本 15；回读确认原版本 1～14、已有业务规则
  和虚拟材配置未改变，新版本包含 18 条定义、9 项评分目标，数据库完整性正常。
  备份：`/Users/miles/dev/dev-py/APSGOV7-bak/apsgo_v7_rules_before_delivery_v14_20260917_045558.sqlite3`。
  本轮未启动服务，未运行测试或求解；上一轮测试记录保持历史含义，不作为本次手动验收。
