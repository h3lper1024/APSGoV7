# APSGo V7 链间宽差优化实施计划

## 1. 当前状态与边界

| 项目 | 内容 |
|---|---|
| 版本 / 日期 | v0.6 / 2026-09-04 |
| 状态 | 用户已授权持续实施；步骤 0～3 已提交，步骤 4 实施及验证中；步骤 5～7 待执行 |
| 唯一专项设计 | [链间宽差优化专项设计](../design/apsgo_v7_inter_chain_width_optimization_design.md) |
| 工作目录 / 分支 | `/Users/miles/dev/dev-py/APSGOV7` / `codex/inter-chain-width-optimization` |
| 实施前基线 | `bb2a31bf49e40e739edc7b6b11be620f6561549d`；来自 V6 `6536acd` 的已提交文件 |
| 基线保护 | `main` 保持迁入版本；不修改 V6 原仓库、不推送远端 |
| 已核验平台 | macOS / Darwin arm64 / zsh |
| Python 环境 | Conda `apsgo_v6_3.10.18`；每次实际执行前核验 |

本计划只实施：合规方案上的宽差节点/连续段调整、切链与联合安置、共享预算下的集中精修及对应验证。不是重新实现完整求解器。

不改七级顺序及公式，不恢复链数上限，不放宽规则，不改变原受控订单拆分资格，不增加第二轮受控拆单、不新建评分器或资源台账。数据库、前端、HTTP、交期评分及多资源层次均不在本专项内。

“切链”是把既有节点分配到更多链；“订单拆分”是改变一个订单的重量分区。代码、计数、证据和报告不得混用这两个名称。

## 2. 固定依据与现状

| 依据 | 本次用途 |
|---|---|
| `tests/baselines/gqga4/inputs/` 的五份输入 | 冻结输入，不从历史排程导出反推新求解输入 |
| `tests/baselines/gqga4/gqga4_rule_set_spec.json` | 保持现有规则和七级顺序；宽差第 5，虚拟重量第 6，链数第 7 |
| `tests/baselines/gqga4/gqga4_solver_policy.json` | 种子 590531、检查上限 200000、总时间 180 秒、收尾预留 10 秒不变 |
| `tests/baselines/gqga4/quality_gate.json` | 零禁止、零欠重、来源守恒、虚拟比例等；链数无上限 |
| `tests/baselines/gqga4/performance_gate.json` | 20 次外层样本的中位数及第 95 百分位不超过 180 秒；参考控制漂移另核验 |
| [最近复测及报告](evidence/solverpy_path_cover_local_search/function_22_inter_chain_width_gap/step_22_6_priority_rerun/README.md) | 历史七级 `(0,0,0,0,11218,320,22)`；保持原产物和身份 |

当前接受器已经能接受宽差改善而链数增加的完整候选；缺的是实际生成此类候选的搜索动作。固定当前 22 条链内容、方向和所属期时，11218 mm 已达到整链调序的数学下界，证明与样本见专项设计第 3 节。

本次收益判定必须调用真实候选生成器及公共求解入口；不能重复仅验证“手工候选能被接受”，更不能把原 2739 项累计通过称为新搜索已实现。

## 3. 计划修改文件

下表是代码实施时的范围，不表示本轮已经修改。

| 文件或范围 | 工作内容 | 不改的边界 |
|---|---|---|
| `src/apsgo_scheduler/core/neighborhoods.py` | 扩展唯一候选入口的宽差阶段保护；必要时提取无提交枚举 | 原调用默认行为、旧阶段计数和受控拆单授权不变 |
| `src/apsgo_scheduler/core/width_optimization.py`，待新建 | 节点/段/切链候选、分批扫描、集中精修入口 | 不定义新领域状态或算子注册平台 |
| `src/apsgo_scheduler/core/solver.py` | 原受控拆单及其一次重放之后、核心审计之前，接入集中精修 | 原构造和修复过程、审计及发布边界保持 |
| `tests/core/search/test_width_optimization_*.py`，按功能新增 | 实际生成候选、保护、预算、重启、期归属等测试 | 不降低旧测试断言，不抹掉历史阶段期望 |
| 现有核心/公开结果/规则集/审计相关测试 | 对新入口及额外阶段观测补回归 | 不把所有旧轨迹强制改成新轨迹 |
| 本计划、本设计、`AGENTS.md`、本项新证据 | 进度、决策和实测记录 | 不覆盖 22.6 历史排程或图表 |

`core/chain_order.py`、`core/virtual_material.py`、规则、评价、资源事实及审计优先原样复用。若确需改动，只允许由具体复用接线或发现的根因驱动，并在该步骤记录精确范围；不得扩大成无关重构。

## 4. 分步实施与提交

每行一个可独立验证的提交单元；步骤编号始终附中文名称，避免只说代号。

| 步骤 | 功能与参照设计 | 修改及完成标准 | 状态 |
|---|---|---|---|
| 0：基线与反例测试 | 设计第 2、3、9 节 | 固定 `main@bb2a31b` 与正式输入/配置身份；保存旧入口的完整对照，建立“切分单独变差、联合安置改善”“零欠重也需移动”的小样本。先登记基线的生成能力缺口，新动作的实际生成断言随对应步骤落地，不用长期跳过或预期失败冒充功能通过 | 已完成，`4a02466` |
| 1：完整候选保护与扫描骨架 | 设计第 6、7 节 | 新增默认关闭的 `width_optimization_only` 参数；候选无违规、前置质量相同、宽差严格降低、全部旧节点保全、新桥按工厂期望核对。枚举器先出轻量描述，扣共享额度后才构造；按剩余额度分批，类内移动/交换交替。测试自然结束、续扫、重启和真实截断，尚不接公共入口 | 已完成，`cddfa3a` |
| 2：单节点移动与交换 | 设计第 5.1、5.2 节 | 实现两条合规链之间的原子移动/交换，复用连接桥与接受器；真实枚举器从零欠重输入找到改善，供体断口与目标接口都校验 | 已完成，`5e5eeb8` |
| 3：连续段移动与交换 | 设计第 5.1、5.3 节 | 扩展到有真实节点的连续段，保持内部原序；覆盖首/尾段与中段交换，不能只做端点集合不变的尾段互换 | 已完成，`eaaf1db` |
| 4：切链与联合安置 | 设计第 3.3、5.4 节 | 一次切链后联合枚举两段的合法位置，生成稳定新链身份；真实生成器能找到 2 链变 3 链、宽差降低的完整候选；不改变任何订单拆分计数 | 实施及验证中 |
| 5：集中精修接入 | 设计第 4、5.5、7、8 节 | 接入四类分批扫描与接受后重启；原修复/受控拆单完成后只进入一次精修，旧受控拆单仍最多一轮和一次旧搜索重放；停止原因、阶段耗时、轨迹及规则关闭回归通过 | 待执行 |
| 6：审计、真实收益与报告 | 设计第 6、8、9 节 | 在相同正式配置下完整复测；无违规和守恒保持，宽差较 11218 严格降低；保存新结果/轨迹/边界/报告，逐项核对两层审计。未降则保持“收益未证实”并分析，不进入完成声明 | 待执行 |
| 7：确定性与性能收口 | 设计第 9、10 节 | 完成固定输入重复运行、目标关闭及其他规则集回归、原门槛要求的 20 次外层性能样本；根据证据判断是否可合并，未获用户批准不合并 `main` | 待执行 |

建议提交信息依次为：

- `#feat 固定链间宽差优化基线与反例`
- `#feat 增加宽差候选保护与分批扫描`
- `#feat 实现合规链间节点移动与交换`
- `#feat 实现链首尾连续段调整`
- `#feat 实现切链与两段联合安置`
- `#feat 接入集中链间宽差精修`
- `#fix 核验宽差优化收益并更新专项报告`
- `#fix 完成宽差专项确定性与性能验证`

若某步发现业务定义或设计有实质变化，先修订并确认，不把新增行为夹在测试适配中。没有失败项不能编造修复；提交前缀按实际变更选择 `#feat` 或 `#fix`。

## 5. 必须验证的案例

| 类别 | 必测内容 |
|---|---|
| 评分 | 前四项相同，宽差减少可接受更多链/更多桥；宽差改善不能补偿前置项退化或规则违规；全部同分拒绝；宽差仍按声明定位 |
| 实际生成能力 | 不是手工注入候选：单节点、连续段、切链联合安置分别由生产枚举器找到改善；至少一个样本增加链数 |
| 节点和段 | 供体不再欠重、接收链不超重；边界、内部新接口、连续规则；首/尾与中段互换；内部无效同分动作不冒充收益；段不反转 |
| 切链 | 两段都含真实节点且合规；原位切分变差但联合安置改善；覆盖两段相对先后和其他链夹在两段之间；新 ID 确定性、碰撞保护和拒绝无序号消耗 |
| 期与未来借用 | 输入非字典序、非固定四期、空期；切出未来来源段后归属变化；借用重新派生但不评分；跨期外侧边界全部更新 |
| 旧拆片与虚拟 | 已有拆片目标期锁；全部旧节点、隔离材属性和谱系原样保留；虚拟首尾计宽差；新桥私有编号、多接口连续序号、比例和完整链规则；向共享接受器直接注入错误用途、关联分区、原型字段、温区、接口锚点或超长新桥时拒绝 |
| 候选安全 | 空链/纯虚拟链在模型构造前拒绝；新链不填入旧受影响链列表；未影响链内容/相对顺序不变；结构动作不能误用纯调序保护 |
| 分批与重启 | 无接受且时间/额度足够时，第一类未扫完也轮到段、切链、调序；大量移动未扫完时交换仍获得机会；桥接失败、重量失败和宽差预筛失败也计一次检查并让出批次；无接受时保留位置，接受后重建候选，同一状态全部自然扫完才完成 |
| 停止 | 额度 0、额度不足一整轮、刚好到上限；桥接中、评价前后、提交前取消/截止；不得重置真实停止或免费补跑；自然完成只在阶段边界续行 |
| 兼容与接线 | 目标关闭/诊断模式保持原行为；残余违规跳过精修；前置修复和拆单轨迹不被新阶段占用预算改变；无第二轮拆单，无重复审计 |
| 最终一致性 | 无缓存审计、公开结果自检、真实订单与片段数、原始重量守恒、完整边界明细与第 5 项一致；切链不增加订单拆分计数 |

新增桥的快速连接可过而链级连续规则失败，必须作为专门反例。最终审计仍不可替代，但也不能把明显违规候选全部留到最终审计才发现。

## 6. 验证命令与证据边界

### 6.1 每次提交前

在 V7 工作目录运行本项专项测试及仓内累计：

```bash
PYTHONDONTWRITEBYTECODE=1 conda run --no-capture-output -n apsgo_v6_3.10.18 python -m pytest -p no:cacheprovider tests/architecture tests/api tests/app tests/core -q
git diff --check
```

先按本项精确白名单暂存，再导出暂存树；以下变量只是临时验证路径，不改变环境目录或项目根目录：

```bash
set -e
set -o pipefail
v7_width_tree=$(git write-tree)
v7_width_export=$(mktemp -d /tmp/apsgo-v7-width-check.XXXXXX)
git archive "$v7_width_tree" | tar -xf - -C "$v7_width_export"
cd "$v7_width_export"
PYTHONDONTWRITEBYTECODE=1 conda run --no-capture-output -n apsgo_v6_3.10.18 python tools/check_workspace_residuals.py --verify
PYTHONDONTWRITEBYTECODE=1 conda run --no-capture-output -n apsgo_v6_3.10.18 python -m pytest -p no:cacheprovider tests/architecture tests/api tests/app tests/core -q
```

最终导出还执行本项同范围专项测试。原残留工具要求 V6 共享目录历史文件存在，因此 V7 只在干净导出中调用该检查；不能复制旧残留来凑齐条件，也不能把干净导出检查称为旧共享工作区检查通过。

暂存文件清单、源代码/配置保护、编译和构建范围继续遵守 `AGENTS.md`。此前纯文档提交也执行了规定的累计回归，但那只验证原代码未受影响，不能证明尚未实现的宽差算法有效；后续每步必须执行其实际新增功能测试。

### 6.2 真实 GQGA4 对照

步骤 0 和步骤 6 分别从 `main@bb2a31b` 与新代码的明确提交导出，运行各自版本，保持相同正式输入、规则、种子和预算。不用旧排程当新求解输入，也不在同一进程混载两个版本。

复用已有 `function_21_complete_acceptance/quality_precheck.py` 的公共入口和防覆盖检查。下面是步骤 0 在**基线导出目录**中可执行的命令示例，实际运行指令及输出路径见第 8 节和证据记录；输出目录必须事先不存在：

```bash
PYTHONDONTWRITEBYTECODE=1 conda run --no-capture-output -n apsgo_v6_3.10.18 python docs/implementation/evidence/solverpy_path_cover_local_search/function_21_complete_acceptance/quality_precheck.py --code-repository /Users/miles/dev/dev-py/APSGOV7 --code-revision bb2a31bf49e40e739edc7b6b11be620f6561549d --output-dir /tmp/apsgo-v7-width-baseline-run-01
```

`--code-repository` 指向保存提交对象的 V7 Git 仓库，不能省略后让工具在没有 `.git` 的导出目录查找提交；实际执行的生产文件仍来自各自导出。新版本使用其真实提交和新输出目录，不沿用基线身份。若工具目前只识别旧阶段，先在独立证据工具中适配阶段观察，不给生产增加另一条求解入口。质量、图表与性能工具保留已验证的数值/输入身份处理；不能直接套用只在 V6 Git 历史中存在的 SHA 查询 V7 仓库。参考脚本身份核验仍依赖外部冻结文件，其可用性在运行前单独检查。

每次新结果保存至独立 `docs/implementation/evidence/apsgo_v7_inter_chain_width_optimization/` 子目录，至少保留：

- 代码、问题、规则、策略、输入文件、结果及轨迹身份。
- 完整排程、逐链重量、全部节点和来源守恒明细。
- 七级质量、违规、借用统计、虚拟节点及拆单事实、两层审计。
- 旧修复结束、新精修开始/结束的质量与累计计数；各类候选、完整评价、接受、批次和停止原因。
- 每个相邻链边界的实际端点、宽度和绝对差；总和、最大单次差及链数变化。
- 单次公共服务时间与外层全流程时间分列，真实指令、退出码及首个差异完整记录。

新报告另建 `report` 目录，复用现有绘图工具，保持真实发布顺序，不覆盖 22.6 报告。不得为了图形更平滑而在展示层另行排序。

### 6.3 收益和性能判定

`main` 是本专项算法收益的对照对象；原 `solver.py` 是既有性能门规定的参考控制，两者不能互相替代。历史 103.485174 秒是单次公共服务观测，不是 V7 同机成对性能基线。

真实新结果需保持全部正式质量门通过，并且宽差严格低于对照的 11218 mm，才可声称本专项实现了实际改善。若相同配置的基线复跑与历史不一致，先解释环境、停止原因和身份差异，不能自行换一个更差结果作对照。

步骤 7 保留原性能门要求的预热、20 次样本、交错参考控制、漂移检查和全部样本；中位数及第 95 百分位均不超过 180 秒。算法收益对照与性能控制分别报告。候选预算相同不代表两个算法探索了相同候选，也不保证时间截断时结果相同。

确定性沿用原实施计划第 8.22.3 节：相同代码、输入、规则、策略、种子及检查上限下，GQGA4 至少连续运行 3 次；未触发墙钟停止或外部取消时，结果指纹、轨迹指纹、检查数、停止原因和最终质量必须完全一致。时间截断样本保留但不作为确定性通过证据，也不擅自提高正式时间上限来补出合格样本。

## 7. 风险、恢复与完成条件

| 风险 | 应对与诚实状态 |
|---|---|
| 细粒度搜索用尽额度，切链没有机会 | 按设计分批并保留位置；持续接受小动作或桥接时钟截断仍可能使后续类未访问，报告实际覆盖，不用“支持该动作”代替运行覆盖 |
| 多次全量评价导致时间增加 | 先用既有边缓存、必要接口桥和宽差预筛；热点有证据后再做等价优化，不擅改 180 秒门槛 |
| 当前结构需要多步同分/暂劣才能改善 | 本版仍可能受限；收益不达标时分析证据，另行确认更大结构重建，不悄悄引入随机接受 |
| 对切链、订单拆分或虚拟清理混淆 | 链切分只变链；旧节点全部保留、拆单授权不变；不新增虚拟清理动作 |
| 原档案被覆盖、身份错配 | 新证据独立目录；按提交导出并校验输入/代码身份，图表只读新审计结果 |

回退以 `main@bb2a31b` 为稳定入口，优先另行导出或检出；不得用破坏性重置抹掉用户未提交内容。不自动合并 `main`，是否合并由用户在验收后决定。

完成需同时满足：真实枚举能力测试、安全/流程测试、GQGA4 实际宽差改善、规定性能及确定性验证。任何一层未完成都单独列出，不能用“代码写完”替代“效果达到”。本专项不自动关闭原功能 21 的其他未完成验收。

## 8. 执行记录

### 2026-09-04：仅文档更新

- 实施前提交 `bb2a31b`；macOS / Darwin arm64，工作分支核验正确，开始时工作区干净。
- 已核对候选接受、旧动作、期序、受控拆单、虚拟生成和最终审计调用链；独立复核固定链下界及原 CSV 身份。
- 新建本计划及专项设计；原两份 V6 文档仅补 V7 衔接指针，`AGENTS.md` 更新当前专项入口。
- 本轮不改 `src/`、测试、工具、规则配置、输入或原结果；步骤 0～7 均尚未实施，没有新算法收益或性能结论。
- 开始前当前提交干净导出残留检查通过，退出 0。文档链接、范围和累计回归在提交前核验；实际命令、数量、耗时与最终暂存树由本次提交正文记录，避免在文件中自引用提交 SHA。

### 2026-09-04：步骤 0，基线与反例测试

- 用户已授权持续按计划推进；实施前 `5de1d1d`，`main` 仍固定为 `bb2a31b`，不改 V6 原仓库或远端。
- 从 `main` 独立导出至 `/tmp/apsgo-v7-width-baseline.SoDSSz`，原残留检查 `clean_export` 通过；使用该副本的唯一公共入口、正式五输入/规则/策略运行，退出 0。
- 基线七级 `(0,0,0,0,11218,320,22)`，94518 次检查、2627 次完整评价、50 次接受、2 次同期间拆单；109.671247 秒公共服务后自然结束，双审计、零禁止、零欠重、来源及重量守恒通过。与 22.6 历史结构、轨迹和计数一致；服务时间不是成对性能验收。
- [独立基线证据](evidence/apsgo_v7_inter_chain_width_optimization/step_00_baseline/README.md)保留全部排程、资源事实、两层审计、轨迹、逐链重量及来源明细，并冻结代码/输入/规则/策略身份。原档案不改。
- `tests/core/search/test_width_optimization_baseline.py` 新增 3 项通过：旧固定搜索不能生成改善、原位切链拒绝而联合安置允许增链、零欠重旧节点动作不搜索但手工移动可被完整接受。它们不代表新枚举器已经实现；没有跳过或预期失败测试。
- 反例只启用真实链重、逆宽和链间宽差规则，并采用正式七级顺序，不冒称完整 GQGA4 规则覆盖。测试首轮 2 项失败来自未来归还计数字段误名，已改为现有 `accepted_future_borrow_return_count`，最终专项 3 项通过（0.53 秒）。
- 本项不修改生产代码/配置；共享树累计 2742 项通过、181.99 秒，专项 3 项和 `git diff --check` 通过。可选 Ruff 检查因项目环境未安装模块而未执行（命令退出 1），未新增依赖。最终暂存树同范围验证和残留检查按第 6.1 节执行，实际数量、耗时、树身份见本项提交正文；步骤 1～7 尚待执行。

### 2026-09-04：步骤 1，完整候选保护与扫描骨架

- 步骤 0 已提交 `4a02466`；其最终导出累计 2742 项通过、173.53 秒，专项 3 项通过、0.50 秒，原残留检查 `clean_export` 通过。步骤 1 开始前再对当前提交独立导出，残留检查通过。
- 仅修改唯一候选接受器，新增默认关闭的 `width_optimization_only`；旧默认及纯链序保护保持，新增桥以同一对旧接口锚点物化比较，两桥不串改第二个桥的锚点。所有旧节点保持完整，旧虚拟温区不因移动被重造。
- 新增 `core/width_optimization.py` 的私有扫描骨架：轻量描述交替、按剩余额度分批、拒绝后续扫、接受后全部重建；只复用共享预算和现有状态。候选描述产生后先扣检查，再进行业务构造；恰好用完但没有后续候选可自然完成，确有下一候选则按原预算报告截断。
- 本阶段没有真实节点/段/切链枚举器，没有新的公共求解阶段，也不更改旧阶段轨迹。共享骨架初次专项 22 项通过、0.58 秒；独立只读复核同范围 22 项通过、0.53 秒。
- [专项证据与验证范围](evidence/apsgo_v7_inter_chain_width_optimization/step_01_guard_and_scan/README.md)记录边界；共享树最终聚焦 156 项通过（0.81 秒）、累计 2837 项通过（178.33 秒），退出均为 0。复用本机已有 Ruff 0.12.0，四个变动 Python 文件风格和格式检查通过，未增加依赖。最终暂存树按同范围复验，实际数量、耗时、树身份见本项提交正文；不提前声称 GQGA4 收益。

### 2026-09-04：步骤 2，单节点移动与交换

- 实施前 `cddfa3a`；步骤 1 最终导出专项 156 项通过（0.79 秒）、累计 2837 项通过（177.24 秒），残留、Ruff 和导出编译检查通过。步骤 2 开始时工作区干净，当前提交独立导出残留检查 `clean_export` 通过。
- 在现有 `width_optimization.py` 中加入真实单节点动作描述和两链原子形成路径，移动与交换交替，链按最大相邻宽差排序，反向交换去重。不再要求接收链欠重；普通真实材、实际过渡材及已有拆片均按原样参与。
- 动作在扣共享额度后才判断角色、规范化期归属和预筛；宽差预筛复用现有规则，不写第二套公式。供体与接收方均有真实节点才构造模型；两链全部新接口按固定次序调用已有桥接，完整候选仍走唯一保护入口。
- 原始节点序列超重可提前拒绝；原始节点序列欠重不能一律提前拒绝，因为必要接口桥的重量可能恰好补足。新桥之后仍按同一链重规则检查全部违规，不主动补重，不硬编码 700/2000。
- 本步不接公共求解入口、不实现连续段枚举和切链、不重放拆单；专项实测及最终暂存树同范围验证见[步骤 2 证据](evidence/apsgo_v7_inter_chain_width_optimization/step_02_nodes/README.md)和本项提交正文。
- 新增 24 项实际节点案例首轮通过，节点/守卫/扫描合计 119 项通过（0.77 秒）；五节点综合扫描宽差 700→100，48 次检查、3 次完整评价/接受；交换专属样本 700→300，33 次检查、1 次完整评价/接受，均自然结束。独立只读复核未发现需要修正的生产问题；累计与最终导出同范围验证在提交前执行并写入提交正文，不替代 GQGA4 验收。

### 2026-09-04：步骤 3，连续段移动与交换

- 实施前 `5e5eeb8`；步骤 2 最终聚焦双树各 122 项通过，共享累计 2861 项/185.39 秒、最终导出 2861 项/186.13 秒，均退出 0。步骤 3 开始时工作区干净，当前提交干净导出原残留检查 `clean_export` 通过。
- 只在 `width_optimization.py` 增加连续段的轻量范围枚举；移动长度至少 2，交换两侧均非整链且至少一侧多于 1，包含单节点与多节点互换。按已确认的链排序、长度和、起点和交替顺序逐个生成，不预生成全部组合。
- 完整候选形成、规范化、桥接及接受全部复用步骤 2；不反转段、不删除段内旧虚拟、不扩大受控拆单。纯虚拟段和移走后没有真实节点的方案仍在扣次后正常拒绝。
- 本项不接公共流程，不提前实现切链。旧节点及扫描先行回归 46 项通过（0.67 秒）；实际连续段能力与双树聚焦/累计结果见[步骤 3 证据](evidence/apsgo_v7_inter_chain_width_optimization/step_03_blocks/README.md)及本项提交正文。
- 新增连续段专项 10 项通过（0.62 秒），连续段加节点 34 项通过（0.68 秒），首轮无失败。块专属样本的单节点族 30 次检查无改善，连续段族 42 次检查、1 次完整评价/接受后宽差 700→300 mm、两链各 700 吨并自然结束；旧虚拟随真实段保留、双向不等长交换、形状/顺序和同分预筛均验证。
- 累计首次命中架构门禁的字面数值 3 保护；未改门禁，改为直接排除节点族已覆盖的“一对一交换”，保持块描述集合及顺序。架构加块专项 75 项通过（1.04 秒）。首次导出保护被并行格式检查创建的 `.ruff_cache` 元数据阻止，退出 1；该副本不算通过，重新导出并先执行保护、后执行显式无缓存格式检查。最终聚焦及累计必须全部复跑，实际记录见本项提交正文。

### 2026-09-04：步骤 4，切链与两段联合安置

- 实施前 `eaaf1db`；步骤 3 最终双树各 132 项聚焦及 2871 项累计通过，导出保护在显式无缓存 Ruff 前后均通过。步骤 4 当前提交独立导出残留检查 `clean_export` 通过，开始时工作区干净。
- 在同一宽差模块增加切点与两个最终位置的轻量枚举，前段保留旧链 ID，后段身份由动作、旧链、切点及有序节点身份派生。碰撞采用现有受控拆单的确定性前置下划线方式，不新增全局序号。
- 消费检查后才形成两段，先确认各有真实节点、各自规范化来源期并满足链重；在同一完整排列中安置两段，其余旧链相对顺序保持。非法跨期原始位置直接拒绝、不排序为重复方案；设计第 5.4 节补明原始描述与合法候选的计费区别，不改变已确认候选能力或接受条件。
- 切链没有新增链内接口，不调用桥工厂、不补重、不拆订单重量、不修改原有虚拟或拆单计数；旧拆片期锁继续由唯一接受器核验。实际生成、身份/期序/预算及最终聚焦累计记录见[步骤 4 证据](evidence/apsgo_v7_inter_chain_width_optimization/step_04_cut/README.md)与本项提交正文。
- 新增切链专项 25 项，与节点/连续段合计 59 项通过（0.68 秒），首轮无失败；架构加既有两类动作 99 项通过（0.98 秒）。真实生成扫描将设计样本 700→400 mm、2→3 链，第 8 次检查接受、最终 32 次检查/1 次完整评价并自然结束。独立只读复核节点守恒、碰撞、期序及所有停止原因，无待修生产问题；双树最终聚焦/累计在提交前运行并记录。
