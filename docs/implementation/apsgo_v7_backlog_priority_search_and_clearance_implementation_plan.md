# APSGo V7 旧欠订单搜索机会与尽早清空实施计划

## 1. 当前状态与执行边界

| 项目 | 内容 |
|---|---|
| 版本 / 日期 | v0.5 / 2026-09-14 |
| 状态 | 阶段 0～4 已实现；新九级专项验证及精确导出结果见阶段 4 记录，阶段 5～6 未执行 |
| 设计依据 | [旧欠订单搜索机会与尽早清空专项设计](../design/apsgo_v7_backlog_priority_search_and_clearance_design.md)，下文“设计第几节”均指此文件 |
| 实施基线 | 本次阶段 4 前 `codex/delivery-objective-optimization@5ce8a39`，工作区干净；原求解基线仍为 `ce0b1f4`，源码、测试、工具与 `b1ab8af` 相同 |
| 本轮交付范围 | 按阶段 0～6 实施后端算法与验证；不改正式配置、数据库或历史结果 |
| 后续执行方式 | 授权后按阶段持续推进，每阶段独立中文提交；只在新增业务取舍、范围变化或关键阻塞时暂停 |
| 验证安排 | 不要求每个小步骤运行测试；阶段 2、4、6 集中验证，阶段 3、5 完整对照；未验证提交标明检查点 |

业务上是两步：**第一步修搜索机会并保留原九级作对照（阶段 0～3），第二步采用旧欠优先九级（阶段 4～6）**。第二步包含新增清空目标、晚交吨位退出评分、交期前移及对应接受保护修订，是一组已确认改动，不能把收益全归于一个指标。两种评分都是九项，禁止仅按长度判断模式；不额外安排八级中间试验。

本计划是新的专项执行入口。原[交期实施计划](apsgo_v7_delivery_objective_optimization_implementation_plan.md)和[临期搜索实施计划](apsgo_v7_urgent_order_search_implementation_plan.md)保留完成记录与已有能力，不重做、不整体作废；后续只同步本计划、本设计、`AGENTS.md` 和本项必要证据。

## 2. 核验基线与保护

### 2.1 已确认口径与当前实现的区别

- 既有：起排时间显式传入，六月样本为 6 月 1 日零点；截止为交期当天 24 点；虚拟材速度 100 米/分钟；原订单最后拆片完成才算整单完成。
- 既有：只允许链重低于下限的最终偏差，禁止、来源守恒、拆单及期锁不放松；正式零欠重门不改。
- 当前代码及第一步对照：欠重第 3/4 位，新增晚交吨位和累计负担第 5/6 位；后置精修欠重数量和缺口分别不增加。已撤回的旧“交期优先”实验并非本次方案，不能直接恢复其代码或历史结果。
- 本次已确认的新九级：禁止数、禁止严重度、旧欠全部完成用时、累计等待与延期负担、欠重链数、欠重缺口、链间宽差、虚拟重量、链数。新增晚交原订单吨位仅统计，不参与评分、同分裁决或隐藏接受门槛。
- 旧欠清空优先于本月延期改善，两项交期目标优先于欠重；新模式取消独立的欠重数量/缺口分别不增加拦截，按完整新九级比较。允许高层目标改善带来更多欠重或新增晚交吨位，仍须满足禁止性规则与最终审计，正式零欠重门不放宽。
- 评分口径已在阶段 4 实现为显式试验模式；默认仍原九级，正式库未启用，完整效果待阶段 5，不再重复询问已经确认的优先级取舍。
- 不增加“全部旧欠必须先于全部本月订单”硬规则，不将旧欠重新混入新增晚交吨位。

### 2.2 环境与冻结输入

| 项目 | 当前核验值 / 位置 |
|---|---|
| 后端根目录 | `/Users/miles/dev/dev-py/APSGOV7` |
| 平台 | macOS / Darwin |
| Python | `/Users/miles/anaconda3/envs/aps_3.10.18/bin/python`，3.10.18 |
| 原九级完整基线 | `diagnostics/urgent_order_search/restored_delivery_400000_9999s_01/` |
| 原始七级实验输入 | `diagnostics/urgent_order_search/input_budget400000_search9999.json`，经原工具派生交期请求 |
| 工时来源 | `diagnostics/delivery_objective/timing_source.json` |
| 原报告与身份清单 | [长时限复测记录](evidence/apsgo_v7_urgent_order_search/restored_search9999.md)、[机器可读对照](evidence/apsgo_v7_urgent_order_search/restored_search9999_comparison.json) |
| 实验预算 | 种子 590531；400000 次检查；搜索 9999 秒 + 收尾 10 秒，总策略 10009 秒 |
| SQLite | `data/apsgo_v7_rules.sqlite3`；SHA-256 `72a60e32fea0d2d55f883a8b743175aa5e8fd94e779388db6ae8c8c26ec2bd6a` |
| YAML | `config/apsgo_v7_service.yaml`；SHA-256 `dc8f113ef85689fdc5ff2fecb0579f68c6892a8d8316c764568946bf341dd4c8` |
| 未覆盖 | 正式活动规则启用、HTTP、C# / Web、Windows 包、部署和正式性能验收 |

原实验输入不依赖重新读取当前活动规则版本；不从 SQLite 静默刷新规则。上面的数据库哈希是保护身份，不是“正式库已启用交期”的声明。后续每阶段开始核对用户改动，有变化先辨别归属，不覆盖、提交或擅自还原。

正式对照冻结为 4 单 / 114.90 吨新晚交、1 条 / 41.01 吨欠重、680 吨虚拟、23 链、宽差 12397、400000 次检查；旧欠 96 单 / 5386.03 吨，最后完成于 6 月 26 日 10:38:27.529921。它仍是 BEST_EFFORT，不能改写成新门槛或最优结果。

## 3. 修改清单

以下是整个专项的实施范围，不代表各行都已实现；当前进度以阶段总表及实际执行记录为准。

| 模块 / 文件 | 第一步 | 第二步 |
|---|---|---|
| `src/apsgo_scheduler/core/chain_order.py` | 复用计时，按原订单形成新晚交/旧欠/尚未晚交队列；旧欠按完成时间晚者优先 | 按声明定位新交期目标，避免移除晚交评分后跳过精修；仅新模式取消独立欠重不增加拦截 |
| `src/apsgo_scheduler/core/width_optimization.py` | 各原单和动作类小批轮转；接受后重建位置、保留访问偏好；保留全部原动作 | 保持第一步，读取规则声明，不增加专属候选构造核心 |
| `src/apsgo_scheduler/core/neighborhoods.py` | 复核原完整接受与动作调用；默认复用，不另写前移实现 | 新九级完整比较，复核整链/精修前缀保护；只统计的晚交吨位不能成为否决或裁决条件 |
| `src/apsgo_scheduler/core/delivery_timing.py` | 不改计时公式 | 从已有原订单完成映射派生旧欠最大值，提取最小共用计算，避免再扫描全部节点 |
| `src/apsgo_scheduler/core/rules/concrete.py` | 不改目标 | 扩展原交期规则的显式布尔选项和清空指标；保留晚交吨位统计，空参数仍原两项 |
| `src/apsgo_scheduler/core/rules/rule_set.py` | 不改加载契约 | 按显式选项校验原九级或新九级的有序键与数值投影；不能只识别长度 |
| `src/apsgo_scheduler/app/delivery_request.py` | 不改原九级准备 | 默认关闭的新选项、新九级顺序、新版本与指纹；晚交吨位不进入新 `quality_spec` |
| `src/apsgo_scheduler/core/final_audit.py`、`app/contract_audit.py` | 复用；测试不能绕过 | 核验既有重算和身份检查覆盖新指标；能通用处理就不新增分支 |
| `src/apsgo_scheduler/app/delivery_report.py` | 旧返回保持，比较工具只读派生额外统计 | 新九级报告增加清空小时，评分与统计分开核对；旧九级返回保持 |
| `tools/run_delivery_comparison.py`、`tools/summarize_delivery_comparison.py` | 复用公开入口，增加必要原订单机会诊断与结果比较 | 新增显式变体；按名称读取评分、欠重正式门及统计，禁止沿用 `quality[2]` 为欠重的假设 |
| `tools/compare_backlog_search_runs.py` | 薄的同请求双运行比较；复用既有模型回读、完整评价及日期计算，输出清空比例、尾部和全部逐单变化；原运行工具增加默认关闭的机会观察，补充复跑验证观察不改变结果 | 延续实际需要，不增加求解逻辑或另一套计时公式 |
| 既有交期 / 精修 / 应用测试 | 补轮转、失效位置、完整接受及冻结样例 | 补最大值、启停、边界、身份、旧路径和双审计 |
| 本专项文档及小证据 | 记录代码身份、输入、机会覆盖和第一步结果 | 记录与第一步的独立比较、取舍及最终状态 |

若现有工具不足以保存单动作诊断，可增加一个薄的只读复现工具，仍调用原动作和审计，不能做成第二个求解器。完整运行产物保存在独立 `diagnostics/` 目录；Git 只提交必要复现工具、小型摘要与哈希清单，不整包复制历史大文件。

## 4. 阶段总表

| 功能 | 完整名称 | 主要设计依据 | 依赖 | 计划提交信息 | 当前状态 |
|---|---|---|---|---|---|
| 文档准备 | 建立两步改善设计与实施入口 | 初次用户要求；设计第 1～8 节 | 分支、源码和原证据核验 | `#feat 编写旧欠订单搜索与清空优化方案` | 完成 `f045b65`；原 v0.1 目标提案已被本次修订替代 |
| 文档修订 | 同步旧欠优先九级评分与仅统计项 | 用户最新确认；设计第 3、5～8 节 | `f045b65` | `#feat 同步旧欠优先九级评分与统计口径` | 完成 `6991c54` |
| 0 | 冻结原九级输入、旧欠明细与有效移动样例 | 设计第 2、7 节 | 用户已授权 | `#feat 冻结旧欠订单搜索对照与有效移动样例` | 完成；两例接受及核心审计通过，见阶段 0 证据 |
| 1 | 实现旧欠尾部优先与按原订单小批枚举 | 设计第 4.1～4.2 节 | 阶段 0 | `#feat 改善旧欠订单的候选搜索顺序` | 完成 `3062099`；阶段 2 相关测试通过，机会收益另行记录 |
| 2 | 完成动作轮转与接受后续访并集中验证 | 设计第 4.2～4.4、6 节 | 阶段 1 | `#fix 避免交期精修反复从队首搜索`；修正 `#fix 优先尝试直接可连接位置并完成阶段二验证` | 完成 `e0d55e5`；两例机会通过，共享/精确导出各 3909 项通过；`de9015f` 保留失败检查点 |
| 3 | 单独复测搜索改善并分析交期和资源代价 | 设计第 7 节 | 阶段 2 通过 | `#feat 记录旧欠订单搜索机会改善复测` | 完成；零欠重、同样 4 单晚交，旧欠等待改善但虚拟/宽差/链数增加；保留为后续对照，不自动正式采用 |
| 4 | 实现旧欠优先九级、接受保护与审计报告接线 | 设计第 5～6 节 | 阶段 3 已记录；本次顺序已确认 | `#feat 实现旧欠优先九级评分与接受语义` | 已实现；50 项新专项通过，最终相关集中/精确导出见阶段 4 记录；尚未完整新评分求解 |
| 5 | 完整比较原方案、仅改搜索、搜索加新评分 | 设计第 7 节 | 阶段 4 通过 | `#feat 记录旧欠优先九级评分对照结果` | 未开始；第二步对照检查点 |
| 6 | 集中回归、保护核验与专项收口 | 设计第 6～8 节 | 阶段 5 | `#fix 完成旧欠订单优化验证与收口记录` | 未开始 |

每行独立提交。新增目标不混进阶段 1～3；不得先改评分再把质量改善全部归因于轮转。暂未运行测试的阶段提交不用于部署，提交说明指向下一集中检查点。

## 5. 各阶段说明与完成标准

### 阶段 0：冻结与复现

1. 核对当前分支、HEAD、差异及原五个运行产物哈希，不重新生成或覆盖历史基线。
2. 从原最终方案输出 96 单旧欠的原编号、原吨位、片段位置和完成时间，并保留本月全部原单明细。
3. 分别复现设计第 2.3 节两个有效节点前移，保存动作参数、原/新有序计划和评价、实际预算计数及核心审计；明确两例分别从相同原方案出发。
4. 保存旧枚举中样例原单和成功动作的次序；确认本专项第一步使用 4 提案/原单、64 提案/动作类的初始工程试验值。

完成标准：源数据和代码身份可追溯，两例在原完整接受入口被接受且核心审计通过。若不能复现，先定位差异，不用新增评分掩盖，也不把上一轮自然语言诊断当作可运行证据。此阶段不要求重跑 821 秒的完整基线。

### 阶段 1：原订单顺序与小批枚举

1. 在现有排序位置区分三组原订单；旧欠使用最后片段完成时间降序，同分稳定。
2. 在原五类动作生成器中按原订单小批继续，较早目标链优先且目标链间轮转；保留移动/交换、片段长度与后续位置覆盖。
3. 写入相应小例测试，但本阶段只做差异与语法级检查；业务执行集中阶段 2。

完成标准：只改“候选从哪里来、何时轮到”，不改九级声明、已有动作构造、工时、预算或接受保护；无交期、无旧欠两种路径不被误接到新轮转。

### 阶段 2：动作轮转、续访和第一步集中验证

1. 把交期且有旧欠的扫描改为有界动作类轮转；每次接受后切到下一动作类和原单访问起点，不能所有队列复位。
2. 接受后重算时间、重建候选，旧下标不得复用；保留的是访问偏好。身份消失、链数改变、目标位置变化要安全重新定位。
3. 覆盖到尾回绕、拒绝后继续、完全穷尽、时间/候选截止、取消和桥接追加耗费；自然结束不能依据旧方案的穷尽状态。
4. 集中运行第 6 节第一组测试；在冻结最终方案上自动枚举到两个样例动作，记录首次发现所需提案/检查数，与旧序比较。不得通过生产订单白名单直接调用成功位置冒充自动搜索。
5. 已确认的位置修正：仅跨链单节点插入，复用现有连接缓存，将两侧直接可连接空位稳定前置，其余保留。原 4 / 64、目标链轮转及接受语义不改；覆盖首尾/方向、无直接位置、分类完整性、访问起点重建、无旧欠/无交期和停止条件。
6. 扩展原机会工具，记录新增缓存查询、命中/未命中和枚举耗时，并重新验证两条独立完整候选及核心审计。只观察枚举的额度与实际构造候选的额度分开记录，不把预排序查询说成免费计算或完整评价。

完成标准：轮转和完整接受正确，样例机会显著前移的证据可回读，旧七级及无旧欠路径回归通过；规则和当前九级评分保持。若没有解决样例机会问题，先修第一步，不进入完整性能结论。

### 阶段 3：仅改善搜索的完整对照

1. 使用同一冻结 400000 次 / 9999 秒输入，执行原公开入口，输出到新目录；不修改正式默认预算。
2. 对照原 `ce0b1f4` 结果，按设计第 6 节输出旧欠清空时刻、平均等待、本月逐单交期、资源、禁止/欠重及双审计。
3. 记录首次路径分歧与动作机会分配、前后阶段耗时、候选与评价数、真实停止原因；预期只改变后置精修，但必须核对实际前置有序轨迹。
4. 原日志未保留被拒绝动作的细粒度计数；使用原公开入口的默认关闭观察选项补一轮同预算复跑，仅包装后置原动作调用，逐次转交原方法一次，不插入候选、评价、预算检查或排序查询。按动作累计已扣费提案、实际检查（含桥接）、完整评价、接受及涉及的旧欠原单；记录旧欠单第一次节点移动的原始描述与当时计数。统计总和须与后置阶段前后差闭合；补充运行的首轮、最终有序方案/评价/轨迹/计数和报告须与无观察运行一致，才引用该统计。这里的首次尝试不等于阶段 0 冻结方案中的成功位置，也不代表动作被接受。

完成标准：得到完整、可审计的第一步结果并作采用/取舍判断，而不是必须给出正收益。若禁止或守恒失败，修复后另目录重测；若欠重、新增晚交吨位等变差，按设计第 7.2 节暂停讨论，不悄悄调参数或跳到下一目标“补救”。

### 阶段 4：旧欠优先九级及完整接受接线

1. 新增最大值派生，使用原订单完成映射、`d_i <= 0` 分类和无旧欠取零；不改变计时和旧累计负担。
2. 原交期规则增加显式 `include_backlog_clearance` 布尔选项；空参数保持原两项，新模式增加清空指标并保留晚交吨位统计。新规则版本与指纹独立，默认旧九级身份及业务返回保持。
3. 后端准备生成第 2.1 节新九级，`quality_spec` 排除晚交吨位；原对照工具已增加 `delivery-backlog-priority` 变体，旧 `delivery` 仍为原九级。该变体已通过小型真实公开入口测试；完整 GQGA4 运行归阶段 5，不保留 v0.1 十级变体。
4. 核对 `chain_order_objective_index()` 及所有调用者，定位实际参与评分的新交期指标；仅新模式取消独立欠重不增加、原前四项必须相同等冲突拦截，复用完整候选、允许偏差检查和新九级严格比较。
5. 报告增加清空小时并区分评分与统计；按名称核对完整评价，晚交吨位即使不在质量序列里也须按原公式复算核验。核心无缓存审计、应用绑定检查复用既有入口。
6. 集中执行第 6 节第二组测试；检查所有读取质量序列的调用者，特别是按下标计算正式欠重门的工具，不让九项同长度掩盖语义变化。

完成标准：原九级和旧欠优先九级按有序指标键与指纹显式区分；目标和统计值、空集/拆片等边界正确；新优先级不再被旧欠重保护抵消，晚交吨位无隐藏参与。没有改数据库、HTTP、YAML、原订单或额外引入重算全节点的循环；新顺序在完整搜索各阶段生效，不伪装成仅改展示。

### 阶段 5：第二步独立对照与重复性

1. 同样输入与预算运行旧欠优先九级，保存独立目录；与阶段 3 比较整组评分/接受变更效果，同时保留原九级基线供三组汇总，不声称分离出单个新增目标的收益。
2. 按指标名称比较；三个版本只读派生清空小时并按新顺序投影作偏好对照，不直接比较同为九项但语义不同的原始质量元组，也不覆盖旧评价或发布指纹。晚交吨位明确为统计列，正式欠重门按指标名称读取。
3. 新模式若未被时间截断，额外重复一次，核验有序方案、业务指标、轨迹、候选/评价/接受次数和停止原因；耗时、时间戳等运行元数据不要求字节相同。
4. 若时间先到，明确记录无法按同候选工作量判断确定性，不无限重跑或自动扩大预算；是否另做试验再讨论。

完成标准：能够区分搜索改善和整组新评分各自效果。报告“提前清空旧欠”是否换来更多平均等待、本月晚交吨位及名单变化、欠重、虚拟材、链数或耗时，不只给出一个总分。高优先级改善伴随晚交吨位或欠重增加属于已确认取舍，不因此额外否决或中断试验；正式质量和性能状态仍如实标记。

### 阶段 6：最终集中验证和交接

1. 在最终精确暂存树干净导出中，统一运行受影响专项与项目累计回归，核验类型回读、无缓存审计、报告和架构约束；不把历史通过数量填成本次结果。
2. 核验原输入、旧输出、YAML 与 SQLite 的保护身份以及精确提交范围，更新执行记录和提交表。
3. 写明最终是“实现正确且建议采用”“实现正确但收益不确定”还是“试验未通过”，列出未关闭的正式质量、性能和 Windows 项。

完成标准：本专项状态真实、记录齐全。未发布、未合并、未推送的事实明确；代码实现完成不自动代表替换正式分支或现场配置。

## 6. 集中验证清单

| 检查点 | 必测内容 | 复用位置 |
|---|---|---|
| 阶段 2：顺序 | 旧欠小吨位但完成最晚；三组轮转；等截止准交；输入同分稳定；拆片按原单最后片段 | `tests/core/test_urgent_order_search.py` |
| 阶段 2：公平性与有效性 | 单个订单/动作不能垄断；拒绝续访；接受后目标下标失效；链删除/插入；回绕覆盖；不从旧穷尽自然结束 | 上述测试及 `tests/core/search/test_width_optimization_scan.py` |
| 阶段 2：规则和预算 | 旧七级、无旧欠路径；欠重两项分别不增加；零禁止；桥接仍计费；原单守恒、期锁；时间/取消优先终止 | `tests/core/test_delivery_search_audit.py`、既有精修测试 |
| 阶段 2：直接位置优先 | 两个有向接口按生产方向查询；首尾、全直接/非直接、原序与续访、其他位置不遗漏；复用缓存，取消/时间截止仍优先 | `tests/core/search/test_direct_insertion_slots.py`，两条冻结机会及独立接受/核心审计 |
| 阶段 4：目标公式 | 无旧欠=0；全部旧欠；开始时间非零时分秒；`D_i=S`；多片最后完成；虚拟占时；最大值不同于求和/加权等待 | `tests/core/test_delivery_objective.py`、`test_delivery_timing.py` |
| 阶段 4：优先级与接受 | 新九级精确顺序；清空改善可换累计负担/欠重增加；清空相同、累计负担改善可换欠重；前四相同则欠重数量先于缺口；只改变晚交吨位不改变接受决定；无旧欠清空=0仍按新顺序 | 目标测试、精修接受与后端准备测试 |
| 阶段 4：搜索接线 | 评分中没有晚交吨位仍进入精修和整链调整；无额外欠重否决或统计裁决；原九级保护不变；禁止/期锁/守恒和桥接计费保持 | `tests/core/test_urgent_order_search.py`、`test_delivery_search_audit.py`、既有精修测试 |
| 阶段 4：启停及身份 | 空参数仍两项；布尔类型/未知字段拒绝；新九级新指纹；同长度不误认模式；默认原九级和旧七级不变；时间输入完整校验 | `tests/app/test_delivery_preparation.py`、规则加载测试 |
| 阶段 4：结果一致性 | 独立重算目标和晚交统计；篡改评分/统计/时间绑定拒绝；诊断不伪装发布；正式欠重门不把清空小时错当欠重链数 | `tests/core/test_delivery_search_audit.py`、应用审计及汇总工具测试 |
| 阶段 3 / 5 | 完整原单明细、清空进度、资源、两个审计及同预算对照；单动作不能代替完整运行 | 原对照工具及本项独立证据 |
| 阶段 3：观察和比较工具 | 空旧欠、整单清空比例/并列、恰好一次调用、异常透传、含桥接计数、小型真实结果回读及评分/计数/日期不一致拒绝；完整观察复跑不改变业务结果 | `tests/app/test_backlog_search_comparison.py`、原测量/准备测试、阶段 3 真实对照 |
| 阶段 6 | 以上专项与架构/API/应用/核心累计回归；因共用诊断工具而受影响的服务/工具测试一并加入 | 最终精确导出 |

命令使用 Conda 环境和现有测试框架，不新建测试体系。代码检查点可按实际改动合并下列既有入口；无需每提交都重复全部命令：

```sh
PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider \
  tests/core/test_urgent_order_search.py tests/core/test_delivery_search_audit.py \
  tests/core/search/test_width_optimization_scan.py

PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider \
  tests/core/test_delivery_objective.py tests/core/test_delivery_timing.py \
  tests/app/test_delivery_preparation.py

PYTHONDONTWRITEBYTECODE=1 /Users/miles/anaconda3/envs/aps_3.10.18/bin/python -m pytest -p no:cacheprovider \
  tests/architecture tests/api tests/app tests/core tests/service
```

文档提交只做 UTF-8、链接、差异白名单、保护哈希及导出检查，不运行业务测试、完整求解或构建。必要的定位小例可随时运行，不必等到集中检查点。

## 7. 证据与提交约定

- 原结果只读；新运行使用 `diagnostics/backlog_priority_search/` 下互不覆盖的目录。证据摘要预定放在 `docs/implementation/evidence/apsgo_v7_backlog_priority_search/`，实施时才建立，不创建空占位。
- 每次记录：实施前提交、源码身份、输入/规则/时间指纹、命令、退出码、经过时间、CPU 时间、候选/评价/接受次数、停止原因、首个分歧和全部原单变化。
- 样例诊断与完整发布审计分开；本轮文档中引用的旧运行不标成“本轮复测”。
- 每阶段中文 `#feat` / `#fix` 独立提交，只暂存本项准确路径，不用 `git add .` 或 `git add -A`；当前提交的身份由 Git 历史提供，不在内容里自引用 SHA。
- 项目残留检查按约定运行。本轮共享树仍报告 8 个 V6 历史稳定残留缺失，不能为通过检查重建旧产物或篡改旧清单；此问题与算法验证分开记录。新增异常另外核对，不一概忽略。
- 按 `git write-tree` / `git archive` 导出精确暂存树检查；文档提交不因导出而重复业务测试，代码累计回归集中阶段 6。
- 不提交 `diagnostics/` 全量数据、IDE 文件、外部 V3 文件；不推送、合并或自动部署。

## 8. 失败处理与恢复

| 情况 | 动作 |
|---|---|
| 样例无法复现、身份不一致 | 阶段 0 停止，报告具体差异，不猜测修正输入 |
| 轮转使候选遗漏、旧下标执行、自然结束错误 | 修复第一步并重新集中验证，不靠加预算绕过 |
| 新指标导致旧九级身份/返回漂移 | 修正显式启用边界；不更新旧基线断言掩盖变化 |
| 禁止、来源或审计失败 | 结果不推荐采用，不发布；保留失败证据后修复 |
| 新模式尾部改善但新增晚交吨位/欠重变差 | 属于已确认取舍，记录明细并继续验证；不得偷偷恢复不增加门槛。原九级对照仍按自身保护检查，最终禁止和审计门不放宽 |
| 仅统计无收益，或资源/耗时代价明显 | 如实记录，讨论是否保留第一步、停用第二步；不得宣称全面更优 |

恢复第二步时可使用阶段 3 已提交的第一步版本；恢复整个试验时以 `ce0b1f4` 为已知原版本。需要修改当前分支时使用明确的反向提交，先核对用户工作区，不执行 `reset --hard` 或整库覆盖。历史产物与新失败产物均保留；本项目无数据库迁移和服务切换，不需要额外停机窗口。

## 9. 实际执行记录

| 日期 / 阶段 | 实际动作与验证 | 结果 | 偏差 / 下一步 |
|---|---|---|---|
| 2026-09-14 / 文档准备 | `pwd`、`uname -s`、`git status --short --branch`、`git log -3 --oneline`；核对当前源码、既有报告和实验输入位置；`git diff ce0b1f4 b1ab8af --stat -- src tests tools` 无差异；Python 3.10.18；记录库与 YAML SHA-256 | 当前交期分支、工作区干净；原基线身份明确 | 只读核验，无新完整求解 |
| 2026-09-14 / 文档准备 | 使用项目 Python 执行 `tools/check_workspace_residuals.py --verify` | 退出 1；8 个 V6 旧稳定残留缺失，附带“这些残留不再处于未跟踪状态”的汇总；不属于本次代码错误 | 不改旧清单、不重建文件；精确导出单独核验 |
| 2026-09-14 / 文档准备 | 编写两份文档并更新 `AGENTS.md`；检查明确的两步范围、指标顺序和测试分组 | 文档交付；阶段 0～6 未执行 | 不改算法，不执行新求解 |
| 2026-09-14 / 文档复核 | `git diff --check`；Node 只读检查两份文档 UTF-8 无 BOM、代码围栏与 11 个新增本地链接；回读原结果核对 96 单旧欠、5386.03 吨、4 单新晚交及两例原始位置；复核库/YAML 哈希 | 全部通过；仅三份文档变化，保护文件未变；本轮没有业务测试或新排程 | 最终精确暂存树、干净导出和提交范围检查结果随本次 Git 提交正文保留 |
| 2026-09-14 / v0.2 评分修订 | 基线 `f045b65`；用户确认旧欠优先九级，晚交吨位仅统计；只读核对目标定位、精修欠重拦截与汇总下标，修订设计、实施范围、测试和验收说明 | 仅三份文档；阶段 0～6 均未开始；不再保留 v0.1 十级作为待实施目标 | 本轮共享残留仍为相同 8 个历史文件缺失；文档与精确导出检查随本次提交正文记录，不运行业务测试或求解 |

后续每阶段追加实际命令、退出码、结果位置与偏差，不删除失败记录，不将计划步骤复制成完成记录。

- 2026-09-14 / 阶段 1：实施前 `c16dba8`；增加显式旧欠尾部优先排序及五类惰性候选生成。每原单 4 提案，目标链以移动/交换 2 个提案为一组轮流访问，保持较早链优先；同一原单拆片不增加队列份额。对称交换用确定的归属去重，不预生成候选方案。新增顺序、拆片、覆盖与轮换检查代码；`git diff --check` 和四文件 `ast.parse` 均退出 0。生产入口尚未接线，未运行业务测试或完整求解，不可部署；阶段 2 集中验证。

- 2026-09-14 / 阶段 2：实施前 `3062099`；接线五类 64 提案轮转，接受后用稳定身份重建全部候选及访问偏好。专项 54 项 / 0.73 秒、扩展相关 867 项 / 26.94 秒均退出 0。冻结样例自动枚举观察三次均复现：原单位次变为 2 / 5，但节点提案位次变为 430854 / 406737；五类共享 400000 次纯枚举额度内均未命中，原方案不变，**机会门未通过**。只读连接检查表明成功位置处于各目标链少数直接可连接位置中。暂停在本阶段，建议确认“直接可连接位置优先、其他保留”后再修订设计和实现；不改评分、不进入阶段 3～6。命令、完整边界与原始证据哈希见[阶段 2 记录](evidence/apsgo_v7_backlog_priority_search/stage2.md)。原五产物、SQLite / YAML 保护通过；共享残留未出现新异常。最终精确树同范围复验结果见提交正文。

- 2026-09-14 / 阶段 0：实施前 `6991c54`，工作区干净；运行新增薄诊断工具，退出 0 / 1.002151 秒 / CPU 0.979611 秒；原五产物及输入保护哈希、原完整方案和评价指纹均通过。保存 531 单含 96 旧欠及两条独立移动，旧提案序 120667 / 165345，各 1 次检查且核心审计通过。共享残留仍仅既知 8 项；不跑全流程、不改评分。实际命令和文件说明见[阶段 0 证据](evidence/apsgo_v7_backlog_priority_search/stage0.md)。下一步阶段 1～2，初值 4 / 64 提案已冻结。

- 2026-09-14 / 阶段 2 修正收口：实施前 `de9015f`；用户确认直接可连接位置优先，设计先同步至 v0.3。仅跨链单节点插入接入现有连接缓存排序，非直接位置保留，原九级、4 / 64 轮转、预算及接受/审计不改。两例节点提案序由原 120667 / 165345 提前为 29741 / 6389，五类共同轮转分别在第 96966 / 25881 次出现；机会门通过。最终独立两例各 1 次实际检查且接受/核心审计通过，与阶段 0 样例结果精确一致。实际命令、成本、重复性及产物哈希见[阶段 2 修正证据](evidence/apsgo_v7_backlog_priority_search/stage2_direct_first.md)。专项 67 / 0.75 秒，相关 880 / 26.89 秒通过；首次累计发现旧动作数字编号触发架构检查，1 失败 / 3549 通过，改用动作名称和元组解包后 143 项 / 1.26 秒通过。最终含服务累计 **3909 项 / 51.06 秒，退出 0**。精确暂存树同范围复验及保护检查结果随独立提交正文保存；旧失败证据保留。本次没有完整 GQGA4 重跑或新评分，下一步阶段 3。

### 阶段 3 执行明细

实施前 `e0d55e5`，工作区干净；源码未修改。命令、逐单说明和所有产物身份见[阶段 3 记录](evidence/apsgo_v7_backlog_priority_search/stage3.md)及[机器记录](evidence/apsgo_v7_backlog_priority_search/stage3_comparison.json)。

| 实际动作 | 验证与结果 | 偏差 / 边界 |
|---|---|---|
| 冻结身份检查 | 原五产物、六项输入/保护哈希通过；类型请求与原基线相等 | 无预算、评分、工时或数据库变化 |
| 原公开入口完整运行 | `stage3_search_only_01` 退出 0；824.528608 秒 / CPU 822.910104 秒，400000 次检查、163814 次完整评价、726 次接受，额度停止 | 不声称自然穷尽或性能提速 |
| 原九级质量与交期 | 零禁止、零欠重、来源守恒和双审计通过；同样 4 单 / 114.90 吨晚交；旧欠清空提前 23.20 小时，加权等待减少约 29.62% | 虚拟 680→1400 吨，宽差 12397→14348，23→24 链；轻小尾单仍在 6 月 24～25 日完成 |
| 首个路径分歧 | 首轮快照及前 341 次采纳完全相同；第 342 次开始分别接受链内前移 / 跨链节点移动 | 确认只改变后置精修；未启用新评分 |
| 同预算机会观察复跑 | `stage3_search_only_observed_02` 退出 0；835.803299 秒 / CPU 833.994244 秒；首轮、最终方案/评价/轨迹/计数/停止原因/缓存及日期报告相同 | 观察开销单列，不能取代主运行计时 |
| 机会统计闭合 | 后置 235354 提案/检查、113970 次完整评价、385 次接受，与总计数逐项闭合；96 单旧欠在各动作并集中获得机会，94 单有节点移动尝试 | 不说每单或每个位置已遍历；两例第一次尝试不是冻结成功空位 |
| 工具与记录验证 | 10 项新检查；含原测量/准备合计 28 项 / 0.69 秒通过，退出 0；三运行全部 531 单通过重算和守恒核对 | 原基线自比较仅为工具自检；完整累计留到阶段 6，最终精确导出同范围复验见提交正文 |
| 采用判断与交接 | 第一版搜索作为后续新评分对照基础；阶段 3 完成，下一步阶段 4 | 不自动正式采用，不改正式零欠重/性能门，不部署、合并或推送；阶段 4～6 尚未执行 |

### 阶段 4 执行明细

实施前 `5ce8a39`；新增清空指标及默认关闭的新九级准备入口，修正规则加载、目标定位和新模式欠重拦截；复用完整候选、计时、双审计及既有工具。原对照继续使用 `delivery`，新变体为 `delivery-backlog-priority`。

| 实际动作 | 验证与结果 | 边界 / 下一步 |
|---|---|---|
| 公式、接受、报告与准备接线 | 新专项 50 项 / 0.68 秒通过；真实 4 单节点移动证明清空可优先于累计负担、晚交吨位及欠重，且核心审计通过 | 小型样例，不冒充 GQGA4 质量改善 |
| 原身份和完整结果回读 | 原九级派生请求与两个冻结请求相等；两份完整评价及 531 单日期/守恒通过 | 旧九级结果不改，原基线仍 BEST_EFFORT |
| 新指标在冻结方案上重算 | 阶段 3 原方案清空 587.4388867756726549279204323 小时，新评分投影和核心审计通过，原拆片仍有效 | 只读重评，不是新搜索 |
| 集中检查及失败记录 | 首次应用专项 3 项被已有公共结果身份保护提前拒绝，修正测试分层后通过；最终相关范围与精确导出见[阶段 4 记录](evidence/apsgo_v7_backlog_priority_search/stage4.md) | 不放松生产校验，不宣称含服务全仓累计已完成 |
| 保护与交接 | 六项输入/保护及原产物哈希保持；原正式规则库/YAML不改；共享残留仍仅历史 8 项 | 下一步阶段 5 完整新九级比较及重复性，阶段 6 最终累计 |

## 10. 提交情况

| 阶段 | 计划提交信息 | 实际提交 | 验证状态 / 证据 |
|---|---|---|---|
| 文档准备 | `#feat 编写旧欠订单搜索与清空优化方案` | `f045b65` | 原 v0.1 文档提交；验证见第 9 节及提交正文 |
| 文档修订 | `#feat 同步旧欠优先九级评分与统计口径` | `6991c54` | 仅文档范围；检查见第 9 节及提交正文 |
| 0 | `#feat 冻结旧欠订单搜索对照与有效移动样例` | `c16dba8` | 两例接受、核心审计及冻结身份通过；见阶段 0 证据 |
| 1 | `#feat 改善旧欠订单的候选搜索顺序` | `3062099` | 已在阶段 2 完成相关代码测试，收益不能据此宣称 |
| 2 原检查点 | `#fix 避免交期精修反复从队首搜索` | `de9015f` | 当时 867 项相关通过但机会门失败；保留失败记录，由下一行修正收口 |
| 2 修正收口 | `#fix 优先尝试直接可连接位置并完成阶段二验证` | `e0d55e5` | 两例机会、原接受/核心审计通过；共享/精确导出各 3909 项通过 |
| 3 | `#feat 记录旧欠订单搜索机会改善复测` | `5ce8a39` | 两次完整业务一致、双审计与机会计数闭合；相关 28 项通过；记录资源代价，完整性能/正式部署不在本项 |
| 4 | `#feat 实现旧欠优先九级评分与接受语义` | 本阶段独立提交，身份见 Git 历史 | 实现及新旧模式专项通过；最终集中检查、干净导出和保护身份见[阶段 4 记录](evidence/apsgo_v7_backlog_priority_search/stage4.md)及提交正文 |
| 5 | `#feat 记录旧欠优先九级评分对照结果` | 未提交 | 未执行 |
| 6 | `#fix 完成旧欠订单优化验证与收口记录` | 未提交 | 未执行 |
